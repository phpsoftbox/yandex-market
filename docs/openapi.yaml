openapi: 3.0.3
info:
  title: API Яндекс Маркета для продавцов
  description: 'API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.


    В числе возможностей интеграции:


    * управление каталогом товаров и витриной,


    * обработка заказов,


    * изменение настроек магазина,


    * получение отчетов.

    '
  version: LATEST
  license:
    name: BSD 3-Clause
    url: https://opensource.org/license/bsd-3-clause/
servers:
- url: https://api.partner.market.yandex.ru
security:
- ApiKey: []
- OAuth:
  - market:partner-api
externalDocs:
  description: Документация API
  url: https://yandex.ru/dev/market/partner-api/doc/
paths:
  /v2/businesses/{businessId}/settings:
    post:
      x-resource-id: 218
      tags:
      - businesses
      - dbs
      - fbs
      - fby
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getBusinessSettings
      summary: Настройки кабинета
      description: '{% include notitle [access](../../_auto/method_scopes/getBusinessSettings.md) %}


        Возвращает информацию о настройках кабинета, идентификатор которого указан в запросе.


        {% include notitle [limit](../../_auto/method_limits/getBusinessSettings.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      responses:
        '200':
          description: Настройки кабинета.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetBusinessSettingsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns:
    get:
      x-resource-id: 1
      tags:
      - campaigns
      - dbs
      - express
      - fbs
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getCampaigns
      summary: Список магазинов пользователя
      description: '{% include notitle [access](../../_auto/method_scopes/getCampaigns.md) %}


        **Для Api-Key-токена:** возвращает список магазинов в кабинете, для которого выдан токен. Нельзя получить список только
        подагентских магазинов.


        **Для OAuth-токена:** возвращает список магазинов, к которым имеет доступ пользователь — владелец токена авторизации,
        использованного в запросе. Для агентских пользователей список состоит из подагентских магазинов.


        {% note warning "Ограничение для параметра `pageSize`" %}


        Не передавайте значение больше 100.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getCampaigns.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit100WithoutDefault'
      - $ref: '#/components/parameters/PageNum'
      - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: Магазины пользователя.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCampaignsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}:
    get:
      x-resource-id: 2
      tags:
      - campaigns
      - dbs
      - express
      - fbs
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getCampaign
      summary: Информация о магазине
      description: '{% include notitle [access](../../_auto/method_scopes/getCampaign.md) %}


        Возвращает информацию о магазине.


        {% include notitle [limit](../../_auto/method_limits/getCampaign.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      responses:
        '200':
          description: Информация о магазине.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCampaignResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/settings:
    get:
      x-resource-id: 25
      tags:
      - campaigns
      - dbs
      - fbs
      - express
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getCampaignSettings
      summary: Настройки магазина
      description: '{% include notitle [access](../../_auto/method_scopes/getCampaignSettings.md) %}


        Возвращает информацию о настройках магазина, идентификатор которого указан в запросе.


        {% include notitle [limit](../../_auto/method_limits/getCampaignSettings.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      responses:
        '200':
          description: Настройки магазина.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCampaignSettingsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}:
    get:
      x-resource-id: 28
      tags:
      - orders
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - communication
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        parallel-limit-value: 6
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-12'
        replacement-operation-id: getBusinessOrders
      operationId: getOrder
      summary: Информация об одном заказе в магазине
      description: '{% include notitle [access](../../_auto/method_scopes/getOrder.md) %}


        Возвращает информацию о заказе в магазине.


        {% note tip "Вы также можете настроить API-уведомления" %}


        Маркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый заказ или
        изменится его статус. А полную информацию можно получить с помощью метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        Получить более подробную информацию о покупателе и его номере телефона можно с помощью запроса [GET v2/campaigns/{campaignId}/orders/{orderId}/buyer](../../reference/order-delivery/getOrderBuyerInfo.md).


        {% include notitle [limit](../../_auto/method_limits/getOrder.md) %}

        '
      deprecated: true
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Информация о заказе.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrderResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders:
    get:
      x-resource-id: 27
      tags:
      - orders
      - fbs
      - dbs
      - fby
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - communication
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        parallel-limit-value: 6
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-12'
        replacement-operation-id: getBusinessOrders
      operationId: getOrders
      summary: Информация о заказах в магазине
      description: '{% include notitle [access](../../_auto/method_scopes/getOrders.md) %}


        Возвращает информацию о заказах в магазине. Запрос можно использовать для отслеживания заказов и их статусов.


        По умолчанию данные о тестовых заказах не приходят. Чтобы их получить, передайте значение `true` в параметре `fake`.


        {% note tip "Вы также можете настроить API-уведомления" %}


        Маркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый заказ или
        изменится его статус. А полную информацию можно получить с помощью метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        Доступна фильтрация по параметрам:


        * дата оформления заказа;


        * дата и время обновления заказа;


        * дата отгрузки;


        * статусы заказов (`statuses`);


        * этапы обработки или причины отмены (`substatuses`);


        * идентификаторы заказов;


        * тип заказа (настоящий или тестовый).


        Не возвращается информация о заказах, которые доставили или отменили больше 30 дней назад. Как ее получить:


        * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md);

        * [POST v2/campaigns/{campaignId}/stats/orders](../../reference/orders-stats/getOrdersStats.md).


        Максимальный диапазон дат за один запрос — 30 дней (передается в параметрах `fromDate` и `toDate`). Если их не передать,
        возвращается информация за последние 30 дней.


        Результаты возвращаются постранично. Для навигации используйте параметры `pageToken` и `limit`.


        Получить более подробную информацию о покупателе и его номере телефона можно с помощью запроса [GET v2/campaigns/{campaignId}/orders/{orderId}/buyer](../../reference/order-delivery/getOrderBuyerInfo.md).


        {% include notitle [limit](../../_auto/method_limits/getOrders.md) %}

        '
      deprecated: true
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/GetOrdersOrderIds'
      - $ref: '#/components/parameters/GetOrdersStatus'
      - $ref: '#/components/parameters/GetOrdersSubstatus'
      - $ref: '#/components/parameters/GetOrdersFromDate'
      - $ref: '#/components/parameters/GetOrdersToDate'
      - $ref: '#/components/parameters/GetOrdersSupplierShipmentDateFrom'
      - $ref: '#/components/parameters/GetOrdersSupplierShipmentDateTo'
      - $ref: '#/components/parameters/GetOrdersUpdatedAtFrom'
      - $ref: '#/components/parameters/GetOrdersUpdatedAtTo'
      - $ref: '#/components/parameters/GetOrdersDispatchType'
      - $ref: '#/components/parameters/GetOrdersFake'
      - $ref: '#/components/parameters/GetOrdersHasCis'
      - $ref: '#/components/parameters/GetOrdersOnlyWaitingForCancellationApprove'
      - $ref: '#/components/parameters/GetOrdersOnlyEstimatedDelivery'
      - $ref: '#/components/parameters/GetOrdersBuyerType'
      - $ref: '#/components/parameters/PageNum'
      - $ref: '#/components/parameters/PageSize'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50TruncateWithoutDefault'
      responses:
        '200':
          description: Информация о заказах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrdersResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/orders:
    post:
      x-resource-id: 283
      tags:
      - orders
      - fbs
      - dbs
      - fby
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        parallel-limit-value: 6
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getBusinessOrders
      summary: Информация о заказах в кабинете
      description: '{% include notitle [access](../../_auto/method_scopes/getBusinessOrders.md) %}


        Возвращает информацию о заказах в кабинете. Запрос можно использовать для отслеживания заказов и их статусов.


        {% note tip "Вы также можете настроить API-уведомления" %}


        Маркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый заказ или
        изменится его статус. А полную информацию можно получить с помощью этого метода.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        Доступна фильтрация по параметрам:


        * дата оформления заказа;


        * дата и время обновления заказа;


        * дата отгрузки;


        * статусы заказов (`statuses`);


        * этапы обработки или причины отмены (`substatuses`);


        * идентификаторы кампаний;


        * идентификаторы заказов;


        * внешние идентификаторы заказов;


        * тип заказа (настоящий или тестовый);


        * модели размещения;


        * наличие запросов от покупателей на отмену заказа.


        Максимальный диапазон дат за один запрос — 30 дней (передается в параметрах `fromDate` и `toDate`). Если их не передать,
        возвращается информация за последние 30 дней.


        Результаты возвращаются постранично. Для навигации используйте параметры `pageToken` и `limit`.


        Получить более подробную информацию о покупателе и его номере телефона можно с помощью запроса [GET v2/campaigns/{campaignId}/orders/{orderId}/buyer](../../reference/order-delivery/getOrderBuyerInfo.md).


        {% include notitle [limit](../../_auto/method_limits/getBusinessOrders.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50Truncate'
      requestBody:
        description: Параметры фильтрации заказов.
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetBusinessOrdersRequest'
      responses:
        '200':
          description: Список заказов в кабинете.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetBusinessOrdersResponse'
        '400':
          $ref: '#/components/responses/400Orders'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/identifiers:
    put:
      x-resource-id: 179
      tags:
      - orders
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: provideOrderItemIdentifiers
      summary: Передача кодов маркировки единиц товара
      description: '{% include notitle [access](../../_auto/method_scopes/provideOrderItemIdentifiers.md) %}


        {% note warning "Если вы работаете по модели FBS" %}


        Используйте метод [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md).


        {% endnote %}


        Передает Маркету коды маркировки для единиц товара в указанном заказе. Подробнее о работе с маркируемыми товарами
        читайте в [Справке продавца на Маркете](https://yandex.ru/support/marketplace/orders/cz.html).


        Маркировка товаров в системе [«Честный ЗНАК»](https://честныйзнак.рф/) **необязательна** для заказов от физических
        лиц, но **обязательна** для заказов от бизнеса.


        Для модели DBS коды маркировки в системе [«Честный ЗНАК»](https://честныйзнак.рф/) не проверяются в ГИС МТ — проверка
        выполняется только для моделей FBS и Экспресс.


        Принимаются коды следующих типов:


        * Коды в системе [«Честный ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов
        :no-translate[Market Yandex Go]).

        * УИН для ювелирных изделий.

        * РНПТ и ГТД для импортных прослеживаемых товаров.


        Для каждой позиции в заказе, требующей маркировки, нужно передать список кодов — по одному для каждой единицы товара.
        Например, если в заказе две пары тапочек и одна пара туфель, получится список из двух кодов для первой позиции и список
        из одного кода для второй.


        {% include notitle [limit](../../_auto/method_limits/provideOrderItemIdentifiers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvideOrderItemIdentifiersRequest'
      responses:
        '200':
          description: Ответ `200` обозначает, что коды успешно записались. Ответ содержит краткие сведения о промаркированных
            товарах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProvideOrderItemIdentifiersResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/items:
    put:
      x-resource-id: 57
      tags:
      - orders
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        parallel-limit-value: 6
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: updateOrderItems
      summary: Удаление товаров из заказа или уменьшение их числа
      description: "{% include notitle [access](../../_auto/method_scopes/updateOrderItems.md) %}\n\n{% note warning \"Если\
        \ вы работаете по модели FBS\" %}\n\nИспользуйте метод [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md).\n\
        \n{% endnote %}\n\nУдаляет один или несколько товаров из заказа, если магазин не может поставить их все.\n\nЗаказ\
        \ должен находится в статусе `\"status\": \"PROCESSING\"` этапа обработки `\"substatus\": \"STARTED\"`. Изменить состав\
        \ нельзя после передачи статуса `\"substatus\": \"READY_TO_SHIP\"`.\n\n{% cut \"Уменьшить количество одинаковых товаров\"\
        \ %}\n\nПередайте обновленное значение в параметре `count`.\n\n{% endcut %}\n\n{% cut \"Удалить товар из заказа\"\
        \ %}\n\nПередайте значение `0` в параметре `count` или не передавайте `item`.\n\n{% endcut %}\n\nНельзя удалить или\
        \ уменьшить количество товара, если он:\n\n* добавлен по акции;\n* составляет 99% стоимости заказа;\n* единственный\
        \ товар в заказе.\n\nВ таком случае отмените заказ — в методе [PUT v2/campaigns/{campaignId}/orders/{orderId}/status](../../reference/orders/updateOrderStatus.md)\
        \ передайте статус заказа `CANCELLED` с причиной отмены `SHOP_FAILED`.\n\n### Как вернутся деньги {#money}\n\n  Если\
        \ покупатель оплатил товар при оформлении, Маркет вернет ему деньги за удаленные из заказа товары в течение двух дней:\n\
        \n  * при оплате банковской картой — с момента, когда магазин переведет заказ в статус `SHIPPED`;\n\n  * при оплате\
        \ через :no-translate[Apple Pay] или :no-translate[Google Pay] — с момента, когда магазин удалит товар из заказа.\n\
        \n{% endcut %}\n\n{% include notitle [limit](../../_auto/method_limits/updateOrderItems.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrderItemRequest'
      responses:
        '200':
          description: Маркет успешно обработал ваш запрос. Выходные данные не ожидаются.
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/status:
    put:
      x-resource-id: 30
      tags:
      - orders
      - fbs
      - dbs
      - laas
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: updateOrderStatus
      summary: Изменение статуса одного заказа
      description: '{% include notitle [access](../../_auto/method_scopes/updateOrderStatus.md) %}


        Изменяет статус заказа. Возможные изменения статусов:


        * Если магазин подтвердил и подготовил заказ к отправке, то заказ из статуса `"status": "PROCESSING"` и этапа обработки
        `"substatus": "STARTED"` нужно перевести в статус `"status": "PROCESSING"` и этап обработки `"substatus": "READY_TO_SHIP"`.

        * Если магазин подтвердил заказ, но не может его выполнить (например, товар числится в базе, но отсутствует на складе
        или нет нужного цвета), то заказ из статуса `"status": "PROCESSING"` и этапа обработки `"substatus": "STARTED"` нужно
        перевести в статус `"status": "CANCELLED"` с причиной отмены заказа `"substatus": "SHOP_FAILED"`.

        * Если магазин подготовил заказ к отгрузке, но не может его выполнить (например, последний товар был поврежден или
        оказался с браком), то заказ из статуса `"status": "PROCESSING"` и этапа обработки `"substatus": "READY_TO_SHIP"`
        нужно перевести в статус `"status": "CANCELLED"` с причиной отмены заказа `"substatus": "SHOP_FAILED"`.


        Полная информация о статусной модели DBS-заказов: [Как изменяются статусы заказов](../../concepts/dbs-order-status-model.md).


        {% cut "**Как подтвердить LaaS-заказ**" %}


        Для подтверждения черновика заказа передайте статус `"status": "PROCESSING"` с подстатусом `"substatus": "STARTED"`.


        Подтверждение заказа, созданного с параметром `draft` равным `false`, не требуется.


        {% endcut %}


        {% cut "**Как отменить LaaS-заказ**" %}


        Передайте статус `"status": "CANCELLED"` с причиной отмены заказа `"substatus": "SHOP_FAILED"`.


        При успешном выполнении запроса отмена произойдет через некоторое время. [Как проверить статус операции](../../reference/operations/getOperations.md)


        {% endcut %}


        {% include notitle [limit](../../_auto/method_limits/updateOrderStatus.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrderStatusRequest'
      responses:
        '200':
          description: В случае успешного изменения статуса заказа возвращается обновленная информация о заказе.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateOrderStatusResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/status-update:
    post:
      x-resource-id: 117
      tags:
      - orders
      - fbs
      - dbs
      - laas
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.result.orders
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: updateOrderStatuses
      summary: Изменение статусов нескольких заказов
      description: '{% include notitle [access](../../_auto/method_scopes/updateOrderStatuses.md) %}


        Изменяет статусы нескольких заказов.


        Возможные изменения статусов:


        * Если магазин подтвердил и подготовил заказ к отправке, то заказ из статуса `"status": "PROCESSING"`и этапа обработки
        `"substatus": "STARTED"` нужно перевести в статус `"status": "PROCESSING"` и этап обработки `"substatus": "READY_TO_SHIP"`.

        * Если магазин подтвердил заказ, но не может его выполнить (например, товар числится в базе, но отсутствует на складе
        или нет нужного цвета), то заказ из статуса `"status": "PROCESSING"` и этапа обработки `"substatus": "STARTED"` нужно
        перевести в статус `"status": "CANCELLED"` с причиной отмены заказа `"substatus": "SHOP_FAILED"`.

        * Если магазин подготовил заказ к отгрузке, но не может его выполнить (например, последний товар был поврежден или
        оказался с браком), то заказ из статуса `"status": "PROCESSING"` и этапа обработки `"substatus": "READY_TO_SHIP"`
        нужно перевести в статус `"status": "CANCELLED"` с причиной отмены заказа `"substatus": "SHOP_FAILED"`.


        Полная информация о статусной модели DBS-заказов: [Как изменяются статусы заказов](../../concepts/dbs-order-status-model.md).


        {% cut "**Как подтвердить LaaS-заказ**" %}


        Для подтверждения черновика заказа передайте статус `"status": "PROCESSING"` с подстатусом `"substatus": "STARTED"`.


        Подтверждение заказа, созданного с параметром `draft` равным `false`, не требуется.


        {% endcut %}


        {% cut "**Как отменить LaaS-заказ**" %}


        Передайте статус `"status": "CANCELLED"` с причиной отмены заказа `"substatus": "SHOP_FAILED"`.


        При успешном выполнении запроса отмена произойдет через некоторое время. [Как проверить статус операции](../../reference/operations/getOperations.md)


        {% endcut %}


        {% include notitle [limit](../../_auto/method_limits/updateOrderStatuses.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrderStatusesRequest'
      responses:
        '200':
          description: Возвращается информация об обновленных статусах заказов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateOrderStatusesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes:
    put:
      x-resource-id: 107
      tags:
      - orders
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-05'
        replacement-operation-id: setOrderBoxLayout
      operationId: setOrderShipmentBoxes
      summary: Передача количества грузовых мест в заказе
      description: "{% include notitle [access](../../_auto/method_scopes/setOrderShipmentBoxes.md) %}\n\nОтгружаемый Маркету\
        \ заказ может не влезть в одну коробку или упаковку — в этом случае получается, что он занимает несколько грузовых\
        \ мест.\n\nКоличество грузовых мест нужно обязательно передавать Маркету, если оно не равно 1. Это делается перед\
        \ переводом его в статус **Готов к отгрузке**. Подробно о том, что в какой момент нужно передавать, рассказано в [пошаговой\
        \ инструкции](../../step-by-step/fbs.md).\n\nМетод устроен немного нестандартно: количество задается длиной массива\
        \ пустых объектов.\n\nРаньше метод требовал передачи большего количества данных. Запросы, оформленные по старому образцу,\
        \ работают, но лучше делать по-новому.\n\n{% cut \"Как было раньше\" %}\n\nСтруктура тела PUT-запроса:\n\n```text\
        \ translate=no\n{\n  \"boxes\":\n  [\n    {\n      \"fulfilmentId\": \"{string}\",\n      \"weight\": {int64},\n \
        \     \"width\": {int64},\n      \"height\": {int64},\n      \"depth\": {int64},\n      \"items\":\n      [\n    \
        \    {\n          \"id\": {int64},\n          \"count\": {int32}\n        },\n        ...\n      ]\n    },\n    ...\n\
        \  ]\n}\n```\n| **Параметр**  | **Тип**  | **Значение**  |\n| ----------- | ----------- | ----------- |\n| `boxes`\
        \       |           | Список грузовых мест.       |\n\n**Параметры, вложенные в `boxes`**\n| **Параметр**  | **Тип**\
        \  | **Значение**  |\n| ----------- | ----------- | ----------- |\n| `fulfilmentId`       |  :no-translate[String]\
        \   | Идентификатор грузового места в системе магазина. Сформируйте идентификатор по шаблону: `номер заказа на Маркете-номер\
        \ грузового места`. Например, `7206821‑1, 7206821‑2` и т. д. |\n| `weight`       | :no-translate[Int64]        | Масса\
        \ брутто грузового места (суммарная масса упаковки и содержимого) в граммах. |\n| `width`       | :no-translate[Int64]\
        \   | Ширина грузового места в сантиметрах.       |\n| `height`       | :no-translate[Int64]   | Высота грузового\
        \ места в сантиметрах.       |\n| `depth`       | :no-translate[Int64]   | Глубина грузового места в сантиметрах.\
        \        |\n| `items`       | :no-translate[Int64]   | Список товаров в грузовом месте.       |\n\n**Параметры, вложенные\
        \ в `items`**\n| **Параметр**  | **Тип**  | **Значение**  |\n| ----------- | ----------- | ----------- |\n| `id` \
        \      | :no-translate[Int64]     | Идентификатор товара в рамках заказа.   |\n| `count`    | :no-translate[Int32]\
        \     | Количество единиц товара в грузовом месте.       |\n\n{% endcut %}\n\n{% include notitle [limit](../../_auto/method_limits/setOrderShipmentBoxes.md)\
        \ %}\n"
      deprecated: true
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ShipmentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetOrderShipmentBoxesRequest'
      responses:
        '200':
          description: Имеет значение только тип ответа. Если ответ `ОК`, количество грузомест записано.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetOrderShipmentBoxesResponse'
        '400':
          $ref: '#/components/responses/400'
          description: Для указанного заказа сейчас нельзя задать количество грузомест, так как у заказа неподходящий статус.
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/cancellation/accept:
    put:
      x-resource-id: 137
      tags:
      - orders
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: hours
          period-duration: 1
        medium:
          amount: 500
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: acceptOrderCancellation
      summary: Отмена заказа покупателем
      description: "{% include notitle [access](../../_auto/method_scopes/acceptOrderCancellation.md) %}\n\nПодтверждает или\
        \ отклоняет заявку покупателя на отмену заказа, который передан службе доставки.\n\nПокупатель может отменить заказ\
        \ в течение его обработки или доставки. Если заказ еще обрабатывается (статус `PROCESSING`), вам не нужно подтверждать\
        \ отмену заказа — он будет отменен автоматически.\n\nЕсли заказ уже передан службе доставки (статус `DELIVERY` или\
        \ `PICKUP`) и пользователь отменил его, вы можете предупредить службу об отмене в течение 48 часов.\n\n  * Служба\
        \ доставки узнала об отмене до передачи заказа покупателю — подтвердите отмену с помощью запроса [PUT v2/campaigns/{campaignId}/orders/{orderId}/cancellation/accept](../../reference/orders/acceptOrderCancellation.md).\n\
        \  * Заказ уже доставлен — отклоните отмену с помощью этого же запроса. Тогда у покупателя останется заказ, и деньги\
        \ за него возвращаться не будут.\n\n**Как узнать об отмененных заказах:**\n\n  * Передайте параметр `onlyWaitingForCancellationApprove`\
        \ в запросе [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).\n  * В кабинете\
        \ или через почту — на нее придет уведомление об отмене.\n  * Подключите API-уведомления. Маркет отправит вам запрос\
        \ [POST notification](../../push-notifications/reference/sendNotification.md), когда появится новая заявка на отмену\
        \ заказа. [{#T}](../../push-notifications/index.md)\n\nЕсли в течение 48 часов вы не подтвердите или отклоните отмену,\
        \ заказ будет отменен автоматически.\n\n{% include notitle [limit](../../_auto/method_limits/acceptOrderCancellation.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AcceptOrderCancellationRequest'
      responses:
        '200':
          description: Ответ на отмену заказа был успешно передан.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/deliverDigitalGoods:
    post:
      x-resource-id: 129
      tags:
      - orders
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-resource-limit-config:
        path: resp
        parallel-limit-value: 6
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: provideOrderDigitalCodes
      summary: Передача ключей цифровых товаров
      description: "{% include notitle [access](../../_auto/method_scopes/provideOrderDigitalCodes.md) %}\n\nПередает ключи\
        \ цифровых товаров, которые покупатель заказал и оплатил. После выполнения запроса Маркет отправит ему письмо с ключами\
        \ и инструкциями по активации. Если письмо будет доставлено, Маркет переведет заказ в финальный статус `DELIVERED`.\n\
        \n{% note tip \"После передачи кода покупателю статус заказа изменится не сразу\" %}\n\nПодключите API-уведомления\
        \ — Маркет отправит вам запрос [POST notification](../../push-notifications/reference/sendNotification.md), когда\
        \ заказ перейдет в статус `DELIVERED`.\n\n[{#T}](../../push-notifications/index.md)\n\n{% endnote %}\n\nКлюч нужно\
        \ передать в течение 30 минут после перехода заказа в статус `PROCESSING`.\n\nЕсли в один заказ входят несколько ключей,\
        \ передавайте их все в одном запросе.\n\nКаждый товар с уникальным `id` передавайте в виде отдельного элемента в массиве\
        \ `items`, а ключи товара — в массиве `codes`.\n\n{% cut \"Пример\" %}\n\n```json translate=no\n{\n  \"items\": [\n\
        \    {\n      \"id\": 1,\n      \"codes\": [\n        \"code1\", \"code2\", \"code3\"\n      ],\n      \"slip\": \"\
        slip\",\n      \"activate_till\": \"2025-02-18\"\n    },\n    {\n      \"id\": 2,\n      \"codes\": [\n        \"\
        code4\", \"code5\", \"code6\"\n      ],\n      \"slip\": \"slip\",\n      \"activate_till\": \"2025-02-18\"\n    }\n\
        \  ]\n}\n```\n{% endcut %}\n\n{% include notitle [limit](../../_auto/method_limits/provideOrderDigitalCodes.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvideOrderDigitalCodesRequest'
      responses:
        '200':
          description: 'Пустой ответ.


            {% note warning "Ответ `200` сам по себе не значит, что ключи переданы покупателю" %}


            Если письмо с ключами удалось доставить, Маркет переведет заказ в финальный статус `DELIVERED`.


            Статус заказа можно узнать с помощью метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).


            {% endnote %}

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/boxes:
    put:
      x-resource-id: 206
      tags:
      - orders
      - fbs
      - express
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: setOrderBoxLayout
      summary: Подготовка заказа
      description: "{% include notitle [access](../../_auto/method_scopes/setOrderBoxLayout.md) %}\n\n{% note tip \"Подходит\
        \ и для DBS\" %}\n\nЗапрос предназначен для работы с FBS-заказами, но вы можете использовать его для обработки DBS-заказов,\
        \ если это удобно.\n\n{% endnote %}\n\nПозволяет выполнить три операции:\n\n* передать Маркету информацию о распределении\
        \ товаров по коробкам;\n* передать Маркету коды маркировки для товаров;\n* удалить товар из заказа, если его не оказалось\
        \ на складе.\n\nЕсли нужно что-то поправить в переданных данных, просто повторите запрос — это можно делать сколько\
        \ угодно раз до перевода заказа в статус **Готов к отгрузке**. ⚠️ Если вы меняете раскладку уже после печати и расклейки\
        \ ярлыков, не забудьте перепечатать их и наклеить заново.\n\n{% cut \"Как передать информацию о распределении товаров\"\
        \ %}\n\nВ этом запросе вам нужно передать Маркету список коробок и указать, какие именно товары лежат в каждой из\
        \ них. Коробки могут быть двух типов:\n\n* **Содержащие товары целиком.** Такая коробка может содержать сколько угодно\
        \ единиц любых товаров.\n\n* **Содержащие часть товара.** Такие коробки содержат по одной части одного товара. Например,\
        \ одна содержит внешний блок кондиционера, а другая — внутренний блок.\n\n⚠️ Одна коробка не может содержать и товары\
        \ целиком, и части товаров.\n\n{% endcut %}\n\n{% cut \"Как передавать коды маркировки и получать статус их проверки\"\
        \ %}\n\n{% note info \"Маркировка товаров в системе [«Честный ЗНАК»](https://честныйзнак.рф/) необязательна для заказов\
        \ от физических лиц\" %}\n\nДля заказов от бизнеса все еще нужно передавать коды маркировки.\n\n{% endnote %}\n\n\
        Если в заказе есть товары, подлежащие маркировке, в запросе нужно передать соответствующие уникальные коды. [Что такое\
        \ маркировка](https://yandex.ru/support/marketplace/orders/cz.html)\n\nПринимаются коды следующих типов:\n\n* Коды\
        \ в системе [«Честный ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов :no-translate[Market\
        \ Yandex Go]).\n* УИН для ювелирных изделий.\n* РНПТ и ГТД для импортных прослеживаемых товаров.\n\nДля каждой позиции\
        \ в заказе, требующей маркировки, нужно передать список кодов — по одному для каждой единицы товара. Например, если\
        \ в заказе две пары тапочек и одна пара туфель, получится список из двух кодов для первой позиции и список из одного\
        \ кода для второй.\n\nЕсли товар едет в нескольких коробках, код маркировки нужно передать для каждой из них.\n\n\
        {% note warning \"Если вы работаете по модели FBS, EXPRESS\" %}\n\nДля заказов, в которых есть ювелирные изделия или\
        \ товары с маркировкой в системе :no-translate[«Честный ЗНАК»], перевод в статус `READY_TO_SHIP` становится доступен,\
        \ только когда:\n\n1. Вы передадите Маркету :no-translate[УИНы] по каждому ювелирному изделию в заказе и коды в системе\
        \ :no-translate[«Честный ЗНАК»] по всем товарам в заказе, для которых обязательна эта маркировка.\n2. Все коды маркировки\
        \ успешно пройдут проверку. [Как получить статусы проверки](../../reference/orders/getOrderIdentifiersStatus.md)\n\
        \n{% endnote %}\n\n{% endcut %}\n\n{% cut \"Как удалить товар из заказа\" %}\n\nЧтобы удалить товар из заказа:\n\n\
        1. Добавьте в запрос `allowRemove: true`.\n2. Передайте распределение по коробкам без товара, который нужно удалить.\n\
        \n{% note warning \"Удаление нельзя отменить\" %}\n\nЭта операция необратима: покупатель сразу получит уведомление,\
        \ а состав заказа изменится.\n\n{% endnote %}\n\nЧтобы удалить позицию целиком, не передавайте соответствующий `OrderBoxLayoutItemDTO`.\
        \ Чтобы уменьшить количество товара, передайте уменьшенное значение в поле `fullCount`.\n\nНельзя удалить или уменьшить\
        \ количество товара, если он:\n\n* добавлен по акции;\n* составляет 99% стоимости заказа;\n* единственный товар в\
        \ заказе.\n\nЕсли вы не можете отгрузить такой товар, отмените заказ. Для этого отправьте запрос методом [PUT v2/campaigns/{campaignId}/orders/{orderId}/status](../../reference/orders/updateOrderStatus.md)\
        \ и передайте статус заказа `CANCELLED` с причиной отмены `SHOP_FAILED`.\n\n{% endcut %}\n\n{% note info \"Увеличить\
        \ заказ нельзя\" %}\n\nС помощью запроса нельзя увеличить количество одинаковых товаров, добавить новые товары в заказ\
        \ или заменить один товар другим.\n\n{% endnote %}\n\n## Примеры\n\n{% cut \"Товар умещается в коробку\" %}\n\nВот\
        \ как будет выглядеть запрос, если в одной коробке едут:\n\n  * три единицы одного товара, требующего маркировки;\n\
        \  * одна единица другого товара, не требущего маркировки.\n\n  ```json translate=no\n  {\n      \"boxes\": [\n  \
        \        {\n              \"items\": [\n                  {\n                      \"id\": 123456,\n             \
        \         \"fullCount\": 3,\n                      \"instances\": [\n                          {\n               \
        \               \"cis\": \"01030410947874432155Qbag!\\u001d93Zjqw\"\n                          },\n              \
        \            {\n                              \"cis\": \"010304109478gftJ14545762!\\u001dhGt264\"\n              \
        \            },\n                          {\n                              \"cis\": \"010304109478fRs28323ks23!\\\
        u001dhet201\"\n                          }\n                      ]\n                  },\n                  {\n \
        \                     \"id\": 654321,\n                      \"fullCount\": 1\n                  }\n             \
        \ ]\n          }\n      ]\n  }\n  ```\n\n{% endcut %}\n\n{% cut \"Товар едет в разных коробках\" %}\n\nВот как будет\
        \ выглядеть запрос, если товар едет в двух коробках:\n\n  ```json translate=no\n  {\n      \"boxes\": [\n        \
        \  {\n              \"items\": [\n                  {\n                      \"id\": 123456,\n                   \
        \   \"partialCount\": {\n                          \"current\": 1,\n                          \"total\": 2\n     \
        \                 },\n                      \"instances\": [\n                          {\n                      \
        \        \"cis\": \"01030410947874432155Qbag!\\u001d93Zjqw\"\n                          }\n                      ]\n\
        \                  }\n              ]\n          },\n          {\n              \"items\": [\n                  {\n\
        \                      \"id\": 123456,\n                      \"partialCount\": {\n                          \"current\"\
        : 2,\n                          \"total\": 2\n                      },\n                      \"instances\": [\n \
        \                         {\n                              \"cis\": \"01030410947874432155Qbag!\\u001d93Zjqw\"\n \
        \                         }\n                      ]\n                  }\n              ]\n          }\n      ]\n\
        \  }\n  ```\n\n{% endcut %}\n\n{% cut \"Одинаковые товары, где каждый едет в нескольких коробках\" %}\n\nВот как будет\
        \ выглядеть запрос, если каждый из двух одинаковых товаров едет в двух коробках:\n\n  ```json translate=no\n  {\n\
        \      \"boxes\": [\n          {\n              \"items\": [\n                  {\n                      \"id\": 123456,\n\
        \                      \"partialCount\": {\n                          \"current\": 1,\n                          \"\
        total\": 2\n                      },\n                      \"instances\": [\n                          {\n      \
        \                        \"cis\": \"01030410947874432155Qbag!\\u001d93Zjqw\"\n                          }\n      \
        \                ]\n                  }\n              ]\n          },\n          {\n              \"items\": [\n\
        \                  {\n                      \"id\": 123456,\n                      \"partialCount\": {\n         \
        \                 \"current\": 2,\n                          \"total\": 2\n                      },\n            \
        \          \"instances\": [\n                          {\n                              \"cis\": \"01030410947874432155Qbag!\\\
        u001d93Zjqw\"\n                          }\n                      ]\n                  }\n              ]\n      \
        \    },\n          {\n              \"items\": [\n                  {\n                      \"id\": 123456,\n   \
        \                   \"partialCount\": {\n                          \"current\": 1,\n                          \"total\"\
        : 2\n                      },\n                      \"instances\": [\n                          {\n             \
        \                 \"cis\": \"01030410947874432155Qbag!\\u001d93Zjqw\"\n                          }\n             \
        \         ]\n                  }\n              ]\n          },\n          {\n              \"items\": [\n       \
        \           {\n                      \"id\": 123456,\n                      \"partialCount\": {\n                \
        \          \"current\": 2,\n                          \"total\": 2\n                      },\n                   \
        \   \"instances\": [\n                          {\n                              \"cis\": \"01030410947874432155Qbag!\\\
        u001d93Zjqw\"\n                          }\n                      ]\n                  }\n              ]\n      \
        \    }\n      ]\n  }\n  ```\n\n{% endcut %}\n\n{% cut \"Разные товары в разных коробках\" %}\n\nВот как будет выглядеть\
        \ запрос, если два разных товара разложены по разным коробкам:\n\n  ```json translate=no\n  {\n      \"boxes\": [\n\
        \          {\n              \"items\": [\n                  {\n                      \"id\": 123456,\n           \
        \           \"fullCount\": 1\n                  }\n              ]\n          },\n          {\n              \"items\"\
        : [\n                  {\n                      \"id\": 654321,\n                      \"fullCount\": 1\n        \
        \          }\n              ]\n          }\n      ]\n  }\n  ```\n\n{% endcut %}\n\n{% include notitle [limit](../../_auto/method_limits/setOrderBoxLayout.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetOrderBoxLayoutRequest'
      responses:
        '200':
          description: 'В ответ придет переданная раскладка с идентификаторами коробок — они понадобятся для запроса ярлыков.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetOrderBoxLayoutResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/external-id:
    post:
      x-resource-id: 255
      tags:
      - orders
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: updateExternalOrderId
      summary: Передача внешнего идентификатора заказа
      description: '{% include notitle [access](../../_auto/method_scopes/updateExternalOrderId.md) %}


        Передает Маркету идентификатор заказа в системе магазина.


        Его нельзя передавать:


        * больше одного раза;

        * после перехода заказа в статус `PROCESSING` с подстатусом `READY_TO_SHIP`.


        Этот идентификатор отражается в документах отгрузки: акте приема-передачи, листе сборки и ярлыках.


        {% note alert "В штрихкодах могут быть только символы ASCII" %}


        Если во внешнем идентификаторе вы используете другие символы, на ярлыке в штрихкоде будет отображаться идентификатор
        заказа Маркета.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/updateExternalOrderId.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateExternalOrderIdRequest'
      responses:
        '200':
          description: Внешний идентификатор заказа успешно передан.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/identifiers/status:
    post:
      x-resource-id: 259
      tags:
      - orders
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getOrderIdentifiersStatus
      summary: Статусы проверки кодов маркировки
      description: "{% include notitle [access](../../_auto/method_scopes/getOrderIdentifiersStatus.md) %}\n\nВозвращает статусы\
        \ проверки кодов маркировки в заказе.\n\nЗаказ, в котором есть ювелирные изделия или товары с обязательной маркировкой\
        \ в системе [«Честный ЗНАК»](https://честныйзнак.рф/), можно перевести в статус `READY_TO_SHIP`, только когда:\n\n\
        1. В методе [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md) вы\
        \ передадите Маркету:\n\n    * [УИНы](:no-translate[*uin]) по каждому ювелирному изделию в заказе;\n\n    * коды маркировки\
        \ в системе :no-translate[«Честный ЗНАК»] по всем товарам в заказе, для которых она обязательна.\n2. Все коды маркировки\
        \ успешно пройдут проверку.\n\n{% include notitle [limit](../../_auto/method_limits/getOrderIdentifiersStatus.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Информация по проверке кодов маркировки.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrderIdentifiersStatusResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/orders/create:
    post:
      x-resource-id: 269
      tags:
      - orders
      - laas
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: createOrder
      summary: Создание заказа
      description: '{% include notitle [access](../../_auto/method_scopes/createOrder.md) %}


        Создает новый заказ, если на складе Маркета есть нужное количество товаров.


        Укажите `courierDelivery` для курьерской доставки или `pickupDelivery` для доставки в пункт выдачи. Не передавайте
        оба параметра одновременно.


        Значение параметра `draft`:


        * `true` — Маркет создаст заказ в статусе `RESERVED` и будет ждать подтверждения от магазина. Когда будете готовы,
        передайте статус `PROCESSING` с подстатусом `STARTED` в методе [PUT v2/campaigns/{campaignId}/orders/{orderId}/status](../../reference/orders/updateOrderStatus.md).
        Если не сделать это в течение часа после создания заказа, Маркет отменит его.

        * `false` — Маркет создаст заказ в статусе `PROCESSING` с подстатусом `STARTED`, подтверждение не требуется.


        Значение параметра `fake`:


        * `true` — тестовый заказ. Позволяет проверить работу магазина и его API на [тестовых заказах](../../concepts/sandbox.md).
        Такой заказ не будет отгружен и не влияет на остатки.

        * `false` — настоящий заказ.


        {% note warning "Перед вызовом метода" %}


        Получите доступные варианты доставки — [POST v2/campaigns/{campaignId}/delivery-options](../../reference/delivery-options/getDeliveryOptions.md).


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/createOrder.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrderRequest'
      responses:
        '200':
          description: Информация о созданных заказах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateOrderResponse'
        '400':
          $ref: '#/components/responses/400Orders'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/orders/update:
    post:
      x-resource-id: 273
      tags:
      - orders
      - laas
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: updateOrder
      summary: Изменение заказа
      description: '{% include notitle [access](../../_auto/method_scopes/updateOrder.md) %}


        Изменяет в заказе:


        * данные получателя;

        * интервал дат курьерской доставки.


        Передавайте только ту информацию, которую хотите изменить. При необходимости вы можете отредактировать и данные получателя,
        и интервал доставки одновременно.


        Заказ можно изменить в любом статусе до вручения покупателю или отмены (`DELIVERED` или `CANCELLED`).


        {% note info "Данные заказа обновляются не мгновенно" %}


        Изменения применяются в течение нескольких минут и только в случае успешного завершения операции. [Как проверить статус
        операции](../../reference/operations/getOperations.md)


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/updateOrder.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrderRequest'
      responses:
        '200':
          description: 'Информация об операции по изменению заказа.


            {% note warning "Ответ `200` не значит, что данные изменены" %}


            При успешном выполнении запроса это произойдет через некоторое время. [Как проверить статус операции](../../reference/operations/getOperations.md)


            {% endnote %}

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateOrderResponse'
        '400':
          $ref: '#/components/responses/400Orders'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/orders/update-options:
    post:
      x-resource-id: 280
      tags:
      - orders
      - laas
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getOrderUpdateOptions
      summary: Получение временных интервалов для изменения заказа
      description: '{% include notitle [access](../../_auto/method_scopes/getOrderUpdateOptions.md) %}


        Возвращает список доступных интервалов для изменения даты и времени курьерской доставки.


        {% include notitle [limit](../../_auto/method_limits/getOrderUpdateOptions.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOrderUpdateOptionsRequest'
      responses:
        '200':
          description: Список доступных интервалов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrderUpdateOptionsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/date:
    put:
      x-resource-id: 138
      tags:
      - order-delivery
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: setOrderDeliveryDate
      summary: Изменение даты доставки заказа
      description: '{% include notitle [access](../../_auto/method_scopes/setOrderDeliveryDate.md) %}


        Метод изменяет дату доставки заказа в статусе `PROCESSING` или `DELIVERY`. Для заказов с другими статусами дату доставки
        изменить нельзя.


        {% include notitle [limit](../../_auto/method_limits/setOrderDeliveryDate.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetOrderDeliveryDateRequest'
      responses:
        '200':
          description: Успешное изменение даты доставки.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/track:
    post:
      x-resource-id: 136
      tags:
      - order-delivery
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: setOrderDeliveryTrackCode
      summary: Передача трек‑номера посылки
      description: '{% include notitle [access](../../_auto/method_scopes/setOrderDeliveryTrackCode.md) %}


        Передает Маркету трек‑номер, по которому покупатель может отследить посылку со своим заказом через службу доставки.
        Если покупатели смогут узнать, на каком этапе доставки находятся их заказы, доверие покупателей к вашему магазину
        может возрасти.


        Передать трек‑номер можно, только если заказ находится в статусе `PROCESSING`, `DELIVERY` или `PICKUP`.


        {% include notitle [limit](../../_auto/method_limits/setOrderDeliveryTrackCode.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetOrderDeliveryTrackCodeRequest'
      responses:
        '200':
          description: Трек‑номер посылки и идентификатор службы доставки были успешно переданы.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/buyer:
    get:
      x-resource-id: 130
      tags:
      - order-delivery
      - dbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 3000
          period: hours
          period-duration: 1
        medium:
          amount: 3000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getOrderBuyerInfo
      summary: Информация о покупателе — физическом лице
      description: '{% include notitle [access](../../_auto/method_scopes/getOrderBuyerInfo.md) %}


        Возвращает информацию о покупателе по идентификатору заказа.


        {% note info "Как получить информацию о покупателе, который является юридическим лицом" %}


        Воспользуйтесь запросом [POST v2/campaigns/{campaignId}/orders/{orderId}/business-buyer](../../reference/order-business-information/getOrderBusinessBuyerInfo.md).


        {% endnote %}


        Получить данные можно, только если заказ находится в статусе `PROCESSING`, `DELIVERY` или `PICKUP`.


        {% include notitle [limit](../../_auto/method_limits/getOrderBuyerInfo.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Информация о покупателе.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrderBuyerInfoResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/verifyEac:
    put:
      x-resource-id: 164
      tags:
      - order-delivery
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: verifyOrderEac
      summary: Передача кода подтверждения
      description: "{% include notitle [access](../../_auto/method_scopes/verifyOrderEac.md) %}\n\nОтправляет Маркету код\
        \ подтверждения для его проверки.\n\n**Если у магазина настроена работа с кодами подтверждения:**\n\n  В параметре\
        \ `delivery`, вложенном в `order`, возвращается параметр `eacType` с типом `Enum` (тип кода подтверждения для передачи\
        \ заказа) в методах:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md);\n\
        \  * [PUT v2/campaigns/{campaignId}/orders/{orderId}/status](../../reference/orders/updateOrderStatus.md).\n\n  Возможные\
        \ значения:\n\n  * `MERCHANT_TO_COURIER` (временно не возвращается) — продавец передает код курьеру для получения\
        \ невыкупа;\n  * `COURIER_TO_MERCHANT` — курьер передает код продавцу для получения заказа.\n\n  Параметр `eacType`\
        \ возвращается при статусах заказа `COURIER_FOUND`, `COURIER_ARRIVED_TO_SENDER` и `DELIVERY_SERVICE_UNDELIVERED`.\
        \ Если заказ в других статусах, параметр может отсутствовать.\n\n{% include notitle [limit](../../_auto/method_limits/verifyOrderEac.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyOrderEacRequest'
      responses:
        '200':
          description: Проверка кода выполнена успешно.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifyOrderEacResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/storage-limit:
    put:
      x-resource-id: 154
      tags:
      - order-delivery
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: updateOrderStorageLimit
      summary: Продление срока хранения заказа
      description: '{% include notitle [access](../../_auto/method_scopes/updateOrderStorageLimit.md) %}


        Продлевает срок хранения заказа в пункте выдачи продавца.


        Заказ должен быть в статусе `PICKUP`. Продлить срок можно только один раз, не больше чем на 30 дней.


        Новый срок хранения можно получить в параметре `outletStorageLimitDate` в ответе метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).


        {% include notitle [limit](../../_auto/method_limits/updateOrderStorageLimit.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrderStorageLimitRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label:
    get:
      x-resource-id: 118
      tags:
      - order-labels
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateOrderLabel
      summary: Готовый ярлык‑наклейка для коробки в заказе
      description: '{% include notitle [access](../../_auto/method_scopes/generateOrderLabel.md) %}


        Формирует ярлык‑наклейку для коробки в заказе и возвращает ярлык в PDF‑файле.


        Чтобы на ярлыке отображался внешний идентификатор заказа, передайте его в методе [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).


        {% include notitle [limit](../../_auto/method_limits/generateOrderLabel.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ShipmentId-2'
      - $ref: '#/components/parameters/BoxId'
      - $ref: '#/components/parameters/PageFormat'
      responses:
        '200':
          description: PDF‑файл с ярлыками для коробки. Файл содержит одну страницу с ярлыком.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/labels:
    get:
      x-resource-id: 119
      tags:
      - order-labels
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateOrderLabels
      summary: Готовые ярлыки‑наклейки на все коробки в одном заказе
      description: '{% include notitle [access](../../_auto/method_scopes/generateOrderLabels.md) %}


        Возвращает PDF-файл с ярлыками, которые нужно наклеить на коробки перед отгрузкой. Подробно о том, зачем они нужны
        и как выглядят, рассказано [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/orders/fbs/packaging/marking.html).


        На вход нужно передать идентификатор заказа и один необязательный параметр, который управляет версткой PDF-файла.


        Чтобы на ярлыке отображался внешний идентификатор заказа, передайте его в методе [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).


        {% include notitle [limit](../../_auto/method_limits/generateOrderLabels.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/PageFormat'
      responses:
        '200':
          description: PDF‑файл с ярлыками на все коробки.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/delivery/labels/data:
    get:
      x-resource-id: 120
      tags:
      - order-labels
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getOrderLabelsData
      summary: Данные для самостоятельного изготовления ярлыков
      description: '{% include notitle [access](../../_auto/method_scopes/getOrderLabelsData.md) %}


        Возвращает информацию на ярлыках, которые клеятся на коробки в заказе.


        {% include notitle [limit](../../_auto/method_limits/getOrderLabelsData.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Информация для печати ярлыков.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrderLabelsDataResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/business-buyer:
    post:
      x-resource-id: 214
      tags:
      - order-business-information
      - fbs
      - fby
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 3000
          period: hours
          period-duration: 1
        medium:
          amount: 3000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getOrderBusinessBuyerInfo
      summary: Информация о покупателе — юридическом лице
      description: '{% include notitle [access](../../_auto/method_scopes/getOrderBusinessBuyerInfo.md) %}


        Возвращает информацию о покупателе по идентификатору заказа.


        {% note info "Как получить информацию о покупателе, который является физическим лицом" %}


        Воспользуйтесь запросом [GET v2/campaigns/{campaignId}/orders/{orderId}/buyer](../../reference/order-delivery/getOrderBuyerInfo.md).


        {% endnote %}


        Получить данные можно, только если заказ находится в статусе `PROCESSING`, `DELIVERY`, `PICKUP` или `DELIVERED`.


        {% include notitle [limit](../../_auto/method_limits/getOrderBusinessBuyerInfo.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Информация о покупателе.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetBusinessBuyerInfoResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/documents:
    post:
      x-resource-id: 215
      tags:
      - order-business-information
      - fbs
      - fby
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 3000
          period: hours
          period-duration: 1
        medium:
          amount: 3000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getOrderBusinessDocumentsInfo
      summary: Информация о документах
      description: '{% include notitle [access](../../_auto/method_scopes/getOrderBusinessDocumentsInfo.md) %}


        Возвращает информацию о документах по идентификатору заказа.


        Получить данные можно после того, как заказ перейдет в статус `DELIVERED`.


        {% include notitle [limit](../../_auto/method_limits/getOrderBusinessDocumentsInfo.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Информация о документах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetBusinessDocumentsInfoResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/returns:
    get:
      x-resource-id: 148
      tags:
      - returns
      - fbs
      - dbs
      - express
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getReturns
      summary: Список невыкупов и возвратов
      description: '{% include notitle [access](../../_auto/method_scopes/getReturns.md) %}


        Получает список невыкупов и возвратов.


        Чтобы получить информацию по одному невыкупу или возврату, выполните запрос [GET v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}](../../reference/returns/getReturn.md).


        {% note tip "Подключите API-уведомления" %}


        Маркет отправит вам запрос [POST notification](../../push-notifications/reference/sendNotification.md), когда появится
        новый невыкуп или возврат.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getReturns.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit100Truncate'
      - description: 'Идентификаторы заказов — для фильтрации результатов.


          Несколько идентификаторов перечисляются через запятую без пробела.

          '
        in: query
        name: orderIds
        required: false
        schema:
          type: array
          maxItems: 50
          uniqueItems: true
          items:
            type: integer
            format: int64
            example: 123543
      - description: 'Фильтр по статусам возврата денег за возвраты.


          Несколько статусов перечисляются через запятую.

          '
        in: query
        name: statuses
        required: false
        example:
        - STARTED_BY_USER
        - WAITING_FOR_DECISION
        schema:
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/RefundStatusType'
      - description: 'Фильтр по логистическим статусам невыкупов и возвратов.


          Несколько статусов перечисляются через запятую.

          '
        in: query
        name: shipmentStatuses
        required: false
        example:
        - READY_FOR_PICKUP
        - IN_TRANSIT
        schema:
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ReturnShipmentStatusType'
      - description: 'Тип заказа для фильтрации:


          * `UNREDEEMED` — невыкуп.


          * `RETURN` — возврат.


          Если не указать, в ответе будут и невыкупы, и возвраты.

          '
        in: query
        name: type
        required: false
        schema:
          $ref: '#/components/schemas/ReturnType'
      - description: 'Начальная дата для фильтрации невыкупов или возвратов по дате обновления.


          Формат: `ГГГГ-ММ-ДД`.

          '
        in: query
        name: fromDate
        required: false
        example: '2022-10-31'
        schema:
          type: string
          format: date
      - description: 'Конечная дата для фильтрации невыкупов или возвратов по дате обновления.


          Формат: `ГГГГ-ММ-ДД`.

          '
        in: query
        name: toDate
        required: false
        example: '2022-11-30'
        schema:
          type: string
          format: date
      - description: '{% note warning "Вместо него используйте `fromDate`." %}


           


          {% endnote %}


          Начальная дата для фильтрации невыкупов или возвратов по дате обновления.

          '
        deprecated: true
        in: query
        name: from_date
        required: false
        example: '2022-10-31'
        schema:
          type: string
          format: date
      - description: '{% note warning "Вместо него используйте `toDate`." %}


           


          {% endnote %}


          Конечная дата для фильтрации невыкупов или возвратов по дате обновления.

          '
        deprecated: true
        in: query
        name: to_date
        required: false
        example: '2022-11-30'
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Постраничные невыкупы или возвраты магазина.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetReturnsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}:
    get:
      x-resource-id: 159
      tags:
      - returns
      - fbs
      - dbs
      - express
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 7000
          period: hours
          period-duration: 1
        medium:
          amount: 7000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getReturn
      summary: Информация о невыкупе или возврате
      description: '{% include notitle [access](../../_auto/method_scopes/getReturn.md) %}


        Получает информацию по одному невыкупу или возврату.


        {% note tip "Подключите API-уведомления" %}


        Маркет отправит вам запрос [POST notification](../../push-notifications/reference/sendNotification.md), когда появится
        новый невыкуп или возврат.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getReturn.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ReturnId'
      responses:
        '200':
          description: Детали невыкупа или возврата.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetReturnResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision:
    post:
      x-resource-id: 160
      tags:
      - returns
      - dbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-05'
        replacement-operation-id: submitReturnDecision
      operationId: setReturnDecision
      summary: Принятие или изменение решения по возврату
      description: '{% include notitle [access](../../_auto/method_scopes/setReturnDecision.md) %}


        Выбирает решение по возврату от покупателя. После этого для подтверждения решения нужно выполнить запрос [POST v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/submit](../../reference/returns/submitReturnDecision.md).


        {% include notitle [limit](../../_auto/method_limits/setReturnDecision.md) %}

        '
      deprecated: true
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ReturnId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetReturnDecisionRequest'
      responses:
        '200':
          description: Детали возврата.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/submit:
    post:
      x-resource-id: 161
      tags:
      - returns
      - dbs
      - fbs
      - express
      - fby
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: submitReturnDecision
      summary: Передача решения по возврату
      description: '{% include notitle [access](../../_auto/method_scopes/submitReturnDecision.md) %}


        Позволяет передать список решений по возврату.


        {% note info "Перед вызовом метода" %}


        Получите список доступных решений — [POST v1/businesses/{businessId}/returns/decisions](../../reference/returns/getReturnAvailableDecisions.md).


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/submitReturnDecision.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ReturnId'
      requestBody:
        required: false
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitReturnDecisionRequest'
      responses:
        '200':
          description: Статус выполнения операции.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/returns/decisions:
    post:
      x-resource-id: 287
      tags:
      - returns
      - dbs
      - fbs
      - express
      - fby
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getReturnAvailableDecisions
      summary: Получение возможных решений по возврату
      description: '{% include notitle [access](../../_auto/method_scopes/getReturnAvailableDecisions.md) %}


        Возвращает список доступных решений по возврату.


        {% include notitle [limit](../../_auto/method_limits/getReturnAvailableDecisions.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetReturnAvailableDecisionsRequest'
      responses:
        '200':
          description: Возможные решения по возврату.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetReturnAvailableDecisionsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/application:
    get:
      x-resource-id: 162
      tags:
      - returns
      - fbs
      - fby
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getReturnApplication
      summary: Получение заявления на возврат
      description: '{% include notitle [access](../../_auto/method_scopes/getReturnApplication.md) %}


        Загружает заявление покупателя на возврат товара.


        {% include notitle [limit](../../_auto/method_limits/getReturnApplication.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ReturnId'
      responses:
        '200':
          description: Заявление на возврат.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}/decision/{itemId}/image/{imageHash}:
    get:
      x-resource-id: 163
      tags:
      - returns
      - fbs
      - dbs
      - express
      - fby
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getReturnPhoto
      summary: Получение фотографий товаров в возврате
      description: '{% include notitle [access](../../_auto/method_scopes/getReturnPhoto.md) %}


        Получает фотографии товаров, которые покупатель приложил к заявлению на возврат.


        Хеш изображения (`imageHash`) можно получить из ответов методов [GET v2/campaigns/{campaignId}/orders/{orderId}/returns/{returnId}](../../reference/returns/getReturn.md)
        и [GET v2/campaigns/{campaignId}/returns](../../reference/returns/getReturns.md) — в поле `images` решения по товару.


        Максимальный размер изображения — 50 МБ.


        Тип изображения можно определить по заголовку `Content-Type` в ответе.


        {% include notitle [limit](../../_auto/method_limits/getReturnPhoto.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OrderId'
      - $ref: '#/components/parameters/ReturnId'
      - $ref: '#/components/parameters/ItemId'
      - $ref: '#/components/parameters/ImageHash'
      responses:
        '200':
          description: Фотография товаров.
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/returns/create:
    post:
      x-resource-id: 270
      tags:
      - returns
      - laas
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: createReturn
      summary: Создание возврата
      description: '{% include notitle [access](../../_auto/method_scopes/createReturn.md) %}


        Создает новый возврат.


        Это можно сделать только для заказа в статусе `DELIVERED`.


        {% note warning "Перед вызовом метода" %}


        Проверьте, подходят ли пункты выдачи для возврата указанных товаров, — [POST v1/campaigns/{campaignId}/return-delivery-options](../../reference/delivery-options/getReturnDeliveryOptions.md).


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/createReturn.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateReturnRequest'
      responses:
        '200':
          description: Информация о cозданном возврате.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateReturnResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/returns/cancel:
    post:
      x-resource-id: 271
      tags:
      - returns
      - laas
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: cancelReturn
      summary: Отмена возврата
      description: '{% include notitle [access](../../_auto/method_scopes/cancelReturn.md) %}


        Отменяет возврат.


        Это можно сделать только до принятия в пункте выдачи (`"shipmentStatus": "CREATED"`).


        {% note info "Возврат отменяется не мгновенно" %}


        Отмена возврата применяется в течение нескольких минут и только в случае успешного завершения операции. [Как проверить
        статус операции](../../reference/operations/getOperations.md)


        {% endnote %}


        {% note tip "Используйте этот метод в подобных ситуациях" %}


        Вы создали возврат, в котором указали 3 товара. Но покупатель передумал и решил вернуть только 2. Отмените возврат
        и создайте новый.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/cancelReturn.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelReturnRequest'
      responses:
        '200':
          description: 'Информация об операции по отмене возврата.


            {% note warning "Ответ `200` не значит, что возврат отменен" %}


            При успешном выполнении запроса это произойдет через некоторое время. [Как проверить статус операции](../../reference/operations/getOperations.md)


            {% endnote %}

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CancelReturnResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/regions:
    get:
      x-resource-id: 17
      tags:
      - regions
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.regions
        none:
          amount: 50000
          period: hours
          period-duration: 1
        medium:
          amount: 50000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: searchRegionsByName
      summary: Поиск регионов по их имени
      description: '{% include notitle [access](../../_auto/method_scopes/searchRegionsByName.md) %}


        Возвращает информацию о регионе, удовлетворяющем заданным в запросе условиям поиска.


        Если найдено несколько регионов, удовлетворяющих условиям поиска, возвращается информация по каждому найденному региону
        (но не более десяти регионов) для возможности определения нужного региона по родительским регионам.


        {% include notitle [limit](../../_auto/method_limits/searchRegionsByName.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/RegionName'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit20'
      responses:
        '200':
          description: Список найденных регионов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetRegionsResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/regions/{regionId}:
    get:
      x-resource-id: 18
      tags:
      - regions
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: searchRegionsById
      summary: Информация о регионе
      description: '{% include notitle [access](../../_auto/method_scopes/searchRegionsById.md) %}


        Возвращает информацию о регионе.


        {% include notitle [limit](../../_auto/method_limits/searchRegionsById.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/RegionId'
      responses:
        '200':
          description: Найденный регион.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetRegionByIdResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/regions/{regionId}/children:
    get:
      x-resource-id: 31
      tags:
      - regions
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.regions.children
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: searchRegionChildren
      summary: Информация о дочерних регионах
      description: '{% include notitle [access](../../_auto/method_scopes/searchRegionChildren.md) %}


        Возвращает информацию о регионах, являющихся дочерними по отношению к региону, идентификатор которого указан в запросе.


        {% include notitle [limit](../../_auto/method_limits/searchRegionChildren.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/RegionId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50TruncateWithoutDefault'
      - $ref: '#/components/parameters/PageNum'
      - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: Регионы, являющиеся дочерними к указанному в запросе.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetRegionWithChildrenResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/regions/countries:
    post:
      x-resource-id: 254
      tags:
      - regions
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getRegionsCodes
      summary: Список допустимых кодов стран
      description: '{% include notitle [access](../../_auto/method_scopes/getRegionsCodes.md) %}


        Возвращает список стран с их кодами в формате :no-translate[ISO 3166-1 alpha-2].


        Страна производства `countryCode` понадобится при продаже товаров из-за рубежа для бизнеса. [Инструкция](../../step-by-step/business-info.md)


        {% include notitle [limit](../../_auto/method_limits/getRegionsCodes.md) %}

        '
      responses:
        '200':
          description: Список стран с их кодами в формате :no-translate[ISO 3166-1 alpha-2].
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetRegionsCodesResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/shipments/reception-transfer-act:
    get:
      x-resource-id: 126
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: downloadShipmentReceptionTransferAct
      summary: Подтверждение ближайшей отгрузки и получение акта приема-передачи для нее
      description: "{% include notitle [access](../../_auto/method_scopes/downloadShipmentReceptionTransferAct.md) %}\n\n\
        Запрос подтверждает ближайшую отгрузку и возвращает акт приема-передачи в формате PDF.\n\nПодтверждение отгрузки доступно\
        \ только после того, как она сформирована, иначе метод вернет код `400` и ошибку :no-translate[\"Closest shipment\
        \ for reception transfer act generation not found.\"].\n\nПодробнее о приеме заказов и расписании отгрузок читайте\
        \ [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/fbs/settings/shipment#schedule).\n\
        \n{% note warning \"Экспресс‑доставка\" %}\n\nЕсли ваш магазин подключен к экспресс‑доставке и вы отгружаете заказы\
        \ курьерам [Яндекс Go](https://go.yandex/), подготавливать акт приема‑передачи не нужно.\n\n{% endnote %}\n\nВ акт\
        \ входят собранные и готовые к отправке заказы, которые отгружаются в сортировочный центр или пункт приема либо передаются\
        \ курьерам Маркета.\n\nПри формировании акта Маркет автоматически находит и подставляет в шаблон следующие данные:\n\
        \n{% cut \"Данные, из которых Маркет формирует акт\" %}\n\n#|\n|| **Данные в акте**\t                            \
        \     | **Описание**                                                                                             \
        \                                                                                                                \
        \                                            ||\n|| Отправитель\t                                     | Название вашего\
        \ юридического лица, указанное в кабинете продавца на Маркете.                                                   \
        \                                                                                                                \
        \                      ||\n|| Исполнитель                                         | Название юридического лица сортировочного\
        \ центра или службы доставки.                                                                                    \
        \                                                                                                            ||\n\
        || № отправления в системе заказчика                   | Внешний идентификатор заказа продавца, который можно передать\
        \ запросом [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).\
        \                                                                            ||\n|| № отправления в системе исполнителя\
        \ (субподрядчика) |\n  Идентификатор заказа на Маркете, как в выходных данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n\
        \n|| Объявленная ценность                                |\n  Общая сумма заказа без учета стоимости доставки, как\
        \ в выходных данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n\
        \n|| Стоимость всех товаров в заказе                     | Стоимость всех заказанных товаров.                    \
        \                                                                                                                \
        \                                                                                               ||\n\n|| Вес     \
        \                                            |\n  Масса брутто грузового места (суммарная масса упаковки и содержимого),\
        \ как в выходных данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n\
        \n|| Количество мест                                     |\n  Количество грузовых мест в заказе, как в выходных данных\
        \ запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n|#\n\n\
        {% endcut %}\n\n{% note info \"Электронная подпись акта\" %}\n\nЕсли вы указываете параметр `signatory`, акт приема-передачи\
        \ подписывается электронной подписью и становится электронным документом. В этом случае печатать и подписывать акт\
        \ вручную не требуется — он уже имеет юридическую силу в электронном виде.\n\nЕсли параметр `signatory` не указан,\
        \ акт нужно распечатать. В распечатанном акте укажите отправителя и исполнителя. Они должны подписать акт и указать\
        \ фамилию и инициалы рядом с подписью. При необходимости также заполните реквизиты доверенности.\n\nПодробнее о работе\
        \ с актами приема-передачи читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/fbs/process#act).\n\
        \n{% endnote %}\n\n{% include notitle [limit](../../_auto/method_limits/downloadShipmentReceptionTransferAct.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - description: Идентификатор склада.
        in: query
        name: warehouse_id
        required: false
        example: 123123
        schema:
          type: integer
          format: int32
          minimum: 1
      - description: "Логин пользователя в Яндекс ID, от имени которого будет подписываться электронный акт приема-передачи.\n\
          \nУказывается без `@yandex.ru`.\n\n{% note info \"Электронная подпись\" %}\n\nЕсли вы указываете параметр `signatory`,\
          \ акт приема-передачи подписывается электронной подписью и становится электронным документом. В этом случае печатать\
          \ и подписывать акт вручную не требуется — он уже имеет юридическую силу в электронном виде.\n\nПодробнее о работе\
          \ с актами приема-передачи читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/fbs/process#act).\n\
          \n{% endnote %}\n\nГде найти логин:\n\n* на странице [Яндекс ID](https://id.yandex.ru);\n* в [кабинете продавца\
          \ на Маркете](https://partner.market.yandex.ru/):\n\n* в правом верхнем углу под иконкой пользователя;\n  * на странице\
          \ **Настройки** → **Сотрудники и доступы**.\n"
        in: query
        name: signatory
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Акт приема-передачи в формате :no-translate[PDF].
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с отгрузками](../../concepts/error-codes#shipments)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}:
    get:
      x-resource-id: 133
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getShipment
      summary: Получение информации об одной отгрузке
      description: '{% include notitle [access](../../_auto/method_scopes/getShipment.md) %}


        Возвращает информацию об отгрузке по ее идентификатору.


        {% include notitle [limit](../../_auto/method_limits/getShipment.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      - $ref: '#/components/parameters/CancelledOrders'
      responses:
        '200':
          description: Найденная отгрузка.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetShipmentResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/orders/info:
    get:
      x-resource-id: 139
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getShipmentOrdersInfo
      summary: Получение информации о возможности печати ярлыков
      description: '{% include notitle [access](../../_auto/method_scopes/getShipmentOrdersInfo.md) %}


        Возвращает информацию о возможности печати ярлыков-наклеек для заказов в отгрузке.


        {% include notitle [limit](../../_auto/method_limits/getShipmentOrdersInfo.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      responses:
        '200':
          description: Информация по годным/негодным для печати ярлыков заказам в отгрузке.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetShipmentOrdersInfoResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/confirm:
    post:
      x-resource-id: 132
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: confirmShipment
      summary: Подтверждение отгрузки
      description: '{% include notitle [access](../../_auto/method_scopes/confirmShipment.md) %}


        Подтверждает отгрузку товаров в сортировочный центр или пункт приема заказов. Действие доступно только после того,
        как отгрузка сформирована.


        График отгрузок настраивается отдельно для каждого склада в личном кабинете и недоступен через API.

        Проверить возможность подтверждения отгрузки можно с помощью метода [GET v2/campaigns/{campaignId}/shipments/{shipmentId}](../../reference/shipments/getShipment):
        среди доступных действий `availableActions` должно быть действие `CONFIRM`.

        До наступления времени подтверждения метод вернет код `400` и ошибку :no-translate["Cutoff time for shipments has
        not been reached yet"].


        Подробнее о приеме заказов и расписании отгрузок читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/fbs/settings/shipment#schedule).


        {% include notitle [limit](../../_auto/method_limits/confirmShipment.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmShipmentRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с отгрузками](../../concepts/error-codes#shipments)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/act:
    get:
      x-resource-id: 134
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: downloadShipmentAct
      summary: Получение акта приема-передачи
      description: "{% include notitle [access](../../_auto/method_scopes/downloadShipmentAct.md) %}\n\n{% note warning \"\
        Экспресс‑доставка\" %}\n\nЕсли ваш магазин подключен к экспресс‑доставке и вы отгружаете заказы курьерам [Яндекс Go](https://go.yandex/),\
        \ подготавливать акт приема‑передачи не нужно.\n\n{% endnote %}\n\nЗапрос формирует акт приема-передачи заказов, входящих\
        \ в отгрузку, и возвращает акт в формате PDF. В акте содержатся собранные и готовые к отправке заказы.\n\nМетод доступен\
        \ только для подтвержденной отгрузки. Сначала подтвердите отгрузку запросом [POST v2/campaigns/{campaignId}/shipments/{shipmentId}/confirm](../../reference/shipments/confirmShipment.md),\
        \ затем вызовите этот метод.\n\nПри формировании акта Маркет автоматически находит и подставляет в шаблон следующие\
        \ данные:\n\n{% cut \"Данные, из которых Маркет формирует акт\" %}\n\n#|\n|| **Данные в акте**                   \
        \                      | **Описание**                                                                            \
        \                                                                                                                \
        \                                                             ||\n|| Дата                                        \
        \              | Дата запроса.                                                                                   \
        \                                                                                                                \
        \                                                     ||\n|| Отправитель                                         \
        \      | Название вашего юридического лица, указанное в кабинете продавца на Маркете.                            \
        \                                                                                                                \
        \                                             ||\n|| Исполнитель                                               | Название\
        \ юридического лица сортировочного центра или службы доставки.                                                   \
        \                                                                                                                \
        \                             ||\n|| № отправления в системе заказчика                         | Внешний идентификатор\
        \ заказа продавца, который можно передать запросом [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).\
        \                                                                            ||\n|| № отправления в системе исполнителя\
        \ (субподрядчика)       |\n  Идентификатор заказа на Маркете, как в выходных данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n\
        \n|| Объявленная ценность                                      |\n  Общая сумма заказа без учета стоимости доставки,\
        \ как в выходных данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n\
        \n|| Вес                                                       |\n  Масса брутто грузового места (суммарная масса\
        \ упаковки и содержимого), как в выходных данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).||\n\
        \n|| Количество мест                                           |\n  Количество грузовых мест в заказе, как в выходных\
        \ данных запроса:\n\n  * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md). ||\n\
        |#\n\n{% endcut %}\n\nВ распечатанном акте укажите отправителя и исполнителя. Они должны подписать акт и указать фамилию\
        \ и инициалы рядом с подписью. При необходимости также заполните реквизиты доверенности.\n\n{% include notitle [limit](../../_auto/method_limits/downloadShipmentAct.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      responses:
        '200':
          description: Акт приема-передачи для отгрузки в формате :no-translate[PDF].
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с отгрузками](../../concepts/error-codes#shipments)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/inbound-act:
    get:
      x-resource-id: 147
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: downloadShipmentInboundAct
      summary: Получение фактического акта приема-передачи
      description: '{% include notitle [access](../../_auto/method_scopes/downloadShipmentInboundAct.md) %}


        Возвращает фактический акт приема-передачи для заданной отгрузки.


        Такой акт становится доступен спустя несколько часов после завершения отгрузки. Он может понадобиться, если после
        отгрузки обнаружатся расхождения.


        {% include notitle [limit](../../_auto/method_limits/downloadShipmentInboundAct.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      responses:
        '200':
          description: Акт в формате PDF.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/transportation-waybill:
    get:
      x-resource-id: 140
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: downloadShipmentTransportationWaybill
      summary: Получение транспортной накладной
      description: '{% include notitle [access](../../_auto/method_scopes/downloadShipmentTransportationWaybill.md) %}


        Возвращает транспортную накладную для заданной отгрузки, если Маркет забирает товары с вашего склада. Подробнее о
        таком способе отгрузки читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/fbs/settings/shipment#at-your-warehouse).


        Накладная не возвращается, если вы привозите товары в ПВЗ или сортировочный центр.


        {% include notitle [limit](../../_auto/method_limits/downloadShipmentTransportationWaybill.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      responses:
        '200':
          description: Транспортная накладная в формате :no-translate[XLSX].
          content:
            application/vnd.ms-excel:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/discrepancy-act:
    get:
      x-resource-id: 150
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: downloadShipmentDiscrepancyAct
      summary: Получение акта расхождений
      description: '{% include notitle [access](../../_auto/method_scopes/downloadShipmentDiscrepancyAct.md) %}


        Возвращает акт расхождений для заданной отгрузки.


        {% include notitle [limit](../../_auto/method_limits/downloadShipmentDiscrepancyAct.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      responses:
        '200':
          description: Акт расхождений в формате :no-translate[XLSX].
          content:
            application/vnd.ms-excel:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallets:
    put:
      x-resource-id: 178
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: setShipmentPalletsCount
      summary: Передача количества упаковок для доверительной приемки
      description: '{% include notitle [access](../../_auto/method_scopes/setShipmentPalletsCount.md) %}


        Передает Маркету количество упаковок в отгрузке для доверительной приемки. Подробнее о таком виде приемки читайте
        в [Справке Маркета для продавцов](https://yandex.ru/support/marketplace/orders/fbs/process.html#acceptance).


        {% note info "Как передавать упаковки" %}


        Передавайте количество упаковок, которые вы везете в отгрузке, а не сумму грузомест по заказам.


        **Пример:** в отгрузке 2 заказа, в каждом по 5 грузомест. Если вы везете их в 2 палетах — передайте в запросе `2`,
        а не `10`.


        {% endnote %}


        Получить PDF-файл с ярлыками для упаковок можно с помощью метода [GET v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallet/labels](../../reference/shipments/downloadShipmentPalletLabels.md).


        {% include notitle [limit](../../_auto/method_limits/setShipmentPalletsCount.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetShipmentPalletsCountRequest'
      responses:
        '200':
          description: Имеет значение только тип ответа. Если ответ `ОК`, количество упаковок записано.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallet/labels:
    get:
      x-resource-id: 180
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: downloadShipmentPalletLabels
      summary: Ярлыки для доверительной приемки
      description: '{% include notitle [access](../../_auto/method_scopes/downloadShipmentPalletLabels.md) %}


        PDF-файл с ярлыками на каждую упаковку в отгрузке для доверительной приемки. Подробнее о таком виде приемки читайте
        в [Справке Маркета для продавцов](https://yandex.ru/support/marketplace/orders/fbs/process.html#acceptance).


        Распечатайте по несколько копий каждого ярлыка: на одну упаковку нужно наклеить минимум 2 ярлыка с разных сторон.


        Количество упаковок в отгрузке передается в методе [PUT v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/pallets](../../reference/shipments/setShipmentPalletsCount.md).


        {% include notitle [limit](../../_auto/method_limits/downloadShipmentPalletLabels.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      - $ref: '#/components/parameters/ShipmentPalletLabelsPageFormat'
      responses:
        '200':
          description: PDF‑файл с ярлыками на все упаковки в отгрузке.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments:
    put:
      x-resource-id: 135
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: searchShipments
      summary: Получение информации о нескольких отгрузках
      description: '{% include notitle [access](../../_auto/method_scopes/searchShipments.md) %}


        Возвращает информацию об отгрузках по заданным параметрам:


        * дате;

        * статусу;

        * идентификаторам заказов.


        Результаты возвращаются постранично.


        {% include notitle [limit](../../_auto/method_limits/searchShipments.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit30'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchShipmentsRequest'
      responses:
        '200':
          description: Найденные отгрузки.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchShipmentsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/first-mile/shipments/{shipmentId}/orders/transfer:
    post:
      x-resource-id: 185
      tags:
      - shipments
      - fbs
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: transferOrdersFromShipment
      summary: Перенос заказов в следующую отгрузку
      description: '{% include notitle [access](../../_auto/method_scopes/transferOrdersFromShipment.md) %}


        Переносит указанные заказы из указанной отгрузки в следующую отгрузку. [Что такое отгрузка?](https://yandex.ru/support/marketplace/orders/fbs/process.html#ship)


        Используйте этот запрос, если не успеваете собрать и упаковать заказы вовремя.


        {% note warning "Такие переносы снижают индекс качества магазина" %}


        Этот запрос предназначен для исключительных случаев. Если вы будете переносить заказы слишком часто, магазин столкнется
        с ограничениями. [Что за ограничения?](https://yandex.ru/support/marketplace/quality/score/fbs.html)


        {% endnote %}


        Переносить заказы можно, если до формирования отгрузки осталось больше получаса.


        Перенос происходит не мгновенно, а занимает несколько минут.


        {% include notitle [limit](../../_auto/method_limits/transferOrdersFromShipment.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ShipmentId-3'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferOrdersFromShipmentRequest'
      responses:
        '200':
          description: Запрос на перенос заказов проверен и принят, и они будут перенесены спустя несколько минут.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
          description: 'Ошибка 400 обозначает, что в переданном вами списке заказов что-то не так — например, не все они относятся
            к заданной отгрузке. В сообщении об ошибке будут все подробности.


            {% note warning "Частичное выполнение запроса невозможно" %}


            Если в списке есть хотя бы один проблемный идентификатор, не будет перенесен ни один заказ из списка.


            {% endnote %}


             

            '
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-mappings/delete:
    post:
      x-resource-id: 171
      tags:
      - business-offer-mappings
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: deleteOffers
      summary: Удаление товаров из каталога
      description: '{% include notitle [access](../../_auto/method_scopes/deleteOffers.md) %}


        Удаляет товары из каталога.


        {% include notitle [limit](../../_auto/method_limits/deleteOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteOffersRequest'
      responses:
        '200':
          description: Если удалось удалить не все товары, с ответом 200 вернется список тех, что были в запросе, но остались
            в магазине.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteOffersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-mappings:
    post:
      x-resource-id: 169
      tags:
      - business-offer-mappings
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 600
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: getOfferMappings
      summary: Информация о товарах в каталоге
      description: '{% include notitle [access](../../_auto/method_scopes/getOfferMappings.md) %}


        Возвращает список товаров в каталоге, их категории на Маркете и характеристики каждого товара.


        Можно использовать тремя способами:

        * задать список интересующих SKU;

        * задать фильтр — в этом случае результаты возвращаются постранично;

        * не передавать тело запроса, чтобы получить список всех товаров в каталоге.


        Чтобы получить категорийные характеристики товаров, воспользуйтесь методом [POST v2/businesses/{businessId}/offer-cards](../../reference/content/getOfferCardsContentStatus.md).


        {% include notitle [limit](../../_auto/method_limits/getOfferMappings.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit100Truncate'
      - $ref: '#/components/parameters/CatalogLanguage'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOfferMappingsRequest'
      responses:
        '200':
          description: Информация о товарах в каталоге.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOfferMappingsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-mappings/update:
    post:
      x-resource-id: 170
      tags:
      - business-offer-mappings
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerMappings
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: updateOfferMappings
      summary: Добавление товаров в каталог и изменение информации о них
      description: "{% include notitle [access](../../_auto/method_scopes/updateOfferMappings.md) %}\n\nДобавляет товары в\
        \ каталог и передает:\n\n* их [листовые категории](*list-categories) на Маркете и категорийные характеристики;\n*\
        \ основные характеристики;\n* цены на товары в кабинете.\n\nТакже объединяет товары на карточке, редактирует и удаляет\
        \ информацию об уже добавленных товарах, в том числе цены в кабинете и категории товаров.\n\nСписок категорий Маркета\
        \ можно получить с помощью запроса [POST v2/categories/tree](../../reference/categories/getCategoriesTree.md), а характеристики\
        \ товаров по категориям с помощью [POST v2/category/{categoryId}/parameters](../../reference/content/getCategoryContentParameters.md).\n\
        \n{% cut \"Добавить новый товар\" %}\n\nПередайте его с новым идентификатором, который раньше никогда не использовался\
        \ в каталоге.\n\nОбязательно укажите параметры: `offerId`, `name`, `marketCategoryId`, `pictures`, `vendor`, `description`.\n\
        \nСтарайтесь сразу передать как можно больше информации — она потребуется Маркету для подбора подходящей карточки\
        \ или создания новой.\n\nЕсли известно, какой карточке на Маркете соответствует товар, можно сразу указать идентификатор\
        \ этой карточки (SKU на Маркете) в поле `marketSKU`.\n\n**Для продавцов Market Yandex Go:**\n\nКогда вы добавляете\
        \ товары в каталог, указывайте значения параметров `name` и `description` на русском языке. Чтобы на витрине они отображались\
        \ и на другом языке, еще раз выполните запрос `POST v2/businesses/{businessId}/offer-mappings/update`, где укажите:\n\
        \n  * язык в параметре `language`;\n  * значения параметров `name` и `description` на указанном языке.\n\n  Повторно\
        \ передавать остальные характеристики товара не нужно.\n\n{% endcut %}\n\n{% cut \"Изменить информацию о товаре\"\
        \ %}\n\nПередайте новые данные, указав в `offerId` SKU товара в вашей системе.\n\nПоля, в которых ничего не меняется,\
        \ можно не передавать.\n\n{% endcut %}\n\n{% cut \"Удалить переданные ранее параметры товара\" %}\n\nВ `deleteParameters`\
        \ укажите значения параметров, которые хотите удалить. Можно передать сразу несколько значений.\n\nДля параметров\
        \ с типом `string` также можно передать пустое значение.\n\n{% endcut %}\n\nПараметр `offerId` (SKU товара в вашей\
        \ системе) должен быть **уникальным** для всех товаров, которые вы передаете.\n\n{% note warning \"Правила использования\
        \ SKU\" %}\n\n* У каждого товара SKU должен быть свой.\n\n* Уже заданный SKU нельзя освободить и использовать заново\
        \ для другого товара. Каждый товар должен получать новый идентификатор, до того никогда не использовавшийся в вашем\
        \ каталоге.\n\nSKU товара можно изменить в кабинете продавца на Маркете. О том, как это сделать, читайте [в Справке\
        \ Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/assortment/operations/edit-sku).\n\n{% endnote\
        \ %}\n\n{% note info \"Данные в каталоге обновляются не мгновенно\" %}\n\nЭто занимает до нескольких минут.\n\n{%\
        \ endnote %}\n\n{% include notitle [limit](../../_auto/method_limits/updateOfferMappings.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/CatalogLanguage'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOfferMappingsRequest'
      responses:
        '200':
          description: 'Запрос выполнен корректно, данные обработаны.


            {% note warning "Ответ `200` сам по себе не значит, что переданные значения корректны" %}


            Обязательно посмотрите детали ответа: `status`, а также перечень ошибок (`results.errors`) и замечаний (`results.warnings`),
            если они есть.


            - Если хотя бы по одному товару вернулась ошибка (`results.errors`), поле `status` = `ERROR`. Изменения по всем
            переданным товарам не будут применены.

            - Если ошибок нет, но хотя бы по одному товару вернулось замечание (`results.warnings`), поле `status` = `OK`,
            и изменения будут применены.


            {% endnote %}

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateOfferMappingsResponse'
        '400':
          description: '⚠️ Даже если проблема связана всего с одним товаром в запросе, в каталог не отправится ни один.


            Запрос содержит неправильные данные. [Подробнее об ошибках при работе с товарами](../../concepts/error-codes#offers)
            и [об ошибках при работе с ценами](../../concepts/error-codes#prices)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-mappings/archive:
    post:
      x-resource-id: 205
      tags:
      - business-offer-mappings
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: addOffersToArchive
      summary: Добавление товаров в архив
      description: '{% include notitle [access](../../_auto/method_scopes/addOffersToArchive.md) %}


        Помещает товары в архив. Товары, помещенные в архив, скрыты с витрины во всех магазинах кабинета.


        {% note warning "В архив нельзя отправить товар, который хранится на складе Маркета" %}


        Вначале такой товар нужно распродать или вывезти.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/addOffersToArchive.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddOffersToArchiveRequest'
      responses:
        '200':
          description: 'Если некоторые товары добавить в архив не удалось, в ответе 200 будет их список.


            Список успешно добавленных товаров не возвращается.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddOffersToArchiveResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-mappings/unarchive:
    post:
      x-resource-id: 204
      tags:
      - business-offer-mappings
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: deleteOffersFromArchive
      summary: Удаление товаров из архива
      description: '{% include notitle [access](../../_auto/method_scopes/deleteOffersFromArchive.md) %}


        Восстанавливает товары из архива.


        {% include notitle [limit](../../_auto/method_limits/deleteOffersFromArchive.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteOffersFromArchiveRequest'
      responses:
        '200':
          description: 'Если некоторые товары восстановить из архива не удалось, в ответе 200 будет их список.


            Список успешно восстановленных товаров не возвращается.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteOffersFromArchiveResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/offer-mappings/barcodes/generate:
    post:
      x-resource-id: 281
      tags:
      - business-offer-mappings
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: generateOfferBarcodes
      summary: Генерация штрихкодов
      description: '{% include notitle [access](../../_auto/method_scopes/generateOfferBarcodes.md) %}


        Генерирует штрихкоды и присваивает их указанным товарам.


        Если у товара на упаковке уже есть штрихкод производителя, передайте его в параметре `barcodes` в методе [POST v2/businesses/{businessId}/offer-mappings/update](../../reference/business-offer-mappings/updateOfferMappings.md).
        Генерировать новый не нужно.


        {% include notitle [limit](../../_auto/method_limits/generateOfferBarcodes.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateOfferBarcodesRequest'
      responses:
        '200':
          description: 'Пустой ответ, если генерация успешно завершилась для всех переданных товаров.


            Или список товаров, для которых не удалось сгенерировать штрихкоды.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateOfferBarcodesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-prices/updates:
    post:
      x-resource-id: 177
      tags:
      - prices
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - pricing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offers
        none:
          amount: 10000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: updateBusinessPrices
      summary: Установка цен на товары для всех магазинов
      description: '{% include notitle [access](../../_auto/method_scopes/updateBusinessPrices.md) %}


        Устанавливает цены, которые действуют во всех магазинах. Чтобы получить рекомендации Маркета, касающиеся цен, выполните
        запрос [POST v2/businesses/{businessId}/offers/recommendations](../../reference/offers/getOfferRecommendations.md).


        При необходимости передавайте НДС с помощью параметра `vat` в запросе [POST v2/campaigns/{campaignId}/offers/update](../../reference/offers/updateCampaignOffers.md).


        {% note info "Данные в каталоге обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/updateBusinessPrices.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBusinessPricesRequest'
      responses:
        '200':
          description: Маркет принял информацию о новых ценах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с ценами](../../concepts/error-codes#prices)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/offer-prices/updates:
    post:
      x-resource-id: 64
      tags:
      - prices
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - pricing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offers
        parallel-limit-value: 12
        none:
          amount: 10000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: updatePrices
      summary: Установка цен на товары в конкретном магазине
      description: '{% include notitle [access](../../_auto/method_scopes/updatePrices.md) %}


        Устанавливает цены на товары в магазине. Чтобы получить рекомендации Маркета, касающиеся цен, выполните запрос [POST v2/businesses/{businessId}/offers/recommendations](../../reference/offers/getOfferRecommendations.md).


        {% note warning "Метод только для отдельных магазинов" %}


        Вам доступен этот метод, если в кабинете продавца на Маркете есть возможность установить уникальные цены в отдельных
        магазинах. Как это проверить — в методе [POST v2/businesses/{businessId}/settings](../../reference/businesses/getBusinessSettings.md)
        в параметре `onlyDefaultPrice` возвращается значение `false`.


        В ином случае используйте метод управления ценами, которые действуют во всех магазинах, — [POST v2/businesses/{businessId}/offer-prices/updates](../../reference/prices/updateBusinessPrices.md).


        {% endnote %}


        {% note info "Данные в каталоге обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/updatePrices.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePricesRequest'
      responses:
        '200':
          description: Маркет принял информацию о новых ценах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с ценами](../../concepts/error-codes#prices)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
          description: 'Возможны две причины такой ошибки.


            * В кабинете используются цены для всех магазинов. Установить цену для отдельного магазина не получится.

            * Магазин находится в процессе переноса в другой кабинет.

            '
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/offer-prices:
    get:
      x-resource-id: 63
      tags:
      - prices
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.result.offers
        none:
          amount: 150000
          period: minutes
          period-duration: 1
        medium:
          amount: 150000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-05'
        replacement-operation-id: getPricesByOfferIds
      operationId: getPrices
      summary: Список цен
      description: '{% include notitle [access](../../_auto/method_scopes/getPrices.md) %}


        Возвращает список цен, установленных вами на товары любым способом: например, через API или в файле с каталогом.


        Способы установки цен описаны [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/assortment/operations/prices.html).


        {% include notitle [limit](../../_auto/method_limits/getPrices.md) %}

        '
      deprecated: true
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500Truncate'
      - description: Фильтр по нахождению в архиве.
        in: query
        name: archived
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: Список всех товаров с установленными ценами.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPricesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    post:
      x-resource-id: 176
      tags:
      - prices
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.result.offers
        none:
          amount: 10000
          period: minutes
          period-duration: 1
        medium:
          amount: 100000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getPricesByOfferIds
      summary: Просмотр цен на указанные товары в конкретном магазине
      description: '{% include notitle [access](../../_auto/method_scopes/getPricesByOfferIds.md) %}


        Возвращает список цен на указанные товары в магазине.


        {% note warning "Метод только для отдельных магазинов" %}


        Используйте этот метод, только если в кабинете установлены уникальные цены в отдельных магазинах.


        Для просмотра цен, которые действуют во всех магазинах, используйте [POST v2/businesses/{businessId}/offer-mappings](../../reference/business-offer-mappings/getOfferMappings.md).


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getPricesByOfferIds.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500Truncate'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetPricesByOfferIdsRequest'
      responses:
        '200':
          description: Список товаров с установленными для заданного магазина ценами.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPricesByOfferIdsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-prices:
    post:
      x-resource-id: 261
      tags:
      - prices
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.result.offers
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getDefaultPrices
      summary: Просмотр цен на указанные товары во всех магазинах
      description: '{% include notitle [access](../../_auto/method_scopes/getDefaultPrices.md) %}


        Возвращает список цен, которые вы установили для всех магазинов любым способом. Например, через API или с помощью
        Excel-шаблона.


        О способах установки цен читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/assortment/operations/prices.html).


        {% include notitle [limit](../../_auto/method_limits/getDefaultPrices.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500Truncate'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetDefaultPricesRequest'
      responses:
        '200':
          description: Список всех товаров с установленными ценами.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetDefaultPricesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/price-quarantine:
    post:
      x-resource-id: 172
      tags:
      - price-quarantine
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getBusinessQuarantineOffers
      summary: Список товаров, находящихся в карантине по цене в кабинете
      description: '{% include notitle [access](../../_auto/method_scopes/getBusinessQuarantineOffers.md) %}


        Возвращает список товаров, которые находятся в карантине по цене, установленной для всех магазинов кабинета.


        Проверьте цену каждого из товаров, который попал в карантин. Если ошибки нет и цена правильная, подтвердите ее с помощью
        запроса [POST v2/businesses/{businessId}/price-quarantine/confirm](../../reference/price-quarantine/confirmBusinessPrices.md).
        Если цена в самом деле ошибочная, установите верную с помощью запроса [POST v2/businesses/{businessId}/offer-prices/updates](../../reference/prices/updateBusinessPrices.md).


        {% note info "Что такое карантин?" %}


        Товар попадает в карантин, если его цена меняется слишком резко или слишком сильно отличается от рыночной. [Подробнее](https://yandex.ru/support/marketplace/assortment/operations/prices.html#quarantine)


        {% endnote %}


        В запросе можно использовать фильтры.


        Результаты возвращаются постранично.


        {% include notitle [limit](../../_auto/method_limits/getBusinessQuarantineOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetQuarantineOffersRequest'
      responses:
        '200':
          description: Список товаров в карантине.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetQuarantineOffersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/price-quarantine/confirm:
    post:
      x-resource-id: 173
      tags:
      - price-quarantine
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - pricing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: confirmBusinessPrices
      summary: Удаление товара из карантина по цене в кабинете
      description: '{% include notitle [access](../../_auto/method_scopes/confirmBusinessPrices.md) %}


        Подтверждает во всех магазинах цену на товары, которые попали в карантин, и удаляет их из карантина.


        Товар попадает в карантин, если его цена меняется слишком резко. [Как настроить карантин](https://yandex.ru/support/marketplace/assortment/operations/prices.html#quarantine)


        Чтобы увидеть список товаров, которые попали в карантин, используйте запрос [POST v2/businesses/{businessId}/price-quarantine](getBusinessQuarantineOffers.md).


        {% include notitle [limit](../../_auto/method_limits/confirmBusinessPrices.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmPricesRequest'
      responses:
        '200':
          description: Ответ `200` обозначает, что цены подтверждены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/price-quarantine:
    post:
      x-resource-id: 174
      tags:
      - price-quarantine
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getCampaignQuarantineOffers
      summary: Список товаров, находящихся в карантине по цене в магазине
      description: '{% include notitle [access](../../_auto/method_scopes/getCampaignQuarantineOffers.md) %}


        Возвращает список товаров, которые находятся в карантине по цене, установленной в заданном магазине.


        Проверьте цену каждого из товаров, который попал в карантин. Если ошибки нет и цена правильная, подтвердите ее с помощью
        запроса [POST v2/campaigns/{campaignId}/price-quarantine/confirm](../../reference/price-quarantine/confirmCampaignPrices.md).
        Если цена в самом деле ошибочная, установите верную с помощью запроса [POST v2/campaigns/{campaignId}/offer-prices/updates](../../reference/prices/updatePrices.md).


        {% note info "Что такое карантин?" %}


        Товар попадает в карантин, если его цена меняется слишком резко или слишком сильно отличается от рыночной. [Подробнее](https://yandex.ru/support/marketplace/assortment/operations/prices.html#quarantine)


        {% endnote %}


        В запросе можно использовать фильтры.


        Результаты возвращаются постранично.


        {% include notitle [limit](../../_auto/method_limits/getCampaignQuarantineOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetQuarantineOffersRequest'
      responses:
        '200':
          description: Список товаров в карантине.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetQuarantineOffersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/price-quarantine/confirm:
    post:
      x-resource-id: 175
      tags:
      - price-quarantine
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - pricing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: confirmCampaignPrices
      summary: Удаление товара из карантина по цене в магазине
      description: '{% include notitle [access](../../_auto/method_scopes/confirmCampaignPrices.md) %}


        Подтверждает в заданном магазине цену на товары, которые попали в карантин, и удаляет их из карантина.


        Товар попадает в карантин, если его цена меняется слишком резко. [Как настроить карантин](https://yandex.ru/support/marketplace/assortment/operations/prices.html#quarantine)


        Чтобы увидеть список товаров, которые попали в карантин, используйте запрос [POST v2/campaigns/{campaignId}/price-quarantine](getCampaignQuarantineOffers.md).


        {% include notitle [limit](../../_auto/method_limits/confirmCampaignPrices.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmPricesRequest'
      responses:
        '200':
          description: Ответ `200` обозначает, что цены подтверждены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/hidden-offers:
    post:
      x-resource-id: 59
      tags:
      - hidden-offers
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.hiddenOffers
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: addHiddenOffers
      summary: Скрытие товаров и настройки скрытия
      description: '{% include notitle [access](../../_auto/method_scopes/addHiddenOffers.md) %}


        Скрывает товары магазина на Маркете.


        {% note info "Данные в каталоге обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/addHiddenOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        description: Запрос на скрытие оферов.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddHiddenOffersRequest'
      responses:
        '200':
          description: Настройки скрытия получены и скоро вступят в силу.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с товарами](../../concepts/error-codes#offers)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
    get:
      x-resource-id: 61
      tags:
      - hidden-offers
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: getHiddenOffers
      summary: Информация о скрытых вами товарах
      description: '{% include notitle [access](../../_auto/method_scopes/getHiddenOffers.md) %}


        Возвращает список скрытых вами товаров для заданного магазина.


        В списке будут товары, скрытые любым способом — через API, с помощью YML-фида, в кабинете и так далее.


        {% include notitle [limit](../../_auto/method_limits/getHiddenOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OfferIds'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500'
      responses:
        '200':
          description: Информация о скрытых вами товарах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetHiddenOffersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/hidden-offers/delete:
    post:
      x-resource-id: 242
      tags:
      - hidden-offers
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.hiddenOffers
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: deleteHiddenOffers
      summary: Возобновление показа товаров
      description: '{% include notitle [access](../../_auto/method_scopes/deleteHiddenOffers.md) %}


        Возобновляет показ скрытых вами товаров магазина на Маркете.


        {% note info "Данные в каталоге обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/deleteHiddenOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        description: Запрос на возобновление показа оферов.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteHiddenOffersRequest'
      responses:
        '200':
          description: Показ товаров возобновлен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/offers/stocks:
    put:
      x-resource-id: 141
      tags:
      - stocks
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.skus
        none:
          amount: 100000
          period: minutes
          period-duration: 1
        medium:
          amount: 100000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: updateStocks
      summary: Передача информации об остатках
      description: '{% include notitle [access](../../_auto/method_scopes/updateStocks.md) %}


        Передает данные об остатках товаров на витрине.


        Для группы складов передавайте остатки только для **одного любого склада**. Информация для остальных складов в этой
        группе обновится автоматически.


        Обязательно указывайте SKU **в точности** так, как он указан в каталоге. Например, _557722_ и _0557722_ — это два
        разных SKU.


        {% note info "Данные в каталоге обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/updateStocks.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateStocksRequest'
      responses:
        '200':
          $ref: '#/components/responses/200'
        '400':
          $ref: '#/components/responses/400Stocks'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    post:
      x-resource-id: 201
      tags:
      - stocks
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.result.warehouses.offers.offerId
        none:
          amount: 100000
          period: minutes
          period-duration: 1
        medium:
          amount: 100000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getStocks
      summary: Информация об остатках и оборачиваемости
      description: '{% include notitle [access](../../_auto/method_scopes/getStocks.md) %}


        Возвращает данные об остатках товаров (для всех моделей) и об [оборачиваемости](*turnover) товаров (для модели FBY).


        {% note info "По умолчанию данные по оборачивамости не возращаются" %}


        Чтобы они были в ответе, передавайте `true` в поле `withTurnover`.


        {% endnote %}


        **Для моделей FBY и LaaS:** информация об остатках может возвращаться с нескольких складов Маркета, у которых будут
        разные `warehouseId`. Получить список складов Маркета можно с помощью метода [GET v2/warehouses](../../reference/warehouses/getFulfillmentWarehouses.md).


        **Для модели FBS:** в ответе может вернуться не только партнерский склад, но и склад возвратов Маркета. Это возможно,
        если возврат поступил в указанную продавцом точку возвратов и долго не был забран.


        {% include notitle [limit](../../_auto/method_limits/getStocks.md) %}


        [//]: <> (turnover: Среднее количество дней, за которое товар продается. Подробно об оборачиваемости рассказано в
        Справке Маркета для продавцов https://yandex.ru/support/marketplace/analytics/turnover.html.)

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetWarehouseStocksRequest'
      responses:
        '200':
          description: Остатки товаров на складах.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetWarehouseStocksResponse'
        '400':
          $ref: '#/components/responses/400Stocks'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/delivery/services:
    get:
      x-resource-id: 55
      tags:
      - delivery-services
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getDeliveryServices
      summary: Справочник служб доставки
      description: '{% include notitle [access](../../_auto/method_scopes/getDeliveryServices.md) %}


        Возвращает справочник служб доставки: идентификаторы и наименования.


        {% include notitle [limit](../../_auto/method_limits/getDeliveryServices.md) %}

        '
      responses:
        '200':
          description: Информация о службах доставки.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetDeliveryServicesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/delivery-options:
    post:
      x-resource-id: 274
      tags:
      - delivery-options
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-resource-limit-config:
        path: resp
        parallel-limit-value: 10
        none:
          amount: 1000
          period: minutes
          period-duration: 1
        medium:
          amount: 1000
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getDeliveryOptions
      summary: Получение доступных вариантов доставки заказов
      description: '{% include notitle [access](../../_auto/method_scopes/getDeliveryOptions.md) %}


        Возвращает список вариантов для доставки заказов. Выберите подходящий вариант доставки из ответа и передайте его при
        создании заказа.


        Укажите `courierDelivery` для курьерской доставки или `pickupDelivery` для доставки в пункт выдачи. Не передавайте
        оба параметра одновременно.


        {% include notitle [limit](../../_auto/method_limits/getDeliveryOptions.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetDeliveryOptionsRequest'
      responses:
        '200':
          description: Список доступных вариантов доставки с разных складов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetDeliveryOptionsResponse'
        '400':
          $ref: '#/components/responses/400Orders'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/campaigns/{campaignId}/return-delivery-options:
    post:
      x-resource-id: 276
      tags:
      - delivery-options
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getReturnDeliveryOptions
      summary: Получение подходящих для возврата пунктов выдачи
      description: '{% include notitle [access](../../_auto/method_scopes/getReturnDeliveryOptions.md) %}


        Возвращает список идентификаторов пунктов выдачи, которые могут принять возврат указанных товаров.


        {% include notitle [limit](../../_auto/method_limits/getReturnDeliveryOptions.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetReturnDeliveryOptionsRequest'
      responses:
        '200':
          description: Информация о пунктах выдачи, которые могут принять возврат указанных товаров.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetReturnDeliveryOptionsResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/stats/orders:
    post:
      x-resource-id: 124
      tags:
      - orders-stats
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: getOrdersStats
      summary: Детальная информация по заказам
      description: '{% include notitle [access](../../_auto/method_scopes/getOrdersStats.md) %}


        Возвращает информацию по заказам на Маркете, в которых есть ваши товары.


        С помощью нее вы можете собрать статистику по вашим заказам и узнать, например, какие из товаров чаще всего возвращаются
        покупателями, какие, наоборот, пользуются большим спросом и т. п.


        {% note tip "Информация по созданным или обновленным заказам может появиться с задержкой до 40 минут" %}


        Чтобы получить данные без задержки, используйте метод [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).


        {% endnote %}


        В одном запросе можно получить информацию не более чем по 200 заказам.


        {% include notitle [limit](../../_auto/method_limits/getOrdersStats.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOrdersStatsRequest'
      responses:
        '200':
          description: Информация по заказам.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOrdersStatsResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках в отчетах и документах](../../concepts/error-codes#reports)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/stats/skus:
    post:
      x-resource-id: 125
      tags:
      - goods-stats
      - fby
      - fbs
      - express
      - dbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.result.shopSkus
        none:
          amount: 2000
          period: minutes
          period-duration: 1
        medium:
          amount: 5000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: getGoodsStats
      summary: Отчет по товарам
      description: '{% include notitle [access](../../_auto/method_scopes/getGoodsStats.md) %}


        Возвращает подробный отчет по товарам, которые вы разместили на Маркете. С помощью отчета вы можете узнать, например,
        об остатках на складе, об условиях хранения ваших товаров и т. д.


        {% include notitle [limit](../../_auto/method_limits/getGoodsStats.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetGoodsStatsRequest'
      responses:
        '200':
          description: Отчет по товарам.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetGoodsStatsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/info/{reportId}:
    get:
      x-resource-id: 188
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 100
          period: minutes
          period-duration: 1
      x-apphost:
        domain: API
        enabled: false
      operationId: getReportInfo
      summary: Получение заданного отчета или документа
      description: '{% include notitle [access](../../_auto/method_scopes/getReportInfo.md) %}


        Возвращает статус генерации заданного отчета или документа и, если он готов, ссылку для скачивания.


        Чтобы воспользоваться этим запросом, вначале нужно запустить генерацию отчета или документа. [Инструкция](../../step-by-step/reports.md)


        {% include notitle [limit](../../_auto/method_limits/getReportInfo.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportId'
      - $ref: '#/components/parameters/SourceType'
      responses:
        '200':
          description: 'Статус генерации отчета или документа и ссылка, если она уже есть.


            {% note tip "Статус генерации `FAILED` или `NO_DATA`" %}


            Проверьте корректность запроса на генерацию. Например, верно ли указан идентификатор кампании, период или номер
            платежного поручения.


            {% endnote %}


             

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetReportInfoResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/united-netting/generate:
    post:
      x-resource-id: 189
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateUnitedNettingReport
      summary: Отчет по платежам
      description: "{% include notitle [access](../../_auto/method_scopes/generateUnitedNettingReport.md) %}\n\nЗапускает\
        \ генерацию отчета по платежам за заданный период. [Что это за отчет](https://yandex.ru/support/marketplace/ru/accounting/transactions#all-pay)\n\
        \nУзнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).\n\
        \nТип отчета зависит от того, какие поля заполнены в запросе:\n\n#|\n|| **Тип отчета** | **Какие поля нужны** | **Комментарий**\
        \ ||\n|| О платежах за период | `dateFrom` и `dateTo` |\n  В отчет попадают все платежи, которые были выплачены и\
        \ начислены в выбранный период.\n\n  Пример: если перевод выполнен 31 августа и зачислен 1 сентября, он попадет в\
        \ отчет за оба месяца.\n||\n|| О платежном поручении | `bankOrderId` и `bankOrderDateTime` |—||\n|| [О баллах Маркета](*баллы_маркета)\
        \ | `monthOfYear` |—||\n|#\n\nЗаказать отчеты нескольких типов одним запросом нельзя.\n\n{% include notitle [reports](../../_auto/reports/united/netting/generator/united_netting.md)\
        \ %}\n\n{% include notitle [limit](../../_auto/method_limits/generateUnitedNettingReport.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/ReportLanguage'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateUnitedNettingReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/united-marketplace-services/generate:
    post:
      x-resource-id: 192
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateUnitedMarketplaceServicesReport
      summary: Отчет по стоимости услуг
      description: '{% include notitle [access](../../_auto/method_scopes/generateUnitedMarketplaceServicesReport.md) %}


        Запускает генерацию отчета по стоимости услуг за заданный период. [Что это за отчет](https://yandex.ru/support/marketplace/ru/accounting/transactions#reports)


        Тип отчета зависит от того, какие поля заполнены в запросе:


        |**Тип отчета**               |**Какие поля нужны**             |

        |-----------------------------|---------------------------------|

        |По дате начисления услуги    |`dateFrom` и `dateTo`            |

        |По дате формирования акта    |`year` и `month`                 |


        Заказать отчеты обоих типов одним запросом нельзя.


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/united/services/generator/united_marketplace_services.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateUnitedMarketplaceServicesReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/ReportLanguage'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateUnitedMarketplaceServicesReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/united-orders/generate:
    post:
      x-resource-id: 212
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - promotion
      - promotion:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateUnitedOrdersReport
      summary: Отчет по заказам
      description: '{% include notitle [access](../../_auto/method_scopes/generateUnitedOrdersReport.md) %}


        Запускает генерацию отчета по заказам за заданный период. [Что это за отчет](https://yandex.ru/support/marketplace/ru/accounting/transactions#get-report)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/united/orders/generator/united_orders.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-unchanged.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateUnitedOrdersReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/ReportLanguage'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateUnitedOrdersRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/united-returns/generate:
    post:
      x-resource-id: 244
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateUnitedReturnsReport
      summary: Отчет по невыкупам и возвратам
      description: '{% include notitle [access](../../_auto/method_scopes/generateUnitedReturnsReport.md) %}


        Запускает генерацию сводного отчета по невыкупам и возвратам за заданный период. [Что это за отчет](https://yandex.ru/support/marketplace/ru/orders/returns/logistic#rejected-orders)


        Отчет содержит информацию о невыкупах и возвратах за указанный период, а также о тех, которые готовы к выдаче.


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/united/returns/generator/united_returns.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-unchanged.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateUnitedReturnsReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateUnitedReturnsRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/goods-realization/generate:
    post:
      x-resource-id: 193
      tags:
      - reports
      - fby
      - fbs
      - express
      - dbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateGoodsRealizationReport
      summary: Отчет по реализации
      description: "{% include notitle [access](../../_auto/method_scopes/generateGoodsRealizationReport.md) %}\n\nЗапускает\
        \ генерацию отчета по реализации за заданный период. [Что это за отчет](https://yandex.ru/support/marketplace/ru/accounting/transactions#sales-report)\n\
        \nУзнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).\n\
        \n{% list tabs %}\n\n- FBY, FBS, Экспресс\n\n  {% include notitle [reports](../../_auto/reports/united/statistics/generator/united_statistics_v2.md)\
        \ %}\n\n- DBS\n\n  {% include notitle [reports](../../_auto/reports/united/statistics/generator/united_statistics_v2_dbs.md)\
        \ %}\n\n{% endlist %}\n\n{% include notitle [limit](../../_auto/method_limits/generateGoodsRealizationReport.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateGoodsRealizationReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/stocks-on-warehouses/generate:
    post:
      x-resource-id: 200
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateStocksOnWarehousesReport
      summary: Отчет по остаткам на складах
      description: "{% include notitle [access](../../_auto/method_scopes/generateStocksOnWarehousesReport.md) %}\n\nЗапускает\
        \ генерацию отчета по остаткам на складах. [Что это за отчет](https://yandex.ru/support/marketplace/ru/storage/logistics#remains-history)\n\
        \n**Какая информация вернется:**\n\n* Для моделей FBY и LaaS, если указать `campaignId`, — об остатках на складах\
        \ Маркета.\n* Для остальных моделей, если указать `campaignId`, — об остатках на соответствующем складе магазина.\n\
        * Для остальных моделей, если указать `businessId`, — об остатках на всех складах магазинов в кабинете, кроме FBY\
        \ и LaaS. Используйте фильтр `campaignIds`, чтобы указать определенные магазины.\n\n⚠️ Не передавайте одновременно\
        \ `campaignId` и `businessId`.\n\nУзнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса\
        \ [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).\n\n{% list tabs %}\n\n- Склад Маркета\n\
        \n  {% include notitle [reports](../../_auto/reports/stocks/stocks_on_warehouses.md) %}\n\n- Склад магазина\n\n  {%\
        \ include notitle [reports](../../_auto/reports/offers/mass/mass_shared_stocks_business_csv_config.md) %}\n\n- Все\
        \ склады магазинов в кабинете, кроме FBY и LaaS\n\n  {% include notitle [reports](../../_auto/reports/offers/stocks_business_config.md)\
        \ %}\n\n{% endlist %}\n\n{% include notitle [limit](../../_auto/method_limits/generateStocksOnWarehousesReport.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateStocksOnWarehousesReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/goods-movement/generate:
    post:
      x-resource-id: 196
      tags:
      - reports
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateGoodsMovementReport
      summary: Отчет по движению товаров
      description: '{% include notitle [access](../../_auto/method_scopes/generateGoodsMovementReport.md) %}


        Запускает генерацию отчета по движению товаров. [Что это за отчет](https://yandex.ru/support/marketplace/analytics/reports-fby-fbs.html#flow)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/sku/movement/movement_config.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-unchanged.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateGoodsMovementReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateGoodsMovementReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/shows-sales/generate:
    post:
      x-resource-id: 203
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 10
        medium:
          amount: 1
          period: minutes
          period-duration: 6
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateShowsSalesReport
      summary: Отчет «Аналитика продаж»
      description: '{% include notitle [access](../../_auto/method_scopes/generateShowsSalesReport.md) %}


        Запускает генерацию отчета «Аналитика продаж» за заданный период. [Что это за отчет](https://yandex.ru/support/marketplace/analytics/shows-sales.html)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/masterstat/sales_funnel_by_created_at.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateShowsSalesReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateShowsSalesReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/competitors-position/generate:
    post:
      x-resource-id: 229
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - promotion
      - promotion:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 10
        medium:
          amount: 1
          period: minutes
          period-duration: 6
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateCompetitorsPositionReport
      summary: Отчет «Конкурентная позиция»
      description: '{% include notitle [access](../../_auto/method_scopes/generateCompetitorsPositionReport.md) %}


        Запускает генерацию отчета «Конкурентная позиция» за заданный период. [Что это за отчет](https://yandex.ru/support2/marketplace/ru/analytics/competitors.html)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% note info "Значение -1 в отчете" %}


        Если в CSV-файле в столбце :no-translate[**POSITION**] стоит -1, в этот день не было заказов с товарами в указанной
        категории.


        {% endnote %}


        {% include notitle [reports](../../_auto/reports/masterstat/competitors_position.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateCompetitorsPositionReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateCompetitorsPositionReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/goods-prices/generate:
    post:
      x-resource-id: 272
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - pricing
      - pricing:read-only
      - promotion
      - promotion:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: generateGoodsPricesReport
      summary: Отчет «Цены»
      description: "{% include notitle [access](../../_auto/method_scopes/generateGoodsPricesReport.md) %}\n\nЗапускает генерацию\
        \ отчета «Цены».\n\n**Какая информация вернется:**\n\n* если передать `businessId` — по единым ценам кабинета;\n*\
        \ если [включены магазинные цены](*onlyDefaultPrice-false) и указать `campaignId` — по ценам в соответствующем магазине.\n\
        \nУзнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).\n\
        \n{% list tabs %}\n\n- Цены во всех магазинах кабинета\n\n  {% include notitle [reports](../../_auto/reports/prices/mass_assortment_business_price_v2.md)\
        \ %}\n\n- Магазинные цены\n\n  {% include notitle [reports](../../_auto/reports/prices/mass_assortment_price_v2.md)\
        \ %}\n\n{% endlist %}\n\n{% include notitle [tariff-period](../../_includes/common/simultaneously-generated-reports-amount.md)\
        \ %}\n\n{% include notitle [limit](../../_auto/method_limits/generateGoodsPricesReport.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateGoodsPricesReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках в отчетах и документах](../../concepts/error-codes#reports)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/goods-turnover/generate:
    post:
      x-resource-id: 213
      tags:
      - reports
      - fby
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateGoodsTurnoverReport
      summary: Отчет по оборачиваемости
      description: '{% include notitle [access](../../_auto/method_scopes/generateGoodsTurnoverReport.md) %}


        Запускает генерацию отчета по оборачиваемости за заданную дату.  [Что это за отчет](https://yandex.ru/support/marketplace/ru/storage/logistics#turnover)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/turnover/turnover.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-unchanged.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateGoodsTurnoverReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateGoodsTurnoverRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/boost-consolidated/generate:
    post:
      x-resource-id: 217
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - promotion
      - promotion:read-only
      - finance-and-accounting
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      - urbanads
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateBoostConsolidatedReport
      summary: Отчет по бусту продаж
      description: '{% include notitle [access](../../_auto/method_scopes/generateBoostConsolidatedReport.md) %}


        Запускает генерацию сводного отчета по бусту продаж за заданный период. {% if audience == "partner" %}[Что такое буст
        продаж](https://yandex.ru/support/marketplace/ru/marketing/campaigns){% endif %}


        Отчет содержит информацию по всем кампаниям, созданным и через API, и в кабинете.


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports]({{ report-columns-boost-consolidated }}) %}


        {% if audience != "advertiser" %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% endif %}


        {% include notitle [limit](../../_auto/method_limits/generateBoostConsolidatedReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateBoostConsolidatedRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/documents/shipment-list/generate:
    post:
      x-resource-id: 223
      tags:
      - reports
      - fbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 200
          period: hours
          period-duration: 1
        medium:
          amount: 200
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateShipmentListDocumentReport
      summary: Получение листа сборки
      description: '{% include notitle [access](../../_auto/method_scopes/generateShipmentListDocumentReport.md) %}


        Запускает генерацию **листа сборки** для отгрузки.


        Чтобы на в листе сборки отображался внешний идентификатор заказа, передайте его в методе [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).


        Узнать статус генерации и получить ссылку на готовый документ можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [limit](../../_auto/method_limits/generateShipmentListDocumentReport.md) %}

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateShipmentListDocumentReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый документ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/shelf-statistics/generate:
    post:
      x-resource-id: 226
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - promotion
      - promotion:read-only
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateShelfsStatisticsReport
      summary: Отчет по полкам
      description: '{% include notitle [access](../../_auto/method_scopes/generateShelfsStatisticsReport.md) %}


        Запускает генерацию сводного отчета по полкам — рекламным блокам с баннером или видео и набором товаров. {% if audience
        == "partner" %}Подробнее о них читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/shelf).{%
        endif %}


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/incuts/shelfs_statistics.md) %}


        {% if audience != "advertiser" %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% endif %}


        {% include notitle [limit](../../_auto/method_limits/generateShelfsStatisticsReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateShelfsStatisticsRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/documents/labels/generate:
    post:
      x-resource-id: 227
      tags:
      - reports
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateMassOrderLabelsReport
      summary: Готовые ярлыки‑наклейки на все коробки в нескольких заказах
      description: '{% include notitle [access](../../_auto/method_scopes/generateMassOrderLabelsReport.md) %}


        Запускает генерацию PDF-файла с ярлыками для переданных заказов. Подробно о том, зачем они нужны и как выглядят, рассказано
        [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/orders/fbs/packaging/marking.html).


        Чтобы на ярлыке отображался внешний идентификатор заказа, передайте его в методе [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).


        Узнать статус генерации и получить ссылку на готовый файл можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [limit](../../_auto/method_limits/generateMassOrderLabelsReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/MassOrderLabelsGenerationPageFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateMassOrderLabelsRequest'
      responses:
        '200':
          description: 'В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый файл.


            Если при генерации не удалось найти часть заказов, в ответе на запрос получения готового файла вернется подстатус
            `RESOURCE_NOT_FOUND`.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/goods-feedback/generate:
    post:
      x-resource-id: 241
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 10
        medium:
          amount: 1
          period: minutes
          period-duration: 6
      x-allowed:
      - seller
      x-apphost:
        domain: UGC
        enabled: false
      operationId: generateGoodsFeedbackReport
      summary: Отчет по отзывам о товарах
      description: '{% include notitle [access](../../_auto/method_scopes/generateGoodsFeedbackReport.md) %}


        Запускает генерацию отчета по отзывам о товарах. [Что это за отчет](https://yandex.ru/support/marketplace/ru/marketing/plus-reviews#stat)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/paid_opinion_models/paid_opinion_models.md) %}


        {% include notitle [tariff-period](../../_includes/common/simultaneously-generated-reports-amount.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateGoodsFeedbackReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateGoodsFeedbackRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/shows-boost/generate:
    post:
      x-resource-id: 257
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - promotion
      - promotion:read-only
      - finance-and-accounting
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      - urbanads
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateShowsBoostReport
      summary: Отчет по бусту показов
      description: '{% include notitle [access](../../_auto/method_scopes/generateShowsBoostReport.md) %}


        Запускает генерацию сводного отчета по бусту показов за заданный период. {% if audience == "partner" %}[Что такое
        буст показов](https://yandex.ru/support/marketplace/ru/marketing/boost-shows){% endif %}


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports]({{ report-columns-shows-boost }}) %}


        {% if audience != "advertiser" %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% endif %}


        {% include notitle [limit](../../_auto/method_limits/generateShowsBoostReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateShowsBoostRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/banners-statistics/generate:
    post:
      x-resource-id: 258
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - promotion
      - promotion:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      - urbanads
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateBannersStatisticsReport
      summary: Отчет по охватному продвижению
      description: '{% include notitle [access](../../_auto/method_scopes/generateBannersStatisticsReport.md) %}


        Запускает генерацию сводного отчета по охватному продвижению. {% if audience == "partner" %}Что это за отчет: [для
        баннеров](https://yandex.ru/support/marketplace/ru/marketing/advertising-tools/banner#statistics), [для пуш-уведомлений](https://yandex.ru/support/marketplace/ru/marketing/advertising-tools/push-notifications#statistics).{%
        endif %}


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/incuts/banners_statistics.md) %}


        {% if audience != "advertiser" %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% endif %}


        {% include notitle [limit](../../_auto/method_limits/generateBannersStatisticsReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateBannersStatisticsRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/closure-documents/generate:
    post:
      x-resource-id: 260
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateClosureDocumentsReport
      summary: Закрывающие документы
      description: "{% include notitle [access](../../_auto/method_scopes/generateClosureDocumentsReport.md) %}\n\nВозвращает\
        \ ZIP-архив с закрывающими документами в формате PDF за указанный месяц.\n\n{% cut \"Состав документов в зависимости\
        \ от типа договора\" %}\n\n* **Договор на размещение**\n\n  * [акт об оказанных услугах](*acts-main-act)\n  * [счет-фактура](*acts-main-invoice)\n\
        \  * [сводный отчет по данным статистики](*acts-main-report)\n  * [отчет об исполнении поручения и о зачете взаимных\
        \ требований](*acts-main-agent) (отчет агента)\n\n* **Договор на продвижение** (в России не заключается после 30 сентября\
        \ 2024 года)\n\n  * [акт об оказании услуг](*acts-discounts-act)\n  * [счет-фактура](*acts-discounts-invoice), если\
        \ этого требует схема налогообложения\n\n* **Договор на маркетинг**\n\n  * [акт об оказанных услугах](*acts-marketing-act)\n\
        \  * [счет-фактура](*acts-main-invoice)\n  * [счет-фактура на аванс](*acts-marketing-invoice)\n  * [выписка по лицевому\
        \ счету](*acts-marketing-account)\n  * [детализация к акту](*acts-marketing-details)\n\n{% endcut %}\n\nУзнать статус\
        \ генерации и получить ссылку на архив можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).\n\
        \n{% include notitle [limit](../../_auto/method_limits/generateClosureDocumentsReport.md) %}\n"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateClosureDocumentsRequest'
      responses:
        '200':
          description: 'ZIP-архив с закрывающими документами в формате :no-translate[PDF].

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/jewelry-fiscal/generate:
    post:
      x-resource-id: 262
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: generateJewelryFiscalReport
      summary: Отчет по заказам с ювелирными изделиями
      description: '{% include notitle [access](../../_auto/method_scopes/generateJewelryFiscalReport.md) %}


        Запускает генерацию отчета по заказам с ювелирными изделиями.


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/identifiers/jewelry/orders_jewelry_fiscal.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-unchanged.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateJewelryFiscalReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateJewelryFiscalReportRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/sales-geography/generate:
    post:
      x-resource-id: 263
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: generateSalesGeographyReport
      summary: Отчет по географии продаж
      description: '{% include notitle [access](../../_auto/method_scopes/generateSalesGeographyReport.md) %}


        Запускает генерацию отчета по географии продаж. [Что это за отчет](https://yandex.ru/support/marketplace/ru/analytics/sales-geography)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/locality/locality_offers_report_v2.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateSalesGeographyReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateSalesGeographyRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/key-indicators/generate:
    post:
      x-resource-id: 264
      tags:
      - reports
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - promotion
      - promotion:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: MONETIZATION
        enabled: false
      operationId: generateKeyIndicatorsReport
      summary: Отчет по ключевым показателям
      description: '{% include notitle [access](../../_auto/method_scopes/generateKeyIndicatorsReport.md) %}


        Запускает генерацию отчета по ключевым показателям. [Что это за отчет](https://yandex.ru/support/marketplace/ru/analytics/key-metrics)


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/key_indicators/key_indicators.md) %}


        {% include notitle [tariff-period](../../_includes/common/report-data-period-400-days.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateKeyIndicatorsReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateKeyIndicatorsRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/reports/closure-documents/detalization/generate:
    post:
      x-resource-id: 266
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateClosureDocumentsDetalizationReport
      summary: Отчет по схождению с закрывающими документами
      description: "{% include notitle [access](../../_auto/method_scopes/generateClosureDocumentsDetalizationReport.md) %}\n\
        \nЗапускает генерацию отчета по схождению с закрывающими документами в зависимости от типа договора.\n\nУзнать статус\
        \ генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).\n\
        \n{% list tabs %}\n\n- Договор на размещение\n\n  {% include notitle [reports](../../_auto/reports/period_closure/period_closure_income.md)\
        \ %}\n\n- Договор на продвижение\n\n  {% include notitle [reports](../../_auto/reports/period_closure/period_closure_outcome.md)\
        \ %}\n\n{% endlist %}\n\n{% include notitle [limit](../../_auto/method_limits/generateClosureDocumentsDetalizationReport.md)\
        \ %}\n"
      parameters:
      - $ref: '#/components/parameters/ReportFormat'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateClosureDocumentsDetalizationRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках в отчетах и документах](../../concepts/error-codes#reports)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/reports/marketing-detalization/generate:
    post:
      x-resource-id: 293
      tags:
      - reports
      - fby
      - dbs
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1
          period: minutes
          period-duration: 2
        medium:
          amount: 1
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: generateMarketingDetalizationReport
      summary: Отчет по счету маркетинга
      description: '{% include notitle [access](../../_auto/method_scopes/generateMarketingDetalizationReport.md) %}


        Запускает генерацию отчета по счету маркетинга.


        Узнать статус генерации и получить ссылку на готовый отчет можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [reports](../../_auto/reports/advertiser_billing_operations/advertiser_billing_operations.md) %}


        {% include notitle [limit](../../_auto/method_limits/generateMarketingDetalizationReport.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/ReportFormat'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateMarketingDetalizationRequest'
      responses:
        '200':
          description: В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый отчет.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках в отчетах и документах](../../concepts/error-codes#reports)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/reports/documents/barcodes/generate:
    post:
      x-resource-id: 282
      tags:
      - reports
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: generateBarcodesReport
      summary: Получение файла со штрихкодами
      description: '{% include notitle [access](../../_auto/method_scopes/generateBarcodesReport.md) %}


        Запускает генерацию PDF-файла со штрихкодами переданных товаров или товаров в указанной заявке на поставку.


        Файл не получится сгенерировать, если в нем будет более 1 500 штрихкодов.


        Узнать статус генерации и получить ссылку на готовый файл можно с помощью запроса [GET v2/reports/info/{reportId}](../../reference/reports/getReportInfo.md).


        {% include notitle [limit](../../_auto/method_limits/generateBarcodesReport.md) %}

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateBarcodesReportRequest'
      responses:
        '200':
          description: 'В ответ приходит идентификатор, который позволяет узнавать статус генерации и скачать готовый файл.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateReportResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/outlets:
    post:
      x-resource-id: 48
      tags:
      - outlets
      - dbs
      x-auth-scopes:
      - all-methods
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 35000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: createOutlet
      summary: Создание точки продаж
      description: '{% include notitle [access](../../_auto/method_scopes/createOutlet.md) %}


        Создает точку продаж магазина на Маркете.


        {% include notitle [limit](../../_auto/method_limits/createOutlet.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeOutletRequest'
      responses:
        '200':
          description: Информация о созданной точке продаж.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateOutletResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с точками продаж](../../concepts/error-codes#outlets)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    get:
      x-resource-id: 32
      tags:
      - outlets
      - dbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp.outlets
        none:
          amount: 100000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getOutlets
      summary: Информация о нескольких точках продаж
      description: '{% include notitle [access](../../_auto/method_scopes/getOutlets.md) %}


        Возвращает список точек продаж магазина.


        {% include notitle [limit](../../_auto/method_limits/getOutlets.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50'
      - description: 'Идентификатор региона.

          Если задать идентификатор родительского региона любого уровня, в выходных данных будут отображены точки продаж всех
          дочерних регионов.

          Идентификатор региона можно получить c помощью метода [GET v2/regions](../../reference/regions/searchRegionsByName.md).

          '
        name: region_id
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - description: Идентификатор точки продаж, присвоенный магазином.
        name: shop_outlet_code
        in: query
        required: false
        schema:
          type: string
      - description: '{% note warning "Вместо него используйте `region_id`." %}


           


          {% endnote %}

          '
        name: regionId
        deprecated: true
        in: query
        required: false
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: Информация о точках продаж.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOutletsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/outlets/{outletId}:
    put:
      x-resource-id: 49
      tags:
      - outlets
      - dbs
      x-auth-scopes:
      - all-methods
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 35000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: updateOutlet
      summary: Изменение информации о точке продаж
      description: '{% include notitle [access](../../_auto/method_scopes/updateOutlet.md) %}


        Изменяет информацию о точке продаж магазина на Маркете.


        {% include notitle [limit](../../_auto/method_limits/updateOutlet.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OutletId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeOutletRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с точками продаж](../../concepts/error-codes#outlets)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    get:
      x-resource-id: 33
      tags:
      - outlets
      - dbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getOutlet
      summary: Информация об одной точке продаж
      description: '{% include notitle [access](../../_auto/method_scopes/getOutlet.md) %}


        Возвращает информацию о точках продаж магазина.


        {% include notitle [limit](../../_auto/method_limits/getOutlet.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OutletId'
      responses:
        '200':
          description: Информация о точке продаж.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOutletResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    delete:
      x-resource-id: 47
      tags:
      - outlets
      - dbs
      x-auth-scopes:
      - all-methods
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 35000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: deleteOutlet
      summary: Удаление точки продаж
      description: '{% include notitle [access](../../_auto/method_scopes/deleteOutlet.md) %}


        Удаляет точку продаж магазина на Маркете.


        {% include notitle [limit](../../_auto/method_limits/deleteOutlet.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OutletId'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/logistics-points:
    post:
      x-resource-id: 277
      tags:
      - logistic-points
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getLogisticPoints
      summary: Получение точек ПВЗ Маркета
      description: '{% include notitle [access](../../_auto/method_scopes/getLogisticPoints.md) %}


        Возвращает список пунктов выдачи заказов Маркета.


        Регулярно запрашивайте эту информацию, чтобы в системе магазина хранить актуальные данные. Например, раз в день.


        {% include notitle [limit](../../_auto/method_limits/getLogisticPoints.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500'
      responses:
        '200':
          description: Информация о пунктах выдачи заказов Маркета.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetLogisticPointsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/outlets/licenses:
    get:
      x-resource-id: 113
      tags:
      - outlet-licenses
      - dbs
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getOutletLicenses
      summary: Информация о лицензиях для точек продаж
      description: '{% include notitle [access](../../_auto/method_scopes/getOutletLicenses.md) %}


        Возвращает информацию о лицензиях для точек продаж.


        {% include notitle [limit](../../_auto/method_limits/getOutletLicenses.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/OutletIds'
      - $ref: '#/components/parameters/LicenseIds'
        description: 'Список идентификаторов лицензий, информацию о которых нужно получить.


          Идентификаторы указываются через запятую. Идентификатор лицензии присваивается Маркетом. Не путайте его с номером,
          указанным на лицензии.


          В запросе должен быть либо параметр `outletIds`, либо параметр `ids`. Запрос с обоими параметрами или без них приведет
          к ошибке.

          '
      responses:
        '200':
          description: Найденные лицензии собственных точек продаж.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOutletLicensesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    delete:
      x-resource-id: 112
      tags:
      - outlet-licenses
      - dbs
      x-auth-scopes:
      - all-methods
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: deleteOutletLicenses
      summary: Удаление лицензий для точек продаж
      description: '{% include notitle [access](../../_auto/method_scopes/deleteOutletLicenses.md) %}


        Удаляет информацию о лицензиях для точек продаж.


        {% include notitle [limit](../../_auto/method_limits/deleteOutletLicenses.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/DeleteLicenseIds'
        description: 'Список идентификаторов лицензий, информацию о которых нужно удалить.


          Идентификаторы указываются через запятую. Идентификатор лицензии присваивается Маркетом. Не путайте его с номером,
          указанным на лицензии.

          '
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    post:
      x-resource-id: 114
      tags:
      - outlet-licenses
      - dbs
      x-auth-scopes:
      - all-methods
      - settings-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100000
          period: hours
          period-duration: 1
        medium:
          amount: 100000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: updateOutletLicenses
      summary: Создание и изменение лицензий для точек продаж
      description: '{% include notitle [access](../../_auto/method_scopes/updateOutletLicenses.md) %}


        Передает информацию о новых и существующих лицензиях для точек продаж. Поддерживаются только лицензии на розничную
        продажу алкоголя.


        Чтобы размещать алкогольную продукцию на Маркете, надо также прислать гарантийное письмо (если вы еще не делали этого
        раньше) и правильно оформить предложения в прайс-листе. Далее информация о лицензиях проходит проверку.


        {% include notitle [limit](../../_auto/method_limits/updateOutletLicenses.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOutletLicenseRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/goods-feedback:
    post:
      x-resource-id: 233
      tags:
      - goods-feedback
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: getGoodsFeedbacks
      summary: Получение отзывов о товарах продавца
      description: '{% include notitle [access](../../_auto/method_scopes/getGoodsFeedbacks.md) %}


        Возвращает отзывы о товарах продавца по указанным фильтрам. **Исключение:** отзывы, которые удалили покупатели или
        Маркет.


        {% note tip "Вы также можете настроить API-уведомления" %}


        Маркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый отзыв.
        А полную информацию о нем можно получить с помощью этого метода.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        Результаты возвращаются постранично.


        Отзывы расположены в порядке публикации, поэтому вы можете передавать определенный идентификатор страницы в `pageToken`,
        если вы получали его ранее.


        {% include notitle [limit](../../_auto/method_limits/getGoodsFeedbacks.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetGoodsFeedbackRequest'
      responses:
        '200':
          description: Список отзывов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetGoodsFeedbackResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/goods-feedback/skip-reaction:
    post:
      x-resource-id: 234
      tags:
      - goods-feedback
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: skipGoodsFeedbacksReaction
      summary: Пропуск реакции на отзывы
      description: '{% include notitle [access](../../_auto/method_scopes/skipGoodsFeedbacksReaction.md) %}


        Пропускает реакцию на отзыв — параметр `needReaction` принимает значение `false` в методе получения всех отзывов [POST v2/businesses/{businessId}/goods-feedback](../../reference/goods-feedback/getGoodsFeedbacks.md).


        {% include notitle [limit](../../_auto/method_limits/skipGoodsFeedbacksReaction.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SkipGoodsFeedbackReactionRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/goods-feedback/comments/update:
    post:
      x-resource-id: 232
      tags:
      - goods-feedback
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: updateGoodsFeedbackComment
      summary: Добавление нового или изменение созданного комментария
      description: '{% include notitle [access](../../_auto/method_scopes/updateGoodsFeedbackComment.md) %}


        Добавляет новый комментарий магазина или изменяет комментарий, который магазин оставлял ранее.


        Для создания комментария к отзыву передайте только идентификатор отзыва `feedbackId`.


        Чтобы добавить комментарий к другому комментарию, передайте:


        * `feedbackId` — идентификатор отзыва;

        * `comment.parentId` — идентификатор родительского комментария.


        Чтобы изменить комментарий, передайте:


        * `feedbackId`— идентификатор отзыва;

        * `comment.id` — идентификатор комментария, который нужно изменить.


        Если передать одновременно `comment.parentId` и `comment.id`, будет изменен существующий комментарий.


        {% include notitle [limit](../../_auto/method_limits/updateGoodsFeedbackComment.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGoodsFeedbackCommentRequest'
      responses:
        '200':
          description: Информация о добавленном или измененном комментарии.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateGoodsFeedbackCommentResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с отзывами о товарах](../../concepts/error-codes#feedback)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/goods-feedback/comments/delete:
    post:
      x-resource-id: 231
      tags:
      - goods-feedback
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: deleteGoodsFeedbackComment
      summary: Удаление комментария к отзыву
      description: '{% include notitle [access](../../_auto/method_scopes/deleteGoodsFeedbackComment.md) %}


        Удаляет комментарий магазина.


        {% include notitle [limit](../../_auto/method_limits/deleteGoodsFeedbackComment.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteGoodsFeedbackCommentRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/goods-feedback/comments:
    post:
      x-resource-id: 230
      tags:
      - goods-feedback
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - urbanads
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: getGoodsFeedbackComments
      summary: Получение комментариев к отзыву
      description: "{% include notitle [access](../../_auto/method_scopes/getGoodsFeedbackComments.md) %}\n\nВозвращает комментарии\
        \ к отзыву, кроме:\n\n  * тех, которые удалили пользователи или Маркет;\n  * комментариев к удаленным отзывам.\n\n\
        Идентификатор родительского комментария `parentId` возвращается только для ответов на другие комментарии, но не для\
        \ ответов на отзывы.\n\n{% if audience == \"partner\" %}\n\n{% note tip \"Вы также можете настроить API-уведомления\"\
        \ %}\n\nМаркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый\
        \ комментарий. А полную информацию о нем можно получить с помощью этого метода.\n\n[{#T}](../../push-notifications/index.md)\n\
        \n{% endnote %}\n\n{% endif %}\n\nРезультаты возвращаются постранично.\n\nКомментарии расположены в порядке публикации,\
        \ поэтому вы можете передавать определенный идентификатор страницы в `pageToken`, если вы получали его ранее.\n\n\
        {% include notitle [limit](../../_auto/method_limits/getGoodsFeedbackComments.md) %}\n"
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetGoodsFeedbackCommentsRequest'
      responses:
        '200':
          description: Дерево комментариев к отзыву.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetGoodsFeedbackCommentsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/goods-feedback-advertiser:
    post:
      x-resource-id: 288
      tags:
      - goods-feedback
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - urbanads
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: true
      operationId: getGoodsFeedbacksUrbanads
      summary: Получение отзывов о товарах для рекламодателей
      description: '{% include notitle [access](../../_auto/method_scopes/getGoodsFeedbacksUrbanads.md) %}


        Возвращает отзывы о товарах бренда по указанным фильтрам. **Исключение:** отзывы, которые удалили покупатели или Маркет.


        Результаты возвращаются постранично.


        Отзывы расположены в порядке публикации, поэтому вы можете передавать определенный идентификатор страницы в `pageToken`,
        если вы получали его ранее.


        {% include notitle [limit](../../_auto/method_limits/getGoodsFeedbacksUrbanads.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50'
      - $ref: '#/components/parameters/SourceType'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetGoodsFeedbackUrbanadsRequest'
      responses:
        '200':
          description: Список отзывов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetGoodsFeedbackUrbanadsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/goods-questions:
    post:
      x-resource-id: 285
      tags:
      - goods-questions
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: getGoodsQuestions
      summary: Получение вопросов о товарах продавца
      description: '{% include notitle [access](../../_auto/method_scopes/getGoodsQuestions.md) %}


        Возвращает вопросы о товарах продавца по указанным фильтрам.


        {% note tip "Вы также можете настроить API-уведомления" %}


        Маркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый вопрос.
        А полную информацию о нем можно получить с помощью этого метода.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        Результаты возвращаются постранично, одна страница содержит не более 50 вопросов.


        {% include notitle [limit](../../_auto/method_limits/getGoodsQuestions.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetQuestionsRequest'
      responses:
        '200':
          description: Список вопросов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetQuestionsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/goods-questions/answers:
    post:
      x-resource-id: 286
      tags:
      - goods-questions
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: getGoodsQuestionAnswers
      summary: Получение ответов на вопрос
      description: '{% include notitle [access](../../_auto/method_scopes/getGoodsQuestionAnswers.md) %}


        Возвращает ответы на вопрос о товаре по указанным фильтрам.


        {% note tip "Вы также можете настроить API-уведомления" %}


        Маркет отправит вам [запрос](../../push-notifications/reference/sendNotification.md), когда появится новый ответ или
        комментарий. А полную информацию о них можно получить с помощью этого метода.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        Результаты возвращаются постранично, одна страница содержит не более 50 ответов.


        {% include notitle [limit](../../_auto/method_limits/getGoodsQuestionAnswers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit50'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetAnswersRequest'
      responses:
        '200':
          description: Список ответов на вопрос.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAnswersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/goods-questions/update:
    post:
      x-resource-id: 284
      tags:
      - goods-questions
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: UGC
        enabled: false
      operationId: updateGoodsQuestionTextEntity
      summary: Создание, изменение и удаление ответа или комментария
      description: '{% include notitle [access](../../_auto/method_scopes/updateGoodsQuestionTextEntity.md) %}


        Создание, изменение и удаление ответа или комментария.


        {% include notitle [limit](../../_auto/method_limits/updateGoodsQuestionTextEntity.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGoodsQuestionTextEntityRequest'
      responses:
        '200':
          description: 'Информация о созданном ответе или комментарии.


            Возвращается только при операции создания (`operationType` = `CREATE`). При обновлении и удалении возвращается
            пустой ответ.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateGoodsQuestionTextEntityResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибке](../../concepts/error-codes.md#questions)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/offers:
    post:
      x-resource-id: 181
      tags:
      - offers
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: getCampaignOffers
      summary: Информация о товарах, которые размещены в заданном магазине
      description: '{% include notitle [access](../../_auto/method_scopes/getCampaignOffers.md) %}


        Возвращает список товаров, которые размещены в заданном магазине. Для каждого товара указываются параметры размещения.


        {% include notitle [limit](../../_auto/method_limits/getCampaignOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetCampaignOffersRequest'
      responses:
        '200':
          description: Список товаров, размещенных в заданном магазине.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCampaignOffersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/offers/update:
    post:
      x-resource-id: 183
      tags:
      - offers
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offers
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: updateCampaignOffers
      summary: Изменение условий продажи товаров в магазине
      description: '{% include notitle [access](../../_auto/method_scopes/updateCampaignOffers.md) %}


        Изменяет параметры размещения товаров в конкретном магазине: доступность товара и применяемый НДС.


        {% include notitle [limit](../../_auto/method_limits/updateCampaignOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCampaignOffersRequest'
      responses:
        '200':
          description: Ответ `200` обозначает, что новые параметры получены Маркетом и скоро вступят в силу.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с ценами](../../concepts/error-codes#prices)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/offers/delete:
    post:
      x-resource-id: 182
      tags:
      - offers
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offerIds
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: deleteCampaignOffers
      summary: Удаление товаров из ассортимента магазина
      description: '{% include notitle [access](../../_auto/method_scopes/deleteCampaignOffers.md) %}


        Удаляет заданные товары из заданного магазина.


        {% note warning "Запрос удаляет товары из конкретного магазина" %}


        На продажи в других магазинах и на наличие товара в общем каталоге он не влияет.


        {% endnote %}


        Товар не получится удалить, если он хранится на складах Маркета.


        {% include notitle [limit](../../_auto/method_limits/deleteCampaignOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteCampaignOffersRequest'
      responses:
        '200':
          description: Если удалось удалить не все товары, с ответом 200 вернется список тех, что были в запросе, но остались
            в магазине.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteCampaignOffersResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offers/recommendations:
    post:
      x-resource-id: 191
      tags:
      - offers
      - dbs
      - fby
      - fbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 50
          period: minutes
          period-duration: 1
        medium:
          amount: 100
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getOfferRecommendations
      summary: Рекомендации Маркета, касающиеся цен
      description: '{% include notitle [access](../../_auto/method_scopes/getOfferRecommendations.md) %}


        Метод возвращает рекомендации нескольких типов.


        1. Порог для привлекательной цены.

        2. Оценка привлекательности цен на витрине.


        Рекомендации показывают, какие цены нужно установить, чтобы привлечь покупателя.


        В запросе можно использовать фильтры. Результаты возвращаются постранично.


        {% include notitle [limit](../../_auto/method_limits/getOfferRecommendations.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOfferRecommendationsRequest'
      responses:
        '200':
          description: Список товаров с рекомендациями.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOfferRecommendationsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/bids:
    put:
      x-resource-id: 165
      tags:
      - bids
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - pricing
      - promotion
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: minutes
          period-duration: 1
        medium:
          amount: 1000
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: MONETIZATION
        enabled: true
      operationId: putBidsForBusiness
      summary: Включение буста продаж и установка ставок
      description: '{% include notitle [access](../../_auto/method_scopes/putBidsForBusiness.md) %}


        Запускает буст продаж — создает и включает кампанию, добавляет в нее товары и назначает на них ставки.


        {% cut "Как в кабинете выглядит кампания, созданная через API" %}


        ![](../../_images/api-boost.png)


        {% endcut %}


        При первом использовании запроса Маркет: создаст единую на все магазины бизнес-аккаунта кампанию, добавит в нее товары
        с указанными ставками, включит для них ценовую стратегию и запустит продвижение. Повторное использование запроса позволит
        обновить ставки на товары в этой кампании или добавить новые. Подробнее о ценовой стратегии читайте в [Справке Маркета
        для продавцов](https://yandex.ru/support/marketplace/marketing/campaigns.html#price-strategy).


        Если товара с указанным SKU нет, он будет проигнорирован. Если в будущем в каталоге появится товар с таким SKU, он
        автоматически будет добавлен в кампанию с указанной ставкой.


        Запрос всегда работает с одной и той же созданной через API кампанией. Если в кабинете удалить ее, при следующем выполнении
        запроса Маркет создаст новую. Другими кампаниями управлять через API не получится. У созданной через API кампании
        всегда наибольший приоритет над остальными — изменить его нельзя.


        Выполнение запроса включает кампанию и ценовую стратегию, если они были отключены.


        Внести другие изменения в созданную через API кампанию можно в кабинете:


        * выключить или включить кампанию;

        * изменить ее название;

        * выключить или включить ценовую стратегию.


        Чтобы остановить продвижение отдельных товаров и удалить их из кампании, передайте для них нулевую ставку в параметре
        `bid`.


        Подробнее о том, как работает буст продаж, читайте в [Справке Маркета для продавцов](https://yandex.ru/support/marketplace/marketing/campaigns.html).


        Узнать расходы на буст продаж можно с помощью запроса [POST v2/campaigns/{campaignId}/stats/orders](../../reference/orders-stats/getOrdersStats.md).
        Сумма содержится в поле `bidFee`.


        {% note info "Данные обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/putBidsForBusiness.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PutSkuBidsRequest'
      responses:
        '200':
          description: 'Все получилось: ставки установлены или обновлены. Если нужно, добавлены новые товары и запущена кампания.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/bids:
    put:
      x-resource-id: 166
      tags:
      - bids
      - dbs
      - fbs
      - express
      - fby
      x-auth-scopes:
      - all-methods
      - pricing
      - promotion
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: minutes
          period-duration: 1
        medium:
          amount: 1000
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: MONETIZATION
        enabled: true
      operationId: putBidsForCampaign
      summary: Включение буста продаж и установка ставок для магазина
      description: '{% include notitle [access](../../_auto/method_scopes/putBidsForCampaign.md) %}


        Запускает буст продаж в указанном магазине — создает и включает кампанию, добавляет в нее товары и назначает на них
        ставки.


        При первом использовании запроса Маркет: создаст кампанию, добавит в нее товары с указанными ставками для заданного
        магазина, включит для них ценовую стратегию и запустит продвижение. Повторное использование запроса позволит обновить
        ставки на товары в этой кампании или добавить новые. Подробнее о ценовой стратегии читайте в [Справке Маркета для
        продавцов](https://yandex.ru/support/marketplace/marketing/campaigns.html#price-strategy).


        Если товара с указанным SKU нет, он будет проигнорирован. Если в будущем в каталоге появится товар с таким SKU, он
        автоматически будет добавлен в кампанию с указанной ставкой.


        Запрос всегда работает с одной и той же кампанией, созданной через этот запрос или [PUT v2/businesses/{businessId}/bids](/reference/bids/putBidsForBusiness).
        Если в кабинете удалить ее, при следующем выполнении запроса Маркет создаст новую. У созданной через API кампании
        всегда наибольший приоритет над остальными — изменить его нельзя.


        Выполнение запроса включает кампанию и ценовую стратегию, если они были отключены.


        Внести другие изменения в созданную через API кампанию можно в кабинете:


        * выключить или включить кампанию;

        * изменить ее название;

        * выключить или включить ценовую стратегию.


        Чтобы остановить продвижение отдельных товаров и удалить их из кампании, передайте для них нулевую ставку в параметре
        `bid`.


        Подробнее о том, как работает буст продаж, читайте в [Справке Маркета для продавцов](https://yandex.ru/support/marketplace/marketing/campaigns.html).


        Узнать расходы на буст продаж можно с помощью запроса [POST v2/campaigns/{campaignId}/stats/orders](../../reference/orders-stats/getOrdersStats.md).
        Сумма содержится в поле `bidFee`.


        {% note info "Данные обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/putBidsForCampaign.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PutSkuBidsRequest'
      responses:
        '200':
          description: Пустой ответ.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/bids/info:
    post:
      x-resource-id: 184
      tags:
      - bids
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      - promotion
      - promotion:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: minutes
          period-duration: 1
        medium:
          amount: 1000
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: MONETIZATION
        enabled: true
      operationId: getBidsInfoForBusiness
      summary: Информация об установленных ставках
      description: '{% include notitle [access](../../_auto/method_scopes/getBidsInfoForBusiness.md) %}


        Возвращает значения ставок для заданных товаров.


        {% note warning "Получить информацию по кампаниям, созданным в кабинете, не получится" %}


        В ответе возвращаются значения только тех ставок, которые вы установили через запрос [PUT v2/businesses/{businessId}/bids](../../reference/bids/putBidsForBusiness.md).


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getBidsInfoForBusiness.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500Truncate'
      requestBody:
        required: false
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetBidsInfoRequest'
      responses:
        '200':
          description: Значения ставок для заданных товаров. В ответ попадают только товары, на которые установлены ставки.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetBidsInfoResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/bids/recommendations:
    post:
      x-resource-id: 187
      tags:
      - bids
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      - promotion
      - promotion:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: minutes
          period-duration: 1
        medium:
          amount: 1000
          period: minutes
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: MONETIZATION
        enabled: true
      operationId: getBidsRecommendations
      summary: Рекомендованные ставки для заданных товаров
      description: '{% include notitle [access](../../_auto/method_scopes/getBidsRecommendations.md) %}


        Возвращает рекомендованные ставки для заданных товаров, что обеспечивает вашим предложениям определенную долю показов,
        и дополнительные инструменты продвижения.


        Для одного товара может возвращаться одна рекомендованная ставка или несколько. Во втором случае разные ставки предназначены
        для достижения разной доли показов и получения дополнительных инструментов продвижения.


        Если товар только добавлен в каталог, но пока не продается, рекомендованной ставки для него не будет.


        В одном запросе может быть максимум 1500 товаров.


        {% include notitle [limit](../../_auto/method_limits/getBidsRecommendations.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        description: description.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetBidsRecommendationsRequest'
      responses:
        '200':
          description: Рекомендованные ставки для заданных товаров.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetBidsRecommendationsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/warehouses:
    get:
      x-resource-id: 194
      tags:
      - warehouses
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 100
          period: minutes
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-05'
        replacement-operation-id: getPagedWarehouses
      operationId: getWarehouses
      summary: Список складов и групп складов
      description: '{% include notitle [access](../../_auto/method_scopes/getWarehouses.md) %}


        Возвращает список складов и, если склады объединены, список групп складов. [Что такое группы складов и зачем они нужны](https://yandex.ru/support/marketplace/assortment/operations/stocks.html#unified-stocks)


        Среди прочего запрос позволяет определить идентификатор, который нужно использовать при передаче остатков для группы
        складов.


        {% include notitle [limit](../../_auto/method_limits/getWarehouses.md) %}

        '
      deprecated: true
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      responses:
        '200':
          description: Список складов и групп складов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetWarehousesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    post:
      x-resource-id: 243
      tags:
      - warehouses
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getPagedWarehouses
      summary: Список складов
      description: '{% include notitle [access](../../_auto/method_scopes/getPagedWarehouses.md) %}


        Возвращает список складов и информацию о них.


        {% include notitle [limit](../../_auto/method_limits/getPagedWarehouses.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit30'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetPagedWarehousesRequest'
      responses:
        '200':
          description: Список складов и их свойства, которые вы запрашивали.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPagedWarehousesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/warehouses:
    get:
      x-resource-id: 202
      tags:
      - warehouses
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getFulfillmentWarehouses
      summary: Идентификаторы фулфилмент-складов Маркета
      description: '{% include notitle [access](../../_auto/method_scopes/getFulfillmentWarehouses.md) %}


        Возвращает список фулфилмент-складов Маркета с их идентификаторами.


        {% include notitle [limit](../../_auto/method_limits/getFulfillmentWarehouses.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/GetFulfillmentWarehousesCampaignId'
      responses:
        '200':
          description: Список складов.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetFulfillmentWarehousesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/warehouse/status:
    post:
      x-resource-id: 245
      tags:
      - warehouses
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - inventory-and-order-processing
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: updateWarehouseStatus
      summary: Изменение статуса склада
      description: '{% include notitle [access](../../_auto/method_scopes/updateWarehouseStatus.md) %}


        Отключает или включает склад.


        После отключения склада товары, которые находятся на нем, скрываются через 15 минут. После включения они возвращаются
        на витрину через 15 минут, а если склад был выключен 30 дней или дольше — через 4 часа.


        {% include notitle [limit](../../_auto/method_limits/updateWarehouseStatus.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWarehouseStatusRequest'
      responses:
        '200':
          description: Новый статус склада.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateWarehouseStatusResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/category/{categoryId}/parameters:
    post:
      x-resource-id: 197
      tags:
      - content
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 100
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: getCategoryContentParameters
      summary: Списки характеристик товаров по категориям
      description: '{% include notitle [access](../../_auto/method_scopes/getCategoryContentParameters.md) %}


        Возвращает список характеристик с допустимыми значениями для заданной [листовой категории](*list-category).


        Поля в ответе определяют правила передачи характеристики в методах:

        - [POST v2/businesses/{businessId}/offer-mappings/update](../../reference/business-offer-mappings/updateOfferMappings.md)

        - [POST v2/businesses/{businessId}/offer-cards/update](../../reference/content/updateOfferContent.md)


        {% include notitle [limit](../../_auto/method_limits/getCategoryContentParameters.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/MarketCategoryId'
      - $ref: '#/components/parameters/BusinessIdQuery'
      responses:
        '200':
          description: Список характеристик товаров из заданной категории.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCategoryContentParametersResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с категориями](../../concepts/error-codes#categories)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-cards:
    post:
      x-resource-id: 198
      tags:
      - content
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 600
          period: minutes
          period-duration: 1
      operationId: getOfferCardsContentStatus
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      summary: Получение информации о заполненности карточек магазина
      description: '{% include notitle [access](../../_auto/method_scopes/getOfferCardsContentStatus.md) %}


        Возвращает сведения о состоянии контента для заданных товаров:


        * создана ли карточка товара и в каком она статусе;

        * рейтинг карточки — на сколько процентов она заполнена;

        * переданные характеристики товаров;

        * есть ли ошибки или предупреждения, связанные с контентом;

        * рекомендации по заполнению карточки.


        Чтобы получить другие характеристики товаров, воспользуйтесь методом [POST v2/businesses/{businessId}/offer-mappings](../../reference/business-offer-mappings/getOfferMappings.md).


        {% include notitle [limit](../../_auto/method_limits/getOfferCardsContentStatus.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit200'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOfferCardsContentStatusRequest'
      responses:
        '200':
          description: Информация о карточках указанных товаров.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOfferCardsContentStatusResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/offer-cards/update:
    post:
      x-resource-id: 199
      tags:
      - content
      - dbs
      - fby
      - fbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - offers-and-cards-management
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: req.offersContent
        none:
          amount: 5000
          period: minutes
          period-duration: 1
        medium:
          amount: 10000
          period: minutes
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: updateOfferContent
      summary: Редактирование категорийных характеристик товара
      description: '{% include notitle [access](../../_auto/method_scopes/updateOfferContent.md) %}


        Редактирует характеристики товара, которые специфичны для категории, к которой он относится.


        {% note warning "Здесь только то, что относится к конкретной категории" %}


        Если вам нужно изменить основные параметры товара (название, описание, изображения, видео, производитель, штрихкод),
        воспользуйтесь запросом [POST v2/businesses/{businessId}/offer-mappings/update](../../reference/business-offer-mappings/updateOfferMappings.md).


        {% endnote %}


        Чтобы удалить характеристики, которые заданы в параметрах с типом `string`, передайте пустое значение.


        {% note info "Данные в каталоге обновляются не мгновенно" %}


        Это занимает до нескольких минут.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/updateOfferContent.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOfferContentRequest'
      responses:
        '200':
          description: 'Запрос выполнен корректно, данные обработаны.


            {% note warning "Ответ `200` сам по себе не значит, что переданные значения корректны" %}


            Обязательно посмотрите детали ответа: `status`, а также перечень ошибок (`results.errors`) и замечаний (`results.warnings`),
            если они есть.


            - Если хотя бы по одному товару вернулась ошибка (`results.errors`), поле `status` = `ERROR`. Изменения по всем
            переданным товарам не будут применены.

            - Если ошибок нет, но хотя бы по одному товару вернулось замечание (`results.warnings`), поле `status` = `OK`,
            и изменения будут применены.


            {% endnote %}


            Если в `status` вернулось `ERROR`, убедитесь, что:


            * все обязательные характеристики заполнены;

            * характеристики действительно существуют в указанных категориях;

            * значения соответствуют характеристикам;

            * ваши собственные значения имеют нужный тип данных.


            Найти проблемы помогут поля `errors` и `warnings`.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateOfferContentResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '423':
          $ref: '#/components/responses/423'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/chats/new:
    post:
      x-resource-id: 207
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: createChat
      summary: Создание нового чата с покупателем
      description: '{% include notitle [access](../../_auto/method_scopes/createChat.md) %}


        Создает новый чат с покупателем и возвращает информацию о нем или созданном ранее.


        Типы чатов, которые может начать продавец:


        * по заказам;

        * по возвратам (доступны только для FBY-, FBS- и Экспресс-магазинов).


        {% include notitle [limit](../../_auto/method_limits/createChat.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChatRequest'
      responses:
        '200':
          description: 'Информация о новом или созданном ранее чате.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateChatResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/chats:
    post:
      x-resource-id: 208
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getChats
      summary: Получение доступных чатов
      description: '{% include notitle [access](../../_auto/method_scopes/getChats.md) %}


        Возвращает чаты с покупателями.


        {% note tip "Подключите API-уведомления" %}


        Маркет отправит вам запрос [POST notification](../../push-notifications/reference/sendNotification.md), когда появится
        новый чат или сообщение.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getChats.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit20'
      requestBody:
        required: true
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetChatsRequest'
      responses:
        '200':
          description: 'Список чатов.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetChatsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/chat:
    get:
      x-resource-id: 249
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getChat
      summary: Получение чата по идентификатору
      description: '{% include notitle [access](../../_auto/method_scopes/getChat.md) %}


        Возвращает чат по его идентификатору.


        {% note tip "Подключите API-уведомления" %}


        Маркет отправит вам запрос [POST notification](../../push-notifications/reference/sendNotification.md), когда появится
        новый чат или сообщение.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getChat.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/ChatId'
      responses:
        '200':
          description: 'Информация о чате.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetChatResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/chats/file/send:
    post:
      x-resource-id: 209
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: sendFileToChat
      summary: Отправка файла в чат
      description: '{% include notitle [access](../../_auto/method_scopes/sendFileToChat.md) %}


        Отправляет файл в чат с покупателем.


        {% include notitle [limit](../../_auto/method_limits/sendFileToChat.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/ChatId'
      requestBody:
        required: true
        description: description
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SendFileToChatRequest'
      responses:
        '200':
          description: Пустой ответ. Означает, что файл отправлен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/chats/message:
    post:
      x-resource-id: 210
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: sendMessageToChat
      summary: Отправка сообщения в чат
      description: '{% include notitle [access](../../_auto/method_scopes/sendMessageToChat.md) %}


        Отправляет сообщение в чат с покупателем.


        {% include notitle [limit](../../_auto/method_limits/sendMessageToChat.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/ChatId'
      requestBody:
        required: true
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendMessageToChatRequest'
      responses:
        '200':
          description: Пустой ответ. Означает, что сообщение отправлено.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmptyApiResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
    get:
      x-resource-id: 248
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getChatMessage
      summary: Получение сообщения в чате
      description: '{% include notitle [access](../../_auto/method_scopes/getChatMessage.md) %}


        Возвращает сообщение по его идентификатору.


        {% note tip "Подключите API-уведомления" %}


        Маркет отправит вам запрос [POST notification](../../push-notifications/reference/sendNotification.md), когда появится
        новый чат или сообщение.


        [{#T}](../../push-notifications/index.md)


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getChatMessage.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/ChatId'
      - $ref: '#/components/parameters/MessageId'
      responses:
        '200':
          description: 'Сообщение и информация о нем.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetChatMessageResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/chats/history:
    post:
      x-resource-id: 211
      tags:
      - chats
      - dbs
      - fbs
      - fby
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - communication
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 10000
          period: hours
          period-duration: 1
        medium:
          amount: 10000
          period: hours
          period-duration: 1
      x-apphost:
        domain: BASE
        enabled: false
      operationId: getChatHistory
      summary: Получение истории сообщений в чате
      description: '{% include notitle [access](../../_auto/method_scopes/getChatHistory.md) %}


        Возвращает историю сообщений в чате с покупателем.


        {% include notitle [limit](../../_auto/method_limits/getChatHistory.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/ChatId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit100'
      requestBody:
        required: true
        description: description
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetChatHistoryRequest'
      responses:
        '200':
          description: 'История сообщений.

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetChatHistoryResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/tariffs/calculate:
    post:
      x-resource-id: 216
      tags:
      - tariffs
      - fbs
      - fby
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - finance-and-accounting
      - pricing
      - pricing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: minutes
          period-duration: 1
        medium:
          amount: 100
          period: minutes
          period-duration: 1
      x-apphost:
        domain: FINANCE
        enabled: false
      operationId: calculateTariffs
      summary: Калькулятор стоимости услуг
      description: '{% include notitle [access](../../_auto/method_scopes/calculateTariffs.md) %}


        Рассчитывает стоимость услуг Маркета для товаров с заданными параметрами. Порядок товаров в запросе и ответе сохраняется,
        чтобы определить,

        для какого товара рассчитана стоимость услуги.


        Обратите внимание: калькулятор осуществляет примерные расчеты. Финальная стоимость для каждого заказа зависит от предоставленных
        услуг.


        Если у вас оформлена подписка, сниженный тариф применится в расчетах. Подробнее о подписке для продавцов читайте [в
        Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/marketing/subscription).


        В запросе можно указать либо параметр `campaignId`, либо `sellingProgram`. Совместное использование параметров приведет
        к ошибке.


        {% include notitle [limit](../../_auto/method_limits/calculateTariffs.md) %}

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalculateTariffsRequest'
      responses:
        '200':
          description: Стоимость услуг.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalculateTariffsResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с тарифами](../../concepts/error-codes#tariffs)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/categories/tree:
    post:
      x-resource-id: 222
      tags:
      - categories
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 50
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      operationId: getCategoriesTree
      summary: Дерево категорий
      description: '{% include notitle [access](../../_auto/method_scopes/getCategoriesTree.md) %}


        Возвращает дерево категорий Маркета.


        {% include notitle [limit](../../_auto/method_limits/getCategoriesTree.md) %}

        '
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetCategoriesRequest'
      responses:
        '200':
          description: Категории Маркета.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCategoriesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/categories/max-sale-quantum:
    post:
      x-resource-id: 225
      deprecated: true
      tags:
      - categories
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: OFFERS_CATALOG
        enabled: false
      x-deprecation-config:
        degradation-start-date: '2027-01-18'
        shutdown-date: '2027-04-05'
      operationId: getCategoriesMaxSaleQuantum
      summary: Лимит на установку кванта продажи и минимального количества товаров в заказе
      description: '{% include notitle [access](../../_auto/method_scopes/getCategoriesMaxSaleQuantum.md) %}


        Возвращает лимит на установку [кванта](*quantum) и минимального количества товаров в заказе, которые вы можете задать
        для товаров указанных категорий.


        Если вы передадите значение кванта или минимального количества товаров выше установленного Маркетом ограничения, товар
        будет скрыт с витрины.


        Подробнее о том, как продавать товары по несколько штук, читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/assortment/fields/quantum).


        {% include notitle [limit](../../_auto/method_limits/getCategoriesMaxSaleQuantum.md) %}

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetCategoriesMaxSaleQuantumRequest'
      responses:
        '200':
          description: Лимит на установку кванта и минимального количества товаров.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetCategoriesMaxSaleQuantumResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/ratings/quality:
    post:
      x-resource-id: 239
      tags:
      - ratings
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 500
          period: hours
          period-duration: 1
        medium:
          amount: 500
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getQualityRatings
      summary: Индекс качества магазинов
      description: '{% include notitle [access](../../_auto/method_scopes/getQualityRatings.md) %}


        Возвращает значение индекса качества магазинов и его составляющие.


        Подробнее об индексе качества читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/quality/score/).


        {% include notitle [limit](../../_auto/method_limits/getQualityRatings.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetQualityRatingRequest'
      responses:
        '200':
          description: Значение индекса качества магазинов и его составляющие.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetQualityRatingResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/ratings/quality/details:
    post:
      x-resource-id: 240
      tags:
      - ratings
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-apphost:
        domain: ORDERS
        enabled: false
      operationId: getQualityRatingDetails
      summary: Заказы, которые повлияли на индекс качества
      description: '{% include notitle [access](../../_auto/method_scopes/getQualityRatingDetails.md) %}


        Возвращает список заказов, которые повлияли на индекс качества магазина. Чтобы узнать значение индекса качества, выполните
        запрос [POST v2/businesses/{businessId}/ratings/quality](../../reference/ratings/getQualityRatings.md).


        {% include notitle [limit](../../_auto/method_limits/getQualityRatingDetails.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      responses:
        '200':
          description: Информация о заказах, которые повлияли на индекс качества.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetQualityRatingDetailsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/promos:
    post:
      x-resource-id: 235
      tags:
      - promos
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - promotion
      - promotion:read-only
      - pricing
      - pricing:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getPromos
      summary: Получение списка акций
      description: '{% include notitle [access](../../_auto/method_scopes/getPromos.md) %}


        Возвращает информацию об акциях Маркета. Не возвращает данные об акциях, которые создал продавец.


        По умолчанию возвращаются акции, в которых продавец участвует или может принять участие.


        Чтобы получить текущие или завершенные акции, передайте параметр `participation`.


        Типы акций, которые возвращаются в ответе:


        * прямая скидка;

        * флеш-акция;

        * скидка по промокоду.


        {% include notitle [limit](../../_auto/method_limits/getPromos.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetPromosRequest'
      responses:
        '200':
          description: Список акций Маркета.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPromosResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/promos/offers:
    post:
      x-resource-id: 236
      tags:
      - promos
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - pricing
      - pricing:read-only
      - promotion
      - promotion:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: getPromoOffers
      summary: Получение списка товаров, которые участвуют или могут участвовать в акции
      description: '{% include notitle [access](../../_auto/method_scopes/getPromoOffers.md) %}


        Возвращает список товаров, которые участвуют или могут участвовать в акции.


        {% note warning "Условия участия в акциях могут меняться" %}


        Например, `maxPromoPrice`.


        Установленные цены меняться не будут — `price` и `promoPrice`.


        {% endnote %}


        {% include notitle [limit](../../_auto/method_limits/getPromoOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetPromoOffersRequest'
      responses:
        '200':
          description: Список товаров, которые участвуют или могут участвовать в акции.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetPromoOffersResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с акциями](../../concepts/error-codes#promos)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/promos/offers/update:
    post:
      x-resource-id: 237
      tags:
      - promos
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - pricing
      - promotion
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: updatePromoOffers
      summary: Добавление товаров в акцию или изменение их цен
      description: '{% include notitle [access](../../_auto/method_scopes/updatePromoOffers.md) %}


        Добавляет товары в акцию или изменяет цены на товары, которые участвуют в акции.


        Изменения начинают действовать в течение 4–6 часов. Узнать, применились ли они, можно с помощью параметра `processing`
        в ответе метода [POST v2/businesses/{businessId}/promos](../../reference/promos/getPromos.md).


        {% include notitle [limit](../../_auto/method_limits/updatePromoOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePromoOffersRequest'
      responses:
        '200':
          description: Результат добавления товаров в акцию или обновления их цен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatePromoOffersResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с акциями](../../concepts/error-codes#promos)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/businesses/{businessId}/promos/offers/delete:
    post:
      x-resource-id: 238
      tags:
      - promos
      - fby
      - fbs
      - dbs
      - express
      x-auth-scopes:
      - all-methods
      - pricing
      - promotion
      x-resource-limit-config:
        path: resp
        none:
          amount: 5000
          period: hours
          period-duration: 1
        medium:
          amount: 5000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: PROMO
        enabled: false
      operationId: deletePromoOffers
      summary: Удаление товаров из акции
      description: '{% include notitle [access](../../_auto/method_scopes/deletePromoOffers.md) %}


        Убирает товары из акции.


        Изменения начинают действовать в течение 4–6 часов. Узнать, применились ли они, можно с помощью параметра `processing`
        в ответе метода [POST v2/businesses/{businessId}/promos](../../reference/promos/getPromos.md).


        {% include notitle [limit](../../_auto/method_limits/deletePromoOffers.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeletePromoOffersRequest'
      responses:
        '200':
          description: Результат удаления товаров из акции.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeletePromoOffersResponse'
        '400':
          description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с акциями](../../concepts/error-codes#promos)

            '
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiClientDataErrorResponse'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/supply-requests:
    post:
      x-resource-id: 253
      tags:
      - supply-requests
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getSupplyRequests
      summary: Получение информации о заявках на поставку, вывоз и утилизацию
      description: '{% include notitle [access](../../_auto/method_scopes/getSupplyRequests.md) %}


        По указанным фильтрам возвращает заявки на поставку, вывоз и утилизацию, а также информацию по ним.


        {% include notitle [limit](../../_auto/method_limits/getSupplyRequests.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit100'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetSupplyRequestsRequest'
      responses:
        '200':
          description: Список заявок и информация по ним.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetSupplyRequestsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/supply-requests/items:
    post:
      x-resource-id: 252
      tags:
      - supply-requests
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      summary: Получение товаров в заявке на поставку, вывоз или утилизацию
      description: '{% include notitle [access](../../_auto/method_scopes/getSupplyRequestItems.md) %}


        Возвращает список товаров в заявке и информацию по ним.


        {% include notitle [limit](../../_auto/method_limits/getSupplyRequestItems.md) %}

        '
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getSupplyRequestItems
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/AliasedPageToken'
      - $ref: '#/components/parameters/PageLimit500'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetSupplyRequestItemsRequest'
      responses:
        '200':
          description: Список товаров в заявке и информация по ним.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetSupplyRequestItemsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/campaigns/{campaignId}/supply-requests/documents:
    post:
      x-resource-id: 251
      tags:
      - supply-requests
      - fby
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      summary: Получение документов по заявке на поставку, вывоз или утилизацию
      description: '{% include notitle [access](../../_auto/method_scopes/getSupplyRequestDocuments.md) %}


        Возвращает документы по заявке.


        {% include notitle [limit](../../_auto/method_limits/getSupplyRequestDocuments.md) %}

        '
      x-apphost:
        domain: LOGISTICS
        enabled: false
      operationId: getSupplyRequestDocuments
      parameters:
      - $ref: '#/components/parameters/CampaignId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetSupplyRequestDocumentsRequest'
      responses:
        '200':
          description: Список документов и ссылки на них.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetSupplyRequestDocumentsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '404':
          $ref: '#/components/responses/404'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v2/auth/token:
    post:
      x-resource-id: 256
      tags:
      - auth
      - fby
      - fbs
      - dbs
      - express
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-allowed:
      - seller
      - market-yandex-go
      x-resource-limit-config:
        path: resp
        none:
          amount: 100
          period: hours
          period-duration: 1
        medium:
          amount: 100
          period: hours
          period-duration: 1
      summary: Получение информации о токене авторизации
      description: '{% include notitle [access](../../_auto/method_scopes/getAuthTokenInfo.md) %}


        {% note info "Метод доступен только для Api-Key-токена." %}


         


        {% endnote %}


        Возвращает информацию о переданном токене авторизации.


        {% include notitle [limit](../../_auto/method_limits/getAuthTokenInfo.md) %}

        '
      x-apphost:
        domain: API
        enabled: false
      operationId: getAuthTokenInfo
      responses:
        '200':
          description: Информация о токене авторизации.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetTokenInfoResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
  /v1/businesses/{businessId}/operations:
    post:
      x-resource-id: 279
      tags:
      - operations
      - laas
      x-auth-scopes:
      - all-methods
      - all-methods:read-only
      - settings-management
      - finance-and-accounting
      - offers-and-cards-management
      - offers-and-cards-management:read-only
      - pricing
      - pricing:read-only
      - inventory-and-order-processing
      - inventory-and-order-processing:read-only
      - promotion
      - promotion:read-only
      - communication
      - supplies-management:read-only
      x-resource-limit-config:
        path: resp
        none:
          amount: 1000
          period: hours
          period-duration: 1
        medium:
          amount: 1000
          period: hours
          period-duration: 1
      x-allowed:
      - seller
      x-apphost:
        domain: API
        enabled: false
      operationId: getOperations
      summary: Получение статусов операций
      description: '{% include notitle [access](../../_auto/method_scopes/getOperations.md) %}


        Возвращает статусы запущенных операций по их идентификаторам.


        {% include notitle [limit](../../_auto/method_limits/getOperations.md) %}

        '
      parameters:
      - $ref: '#/components/parameters/BusinessId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GetOperationsRequest'
      responses:
        '200':
          description: Информация об операциях.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetOperationsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '403':
          $ref: '#/components/responses/403'
        '420':
          $ref: '#/components/responses/420'
        '500':
          $ref: '#/components/responses/500'
components:
  schemas:
    AcceptOrderCancellationRequest:
      type: object
      required:
      - accepted
      properties:
        accepted:
          description: 'Решение об отмене заказа:


            * `true` — заказ отменяется, служба доставки узнала об отмене до передачи заказа покупателю.

            * `false` — заказ не отменяется, так как он уже доставлен покупателю курьером или передан в пункт выдачи заказов.

            '
          type: boolean
        reason:
          $ref: '#/components/schemas/OrderCancellationReasonType'
          description: 'Причина, по которой заказ не может быть отменен (она сообщается покупателю).


            Обязательный параметр, если вы передаете `accepted="false"`.

            '
    AddHiddenOffersRequest:
      description: Запрос на скрытие оферов.
      type: object
      required:
      - hiddenOffers
      properties:
        hiddenOffers:
          description: 'Список скрытых товаров.


            В рамках одного запроса все значения `offerId` должны быть уникальными. Не допускается передача двух объектов
            с одинаковым `offerId`.

            '
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/HiddenOfferDTO'
    AddOffersToArchiveDTO:
      description: Товары, которые не удалось поместить в архив.
      type: object
      properties:
        notArchivedOffers:
          description: Список товаров, которые не удалось поместить в архив.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/AddOffersToArchiveErrorDTO'
    AddOffersToArchiveErrorDTO:
      description: Товар, который не удалось поместить в архив.
      type: object
      required:
      - offerId
      - error
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        error:
          $ref: '#/components/schemas/AddOffersToArchiveErrorType'
          description: Причина, по которой товар не удалось поместить в архив.
    AddOffersToArchiveErrorType:
      description: 'Причина, по которой товар не удалось поместить в архив:


        * `OFFER_HAS_STOCKS` — товар хранится на складе Маркета.


        * `UNKNOWN` — неизвестная причина ошибки. Скорее всего произошел сбой на стороне Маркета. Если ошибка повторяется
        долгое время, обратитесь в поддержку.

        '
      type: string
      enum:
      - OFFER_HAS_STOCKS
      - UNKNOWN
    AddOffersToArchiveRequest:
      description: 'Товары, которые нужно поместить в архив.

        '
      type: object
      required:
      - offerIds
      properties:
        offerIds:
          description: Список товаров, которые нужно поместить в архив.
          type: array
          minItems: 1
          maxItems: 200
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    AddOffersToArchiveResponse:
      description: Результат архивации товаров.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/AddOffersToArchiveDTO'
            description: Товары, которые не удалось поместить в архив.
    AffectedOrderQualityRatingComponentType:
      description: 'Составляющие индекса качества.


        **Для модели DBS:**

        * `DBS_CANCELLATION_RATE` — доля отмененных товаров.

        * `DBS_LATE_DELIVERY_RATE` — доля заказов, доставленных после плановой даты.


        **Для моделей FBS и Экспресс:**

        * `FBS_CANCELLATION_RATE` — доля отмененных товаров.

        * `FBS_LATE_SHIP_RATE` — доля не вовремя отгруженных заказов.

        '
      type: string
      enum:
      - DBS_CANCELLATION_RATE
      - DBS_LATE_DELIVERY_RATE
      - FBS_CANCELLATION_RATE
      - FBS_LATE_SHIP_RATE
    AgeDTO:
      description: Возраст в заданных единицах измерения.
      type: object
      required:
      - value
      - ageUnit
      properties:
        value:
          description: 'Значение.

            '
          type: number
          minimum: 0
        ageUnit:
          $ref: '#/components/schemas/AgeUnitType'
          description: 'Единица измерения.

            '
    AgeUnitType:
      description: 'Единицы измерения возраста:


        * `YEAR` — год.

        * `MONTH` — месяц.

        '
      type: string
      enum:
      - YEAR
      - MONTH
    AnswerDTO:
      description: Ответ на вопрос.
      type: object
      required:
      - id
      - text
      - canModify
      - status
      - questionId
      - createdAt
      - votes
      properties:
        id:
          $ref: '#/components/schemas/AnswerId'
        text:
          $ref: '#/components/schemas/QuestionsTextContent'
        canModify:
          description: Может ли продавец изменять комментарий или удалять его.
          type: boolean
        author:
          $ref: '#/components/schemas/QuestionsTextContentAuthorDTO'
        status:
          $ref: '#/components/schemas/QuestionsTextContentModerationStatusType'
        questionId:
          $ref: '#/components/schemas/QuestionId'
        createdAt:
          type: string
          description: Дата и время создания ответа.
          format: date-time
        votes:
          $ref: '#/components/schemas/VotesDTO'
        comments:
          description: Список комментариев.
          type: array
          minItems: 0
          maxItems: 100
          nullable: true
          items:
            $ref: '#/components/schemas/CommentDTO'
    AnswerId:
      description: 'Идентификатор ответа на вопрос.

        '
      type: integer
      format: int64
      minimum: 1
    AnswerListDTO:
      description: Ответы на вопрос.
      type: object
      required:
      - answers
      properties:
        answers:
          description: Список ответов.
          type: array
          items:
            $ref: '#/components/schemas/AnswerDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    ApiAvailabilityStatusType:
      description: 'Возможность использовать API:


        * `AVAILABLE` — методы API доступны для выполнения запросов.

        * `DISABLED_BY_INACTIVITY` — методы API недоступны, так как магазин не размещал товары на витрине больше 90 дней.

        * `DISABLED_BY_NO_ACTIVE_CONTRACT` — методы API недоступны из-за отсутствия активного договора с Маркетом.

        * `MANUALLY_DISABLED` — методы API недоступны, так как интеграция выключена вручную.

        * `DISABLED_BY_NO_PLACEMENT_TYPE` — методы API недоступны, так как магазин не подключен к программе размещения.


        [Подробная инструкция по восстановлению доступа](../../concepts/api-access.md)

        '
      type: string
      enum:
      - AVAILABLE
      - DISABLED_BY_INACTIVITY
      - DISABLED_BY_NO_ACTIVE_CONTRACT
      - MANUALLY_DISABLED
      - DISABLED_BY_NO_PLACEMENT_TYPE
    ApiClientDataErrorResponse:
      description: Ошибка в данных переданных от клиента.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    ApiErrorDTO:
      type: object
      description: Общий формат ошибки.
      required:
      - code
      properties:
        code:
          description: Код ошибки.
          type: string
        message:
          description: Описание ошибки.
          type: string
    ApiErrorResponse:
      type: object
      description: Стандартная обертка для ошибок сервера.
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          errors:
            description: Список ошибок.
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/ApiErrorDTO'
    ApiForbiddenErrorResponse:
      description: Неверны авторизационные данные, указанные в запросе, или запрещен доступ к запрашиваемому ресурсу.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    ApiKeyDTO:
      description: Информация о Api-Key-токене.
      type: object
      required:
      - name
      - authScopes
      properties:
        name:
          description: Название токена.
          type: string
          minLength: 1
          maxLength: 100
        authScopes:
          description: Доступы к методам по Api-Key-токену.
          type: array
          nullable: false
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ApiKeyScopeType'
    ApiKeyScopeType:
      description: 'Доступы к методам:


        * `ALL_METHODS` — полное управление кабинетом.

        * `ALL_METHODS_READ_ONLY` — просмотр всей информации в кабинете.

        * `INVENTORY_AND_ORDER_PROCESSING` — обработка заказов и учет товаров.

        * `INVENTORY_AND_ORDER_PROCESSING_READ_ONLY` — просмотр информации о заказах.

        * `PRICING` — управление ценами.

        * `PRICING_READ_ONLY` — просмотр цен.

        * `OFFERS_AND_CARDS_MANAGEMENT` — управление товарами и карточками.

        * `OFFERS_AND_CARDS_MANAGEMENT_READ_ONLY` — просмотр товаров и карточек.

        * `PROMOTION` — продвижение товаров.

        * `PROMOTION_READ_ONLY` — просмотр информации о продвижении товаров.

        * `FINANCE_AND_ACCOUNTING` — просмотр финансовой информации и отчётности.

        * `COMMUNICATION` — общение с покупателями.

        * `SETTINGS_MANAGEMENT` — настройка магазинов.

        * `SUPPLIES_MANAGEMENT_READ_ONLY` — получение информации по заявкам на поставку, вывоз и утилизацию (FBY, LaaS).

        '
      type: string
      enum:
      - ALL_METHODS
      - ALL_METHODS_READ_ONLY
      - INVENTORY_AND_ORDER_PROCESSING
      - INVENTORY_AND_ORDER_PROCESSING_READ_ONLY
      - PRICING
      - PRICING_READ_ONLY
      - OFFERS_AND_CARDS_MANAGEMENT
      - OFFERS_AND_CARDS_MANAGEMENT_READ_ONLY
      - PROMOTION
      - PROMOTION_READ_ONLY
      - FINANCE_AND_ACCOUNTING
      - COMMUNICATION
      - SETTINGS_MANAGEMENT
      - SUPPLIES_MANAGEMENT_READ_ONLY
    ApiLimitErrorResponse:
      description: Превышено ограничение на доступ к ресурсу.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    ApiLockedErrorResponse:
      description: Ресурс из запроса заблокирован от применения к нему указанного метода.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    ApiNotFoundErrorResponse:
      description: Запрашиваемый ресурс не найден.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    ApiResponse:
      type: object
      description: Стандартная обертка для ответов сервера.
      required:
      - status
      properties:
        status:
          $ref: '#/components/schemas/ApiResponseStatusType'
    ApiResponseStatusType:
      type: string
      description: 'Тип ответа.

        Возможные значения:

        * `OK` — ошибок нет.

        * `ERROR` — при обработке запроса произошла ошибка.

        '
      enum:
      - OK
      - ERROR
    ApiServerErrorResponse:
      description: Внутренняя ошибка сервера.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    ApiUnauthorizedErrorResponse:
      description: В запросе не указаны авторизационные данные.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiErrorResponse'
    BarcodeFormatType:
      description: 'Формат страницы и размер штрихкодов:


        * `F_30_20` — А4, штрихкоды размера 30 × 20 мм.

        * `F_43_25` — А4, штрихкоды размера 43 × 25 мм.

        * `F_58_40` — А4, штрихкоды размера 58 × 40 мм.

        * `F_43_25_SINGLE` — для печати этикеток.

        '
      type: string
      enum:
      - F_30_20
      - F_43_25
      - F_58_40
      - F_43_25_SINGLE
    BarcodeOfferInfoDTO:
      description: Информация о штрихкодах.
      type: object
      required:
      - offerId
      - barcodeCount
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        barcodeCount:
          description: Количество штрихкодов для печати.
          type: integer
          format: int64
          maximum: 100
          minimum: 1
    BaseCampaignOfferDTO:
      description: Информация о новой цене на товар.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        available:
          description: '{% note warning "Вместо него используйте методы скрытия товаров с витрины" %}


            * [GET v2/campaigns/{campaignId}/hidden-offers](../../reference/hidden-offers/getHiddenOffers.md) — просмотр скрытых
            товаров;

            * [POST v2/campaigns/{campaignId}/hidden-offers](../../reference/hidden-offers/addHiddenOffers.md) — скрытие товаров;

            * [POST v2/campaigns/{campaignId}/hidden-offers/delete](../../reference/hidden-offers/deleteHiddenOffers.md) —
            возобновление показа.


            {% endnote %}


            Есть ли товар в продаже.

            '
          deprecated: true
          type: boolean
    BaseOfferAdult:
      description: 'Параметр включает для товара пометку 18+. Устанавливайте ее только для товаров, которые относятся к удовлетворению
        сексуальных потребностей.

        '
      type: boolean
    BaseOfferBoxCount:
      description: 'Количество грузовых мест.


        Параметр используется, если товар представляет собой несколько коробок, упаковок и так далее. Например, кондиционер
        занимает два места — внешний и внутренний блоки в двух коробках.


        Для товаров, занимающих одно место, не передавайте этот параметр.

        '
      type: integer
      format: int32
      minimum: 1
    BaseOfferCommodityCodes:
      description: 'Товарные коды.

        '
      type: array
      nullable: true
      minItems: 1
      items:
        $ref: '#/components/schemas/CommodityCodeDTO'
    BaseOfferCustomsCommodityCode:
      description: '{% note warning "Вместо него используйте `commodityCodes` с типом `CUSTOMS_COMMODITY_CODE`." %}


         


        {% endnote %}


        Код товара в единой Товарной номенклатуре внешнеэкономической деятельности (ТН ВЭД) — 10 или 14 цифр без пробелов.


        Обязательно укажите, если он есть.

        '
      deprecated: true
      example: '8517610008'
      type: string
    BaseOfferDTO:
      description: Основные параметры товара.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        name:
          $ref: '#/components/schemas/OfferName'
        marketCategoryId:
          $ref: '#/components/schemas/PartnerMarketCategoryId'
        category:
          $ref: '#/components/schemas/OfferCategory'
        pictures:
          description: 'Ссылки на изображения товара. Изображение по первой ссылке считается основным, остальные дополнительными.


            **Требования к ссылкам**


            * Указывайте ссылку целиком, включая протокол http или https.

            * Русские буквы в URL можно.

            * Можно использовать прямые ссылки на изображения и на Яндекс Диск. Ссылки на Яндекс Диске нужно копировать с
            помощью функции **Поделиться**. Относительные ссылки и ссылки на другие облачные хранилища — не работают.


            ✅ `https://example-shop.ru/images/sku12345.jpg`


            ✅ `https://yadi.sk/i/NaBoRsimVOLov`


            ❌ `/images/sku12345.jpg`


            ❌ `https://www.dropbox.com/s/818f/tovar.jpg`


            Ссылки на изображение должны быть постоянными. Нельзя использовать динамические ссылки, меняющиеся от выгрузки
            к выгрузке.


            Если нужно заменить изображение, выложите новое изображение по новой ссылке, а ссылку на старое удалите. Если
            просто заменить изображение по старой ссылке, оно не обновится.


            [Требования к изображениям](https://yandex.ru/support/marketplace/assortment/fields/images.html)

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 30
          uniqueItems: false
          items:
            $ref: '#/components/schemas/Url'
        videos:
          description: 'Ссылки (URL) на видео товара.


            **Требования к ссылке**


            * Указывайте ссылку целиком, включая протокол http или https.

            * Русские буквы в URL можно.

            * Можно использовать прямые ссылки на видео и на Яндекс Диск. Ссылки на Яндекс Диске нужно копировать с помощью
            функции **Поделиться**. Относительные ссылки и ссылки на другие облачные хранилища — не работают.


            ✅ `https://example-shop.ru/video/sku12345.avi`


            ✅ `https://yadi.sk/i/NaBoRsimVOLov`


            ❌ `/video/sku12345.avi`


            ❌ `https://www.dropbox.com/s/818f/super-tovar.avi`


            Ссылки на видео должны быть постоянными. Нельзя использовать динамические ссылки, меняющиеся от выгрузки к выгрузке.


            Если нужно заменить видео, выложите новое видео по новой ссылке, а ссылку на старое удалите. Если просто заменить
            видео по старой ссылке, оно не обновится.


            [Требования к видео](https://yandex.ru/support/marketplace/assortment/fields/video.html)

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 6
          uniqueItems: false
          items:
            $ref: '#/components/schemas/Url'
        manuals:
          description: 'Список инструкций по использованию товара.

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 6
          items:
            $ref: '#/components/schemas/OfferManualDTO'
        vendor:
          $ref: '#/components/schemas/OfferVendor'
        barcodes:
          $ref: '#/components/schemas/OfferBarcodes'
        description:
          $ref: '#/components/schemas/OfferDescription'
        manufacturerCountries:
          $ref: '#/components/schemas/BaseOfferManufacturerCountries'
        weightDimensions:
          $ref: '#/components/schemas/OfferWeightDimensionsDTO'
          description: 'Габариты упаковки и вес товара.


            Должны быть больше 0.

            '
        vendorCode:
          $ref: '#/components/schemas/OfferVendorCode'
        tags:
          $ref: '#/components/schemas/BaseOfferTags'
        shelfLife:
          $ref: '#/components/schemas/TimePeriodDTO'
          description: 'Срок годности — период, по прошествии которого товар становится непригоден.


            Указывайте срок, указанный на банке или упаковке. Текущая дата, дата поставки или дата отгрузки значения не имеет.


            Обязательно указывайте срок, если он есть.


            В комментарии укажите условия хранения. Например, `Хранить в сухом помещении`.

            '
        lifeTime:
          $ref: '#/components/schemas/TimePeriodDTO'
          description: 'Срок службы — период, в течение которого товар должен исправно выполнять свою функцию.


            Обязательно указывайте срок, если он есть.


            В комментарии укажите условия хранения. Например, `Использовать при температуре не ниже −10 градусов`.

            '
        guaranteePeriod:
          $ref: '#/components/schemas/TimePeriodDTO'
          description: 'Гарантийный срок — период, в течение которого можно заменить или починить товар без дополнительной
            платы.


            Обязательно указывайте срок, если он есть.


            В комментарии опишите особенности гарантийного обслуживания. Например, `Гарантия на аккумулятор — 6 месяцев`.

            '
        customsCommodityCode:
          $ref: '#/components/schemas/BaseOfferCustomsCommodityCode'
        commodityCodes:
          $ref: '#/components/schemas/BaseOfferCommodityCodes'
        certificates:
          description: 'Номера документов на товар: сертификата, декларации соответствия и т. п.


            Передавать можно только номера документов, сканы которого загружены в кабинете продавца по [инструкции](https://yandex.ru/support/marketplace/assortment/restrictions/certificates.html).

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 6
          uniqueItems: true
          items:
            type: string
        boxCount:
          $ref: '#/components/schemas/BaseOfferBoxCount'
        condition:
          $ref: '#/components/schemas/OfferConditionDTO'
          description: 'Состояние уцененного товара.


            Используется только для товаров, продаваемых с уценкой.


            [Правила продажи уцененных товаров](https://yandex.ru/support/marketplace/assortment/restrictions/used-goods.html)

            '
        type:
          $ref: '#/components/schemas/OfferType'
          description: 'Особый тип товара. Указывается, если товар:


            * имеет особый тип, который хотите убрать;

            * лекарство;

            * бумажная или электронная книга;

            * аудиокнига;

            * музыка или видео;

            * изготовляется на заказ;

            * алкоголь.

            '
        downloadable:
          $ref: '#/components/schemas/BaseOfferDownloadable'
        adult:
          $ref: '#/components/schemas/BaseOfferAdult'
        age:
          $ref: '#/components/schemas/AgeDTO'
          description: 'Если товар не предназначен для детей младше определенного возраста, укажите это.


            Возрастное ограничение можно задавать в годах (с нуля, с 6, 12, 16 или 18) или в месяцах (любое число от 0 до
            12).

            '
        params:
          $ref: '#/components/schemas/BaseOfferParams'
    BaseOfferDownloadable:
      description: 'Признак цифрового товара. Укажите `true`, если товар доставляется по электронной почте.


        [Как работать с цифровыми товарами](../../step-by-step/digital.md)

        '
      type: boolean
    BaseOfferManufacturerCountries:
      description: 'Страна, где был произведен товар.


        Записывайте названия стран так, как они записаны в [списке](https://yastatic.net/s3/doc-binary/src/support/market/ru/countries.xlsx).

        '
      type: array
      nullable: true
      minItems: 1
      uniqueItems: true
      items:
        type: string
        example: Россия
    BaseOfferParams:
      description: '{% note warning "При передаче характеристик используйте `parameterValues`." %}


         


        {% endnote %}


        Характеристики, которые есть только у товаров конкретной категории — например, диаметр колес велосипеда или материал
        подошвы обуви.

        '
      type: array
      nullable: true
      minItems: 1
      deprecated: true
      items:
        $ref: '#/components/schemas/OfferParamDTO'
    BaseOfferResponseDTO:
      description: Основные параметры товара.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        name:
          description: 'Составляйте название по схеме: тип + бренд или производитель + модель + особенности, если есть (например,
            цвет, размер или вес) и количество в упаковке.


            Не включайте в название условия продажи (например, «скидка», «бесплатная доставка» и т. д.), эмоциональные характеристики
            («хит», «супер» и т. д.). Не пишите слова большими буквами — кроме устоявшихся названий брендов и моделей.


            Оптимальная длина — 50–60 символов.


            [Рекомендации и правила](https://yandex.ru/support/marketplace/assortment/fields/title.html)

            '
          example: Ударная дрель Makita HP1630, 710 Вт
          type: string
        marketCategoryId:
          $ref: '#/components/schemas/PartnerMarketCategoryId'
        category:
          $ref: '#/components/schemas/OfferCategory'
        pictures:
          description: 'Ссылки на изображения товара. Изображение по первой ссылке считается основным, остальные дополнительными.


            **Требования к ссылкам**


            * Ссылок может быть до 30.

            * Указывайте ссылку целиком, включая протокол http или https.

            * Максимальная длина — 512 символов.

            * Русские буквы в URL можно.

            * Можно использовать прямые ссылки на изображения и на Яндекс Диск. Ссылки на Яндекс Диске нужно копировать с
            помощью функции **Поделиться**. Относительные ссылки и ссылки на другие облачные хранилища — не работают.


            ✅ `https://example-shop.ru/images/sku12345.jpg`


            ✅ `https://yadi.sk/i/NaBoRsimVOLov`


            ❌ `/images/sku12345.jpg`


            ❌ `https://www.dropbox.com/s/818f/tovar.jpg`


            Ссылки на изображение должны быть постоянными. Нельзя использовать динамические ссылки, меняющиеся от выгрузки
            к выгрузке.


            Если нужно заменить изображение, выложите новое изображение по новой ссылке, а ссылку на старое удалите. Если
            просто заменить изображение по старой ссылке, оно не обновится.


            [Требования к изображениям](https://yandex.ru/support/marketplace/assortment/fields/images.html)

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: false
          items:
            $ref: '#/components/schemas/Url'
        videos:
          description: 'Ссылки (URL) на видео товара.


            **Требования к ссылке**


            * Указывайте ссылку целиком, включая протокол http или https.

            * Максимальная длина — 512 символов.

            * Русские буквы в URL можно.

            * Можно использовать прямые ссылки на видео и на Яндекс Диск. Ссылки на Яндекс Диске нужно копировать с помощью
            функции **Поделиться**. Относительные ссылки и ссылки на другие облачные хранилища — не работают.


            ✅ `https://example-shop.ru/video/sku12345.avi`


            ✅ `https://yadi.sk/i/NaBoRsimVOLov`


            ❌ `/video/sku12345.avi`


            ❌ `https://www.dropbox.com/s/818f/super-tovar.avi`


            Ссылки на видео должны быть постоянными. Нельзя использовать динамические ссылки, меняющиеся от выгрузки к выгрузке.


            Если нужно заменить видео, выложите новое видео по новой ссылке, а ссылку на старое удалите. Если просто заменить
            видео по старой ссылке, оно не обновится.


            [Требования к видео](https://yandex.ru/support/marketplace/assortment/fields/video.html)

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: false
          items:
            $ref: '#/components/schemas/Url'
        manuals:
          description: 'Список инструкций по использованию товара.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferManualDTO'
        vendor:
          $ref: '#/components/schemas/OfferVendor'
        barcodes:
          $ref: '#/components/schemas/OfferBarcodes'
        description:
          description: 'Подробное описание товара: например, его преимущества и особенности.


            Не давайте в описании инструкций по установке и сборке. Не используйте слова «скидка», «распродажа», «дешевый»,
            «подарок» (кроме подарочных категорий), «бесплатно», «акция», «специальная цена», «новинка», «new», «аналог»,
            «заказ», «хит». Не указывайте никакой контактной информации и не давайте ссылок.


            Для форматирования текста можно использовать теги HTML:


            * \<h>, \<h1>, \<h2> и так далее — для заголовков;

            * \<br> и \<p> — для переноса строки;

            * \<ol> — для нумерованного списка;

            * \<ul> — для маркированного списка;

            * \<li> — для создания элементов списка (должен находиться внутри \<ol> или \<ul>);

            * \<div> — поддерживается, но не влияет на отображение текста.


            Оптимальная длина — 400–600 символов.


            [Рекомендации и правила](https://yandex.ru/support/marketplace/assortment/fields/description.html)

            '
          type: string
        manufacturerCountries:
          $ref: '#/components/schemas/BaseOfferManufacturerCountries'
        weightDimensions:
          $ref: '#/components/schemas/OfferWeightDimensionsDTO'
          description: 'Габариты упаковки и вес товара.

            '
        vendorCode:
          $ref: '#/components/schemas/OfferVendorCode'
        tags:
          $ref: '#/components/schemas/BaseOfferTags'
        shelfLife:
          $ref: '#/components/schemas/TimePeriodDTO'
          description: 'Срок годности — период, по прошествии которого товар становится непригоден.


            Указывайте срок, указанный на банке или упаковке. Текущая дата, дата поставки или дата отгрузки значения не имеет.


            Обязательно указывайте срок, если он есть.


            В комментарии укажите условия хранения. Например, `Хранить в сухом помещении`.

            '
        lifeTime:
          $ref: '#/components/schemas/TimePeriodDTO'
          description: 'Срок службы — период, в течение которого товар должен исправно выполнять свою функцию.


            Обязательно указывайте срок, если он есть.


            В комментарии укажите условия хранения. Например, `Использовать при температуре не ниже −10 градусов`.

            '
        guaranteePeriod:
          $ref: '#/components/schemas/TimePeriodDTO'
          description: 'Гарантийный срок — период, в течение которого можно заменить или починить товар без дополнительной
            платы.


            Обязательно указывайте срок, если он есть.


            В комментарии опишите особенности гарантийного обслуживания. Например, `Гарантия на аккумулятор — 6 месяцев`.

            '
        customsCommodityCode:
          $ref: '#/components/schemas/BaseOfferCustomsCommodityCode'
        commodityCodes:
          $ref: '#/components/schemas/BaseOfferCommodityCodes'
        certificates:
          description: 'Номера документов на товар: сертификата, декларации соответствия и т. п.


            Передавать можно только номера документов, сканы которого загружены в кабинете продавца по [инструкции](https://yandex.ru/support/marketplace/assortment/restrictions/certificates.html).

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        boxCount:
          $ref: '#/components/schemas/BaseOfferBoxCount'
        condition:
          $ref: '#/components/schemas/OfferConditionDTO'
          description: 'Состояние уцененного товара.


            Используется только для товаров, продаваемых с уценкой.


            [Правила продажи уцененных товаров](https://yandex.ru/support/marketplace/assortment/restrictions/used-goods.html)

            '
        type:
          $ref: '#/components/schemas/OfferType'
          description: 'Особый тип товара. Указывается, если товар:


            * имеет особый тип, который хотите убрать;

            * лекарство;

            * бумажная или электронная книга;

            * аудиокнига;

            * музыка или видео;

            * изготовляется на заказ;

            * алкоголь.

            '
        downloadable:
          $ref: '#/components/schemas/BaseOfferDownloadable'
        adult:
          $ref: '#/components/schemas/BaseOfferAdult'
        age:
          $ref: '#/components/schemas/AgeDTO'
          description: 'Если товар не предназначен для детей младше определенного возраста, укажите это.


            Возрастное ограничение можно задавать в годах (с нуля, с 6, 12, 16 или 18) или в месяцах (любое число от 0 до
            12).

            '
        params:
          $ref: '#/components/schemas/BaseOfferParams'
    BaseOfferTags:
      description: 'Метки товара, которые использует магазин. Покупателям теги не видны. По тегам можно группировать и фильтровать
        разные товары в каталоге — например, товары одной серии, коллекции или линейки.


        Максимальная длина тега — 20 символов. У одного товара может быть максимум 10 тегов.

        '
      type: array
      nullable: true
      minItems: 1
      maxItems: 50
      uniqueItems: true
      items:
        type: string
        example: до 500 рублей
    BasePriceDTO:
      description: Цена товара.
      type: object
      required:
      - value
      - currencyId
      properties:
        value:
          description: Цена товара.
          type: number
          minimum: 0
          exclusiveMinimum: true
        currencyId:
          $ref: '#/components/schemas/CurrencyType'
          description: Валюта.
    BaseShipmentDTO:
      type: object
      description: Информация об отгрузке.
      required:
      - orderIds
      - id
      - planIntervalFrom
      - planIntervalTo
      - draftCount
      - plannedCount
      - factCount
      - signature
      properties:
        id:
          description: Идентификатор отгрузки.
          format: int64
          type: integer
          minimum: 1
        planIntervalFrom:
          description: 'Начало планового интервала отгрузки.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          format: date-time
          type: string
          example: '2017-11-21T00:00:00+03:00'
        planIntervalTo:
          description: 'Конец планового интервала отгрузки.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          format: date-time
          type: string
          example: '2017-11-21T00:00:00+03:00'
        shipmentType:
          $ref: '#/components/schemas/ShipmentType'
          description: Способ отгрузки заказов.
        warehouse:
          $ref: '#/components/schemas/PartnerShipmentWarehouseDTO'
          description: Данные о складе отправления.
        warehouseTo:
          $ref: '#/components/schemas/PartnerShipmentWarehouseDTO'
          description: Данные о складе назначения.
        externalId:
          description: Идентификатор отгрузки в вашей системе. Если вы еще не передавали идентификатор, вернется идентификатор
            из параметра `id`.
          type: string
        deliveryService:
          $ref: '#/components/schemas/DeliveryServiceDTO'
          description: Служба доставки.
        palletsCount:
          $ref: '#/components/schemas/PalletsCountDTO'
          description: Данные о палетах в отгрузке.
        orderIds:
          description: Идентификаторы заказов в отгрузке.
          type: array
          uniqueItems: true
          items:
            type: integer
            format: int64
            minimum: 1
        draftCount:
          description: Количество заказов, которое Маркет запланировал к отгрузке.
          type: integer
          format: int32
          minimum: 0
        plannedCount:
          description: Количество заказов, которое Маркет подтвердил к отгрузке.
          type: integer
          format: int32
          minimum: 0
        factCount:
          description: Количество заказов, принятых в сортировочном центре или пункте приема.
          type: integer
          format: int32
          minimum: 0
        signature:
          $ref: '#/components/schemas/SignatureDTO'
    BasicCourierDeliveryAddressDTO:
      type: object
      description: Адрес доставки.
      required:
      - fullAddress
      properties:
        fullAddress:
          type: string
          description: Полный адрес с точностью до номера дома.
          minLength: 1
          maxLength: 512
    BasicOrderItemDTO:
      description: Товар в заказе или возврате.
      type: object
      required:
      - offerId
      - count
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        count:
          description: Количество единиц товара.
          type: integer
          format: int32
          minimum: 1
          maximum: 1000
    BenefitType:
      description: 'Типы дополнительных инструментов продвижения:


        * `BESTS` — участие в акции «Бестселлеры Маркета».

        * `SPLIT_0_0_4` — возможность оплаты со Сплитом сроком на 4 месяца.

        * `SPLIT_0_0_6` — возможность оплаты со Сплитом сроком на 6 месяцев.

        * `SPLIT_0_0_12` — возможность оплаты со Сплитом сроком на 12 месяцев.

        * `MARKET_SUBSIDY_1_4` — скидка от Маркета от 1 до 4%.

        * `MARKET_SUBSIDY_5_9` — скидка от Маркета от 5 до 9%.

        * `MARKET_SUBSIDY_10` — скидка от Маркета от 10%.

        '
      type: string
      enum:
      - BESTS
      - SPLIT_0_0_4
      - SPLIT_0_0_6
      - SPLIT_0_0_12
      - MARKET_SUBSIDY_1_4
      - MARKET_SUBSIDY_5_9
      - MARKET_SUBSIDY_10
    Bid:
      description: Значение ставки.
      type: integer
      format: int32
      minimum: 0
      maximum: 9999
      example: 570
    BidRecommendationItemDTO:
      description: Рекомендованная ставка, возможная доля показов и доступные дополнительные инструменты продвижения.
      type: object
      required:
      - bid
      - showPercent
      properties:
        bid:
          $ref: '#/components/schemas/Bid'
          description: 'Значение рекомендованной ставки для товара из параметра `sku`, от 50 до 9999.


            Указывается в процентах от стоимости товара и умножается на 100. Например, ставка 5% обозначается как 500.

            '
        showPercent:
          description: 'Доля показов.

            '
          type: integer
          format: int64
          minimum: 0
          maximum: 100
        benefits:
          description: 'Список доступных субсидий.


            Чтобы получить необходимый инструмент продвижения, установите ставку, которая будет рекомендована для этого инструмента
            или выше.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/BenefitType'
    BriefOrderItemDTO:
      description: Информация о маркированном товаре.
      type: object
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Позволяет идентифицировать товар в рамках заказа.

            '
          type: integer
          format: int64
        vat:
          $ref: '#/components/schemas/OrderVatType'
          description: Налог на добавленную стоимость (НДС) на услугу доставки заказа.
        count:
          description: Количество единиц товара.
          type: integer
          format: int32
        price:
          description: 'Цена товара. Указана в той валюте, которая была задана в каталоге. Разделитель целой и дробной части
            — точка.

            '
          type: number
        offerName:
          description: Название товара.
          type: string
        offerId:
          $ref: '#/components/schemas/ShopSku'
          description: Ваш идентификатор товара.
        instances:
          description: Переданные коды маркировки.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemInstanceDTO'
    BriefOrderItemInstanceDTO:
      description: 'Идентификатор единицы товара.


        Заполните только одно поле в зависимости от того, в какой системе маркирован товар.


        Подробно о работе с маркируемыми товарами читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/orders/cz.html).

        '
      type: object
      properties:
        cis:
          $ref: '#/components/schemas/Cis'
        uin:
          description: 'Уникальный идентификационный номер ювелирного изделия.


            Представляет собой число из 16 цифр.

            '
          type: string
        rnpt:
          description: 'Регистрационный номер партии товара.


            Представляет собой строку из четырех чисел, разделенных косой чертой: ХХХХХХХХ/ХХХХХХ/ХХХХХХХ/ХХХ.


            Первая часть — код таможни, которая зарегистрировала декларацию на партию товара. Далее — дата, номер декларации
            и номер маркированного товара в декларации.

            '
          type: string
        gtd:
          description: 'Грузовая таможенная декларация.


            Представляет собой строку из трех чисел, разделенных косой чертой: ХХХХХХХХ/ХХХХХХ/ХХХХХХХ.


            Первая часть — код таможни, которая зарегистрировала декларацию на ввезенные товары. Далее — дата и номер декларации.

            '
          type: string
        countryCode:
          $ref: '#/components/schemas/CountryCode'
    BusinessDTO:
      description: Информация о кабинете.
      type: object
      properties:
        id:
          $ref: '#/components/schemas/BusinessId'
        name:
          description: Название бизнеса.
          type: string
    BusinessId:
      description: 'Идентификатор кабинета. {% if audience == "partner" %}Чтобы его узнать, воспользуйтесь запросом [GET v2/campaigns](../../reference/campaigns/getCampaigns.md).


        ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html)

        {% endif %}

        '
      type: integer
      format: int64
      minimum: 1
    BusinessOrderBoxLayoutDTO:
      description: Информация о коробке (для заказов в кабинете).
      type: object
      required:
      - items
      - boxId
      - barcode
      properties:
        items:
          description: 'Список товаров в коробке.


            Если в коробке едет часть большого товара, в списке может быть только один пункт.

            '
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/BusinessOrderBoxLayoutItemDTO'
        boxId:
          description: Идентификатор коробки.
          type: integer
          format: int64
        barcode:
          description: Идентификатор грузового места в системе магазина.
          type: string
    BusinessOrderBoxLayoutItemDTO:
      description: Информация о товаре в коробке.
      type: object
      required:
      - id
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Параметр `id` в `items`.

            '
          type: integer
          format: int64
        fullCount:
          description: 'Количество единиц товара в коробке.

            '
          type: integer
          format: int32
          minimum: 1
        partialCount:
          $ref: '#/components/schemas/BusinessOrderBoxLayoutPartialCountDTO'
          description: 'Информация о части товара в коробке.

            '
        instances:
          description: Переданные коды маркировки.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/BriefOrderItemInstanceDTO'
    BusinessOrderBoxLayoutPartialCountDTO:
      description: Информация о части товара в коробке.
      type: object
      required:
      - current
      - total
      properties:
        current:
          description: Номер части, начиная с 1.
          type: integer
          format: int32
          minimum: 1
        total:
          description: На сколько всего частей разделен товар.
          type: integer
          format: int32
          minimum: 2
    BusinessOrderCourierDeliveryDTO:
      description: Информация о курьерской доставке.
      type: object
      properties:
        address:
          $ref: '#/components/schemas/BusinessOrderDeliveryAddressDTO'
        region:
          $ref: '#/components/schemas/RegionDTO'
    BusinessOrderDTO:
      description: Информация о заказе.
      type: object
      required:
      - orderId
      - campaignId
      - status
      - substatus
      - creationDate
      - paymentType
      - paymentMethod
      - fake
      - items
      - delivery
      properties:
        orderId:
          description: Идентификатор заказа.
          type: integer
          format: int64
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        programType:
          $ref: '#/components/schemas/SellingProgramType'
          description: Тип программы кампании магазина.
        externalOrderId:
          $ref: '#/components/schemas/ExternalOrderId'
        status:
          $ref: '#/components/schemas/OrderStatusType'
        substatus:
          $ref: '#/components/schemas/OrderSubstatusType'
        creationDate:
          description: 'Дата и время оформления заказа.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        updateDate:
          description: 'Дата и время последнего обновления заказа.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        paymentType:
          $ref: '#/components/schemas/OrderPaymentType'
        paymentMethod:
          $ref: '#/components/schemas/OrderPaymentMethodType'
        fake:
          description: 'Тип заказа:


            * `false` — настоящий заказ покупателя.


            * `true` — [тестовый заказ](../../concepts/sandbox.md) Маркета.

            '
          type: boolean
        items:
          description: Список товаров в заказе.
          type: array
          items:
            $ref: '#/components/schemas/BusinessOrderItemDTO'
        prices:
          $ref: '#/components/schemas/OrderPriceDTO'
          description: 'Информация о стоимости заказа, доставки и вознаграждениях.

            '
        delivery:
          $ref: '#/components/schemas/BusinessOrderDeliveryDTO'
        services:
          $ref: '#/components/schemas/BusinessOrderServicesDTO'
        buyerType:
          $ref: '#/components/schemas/OrderBuyerType'
          description: 'Тип покупателя: физическое лицо или организация.


            Только для FBS- и FBY-магазинов, которые размещают товары на витрине [business.market.yandex.ru](https://business.market.yandex.ru).

            '
        notes:
          description: Комментарий к заказу.
          type: string
        cancelRequested:
          description: '**Только для модели DBS**


            Запрошена ли отмена.

            '
          type: boolean
        sourcePlatform:
          $ref: '#/components/schemas/OrderSourcePlatformType'
    BusinessOrderDeliveryAddressDTO:
      description: 'Адрес доставки.


        Указывается, если параметр `type` принимает значение `DELIVERY`, `POST` или `PICKUP` (только для модели DBS). Если
        `type=PICKUP`, возвращается адрес пункта выдачи.

        '
      type: object
      properties:
        country:
          description: Страна.
          type: string
        postcode:
          description: Почтовый индекс.
          type: string
        city:
          description: Город или населенный пункт.
          type: string
        district:
          description: Район.
          type: string
        subway:
          description: Станция метро.
          type: string
        street:
          description: Улица.
          type: string
        house:
          description: Номер дома.
          type: string
        block:
          description: Корпус.
          type: string
        entrance:
          description: Номер подъезда.
          type: string
        entryphone:
          description: Код домофона.
          type: string
        floor:
          description: Этаж.
          type: string
        apartment:
          description: Номер квартиры или офиса.
          type: string
        gps:
          $ref: '#/components/schemas/GpsDTO'
          description: GPS-координаты.
    BusinessOrderDeliveryDTO:
      description: Информация о доставке заказа.
      type: object
      required:
      - type
      - serviceName
      - deliveryServiceId
      - deliveryPartnerType
      - dates
      properties:
        type:
          $ref: '#/components/schemas/OrderDeliveryType'
          description: 'Способ доставки заказа.

            '
        serviceName:
          description: Название службы доставки.
          type: string
        deliveryServiceId:
          description: Идентификатор службы доставки.
          type: integer
          format: int64
        warehouseId:
          description: Идентификатор склада в системе магазина, на который сформирован заказ.
          type: string
        deliveryPartnerType:
          $ref: '#/components/schemas/OrderDeliveryPartnerType'
          description: Тип сотрудничества со службой доставки в рамках указанного заказа.
        dispatchType:
          $ref: '#/components/schemas/OrderDeliveryDispatchType'
        dates:
          $ref: '#/components/schemas/BusinessOrderDeliveryDatesDTO'
        shipment:
          $ref: '#/components/schemas/BusinessOrderShipmentDTO'
          description: Информация об отгрузке.
        courier:
          $ref: '#/components/schemas/BusinessOrderCourierDeliveryDTO'
          description: Информация о курьерской доставке.
        pickup:
          $ref: '#/components/schemas/BusinessOrderPickupDeliveryDTO'
          description: Информация о доставке в пункт выдачи.
        transfer:
          $ref: '#/components/schemas/BusinessOrderTransferDTO'
          description: Информация о курьере и код подтверждения.
        boxesLayout:
          description: Раскладка товаров по коробкам.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/BusinessOrderBoxLayoutDTO'
        tracks:
          description: Информация для отслеживания посылки.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderTrackDTO'
        estimated:
          description: Приблизительная ли дата доставки.
          type: boolean
        receiveBarcode:
          description: '**Только для модели LaaS**


            Штрихкод получения заказа на ПВЗ.

            '
          type: string
        receiveCode:
          description: '**Только для модели LaaS**


            Код получения заказа на ПВЗ.

            '
          type: string
        digitalGoods:
          $ref: '#/components/schemas/DigitalGoodsDeliveryDetailsDTO'
          description: Информация о доставке цифрового товара.
    BusinessOrderDeliveryDatesDTO:
      description: Диапазон дат доставки.
      type: object
      required:
      - fromDate
      properties:
        fromDate:
          description: 'Ближайшая дата доставки.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        toDate:
          description: 'Самая поздняя дата доставки.


            Если `toDate` не указан, считается дата в параметре `fromDate`.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        fromTime:
          description: 'Начало интервала времени доставки.


            Передается только вместе с параметром `type=DELIVERY`.


            Формат времени: 24-часовой, `ЧЧ:ММ`. Вместо `ММ` всегда указывайте `00` (исключение — `23:59`).


            Минимальное значение: `00:00`.

            '
          type: string
          format: time
        toTime:
          description: 'Конец интервала времени доставки.


            Передается только вместе с параметром `type=DELIVERY`.


            Формат времени: 24-часовой, `ЧЧ:ММ`. Вместо `ММ` всегда указывайте `00` (исключение — `23:59`).


            Максимальное значение: `23:59`.

            '
          type: string
          format: time
        realDeliveryDate:
          description: 'Дата, когда товар доставлен до пункта выдачи заказа (в случае самовывоза) или до покупателя (если
            заказ доставляет курьер).


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    BusinessOrderEacDTO:
      description: Информация о коде подтверждения.
      type: object
      required:
      - eacType
      properties:
        eacType:
          $ref: '#/components/schemas/OrderDeliveryEacType'
          description: Тип кода подтверждения ЭАПП.
        eacCode:
          description: 'Код подтверждения ЭАПП (для типа `MERCHANT_TO_COURIER`).

            '
          type: string
    BusinessOrderItemDTO:
      description: Информация о товаре.
      type: object
      required:
      - id
      - offerId
      - offerName
      - count
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Позволяет идентифицировать товар в рамках заказа.

            '
          type: integer
          format: int64
        offerId:
          $ref: '#/components/schemas/ShopSku'
          description: Идентификатор товарного предложения.
        offerName:
          description: Название товара.
          type: string
        count:
          description: Количество единиц товара.
          type: integer
        prices:
          $ref: '#/components/schemas/ItemPriceDTO'
          description: 'Информация о выплатах и вознаграждениях.

            '
        instances:
          description: 'Информация о маркировке единиц товара.


            Возвращаются данные для маркировки, переданные в запросе:


            * Для DBS — [PUT v2/campaigns/{campaignId}/orders/{orderId}/identifiers](../../reference/orders/provideOrderItemIdentifiers.md)
            или [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md).

            * Для FBS и EXPRESS — [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md).


            Для FBY возвращаются коды маркировки, переданные во время поставки.


            Если магазин еще не передавал коды для этого заказа, `instances` отсутствует.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemInstanceDTO'
        requiredInstanceTypes:
          description: Список необходимых маркировок товара.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OrderItemInstanceType'
        itemStatuses:
          description: 'Информация о статусах отдельных единиц товара в заказе.


            Если данных о статусах отдельных единиц товара нет, поле отсутствует.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemUnitStatusDTO'
        tags:
          description: Признаки товара.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OrderItemTagType'
    BusinessOrderPickupDeliveryDTO:
      description: Информация о доставке в пункт выдачи.
      type: object
      properties:
        address:
          $ref: '#/components/schemas/BusinessOrderDeliveryAddressDTO'
        region:
          $ref: '#/components/schemas/RegionDTO'
        logisticPointId:
          $ref: '#/components/schemas/LogisticPointId'
        outletCode:
          description: Идентификатор пункта самовывоза, присвоенный магазином.
          type: string
        outletStorageLimitDate:
          description: 'Дата, до которой заказ будет храниться в пункте выдачи. Возвращается, когда заказ переходит в статус
            `PICKUP`.


            Один раз дату можно поменять с помощью метода [PUT v2/campaigns/{campaignId}/orders/{orderId}/delivery/storage-limit](../../reference/order-delivery/updateOrderStorageLimit.md).


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    BusinessOrderServicesDTO:
      description: Услуги, добавленные в заказ.
      type: object
      properties:
        liftType:
          $ref: '#/components/schemas/OrderLiftType'
    BusinessOrderShipmentDTO:
      description: Информация об отгрузке заказа.
      type: object
      required:
      - shipmentDate
      properties:
        id:
          description: Идентификатор отгрузки.
          nullable: true
          type: integer
          format: int64
        shipmentDate:
          description: 'Дата отгрузки.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        shipmentTime:
          description: Время отгрузки.
          nullable: true
          type: string
          format: time
    BusinessOrderTransferDTO:
      description: Информация о курьере и код подтверждения.
      type: object
      properties:
        courier:
          $ref: '#/components/schemas/OrderCourierDTO'
        eac:
          $ref: '#/components/schemas/BusinessOrderEacDTO'
    BusinessSettingsDTO:
      type: object
      description: Настройки кабинета.
      properties:
        onlyDefaultPrice:
          description: "Управление ценами на товары:\n\n* `false` — можно установить цену, которая действует:\n  * во всех\
            \ магазинах кабинета — [POST v2/businesses/{businessId}/offer-prices/updates](../../reference/prices/updateBusinessPrices.md);\n\
            \  * в конкретном магазине — [POST v2/campaigns/{campaignId}/offer-prices/updates](../../reference/prices/updatePrices.md).\n\
            * `true` — можно установить только цену, которая действует во всех магазинах кабинета, — [POST v2/businesses/{businessId}/offer-prices/updates](../../reference/prices/updateBusinessPrices.md).\n"
          type: boolean
        currency:
          $ref: '#/components/schemas/CurrencyType'
          description: Валюта [в кабинете продавца на Маркете](https://partner.market.yandex.ru/).
    BusinessSubscriptionLevelType:
      type: string
      description: 'Уровень подписки кабинета:


        * `NONE` — подписка отсутствует.

        * `LIGHT` — уровень подписки Лайт.

        * `MEDIUM` — уровень подписки Медиум.

        '
      enum:
      - NONE
      - LIGHT
      - MEDIUM
    BusinessTraitType:
      description: 'Свойства кабинета:


        * `MARKET_YANDEX_GO` — продавец Market Yandex Go.

        '
      type: string
      enum:
      - MARKET_YANDEX_GO
    CalculateTariffsOfferDTO:
      description: Параметры товара, для которого нужно рассчитать стоимость услуг.
      type: object
      required:
      - categoryId
      - price
      - length
      - width
      - height
      - weight
      properties:
        categoryId:
          description: 'Идентификатор категории товара на Маркете.


            Для расчета стоимости услуг необходимо указать идентификатор [листовой категории](*list-category) товара.


            Чтобы узнать идентификатор категории, к которой относится товар, воспользуйтесь запросом [POST v2/categories/tree](../../reference/categories/getCategoriesTree.md).

            '
          type: integer
          format: int64
          minimum: 0
          exclusiveMinimum: true
        price:
          description: Цена товара в рублях.
          type: number
          minimum: 0
          exclusiveMinimum: true
        length:
          description: Длина товара в сантиметрах.
          type: number
          minimum: 0
          exclusiveMinimum: true
        width:
          description: Ширина товара в сантиметрах.
          type: number
          minimum: 0
          exclusiveMinimum: true
        height:
          description: Высота товара в сантиметрах.
          type: number
          minimum: 0
          exclusiveMinimum: true
        weight:
          description: Вес товара в килограммах.
          type: number
          minimum: 0
          exclusiveMinimum: true
        quantity:
          description: Квант продажи — количество единиц товара в одном товарном предложении.
          type: integer
          format: int32
          minimum: 1
          default: 1
    CalculateTariffsOfferInfoDTO:
      description: Стоимость услуг.
      type: object
      required:
      - offer
      - tariffs
      properties:
        offer:
          $ref: '#/components/schemas/CalculateTariffsOfferDTO'
          description: Информация о товаре, которую вы передали в запросе для расчета стоимости услуг.
        tariffs:
          description: 'Список услуг и их стоимость.


            По некоторым услугам могут возвращаться несколько разных стоимостей. Например, в модели FBS стоимость услуги `SORTING`
            (обработка заказа) зависит от способа отгрузки и количества заказов в отгрузке. Подробнее о тарифах на услуги
            читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/introduction/rates/models/).

            '
          type: array
          items:
            $ref: '#/components/schemas/CalculatedTariffDTO'
    CalculateTariffsParametersDTO:
      description: Параметры для расчета стоимости услуг. Обязательно необходимо указать параметр `campaignId` либо `sellingProgram`.
        Совместное использование параметров приведет к ошибке.
      type: object
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        sellingProgram:
          $ref: '#/components/schemas/SellingProgramType'
          description: 'Модель работы.


            **Для продавцов Market Yandex Go** недоступны модели DBS и Экспресс.


            Обязательный параметр, если не указан параметр `campaignId`. Совместное использование параметров приведет к ошибке.

            '
        frequency:
          $ref: '#/components/schemas/PaymentFrequencyType'
          description: Частота выплат.
        paymentDelayWeeks:
          description: 'Отсрочка выплат при еженедельном графике — сколько недель назад были доставлены заказы, за которые
            приходит выплата.


            Допустимые значения: 0, 1, 2 или 4.


            Значения параметра `paymentDelayWeeks`, отличные от 0, допускаются только вместе с параметром `frequency` равным
            ''WEEKLY''.

            Использование других значений параметра `frequency` совместно с `paymentDelayWeeks` приведет к ошибке.

            '
          type: integer
          format: int32
          minimum: 0
          maximum: 4
        currency:
          $ref: '#/components/schemas/CurrencyType'
          description: 'Валюта, в которой рассчитывается услуга.


            Значение по умолчанию: `RUR`.

            '
    CalculateTariffsRequest:
      type: object
      required:
      - parameters
      - offers
      properties:
        parameters:
          $ref: '#/components/schemas/CalculateTariffsParametersDTO'
          description: Параметры для расчета стоимости услуг.
        offers:
          description: Товары, для которых нужно рассчитать стоимость услуг.
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: '#/components/schemas/CalculateTariffsOfferDTO'
    CalculateTariffsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CalculateTariffsResponseDTO'
            description: Стоимость услуг.
    CalculateTariffsResponseDTO:
      description: Расчет стоимости услуг.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Стоимость услуг.
          type: array
          items:
            $ref: '#/components/schemas/CalculateTariffsOfferInfoDTO'
    CalculatedTariffDTO:
      description: Информация об услугах Маркета.
      type: object
      required:
      - type
      - parameters
      properties:
        type:
          $ref: '#/components/schemas/CalculatedTariffType'
          description: Услуга Маркета.
        amount:
          description: Стоимость услуги в рублях.
          type: number
        currency:
          $ref: '#/components/schemas/CurrencyType'
          description: Валюта, в которой рассчитывается услуга.
        parameters:
          description: Параметры расчета тарифа.
          type: array
          items:
            $ref: '#/components/schemas/TariffParameterDTO'
    CalculatedTariffType:
      description: 'Услуга Маркета:


        * `AGENCY_COMMISSION` — прием платежа покупателя.


        * `PAYMENT_TRANSFER` — перевод платежа покупателя.


        * `FEE` — размещение товара на Маркете.


        * `DELIVERY_TO_CUSTOMER` — доставка покупателю.


        * `CROSSREGIONAL_DELIVERY` — доставка в федеральный округ, город или населенный пункт.


        * `EXPRESS_DELIVERY` — экспресс-доставка покупателю.


        * `SORTING` — обработка заказа.


        * `MIDDLE_MILE` — средняя миля.


        * `ITEM_BOOKING` — бронирование товара.


        * `SHOP_SERVICE_PLACEMENT` — размещение услуг магазина.


        Подробнее об услугах Маркета читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/introduction/rates/index.html).

        '
      type: string
      enum:
      - AGENCY_COMMISSION
      - PAYMENT_TRANSFER
      - FEE
      - DELIVERY_TO_CUSTOMER
      - CROSSREGIONAL_DELIVERY
      - EXPRESS_DELIVERY
      - SORTING
      - MIDDLE_MILE
      - ITEM_BOOKING
      - SHOP_SERVICE_PLACEMENT
    CampaignDTO:
      description: Информация о магазине.
      type: object
      properties:
        domain:
          description: Название магазина.
          type: string
        id:
          $ref: '#/components/schemas/CampaignId'
        clientId:
          description: Идентификатор плательщика в Яндекс Балансе.
          type: integer
          format: int64
          deprecated: true
        business:
          $ref: '#/components/schemas/BusinessDTO'
        placementType:
          $ref: '#/components/schemas/PlacementType'
        apiAvailability:
          $ref: '#/components/schemas/ApiAvailabilityStatusType'
    CampaignId:
      description: 'Идентификатор кампании (магазина) — технический идентификатор, который представляет ваш магазин в системе
        Яндекс Маркета при работе через API. Он однозначно связывается с вашим магазином, но предназначен только для автоматизированного
        взаимодействия.


        Его можно узнать с помощью запроса [GET v2/campaigns](../../reference/campaigns/getCampaigns.md) или найти в кабинете
        продавца на Маркете. Нажмите на иконку вашего аккаунта → **Настройки** и в меню слева выберите **API и модули**:


        * блок **Идентификатор кампании**;

        * вкладка **Лог запросов** → выпадающий список в блоке **Показывать логи**.


        ⚠️ Не путайте его с:

        - идентификатором магазина, который отображается в личном кабинете продавца;

        - рекламными кампаниями.

        '
      type: integer
      format: int64
      minimum: 1
    CampaignQualityRatingDTO:
      description: Информация об индексе качества магазина.
      type: object
      required:
      - campaignId
      - ratings
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        ratings:
          description: Список значений индекса качества.
          type: array
          items:
            $ref: '#/components/schemas/QualityRatingDTO'
    CampaignSettingsDTO:
      description: Настройки магазина.
      type: object
      properties:
        countryRegion:
          description: Идентификатор региона, в котором находится магазин.
          type: integer
          format: int64
        shopName:
          description: 'Наименование магазина на Яндекс Маркете.

            Если наименование отсутствует, значение параметра выводится — `null`.

            '
          type: string
        showInContext:
          description: 'Признак размещения магазина на сайтах партнеров Яндекс Дистрибуции.

            Возможные значения:

            * `false` — магазин не размещен на сайтах партнеров Яндекс Дистрибуции.

            * `true` — магазин размещен на сайтах партнеров Яндекс Дистрибуции.

            '
          type: boolean
          deprecated: true
        showInPremium:
          description: 'Признак показа предложений магазина в блоке над результатами поиска (cпецразмещение).

            Возможные значения:

            * `false` — предложения не показываются в блоке cпецразмещения.

            * `true` — предложения показываются в блоке cпецразмещения.

            '
          type: boolean
          deprecated: true
        useOpenStat:
          description: 'Признак использования внешней интернет-статистики.

            Возможные значения:

            * `false` — внешняя интернет-статистика не используется.

            * `true` — внешняя интернет-статистика используется.

            '
          type: boolean
          deprecated: true
        localRegion:
          $ref: '#/components/schemas/CampaignSettingsLocalRegionDTO'
    CampaignSettingsDeliveryDTO:
      description: Информация о доставке в своем регионе магазина.
      type: object
      properties:
        schedule:
          $ref: '#/components/schemas/CampaignSettingsScheduleDTO'
    CampaignSettingsLocalRegionDTO:
      description: Информация о своем регионе магазина.
      type: object
      properties:
        id:
          description: Идентификатор региона.
          type: integer
          format: int64
        name:
          description: Название региона.
          type: string
        type:
          $ref: '#/components/schemas/RegionType'
        deliveryOptionsSource:
          $ref: '#/components/schemas/CampaignSettingsScheduleSourceType'
        delivery:
          $ref: '#/components/schemas/CampaignSettingsDeliveryDTO'
    CampaignSettingsScheduleDTO:
      description: Расписание работы службы доставки в своем регионе.
      type: object
      required:
      - customHolidays
      - customWorkingDays
      - totalHolidays
      - weeklyHolidays
      properties:
        availableOnHolidays:
          description: 'Признак работы службы доставки в государственные праздники.

            Возможные значения.

            * `false` — служба доставки не работает в праздничные дни.

            * `true` — служба доставки работает в праздничные дни.

            '
          type: boolean
        customHolidays:
          description: Список дней, в которые служба доставки не работает. Дни магазин указал в кабинете продавца на Маркете.
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/DateDdMmYyyy'
        customWorkingDays:
          description: Список выходных и праздничных дней, в которые служба доставки работает. Дни магазин указал в кабинете
            продавца на Маркете.
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/DateDdMmYyyy'
        period:
          $ref: '#/components/schemas/CampaignSettingsTimePeriodDTO'
        totalHolidays:
          description: Итоговый список нерабочих дней службы доставки. Список рассчитывается с учетом выходных, нерабочих
            дней и государственных праздников. Информацию по ним магазин указывает в кабинете продавца на Маркете.
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/DateDdMmYyyy'
        weeklyHolidays:
          description: Список выходных дней недели и государственных праздников.
          type: array
          uniqueItems: true
          items:
            description: 'Номер дня недели.

              Возможные значения:

              * `1` — понедельник.

              * `2` — вторник.

              * `3` — среда.

              * `4` — четверг.

              * `5` — пятница.

              * `6` — суббота.

              * `7` — воскресенье.


              Для формата JSON выводится номер дня в виде числа.

              '
            type: integer
            format: int32
            minimum: 1
            maximum: 7
    CampaignSettingsScheduleSourceType:
      description: 'Источник информации о расписании работы службы доставки.

        Возможные значения:

        * `WEB` — информация получена из настроек кабинета продавца на Маркете.

        * `YML` — информация получена из прайс-листа магазина.

        '
      type: string
      enum:
      - WEB
      - YML
    CampaignSettingsTimePeriodDTO:
      description: Период, за который рассчитывается итоговый список нерабочих дней службы доставки.
      type: object
      properties:
        fromDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: 'Дата (включительно) начала периода, по которому рассчитан итоговый список нерабочих дней службы доставки.


            Формат даты: `ДД-ММ-ГГГГ`.

            '
        toDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: 'Дата (включительно) окончания периода, по которому рассчитан итоговый список нерабочих дней службы
            доставки.


            Формат даты: `ДД-ММ-ГГГГ`.

            '
    CampaignsQualityRatingDTO:
      description: Информация об индексе качества магазинов.
      type: object
      required:
      - campaignRatings
      properties:
        campaignRatings:
          description: Список магазинов c информацией об их индексе качества.
          type: array
          items:
            $ref: '#/components/schemas/CampaignQualityRatingDTO'
    CancelReturnDTO:
      type: object
      description: Информация об операции по отмене возврата.
      required:
      - operation
      properties:
        operation:
          $ref: '#/components/schemas/OperationDTO'
          description: Запущенная операция отмены возврата.
    CancelReturnRequest:
      type: object
      required:
      - returnId
      properties:
        returnId:
          description: Идентификатор возврата.
          type: integer
          format: int64
    CancelReturnResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CancelReturnDTO'
    CatalogLanguageType:
      description: 'Язык:


        * `RU` — русский.

        * `UZ` — узбекский.

        '
      type: string
      enum:
      - RU
      - UZ
    CategoryContentParametersDTO:
      description: Информация о параметрах категории.
      type: object
      required:
      - categoryId
      properties:
        categoryId:
          $ref: '#/components/schemas/CategoryId'
        parameters:
          description: Список характеристик.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/CategoryParameterDTO'
    CategoryDTO:
      description: 'Информация о категории.


        Категория считается листовой, если у нее нет дочерних категорий.

        '
      type: object
      required:
      - id
      - name
      properties:
        id:
          description: Идентификатор категории.
          type: integer
          format: int64
        name:
          description: Название категории.
          type: string
        children:
          description: Дочерние категории.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/CategoryDTO'
    CategoryErrorDTO:
      description: Текст ошибки.
      type: object
      properties:
        categoryId:
          description: Идентификатор категории.
          type: integer
          format: int64
        type:
          $ref: '#/components/schemas/CategoryErrorType'
    CategoryErrorType:
      description: 'Типы ошибок:


        * `UNKNOWN_CATEGORY` — указана неизвестная категория.

        * `CATEGORY_IS_NOT_LEAF` — указана нелистовая категория. Укажите ту, которая не имеет дочерних категорий.

        '
      type: string
      enum:
      - UNKNOWN_CATEGORY
      - CATEGORY_IS_NOT_LEAF
    CategoryId:
      description: 'Идентификатор категории на Маркете.


        При изменении категории убедитесь, что характеристики товара и их значения в параметре `parameterValues` вы передаете
        для новой категории.


        Список категорий Маркета можно получить с помощью запроса  [POST v2/categories/tree](../../reference/categories/getCategoriesTree.md).

        '
      format: int32
      type: integer
      minimum: 0
      exclusiveMinimum: true
    CategoryParameterDTO:
      description: Характеристика товара.
      type: object
      required:
      - id
      - type
      - required
      - filtering
      - distinctive
      - multivalue
      - allowCustomValues
      properties:
        id:
          description: Идентификатор характеристики.
          type: integer
          format: int64
          minimum: 1
        name:
          description: Название характеристики.
          type: string
        type:
          $ref: '#/components/schemas/ParameterType'
          description: Тип данных.
        unit:
          $ref: '#/components/schemas/CategoryParameterUnitDTO'
        description:
          description: Описание характеристики.
          type: string
        recommendationTypes:
          description: Перечень возможных рекомендаций по заполнению карточки, к которым относится данная характеристика.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OfferCardRecommendationType'
        required:
          description: Обязательность характеристики.
          type: boolean
        filtering:
          description: Используется ли характеристика в фильтре.
          type: boolean
        distinctive:
          description: Является ли характеристика особенностью варианта.
          type: boolean
        multivalue:
          description: Можно ли передать сразу несколько значений.
          type: boolean
        allowCustomValues:
          description: Можно ли передавать собственное значение, которого нет в списке вариантов Маркета. Только для характеристик
            типа `ENUM`.
          type: boolean
        values:
          description: Список допустимых значений параметра. Только для характеристик типа `ENUM`.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ParameterValueOptionDTO'
        constraints:
          $ref: '#/components/schemas/ParameterValueConstraintsDTO'
          description: 'Ограничения на значения. Только для характеристик типа `TEXT` и `NUMERIC`.


            - Для `NUMERIC` используются поля `minValue` и `maxValue`.

            - Для `TEXT` используется поле `maxLength`.

            '
        valueRestrictions:
          description: Ограничения на значения, накладываемые другими характеристиками. Только для характеристик типа `ENUM`.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ValueRestrictionDTO'
    CategoryParameterUnitDTO:
      description: Единицы измерения характеристики товара.
      type: object
      required:
      - defaultUnitId
      - units
      properties:
        defaultUnitId:
          description: Единица измерения по умолчанию.
          type: integer
          format: int64
        units:
          description: Допустимые единицы измерения.
          type: array
          items:
            $ref: '#/components/schemas/UnitDTO'
    ChangeOutletRequest:
      type: object
      allOf:
      - $ref: '#/components/schemas/OutletDTO'
    ChannelType:
      description: 'Каналы продвижения товаров:


        * `PUSH` — пуш-уведомление из приложения Яндекс Маркет.


        * `STRETCH_MAIN` — верхний баннер-растяжка на главной странице Яндекс Маркета.


        * `MAIN_PAGE_CAROUSEL` — карусель акций на главной странице Яндекс Маркета.


        * `PRODUCT_RETAIL_PAGE` — товар на странице ритейл-повода.


        * `MAIN_PAGE_CAROUSEL_WEB` — карусель акций на главной странице веб версии Яндекс Маркета.


        * `PRODUCT_SEPARATE_LANDING` — товар на лендинге акции.


        * `SUPER_SHELF_CATEGORY` — полка в категориях.


        * `CAROUSEL_RETAIL_PAGE` — карусель на лендинге ритейл-повода.


        * `POPUP_APPLICATION` — всплывающее окно в приложении Яндекс Маркет.


        * `POST_TELEGRAM` — пост в Телеграм-канале Яндекс Маркета.


        * `CPA` — реклама в партнерской сети Яндекс Маркета.


        * `WEB_PERFORMANCE_DIRECT` — реклама в Яндекс Директе.


        * `APP_PERFORMANCE` — реклама в :no-translate[AppStore] и :no-translate[Google Play].


        * `BANNER_PICKUP_POINT` — баннер в ПВЗ Маркета.


        * `BLOGGER_PERFORMANCE` — рекламная интеграция у блогеров.


        * `DIGITAL_CHANNEL_BANNER` — баннер в digital-каналах и социальных сетях :no-translate[VK], :no-translate[Одноклассники].


        * `YANDEX_ECOSYSTEM_CHANNELS` — реклама в других сервисах Яндекса: :no-translate[GO], :no-translate[Delivery], :no-translate[Еда].


        * `PARTNERS_MAIN_BANNER` — баннер на главной странице :no-translate[mail.ru], :no-translate[auto.ru], :no-translate[ya.ru].


        * `OTHER` — прочее.

        '
      type: string
      enum:
      - PUSH
      - STRETCH_MAIN
      - MAIN_PAGE_CAROUSEL
      - PRODUCT_RETAIL_PAGE
      - MAIN_PAGE_CAROUSEL_WEB
      - PRODUCT_SEPARATE_LANDING
      - SUPER_SHELF_CATEGORY
      - CAROUSEL_RETAIL_PAGE
      - POPUP_APPLICATION
      - POST_TELEGRAM
      - CPA
      - WEB_PERFORMANCE_DIRECT
      - APP_PERFORMANCE
      - BANNER_PICKUP_POINT
      - BLOGGER_PERFORMANCE
      - DIGITAL_CHANNEL_BANNER
      - YANDEX_ECOSYSTEM_CHANNELS
      - PARTNERS_MAIN_BANNER
      - OTHER
    ChatContextDTO:
      description: Информация о заказе или возврате, по которому начат чат.
      type: object
      required:
      - type
      - id
      properties:
        type:
          $ref: '#/components/schemas/ChatContextIdentifiableType'
        id:
          description: Идентификатор заказа или возврата.
          type: integer
          format: int64
          minimum: 1
    ChatContextIdentifiableType:
      description: 'Тип чата:


        * `ORDER` — по заказам.

        * `RETURN` — по возвратам (FBY, FBS и Экспресс).


        Подробнее о чатах по заказам и возвратам читайте в [Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/communication/about-orders).

        '
      type: string
      enum:
      - ORDER
      - RETURN
    ChatContextType:
      description: 'Тип контекста:


        * `ORDER` — чат по заказу. [Чаты о заказах и возвратах](https://yandex.ru/support/marketplace/ru/orders/communication/about-orders)

        * `RETURN` — чат по возврату (FBY, FBS и Экспресс). [Чаты о заказах и возвратах](https://yandex.ru/support/marketplace/ru/orders/communication/about-orders)

        * `DIRECT` — чат, который начал покупатель. [Сообщения от покупателей](https://yandex.ru/support/marketplace/ru/orders/communication/with-users)

        '
      type: string
      enum:
      - ORDER
      - RETURN
      - DIRECT
    ChatCustomerDTO:
      description: Информация о покупателе в чате.
      type: object
      properties:
        name:
          description: 'Публичное имя покупателя в Яндекс Паспорте, которое отображается в сервисах Яндекса.

            '
          type: string
          minLength: 1
        publicId:
          description: 'Публичный идентификатор пользователя в Яндекс Паспорте.


            {% cut "Примеры, где используется" %}


            * Маркет: `https://market.yandex.ru/user/{public-id}/reviews`

            * Дзен: `https://zen.yandex.ru/user/{public-id}`

            * Отзывы: `https://yandex.ru/user/{public-id}`


            {% endcut %}


            Подробнее о публичных данных читайте в [документации Яндекс ID](https://yandex.ru/support/id/ru/data/public-data).

            '
          type: string
          minLength: 1
    ChatFullContextDTO:
      description: Информация о заказе или возврате, по которому начат чат.
      type: object
      required:
      - type
      properties:
        type:
          $ref: '#/components/schemas/ChatContextType'
        customer:
          $ref: '#/components/schemas/ChatCustomerDTO'
          description: Информация о покупателе.
        campaignId:
          $ref: '#/components/schemas/CampaignId'
          description: Возвращается для заказов и возвратов.
        orderId:
          description: 'Идентификатор заказа.


            Возвращается для заказов и возвратов.

            '
          type: integer
          format: int64
          minimum: 1
        returnId:
          description: 'Идентификатор возврата.


            Возвращается только для возвратов.

            '
          type: integer
          format: int64
          minimum: 1
    ChatMessageDTO:
      description: Информация о сообщении.
      type: object
      required:
      - messageId
      - createdAt
      - sender
      properties:
        messageId:
          description: Идентификатор сообщения.
          type: integer
          format: int64
          minimum: 1
        createdAt:
          description: 'Дата и время создания сообщения.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2017-11-21T00:00:00+03:00'
        sender:
          $ref: '#/components/schemas/ChatMessageSenderType'
          description: Отправитель.
        message:
          description: 'Текст сообщения.


            Необязательный параметр, если возвращается параметр `payload`.

            '
          type: string
        payload:
          description: 'Информация о приложенных к сообщению файлах.


            Необязательный параметр, если возвращается параметр `message`.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ChatMessagePayloadDTO'
    ChatMessagePayloadDTO:
      description: Информация о приложенных к сообщению файлах.
      type: object
      required:
      - name
      - size
      - url
      properties:
        name:
          description: Название файла.
          type: string
        url:
          $ref: '#/components/schemas/Url'
          description: Ссылка для скачивания файла.
        size:
          description: Размер файла в байтах.
          type: integer
          format: int32
    ChatMessageSenderType:
      description: 'Кто отправил сообщение:


        * `PARTNER` — магазин.

        * `CUSTOMER` — покупатель.

        * `MARKET` — Маркет ([автоматическое сообщение](*market-notification)).

        * `SUPPORT` — сотрудник службы поддержки Маркета.

        '
      type: string
      enum:
      - PARTNER
      - CUSTOMER
      - MARKET
      - SUPPORT
    ChatMessagesResultDTO:
      description: Информация о сообщениях.
      type: object
      required:
      - context
      - messages
      properties:
        orderId:
          description: Идентификатор заказа.
          type: integer
          format: int64
          deprecated: true
        context:
          $ref: '#/components/schemas/ChatFullContextDTO'
          description: Информация о заказе или возврате, по которому начат чат.
        messages:
          description: Информация о сообщениях.
          type: array
          items:
            $ref: '#/components/schemas/ChatMessageDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах с результатами.
    ChatStatusType:
      description: 'Статус чата:


        * `NEW` — новый чат.

        * `WAITING_FOR_CUSTOMER` — нужен ответ покупателя.

        * `WAITING_FOR_PARTNER` — нужен ответ магазина.

        * `WAITING_FOR_ARBITER` — нужен ответ арбитра.

        * `WAITING_FOR_MARKET` — нужен ответ Маркета.

        * `FINISHED` — чат завершен.

        '
      type: string
      enum:
      - NEW
      - WAITING_FOR_CUSTOMER
      - WAITING_FOR_PARTNER
      - WAITING_FOR_ARBITER
      - WAITING_FOR_MARKET
      - FINISHED
    ChatType:
      description: 'Тип чата:


        * `CHAT` — чат с покупателем.

        * `ARBITRAGE` — спор.

        '
      type: string
      enum:
      - CHAT
      - ARBITRAGE
    Cis:
      description: '[Код идентификации](*cis-regular-value) единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/)
        или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов Market Yandex Go).


        {% note warning "Не экранируйте косую черту в коде символа-разделителя `\u001d`" %}


        ✅ `01030410947874432155Qbag!\u001d93Zjqw`


        ❌ `01030410947874432155Qbag!\\u001d93Zjqw`


        Косые черты и кавычки в других местах экранируйте по правилам JSON: `\\` и `\"`


        {% endnote %}

        '
      type: string
    CisDTO:
      description: Статус проверки и код маркировки в системе :no-translate[«Честный ЗНАК»].
      type: object
      required:
      - value
      - status
      properties:
        value:
          type: string
          description: Код маркировки в системе :no-translate[«Честный ЗНАК»].
        status:
          $ref: '#/components/schemas/CisStatusType'
        substatus:
          $ref: '#/components/schemas/CisSubstatusType'
        crptRequestId:
          description: '**Только для модели LaaS**


            Идентификатор запроса проверки кода маркировки в [ЦРПТ](https://crpt.ru/), на основании которой принято решение
            о продаже товара.

            '
          type: string
        crptRequestDateTime:
          description: '**Только для модели LaaS**


            Время проверки кода маркировки в [ЦРПТ](https://crpt.ru/), на основании которой принято решение о продаже товара.

            '
          type: string
          format: date-time
    CisStatusType:
      type: string
      description: "Статус проверки кода маркировки в системе :no-translate[«Честный ЗНАК»]:\n\n* `FAILED` — не удалось проверить\
        \ код.\n\n  Повторите попытку позже или удалите код маркировки.\n\n* `IN_PROGRESS` — в процессе проверки.\n\n* `NOT_ON_VALIDATION`\
        \ — код маркировки не отправлен на проверку.\n\n* `OK` — проверка успешно пройдена.\n\n* `INVALID` — проверка не пройдена.\
        \ Продажа товара с этим кодом запрещена.\n"
      enum:
      - OK
      - FAILED
      - IN_PROGRESS
      - INVALID
      - NOT_ON_VALIDATION
    CisSubstatusType:
      type: string
      description: 'Детализация ошибки при проверке кода маркировки в системе :no-translate[«Честный ЗНАК»]:


        * `WRONG_OWNER_INN` — проверка не пройдена. ИНН владельца кода отличается от ИНН продавца.


        * `CIS_VALIDATION_ERROR` — проверка не пройдена.


        * `CIS_GTIN_NOT_FOUND` — код маркировки не содержит [GTIN](:no-translate[*gtin]).


        * `CIS_SERIAL_NUMBER_NOT_FOUND` — код маркировки не содержит серийный номер.


        * `INVALID_SYMBOLS_FOUND` — код маркировки содержит недопустимые символы.


        * `CRYPTO_TAIL_FORMAT_MISMATCH_CIS_TYPE` — формат криптоподписи не соответствует типу кода маркировки.


        * `INVALID_CRYPTO_TAIL` — криптоподпись не валидна.


        * `INVALID_CRYPTO_KEY` — криптоключ не валиден.


        * `VERIFICATION_FAILED_IN_EMITTER_COUNTRY` — код маркировки не прошел верификацию в стране эмитента.


        * `UNSUPPORTED_AI_FOUND` — найденные в коде маркировки AI не поддерживаются.


        * `CIS_NOT_FOUND_IN_GIS_MT` — код маркировки не найден в :no-translate[ГИС МТ].


        * `NOT_PLACED_ON_MARKET` — код маркировки не введен в оборот.


        * `NOT_PRINTED_ON_PACKAGE` — код маркировки не нанесен на упаковку.


        * `EXPIRED_ITEM` — у маркированного товара истек срок годности.


        * `SALE_BLOCKED_BY_OGB` — розничная продажа продукции заблокирована по решению ОГВ.


        * `ITEM_SOLD` — маркированный товар был продан.


        Возвращается только для статуса `INVALID`.

        '
      enum:
      - WRONG_OWNER_INN
      - CIS_VALIDATION_ERROR
      - CIS_GTIN_NOT_FOUND
      - CIS_SERIAL_NUMBER_NOT_FOUND
      - INVALID_SYMBOLS_FOUND
      - CRYPTO_TAIL_FORMAT_MISMATCH_CIS_TYPE
      - INVALID_CRYPTO_TAIL
      - INVALID_CRYPTO_KEY
      - VERIFICATION_FAILED_IN_EMITTER_COUNTRY
      - UNSUPPORTED_AI_FOUND
      - CIS_NOT_FOUND_IN_GIS_MT
      - NOT_PLACED_ON_MARKET
      - NOT_PRINTED_ON_PACKAGE
      - EXPIRED_ITEM
      - SALE_BLOCKED_BY_OGB
      - ITEM_SOLD
    ClosureDocumentsContractType:
      description: 'Тип договора:


        * `INCOME` — договор на размещение.


        * `OUTCOME` — договор на продвижение.

        '
      type: string
      enum:
      - INCOME
      - OUTCOME
      - MARKETING
    ClosureDocumentsMonthOfYearDTO:
      description: 'Месяц и год.

        '
      type: object
      required:
      - year
      - month
      properties:
        year:
          description: Год.
          type: integer
          format: int32
          example: 2025
        month:
          $ref: '#/components/schemas/Month'
    CommentDTO:
      description: Комментарий к ответу.
      type: object
      required:
      - id
      - text
      - status
      - answerId
      - createdAt
      properties:
        id:
          $ref: '#/components/schemas/CommentId'
        text:
          $ref: '#/components/schemas/QuestionsTextContent'
        canModify:
          description: Может ли продавец изменять комментарий или удалять его.
          type: boolean
        parentId:
          $ref: '#/components/schemas/CommentId'
          description: Идентификатор родительского комментария.
        author:
          $ref: '#/components/schemas/QuestionsTextContentAuthorDTO'
        status:
          $ref: '#/components/schemas/QuestionsTextContentModerationStatusType'
        answerId:
          $ref: '#/components/schemas/AnswerId'
        createdAt:
          description: Дата создания комментария.
          type: string
          format: date-time
        votes:
          $ref: '#/components/schemas/VotesDTO'
    CommentId:
      description: 'Идентификатор комментария к ответу.

        '
      type: integer
      format: int64
      minimum: 1
    CommodityCodeDTO:
      description: Товарный код.
      type: object
      required:
      - code
      - type
      properties:
        code:
          description: Товарный код.
          type: string
        type:
          $ref: '#/components/schemas/CommodityCodeType'
          description: Тип товарного кода.
    CommodityCodeType:
      description: 'Тип товарного кода:


        * `CUSTOMS_COMMODITY_CODE` — код товара в единой Товарной номенклатуре внешнеэкономической деятельности (ТН ВЭД) —
        10 или 14 цифр без пробелов.

        * `IKPU_CODE` — идентификационный код продукции и услуг (ИКПУ) в Узбекистане – 17 цифр без пробелов.


        Не передавайте несколько кодов одного типа.

        '
      type: string
      enum:
      - CUSTOMS_COMMODITY_CODE
      - IKPU_CODE
    ConfirmPricesRequest:
      description: 'Запрос на подтверждение цены.

        '
      type: object
      required:
      - offerIds
      properties:
        offerIds:
          description: Идентификаторы товаров, у которых подтверждается цена.
          type: array
          minItems: 1
          maxItems: 200
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    ConfirmShipmentRequest:
      description: Запрос для подтверждения отгрузки.
      type: object
      properties:
        externalShipmentId:
          description: Идентификатор отгрузки в системе поставщика.
          type: string
        signatory:
          description: "Логин пользователя в Яндекс ID, от имени которого будет подписываться электронный акт приема-передачи.\n\
            \nУказывается без `@yandex.ru`.\n\nГде его найти:\n\n* на странице [Яндекс ID](https://id.yandex.ru);\n* в [кабинете\
            \ продавца на Маркете](https://partner.market.yandex.ru/):\n\n  * в верхнем правом углу под иконкой пользователя;\n\
            \  * на странице **Настройки** → **Сотрудники и доступы**.\n"
          type: string
    CountryCode:
      description: 'Страна производства в формате ISO 3166-1 alpha-2. [Как получить](../../reference/regions/getRegionsCodes.md)

        '
      type: string
      minLength: 2
      maxLength: 2
      pattern: ^[A-Z]{2}$
      example: RU
    CountryDTO:
      description: Страна и ее код в формате :no-translate[ISO 3166-1 alpha-2].
      type: object
      required:
      - region
      - countryCode
      properties:
        region:
          $ref: '#/components/schemas/RegionDTO'
        countryCode:
          $ref: '#/components/schemas/CountryCode'
    CourierDeliveryAddressDTO:
      type: object
      description: Адрес доставки.
      allOf:
      - $ref: '#/components/schemas/BasicCourierDeliveryAddressDTO'
      - properties:
          entrance:
            type: string
            description: Номер подъезда.
            minLength: 1
            maxLength: 16
          floor:
            type: integer
            format: int32
            description: Этаж.
          apartment:
            type: string
            description: Номер квартиры или офиса.
            minLength: 1
            maxLength: 16
    CourierDeliveryOptionDTO:
      type: object
      description: Временные интервалы и способ оплаты.
      required:
      - deliveryDateInterval
      - deliveryTimeInterval
      - paymentMethods
      - price
      properties:
        deliveryDateInterval:
          $ref: '#/components/schemas/DeliveryDateIntervalDTO'
        deliveryTimeInterval:
          $ref: '#/components/schemas/TimeIntervalDTO'
        price:
          $ref: '#/components/schemas/DeliveryOptionPriceDTO'
    CourierDeliveryOptionsDTO:
      type: object
      description: Информация о курьерской доставке.
      required:
      - courierDeliveryOptions
      properties:
        courierDeliveryOptions:
          type: array
          description: Информация о курьерской доставке.
          minItems: 1
          items:
            $ref: '#/components/schemas/CourierDeliveryOptionDTO'
    CourierDeliveryParametersDTO:
      type: object
      description: 'Информация о курьерской доставке.


        Не передавайте вместе с `pickupDelivery`.

        '
      required:
      - fullAddress
      properties:
        fullAddress:
          type: string
          description: Полный адрес с точностью до номера дома.
          minLength: 1
          maxLength: 512
    CreateChatRequest:
      description: 'Заказ, для которого нужно создать чат.

        '
      type: object
      properties:
        orderId:
          description: Идентификатор заказа на Маркете.
          type: integer
          format: int64
          deprecated: true
        context:
          $ref: '#/components/schemas/ChatContextDTO'
    CreateChatResponse:
      description: Результат создания чата.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - type: object
        properties:
          result:
            $ref: '#/components/schemas/CreateChatResultDTO'
    CreateChatResultDTO:
      description: Информация о созданном чате.
      type: object
      required:
      - chatId
      properties:
        chatId:
          description: Идентификатор чата.
          type: integer
          format: int64
          minimum: 1
    CreateOrderDTO:
      type: object
      description: 'Информация о заказе.


        Передайте выбранный вариант доставки из ответа метода [POST v1/campaigns/{campaignId}/delivery-options](../../reference/delivery-options/getDeliveryOptions.md).

        '
      required:
      - externalOrderId
      - itemsDelivery
      - destination
      - customer
      - packaging
      - paymentType
      properties:
        externalOrderId:
          description: Внешний идентификатор заказа в системе магазина.
          type: string
          minLength: 1
        itemsDelivery:
          description: Список товаров в заказе.
          type: array
          items:
            $ref: '#/components/schemas/CreateOrderWarehouseItemsDTO'
          minItems: 1
          maxItems: 1000
        destination:
          $ref: '#/components/schemas/CreateOrderDeliveryOptionDTO'
        customer:
          $ref: '#/components/schemas/CustomerDTO'
        packaging:
          $ref: '#/components/schemas/CreateOrderPackagingDTO'
        paymentType:
          $ref: '#/components/schemas/DeliveryPaymentType'
        draft:
          type: boolean
          description: 'Признак создания черновика заказа.


            * `true` — Маркет создаст заказ в статусе `RESERVED` и будет ждать подтверждения от магазина.

            * `false` — Маркет создаст заказ в статусе `PROCESSING` с подстатусом `STARTED` и начнёт его обработку, дополнительных
            подтверждений не требуется.

            '
          default: false
        fake:
          type: boolean
          description: 'Признак тестового заказа.


            * `true` — позволяет проверить работу магазина и его API на [тестовых заказах](../../concepts/sandbox.md). Такой
            заказ не будет отгружен и не влияет на остатки.

            * `false` — настоящий заказ.

            '
          default: false
    CreateOrderDeliveryOptionDTO:
      type: object
      description: 'Информация о доставке.


        Не передавайте одновременно `courierDelivery` и `pickupDelivery`.

        '
      properties:
        pickupDelivery:
          $ref: '#/components/schemas/OrderPickupDeliveryDTO'
        courierDelivery:
          $ref: '#/components/schemas/OrderCourierDeliveryDTO'
    CreateOrderItemDTO:
      description: Товар в заказе.
      type: object
      required:
      - price
      allOf:
      - $ref: '#/components/schemas/BasicOrderItemDTO'
      - properties:
          price:
            $ref: '#/components/schemas/BasePriceDTO'
    CreateOrderPackageType:
      description: 'Требования к упаковке:


        * `WHITELABEL` — коробка.


        * `BRAND` — [брендированная упаковка](*brand-package) магазина.


        Если не передать `packageType`, заказ приедет в коробке или без упаковки.

        '
      type: string
      enum:
      - WHITELABEL
      - BRAND
    CreateOrderPackagingDTO:
      type: object
      description: Правила упаковки товаров в заказе.
      properties:
        packageType:
          $ref: '#/components/schemas/CreateOrderPackageType'
    CreateOrderRequest:
      type: object
      required:
      - order
      properties:
        order:
          $ref: '#/components/schemas/CreateOrderDTO'
    CreateOrderResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CreatedOrdersDTO'
    CreateOrderWarehouseItemsDTO:
      type: object
      description: Список товаров в заказе.
      required:
      - warehouseId
      - items
      - deliveryDateInterval
      properties:
        warehouseId:
          description: 'Идентификатор фулфилмент-склада Маркета.


            Получите его с помощью метода [POST v2/campaigns/{campaignId}/delivery-options](../../reference/delivery-options/getDeliveryOptions.md).

            '
          type: integer
          format: int64
          minimum: 1
        items:
          type: array
          description: 'Список товаров в заказе.


            В рамках одного запроса все значения `offerId` должны быть уникальными. Не допускается передача двух объектов
            с одинаковым `offerId`.

            '
          minItems: 1
          maxItems: 1000
          items:
            $ref: '#/components/schemas/CreateOrderItemDTO'
        deliveryDateInterval:
          $ref: '#/components/schemas/DeliveryDateIntervalDTO'
        deliveryTimeInterval:
          $ref: '#/components/schemas/TimeIntervalDTO'
          description: 'Интервал времени доставки.


            Обязателен для курьерской доставки.

            '
    CreateOutletResponse:
      description: Ответ на запрос о создании точки продаж.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OutletResponseDTO'
    CreateReturnDTO:
      type: object
      description: Информация о возврате.
      required:
      - externalReturnId
      - orderId
      - items
      - customer
      - returnOption
      properties:
        externalReturnId:
          description: Внешний идентификатор возврата в системе магазина.
          type: string
          minLength: 1
        orderId:
          description: Идентификатор заказа, по которому нужно сделать возврат.
          type: integer
          format: int64
        items:
          description: Список товаров в возврате.
          type: array
          items:
            $ref: '#/components/schemas/CreateReturnItemDTO'
          minItems: 1
          maxItems: 1000
        customer:
          $ref: '#/components/schemas/CustomerDTO'
        returnOption:
          $ref: '#/components/schemas/CreateReturnOptionDTO'
    CreateReturnItemDTO:
      description: Товар в возврате.
      type: object
      required:
      - offerId
      - count
      - reasonType
      - subreasonType
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        count:
          description: Количество единиц товара.
          type: integer
          format: int32
          minimum: 1
          maximum: 1000
        reasonType:
          $ref: '#/components/schemas/ExternalReturnDecisionReasonType'
        subreasonType:
          $ref: '#/components/schemas/ExternalReturnDecisionSubreasonType'
        comment:
          description: Комментарий к товару в возврате.
          type: string
          minLength: 1
          maxLength: 3000
        pictures:
          description: Ссылки (URL) на изображения товара в возврате.
          type: array
          nullable: true
          minItems: 1
          maxItems: 10
          uniqueItems: true
          items:
            type: string
            minLength: 1
            maxLength: 2000
    CreateReturnOptionDTO:
      type: object
      description: Информация о способе возврата.
      required:
      - pickupReturn
      properties:
        pickupReturn:
          $ref: '#/components/schemas/OrderPickupReturnDTO'
    CreateReturnRequest:
      type: object
      required:
      - newReturn
      properties:
        newReturn:
          $ref: '#/components/schemas/CreateReturnDTO'
    CreateReturnResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CreatedReturnDTO'
    CreatedOrderDTO:
      type: object
      description: Информация о созданном заказе.
      required:
      - id
      properties:
        id:
          description: Идентификатор заказа.
          type: integer
          format: int64
    CreatedOrdersDTO:
      type: object
      description: 'Информация о созданных заказах.


        По одному заказу для каждого склада.

        '
      required:
      - orders
      properties:
        orders:
          type: array
          description: Созданные заказы.
          minItems: 1
          items:
            $ref: '#/components/schemas/CreatedOrderDTO'
    CreatedReturnDTO:
      type: object
      description: Информация о созданном возврате.
      required:
      - id
      properties:
        id:
          description: Идентификатор возврата.
          type: integer
          format: int64
    CurrencyType:
      type: string
      description: 'Коды валют:


        * `RUR` — российский рубль.

        * `UAH` — украинская гривна.

        * `BYR` — белорусский рубль.

        * `KZT` — казахстанский тенге.

        * `UZS` — узбекский сум.

        '
      enum:
      - RUR
      - USD
      - EUR
      - UAH
      - AUD
      - GBP
      - BYR
      - BYN
      - DKK
      - ISK
      - KZT
      - CAD
      - CNY
      - NOK
      - XDR
      - SGD
      - TRY
      - SEK
      - CHF
      - JPY
      - AZN
      - ALL
      - DZD
      - AOA
      - ARS
      - AMD
      - AFN
      - BHD
      - BGN
      - BOB
      - BWP
      - BND
      - BRL
      - BIF
      - HUF
      - VEF
      - KPW
      - VND
      - GMD
      - GHS
      - GNF
      - HKD
      - GEL
      - AED
      - EGP
      - ZMK
      - ILS
      - INR
      - IDR
      - JOD
      - IQD
      - IRR
      - YER
      - QAR
      - KES
      - KGS
      - COP
      - CDF
      - CRC
      - KWD
      - CUP
      - LAK
      - LVL
      - SLL
      - LBP
      - LYD
      - SZL
      - LTL
      - MUR
      - MRO
      - MKD
      - MWK
      - MGA
      - MYR
      - MAD
      - MXN
      - MZN
      - MDL
      - MNT
      - NPR
      - NGN
      - NIO
      - NZD
      - OMR
      - PKR
      - PYG
      - PEN
      - PLN
      - KHR
      - SAR
      - RON
      - SCR
      - SYP
      - SKK
      - SOS
      - SDG
      - SRD
      - TJS
      - THB
      - TWD
      - BDT
      - TZS
      - TND
      - TMM
      - UGX
      - UZS
      - UYU
      - PHP
      - DJF
      - XAF
      - XOF
      - HRK
      - CZK
      - CLP
      - LKR
      - EEK
      - ETB
      - RSD
      - ZAR
      - KRW
      - NAD
      - TL
      - UE
    CurrencyValueDTO:
      description: Валюта и ее значение.
      type: object
      required:
      - value
      - currencyId
      properties:
        value:
          description: Значение.
          type: number
        currencyId:
          $ref: '#/components/schemas/CurrencyType'
          description: Валюта.
    CustomerDTO:
      type: object
      description: Данные получателя заказа или отправителя возврата.
      required:
      - firstName
      - lastName
      - phone
      properties:
        firstName:
          type: string
          description: Имя.
          minLength: 1
          maxLength: 512
        lastName:
          type: string
          description: Фамилия.
          minLength: 1
          maxLength: 512
        middleName:
          type: string
          description: Отчество.
          minLength: 1
          maxLength: 512
        phone:
          type: string
          description: 'Номер телефона.


            Формат: `+<код_страны><код_региона><номер_телефона>`.

            '
          minLength: 5
          maxLength: 16
          pattern: ^\+[0-9]+$
    DateDdMmYyyy:
      type: string
      description: 'Формат даты: `ДД-ММ-ГГГГ`.

        '
      format: date-dd-MM-yyyy
      example: 23-09-2022
    DateDdMmYyyyHhMmSs:
      type: string
      format: date-dd-MM-yyyy-HH-mm-ss
      example: 23-09-2022 09:12:41
    DayOfWeekType:
      description: 'День недели:


        * `MONDAY` — понедельник.

        * `TUESDAY` — вторник.

        * `WEDNESDAY` — среда.

        * `THURSDAY` — четверг.

        * `FRIDAY` — пятница.

        * `SATURDAY` — суббота.

        * `SUNDAY` — воскресенье.

        '
      type: string
      enum:
      - MONDAY
      - TUESDAY
      - WEDNESDAY
      - THURSDAY
      - FRIDAY
      - SATURDAY
      - SUNDAY
    DeleteCampaignOffersDTO:
      description: Список товаров, которые не удалось удалить, потому что они не найдены или хранятся на складе Маркета.
      type: object
      properties:
        notDeletedOfferIds:
          description: Список SKU.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeleteCampaignOffersRequest:
      description: 'Фильтрации удаляемых товаров по `offerIds`.

        '
      type: object
      required:
      - offerIds
      properties:
        offerIds:
          description: Идентификаторы товаров в каталоге.
          type: array
          minItems: 1
          maxItems: 500
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeleteCampaignOffersResponse:
      description: Результат удаления товаров.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/DeleteCampaignOffersDTO'
    DeleteGoodsFeedbackCommentRequest:
      description: 'Фильтр запроса отзывов по бизнесу.

        '
      type: object
      required:
      - id
      properties:
        id:
          $ref: '#/components/schemas/GoodsFeedbackCommentId'
    DeleteHiddenOffersRequest:
      description: Запрос на возобновление показа оферов.
      type: object
      required:
      - hiddenOffers
      properties:
        hiddenOffers:
          description: 'Список скрытых товаров.

            '
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/HiddenOfferDTO'
    DeleteOfferParameterType:
      description: 'Значения параметров, которые хотите удалить, и соответствующие параметры в `UpdateOfferDTO`, в которых
        вы передали эти значения ранее:


        * `ADDITIONAL_EXPENSES` — дополнительные расходы на товар (параметр `additionalExpenses`).

        * `ADULT` — пометка 18+ (параметр `adult`)

        * `AGE` — возрастное ограничение для детей (параметр `age`).

        * `BARCODES` — штрихкод (параметр `barcodes`).

        * `BOX_COUNT` — количество грузовых мест (параметр `boxCount`).

        * `CERTIFICATES` — номера документов на товар (параметр `certificates`).

        * `COMMODITY_CODES` — товарные коды (параметр `commodityCodes`).

        * `CONDITION` — состояние уцененного товара (параметр `condition`).

        * `CUSTOMS_COMMODITY_CODE` — код товара в ТН ВЭД (параметр `customsCommodityCode`).

        * `DESCRIPTION` — описание товара (параметр `description`).

        * `DOWNLOADABLE` — признак цифрового товара (параметр `downloadable`).

        * `GUARANTEE_PERIOD` — гарантийный срок (параметр `guaranteePeriod`).

        * `LIFE_TIME` — срок службы (параметр `lifeTime`).

        * `MANUALS` — список инструкций по использованию товара (параметр `manuals`).

        * `MANUFACTURER_COUNTRIES` — страна производства (параметр `manufacturerCountries`).

        * `PARAMETERS` — характеристики товара (параметры `params`, `parameterValues`).

        * `PICTURES` — ссылки на изображения товара (параметр `pictures`).

        * `PURCHASE_PRICE` — себестоимость (параметр `purchasePrice`).

        * `SHELF_LIFE` — срок годности (параметр `shelfLife`).

        * `TAGS` — метки товара, которые использует магазин (параметр `tags`).

        * `TYPE` — особый тип товара (параметр `type`).

        * `VENDOR_CODE` — название бренда или производителя (параметр `vendorCode`).

        * `VIDEOS` — ссылки на видео товара (параметр `videos`).

        '
      type: string
      enum:
      - ADDITIONAL_EXPENSES
      - ADULT
      - AGE
      - BARCODES
      - BOX_COUNT
      - CERTIFICATES
      - COMMODITY_CODES
      - CONDITION
      - CUSTOMS_COMMODITY_CODE
      - DESCRIPTION
      - DOWNLOADABLE
      - GUARANTEE_PERIOD
      - LIFE_TIME
      - MANUALS
      - MANUFACTURER_COUNTRIES
      - PARAMETERS
      - PICTURES
      - PURCHASE_PRICE
      - SHELF_LIFE
      - TAGS
      - TYPE
      - VENDOR_CODE
      - VIDEOS
    DeleteOffersDTO:
      description: Список товаров, которые не удалось удалить, потому что они хранятся на складе Маркета.
      type: object
      properties:
        notDeletedOfferIds:
          description: Список SKU товаров, которые не удалось удалить.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeleteOffersFromArchiveDTO:
      description: Товары, которые не удалось восстановить из архива.
      type: object
      properties:
        notUnarchivedOfferIds:
          description: Список товаров, которые не удалось восстановить из архива.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeleteOffersFromArchiveRequest:
      description: 'Товары, которые нужно восстановить из архива.

        '
      type: object
      required:
      - offerIds
      properties:
        offerIds:
          description: Список товаров, которые нужно восстановить из архива.
          type: array
          minItems: 1
          maxItems: 200
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeleteOffersFromArchiveResponse:
      description: Результат разархивации товаров.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/DeleteOffersFromArchiveDTO'
            description: Товары, которые не удалось восстановить из архива.
    DeleteOffersRequest:
      description: 'Фильтрации удаляемых товаров по offerIds.

        '
      type: object
      required:
      - offerIds
      properties:
        offerIds:
          description: Список SKU товаров, которые нужно удалить.
          type: array
          minItems: 1
          maxItems: 500
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeleteOffersResponse:
      description: Результат удаления товаров.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/DeleteOffersDTO'
            description: Результат удаления.
    DeletePromoOffersRequest:
      description: 'Удаление товаров из акции.


        Чтобы убрать:


        * все товары из акции и больше не участвовать в ней, передайте значение `true` в параметре `deleteAllOffers`;


        * часть товаров, передайте их идентификаторы в параметре `offersIds`.

        '
      type: object
      required:
      - promoId
      properties:
        promoId:
          description: Идентификатор акции.
          type: string
        deleteAllOffers:
          description: Чтобы убрать все товары из акции и больше не участвовать в ней, передайте значение `true` и не передавайте
            параметр `offerIds`.
          type: boolean
        offerIds:
          description: Товары, которые нужно убрать из акции.
          type: array
          nullable: true
          minItems: 1
          maxItems: 500
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    DeletePromoOffersResponse:
      description: Результат удаления товаров из акции.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/DeletePromoOffersResultDTO'
    DeletePromoOffersResultDTO:
      description: 'Результат удаления товаров из акции.


        Возвращается, только если в запросе был передан параметр `offerIds`.

        '
      type: object
      properties:
        rejectedOffers:
          description: 'Товары, при удалении которых появились ошибки.


            Возвращается, только если есть такие товары.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/RejectedPromoOfferDeleteDTO'
    DeliveryDateIntervalDTO:
      type: object
      description: Интервал дат доставки.
      required:
      - fromDate
      - toDate
      properties:
        fromDate:
          type: string
          format: date
          description: 'Начало интервала.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
        toDate:
          type: string
          format: date
          description: 'Конец интервала.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
    DeliveryIntervalsUpdateOptionDTO:
      type: object
      description: Интервалы дат и времени.
      required:
      - deliveryDateInterval
      - deliveryTimeInterval
      properties:
        deliveryDateInterval:
          $ref: '#/components/schemas/DeliveryDateIntervalDTO'
        deliveryTimeInterval:
          $ref: '#/components/schemas/TimeIntervalDTO'
    DeliveryIntervalsUpdateOptionsDTO:
      type: object
      description: 'Доступные интервалы для курьерской доставки.


        Возвращается только для типа изменения `UPDATE_DELIVERY_INTERVAL`.

        '
      required:
      - availableDeliveryIntervals
      properties:
        availableDeliveryIntervals:
          description: 'Интервалы дат и времени.


            Если доступных интервалов нет, возвращается пустой массив.

            '
          type: array
          minItems: 0
          items:
            $ref: '#/components/schemas/DeliveryIntervalsUpdateOptionDTO'
    DeliveryOptionPriceDTO:
      type: object
      description: Стоимость, которую магазин должен заплатить за доставку с выбранным вариантом.
      allOf:
      - $ref: '#/components/schemas/CurrencyValueDTO'
    DeliveryPaymentType:
      description: 'Тип оплаты заказа:


        * `PREPAID` — оплата при оформлении заказа.

        '
      type: string
      enum:
      - PREPAID
    DeliveryPriceDTO:
      description: 'Информация о стоимости доставки, включая подъем на этаж.


        {% note info "Как рассчитывается стоимость доставки" %}


        * Стоимость доставки складывается из параметров `payment` и `subsidy`.

        * Все суммы указаны в валюте магазина.


        {% endnote %}

        '
      type: object
      properties:
        payment:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Платеж покупателя за доставку.
        subsidy:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Вознаграждение Маркета за доставку.
        vat:
          $ref: '#/components/schemas/OrderVatType'
          description: НДС на доставку.
    DeliveryServiceDTO:
      description: Служба доставки.
      type: object
      properties:
        id:
          description: Идентификатор службы доставки.
          format: int64
          type: integer
        name:
          description: Название службы доставки.
          type: string
    DeliveryServiceInfoDTO:
      description: Информация о службе доставки.
      type: object
      required:
      - id
      - name
      properties:
        id:
          description: Идентификатор службы доставки.
          type: integer
          format: int64
        name:
          description: Название службы доставки.
          type: string
    DeliveryServicesDTO:
      description: Информация о службах доставки.
      type: object
      required:
      - deliveryService
      properties:
        deliveryService:
          description: Информация о службе доставки.
          type: array
          items:
            $ref: '#/components/schemas/DeliveryServiceInfoDTO'
    DigitalGoodsDeliveryDetailsDTO:
      description: Информация о доставке цифрового товара.
      type: object
      required:
      - type
      properties:
        type:
          $ref: '#/components/schemas/DigitalGoodsDeliveryType'
          description: Тип цифрового товара.
        steamLink:
          description: Ссылка на Steam-аккаунт покупателя. Передается только для типа `STEAM_GIFT`.
          type: string
    DigitalGoodsDeliveryType:
      description: 'Способ получения цифрового товара:


        * `EMAIL` — код активации по почте: покупатель получит письмо с кодом и инструкцией на почту.


        * `ACTIVATION_CODE` — код активации в заказе на Маркете: покупатель получит инструкцию и код в чате с магазином, а
        в списке заказов сможет скопировать код.


        * `STEAM_GIFT` — игры подарком в Steam: вы получите от покупателя ссылку формата https://s.team/p/m**-***/********
        на добавление в друзья. Перейдите по ссылке, добавьте покупателя в друзья и отправьте ему игру. В течение трех часов
        передайте Маркету статус, что заказ доставлен — через API или на странице заказа.


        * `CHAT` — игры и товары в чате с покупателем на Маркете: покупатель получит сертификат в чате с магазином. Передайте
        Маркету статус, что заказ доставлен — через API или на странице заказа.

        '
      type: string
      enum:
      - EMAIL
      - ACTIVATION_CODE
      - STEAM_GIFT
      - CHAT
    DiscountBase:
      description: 'Зачеркнутая цена.


        Число должно быть целым. Вы можете указать цену со скидкой от 5 до 99%.


        Передавайте этот параметр при каждом обновлении цены, если предоставляете скидку на товар.

        '
      type: number
      minimum: 0
      exclusiveMinimum: true
    DocumentDTO:
      description: Информация о документе.
      type: object
      properties:
        status:
          $ref: '#/components/schemas/OrderDocumentStatusType'
          description: Статус документа.
        number:
          description: Номер документа.
          type: string
        date:
          description: 'Дата создания документа.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    EacVerificationResultDTO:
      description: Результат выполнения запроса.
      type: object
      properties:
        verificationResult:
          $ref: '#/components/schemas/EacVerificationStatusType'
          description: Статус проверки кода подтверждения.
        attemptsLeft:
          description: 'Количество оставшихся попыток проверки кода.


            Возвращается, если магазин отправил некорректный код.


            Когда все попытки будут исчерпаны, код обновится.

            '
          type: integer
          format: int32
    EacVerificationStatusType:
      description: 'Статус проверки кода подтверждения:


        * `ACCEPTED` — код верный.

        * `REJECTED` — код неверный.

        * `NEED_UPDATE` — Маркет отправит новый код. Значение возвращается, если превышено количество попыток отправки кода.

        '
      type: string
      enum:
      - ACCEPTED
      - REJECTED
      - NEED_UPDATE
    EmptyApiResponse:
      type: object
      description: Пустой ответ сервера.
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
    EnrichedOrderBoxLayoutDTO:
      description: Информация о коробке.
      type: object
      allOf:
      - $ref: '#/components/schemas/OrderBoxLayoutDTO'
      - properties:
          boxId:
            description: Идентификатор коробки.
            type: integer
            format: int64
    ExtensionShipmentDTO:
      description: Информация об отгрузке.
      type: object
      required:
      - availableActions
      properties:
        currentStatus:
          $ref: '#/components/schemas/ShipmentStatusChangeDTO'
          description: Статус отгрузки.
        availableActions:
          description: Доступные действия над отгрузкой.
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShipmentActionType'
    ExternalOrderId:
      description: Внешний идентификатор заказа, который вы передали в [POST v2/campaigns/{campaignId}/orders/{orderId}/external-id](../../reference/orders/updateExternalOrderId.md).
      type: string
      minLength: 1
    ExternalReturnDecisionReasonType:
      description: 'Причины возврата:


        * `BAD_QUALITY` — бракованный товар (есть недостатки).


        * `DOES_NOT_FIT` — товар не подошел.


        * `WRONG_ITEM` — привезли не тот товар.

        '
      type: string
      enum:
      - BAD_QUALITY
      - DOES_NOT_FIT
      - WRONG_ITEM
    ExternalReturnDecisionSubreasonType:
      description: "Детали причин возврата:\n  * `DOES_NOT_FIT`:\n    * `USER_DID_NOT_LIKE` — товар не понравился.\n    *\
        \ `USER_CHANGED_MIND` — передумал покупать.\n    * `DELIVERED_TOO_LONG` — передумал покупать из-за длительного срока\
        \ доставки.\n\n  * `BAD_QUALITY`:\n    * `BAD_PACKAGE` — заводская упаковка повреждена.\n    * `DAMAGED` — царапины,\
        \ сколы.\n    * `NOT_WORKING` — не включается, не работает.\n    * `INCOMPLETENESS` — некомплект (не хватает детали\
        \ в наборе, к товару).\n\n  * `WRONG_ITEM`:\n    * `WRONG_ITEM` — не тот товар.\n    * `WRONG_COLOR` — цвет не соответствует\
        \ заявленному.\n    * `DID_NOT_MATCH_DESCRIPTION` — описание или характеристики не соответствуют заявленным.\n"
      type: string
      enum:
      - USER_DID_NOT_LIKE
      - USER_CHANGED_MIND
      - DELIVERED_TOO_LONG
      - BAD_PACKAGE
      - DAMAGED
      - NOT_WORKING
      - INCOMPLETENESS
      - WRONG_ITEM
      - WRONG_COLOR
      - DID_NOT_MATCH_DESCRIPTION
    FeedbackReactionStatusType:
      description: 'Статус реакции на отзыв:


        * `ALL` — все отзывы.


        * `NEED_REACTION` — отзывы, на которые нужно ответить.

        '
      type: string
      enum:
      - ALL
      - NEED_REACTION
    FlippingPagerDTO:
      description: Модель для пагинации.
      type: object
      properties:
        total:
          type: integer
          format: int32
          description: Сколько всего найдено элементов.
        from:
          type: integer
          format: int32
          description: Начальный номер найденного элемента на странице.
        to:
          type: integer
          format: int32
          description: Конечный номер найденного элемента на странице.
        currentPage:
          type: integer
          format: int32
          description: Текущая страница.
        pagesCount:
          type: integer
          format: int32
          description: Общее количество страниц.
        pageSize:
          type: integer
          format: int32
          description: Размер страницы.
    FulfillmentWarehouseDTO:
      description: Фулфилмент-склад Маркета.
      type: object
      required:
      - id
      - name
      properties:
        id:
          description: Идентификатор склада.
          type: integer
          format: int64
        name:
          description: Название склада.
          type: string
        address:
          $ref: '#/components/schemas/WarehouseAddressDTO'
          description: Адрес склада.
    FulfillmentWarehousesDTO:
      description: Список фулфилмент-складов Маркета.
      type: object
      required:
      - warehouses
      properties:
        warehouses:
          description: Список фулфилмент-складов Маркета.
          type: array
          items:
            $ref: '#/components/schemas/FulfillmentWarehouseDTO'
    FullOutletDTO:
      type: object
      description: Информация о точке продаж.
      required:
      - id
      allOf:
      - $ref: '#/components/schemas/OutletDTO'
      - properties:
          id:
            description: Идентификатор точки продаж, присвоенный Маркетом.
            type: integer
            format: int64
          status:
            $ref: '#/components/schemas/OutletStatusType'
          region:
            $ref: '#/components/schemas/RegionDTO'
          shopOutletId:
            description: '{% note warning "Вместо него используйте `shopOutletCode`." %}


               


              {% endnote %}


              Идентификатор точки продаж, заданный магазином.

              '
            type: string
            deprecated: true
          workingTime:
            description: '{% note warning "Вместо него используйте `workingSchedule`." %}


               


              {% endnote %}


              Рабочее время.

              '
            type: string
            deprecated: true
          moderationReason:
            description: Статус модерации.
            type: string
    FullOutletLicenseDTO:
      description: Информация о лицензии.
      type: object
      allOf:
      - $ref: '#/components/schemas/OutletLicenseDTO'
      - properties:
          checkStatus:
            $ref: '#/components/schemas/LicenseCheckStatusType'
          checkComment:
            description: 'Причина, по которой лицензия не прошла проверку.


              Параметр возвращается, только если параметр `checkStatus` имеет значение `FAIL`.

              '
            type: string
    GenerateBannersStatisticsRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - dateFrom
      - dateTo
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
    GenerateBarcodesReportRequest:
      description: 'Данные, необходимые для генерации файла.

        '
      type: object
      required:
      - campaignId
      - barcodeFormat
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        barcodeFormat:
          $ref: '#/components/schemas/BarcodeFormatType'
        barcodeOfferInfos:
          description: 'Список товаров.


            Передайте этот параметр и уникальные `offerId`, чтобы вернуть файл со штрихкодами конкретных товаров.


            В запросе обязательно должен быть либо `barcodeOfferInfos`, либо `supplyRequestId`, но не оба сразу.


            Если передается информация о товаре, у которого несколько штрихкодов, все штрихкоды будут добавлены в файл, их
            количество будет задано параметром `barcodeCount`.

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/BarcodeOfferInfoDTO'
        supplyRequestId:
          $ref: '#/components/schemas/SupplyRequestId'
          description: 'Идентификатор заявки на поставку.


            Передайте этот параметр, чтобы вернуть файл со штрихкодами товаров в указанной заявке на поставку. Вернутся штрихкоды
            каждого товара в заявке.


            В запросе обязательно должен быть либо `barcodeOfferInfos`, либо `supplyRequestId`, но не оба сразу.

            '
    GenerateBoostConsolidatedRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - dateFrom
      - dateTo
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
    GenerateClosureDocumentsDetalizationRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - campaignId
      - monthOfYear
      - contractType
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        monthOfYear:
          $ref: '#/components/schemas/ClosureDocumentsMonthOfYearDTO'
          description: Месяц, за который сформированы необходимые закрывающие документы.
        contractType:
          $ref: '#/components/schemas/ClosureDocumentsContractType'
          description: Тип договора, по которому нужно сгенерировать отчет по схождению с закрывающими документами.
    GenerateClosureDocumentsRequest:
      description: 'Данные, необходимые для запроса закрывающих документов.

        '
      type: object
      required:
      - campaignId
      - monthOfYear
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        monthOfYear:
          $ref: '#/components/schemas/ClosureDocumentsMonthOfYearDTO'
          description: Месяц, за который нужны закрывающие документы.
        contractTypes:
          description: 'Типы договоров, по которым нужны закрывающие документы.


            Если их не указать, вернется архив с документами по всем найденным договорам.

            '
          type: array
          uniqueItems: true
          minItems: 1
          maxItems: 3
          nullable: true
          items:
            $ref: '#/components/schemas/ClosureDocumentsContractType'
    GenerateCompetitorsPositionReportRequest:
      description: Данные, необходимые для генерации отчета.
      type: object
      required:
      - dateFrom
      - dateTo
      - businessId
      - categoryId
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        categoryId:
          description: Идентификатор категории.
          type: integer
          format: int64
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
    GenerateGoodsFeedbackRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
    GenerateGoodsMovementReportRequest:
      description: Данные, необходимые для генерации отчета.
      type: object
      required:
      - campaignId
      - dateFrom
      - dateTo
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
        shopSku:
          $ref: '#/components/schemas/ShopSku'
    GenerateGoodsPricesReportRequest:
      description: Данные, необходимые для генерации отчета.
      type: object
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        categoryIds:
          description: Фильтр по категориям на Маркете.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: integer
            format: int32
            minimum: 0
            exclusiveMinimum: true
        campaignId:
          $ref: '#/components/schemas/CampaignId'
    GenerateGoodsRealizationReportRequest:
      description: 'Данные, необходимые для генерации отчета: идентификатор кампании и период, за который нужен отчет.

        '
      type: object
      required:
      - campaignId
      - year
      - month
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        year:
          $ref: '#/components/schemas/Year'
        month:
          $ref: '#/components/schemas/Month'
    GenerateGoodsTurnoverRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - campaignId
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        date:
          description: 'Дата, за которую нужно рассчитать оборачиваемость. Если параметр не указан, используется текущая дата.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    GenerateJewelryFiscalReportRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - campaignId
      - dateFrom
      - dateTo
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
    GenerateKeyIndicatorsRequest:
      description: 'Данные, необходимые для генерации отчета.


        В запросе обязательно должен быть либо `businessId`, либо `campaignId`, но не оба сразу.

        '
      type: object
      required:
      - detalizationLevel
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        detalizationLevel:
          $ref: '#/components/schemas/KeyIndicatorsReportDetalizationLevelType'
          description: За какой период нужна детализация.
    GenerateMarketingDetalizationRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - monthOfYear
      properties:
        monthOfYear:
          $ref: '#/components/schemas/ClosureDocumentsMonthOfYearDTO'
          description: Период генерации отчета.
    GenerateMassOrderLabelsRequest:
      description: 'Данные, необходимые для генерации файла.

        '
      type: object
      required:
      - businessId
      - orderIds
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        orderIds:
          description: Список идентификаторов заказов.
          type: array
          minItems: 1
          maxItems: 1000
          uniqueItems: true
          items:
            description: Идентификатор заказа.
            format: int64
            type: integer
        sortingType:
          $ref: '#/components/schemas/LabelsSortingType'
          description: Тип сортировки ярлыков в файле.
    GenerateOfferBarcodesRequest:
      description: 'Товары, для которых нужно сгенерировать штрихкоды.

        '
      type: object
      required:
      - offerIds
      properties:
        offerIds:
          description: Список товаров, для которых нужно сгенерировать штрихкоды.
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
        skipIfExists:
          description: 'Для каких товаров нужно сгенерировать штрихкоды:


            * `false` — для всех, которые переданы в запросе.

            * `true` — только для тех, у которых их нет.

            '
          type: boolean
          default: true
    GenerateOfferBarcodesResponse:
      description: Результат генерации штрихкодов.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GenerateOfferBarcodesResultDTO'
    GenerateOfferBarcodesResultDTO:
      description: 'Товары, для которых не удалось сгенерировать штрихкоды.


        Проверьте корректность переданных `offerId`. Если ошибка повторяется, обратитесь в службу поддержки — перейдите в
        [кабинет продавца на Маркете](https://partner.market.yandex.ru/business/any/support) и нажмите кнопку **Создать обращение**.

        '
      type: object
      required:
      - unprocessedOfferIds
      properties:
        unprocessedOfferIds:
          description: Список товаров, для которых не удалось сгенерировать штрихкоды.
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    GenerateReportDTO:
      description: Идентификатор, который понадобится для отслеживания статуса генерации и получения готового отчета или документа.
      type: object
      required:
      - reportId
      - estimatedGenerationTime
      properties:
        reportId:
          description: Идентификатор, который понадобится для отслеживания статуса генерации и получения готового отчета или
            документа.
          type: string
        estimatedGenerationTime:
          description: Ожидаемая продолжительность генерации в миллисекундах.
          type: integer
          format: int64
    GenerateReportResponse:
      description: Ответ на запрос генерации отчета или документа.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GenerateReportDTO'
    GenerateSalesGeographyRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - dateFrom
      - dateTo
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
        categoryIds:
          description: Идентификаторы категорий.
          type: array
          uniqueItems: true
          minItems: 1
          maxItems: 1000
          nullable: true
          items:
            type: integer
            format: int32
            minimum: 0
            exclusiveMinimum: true
        offerIds:
          description: Идентификаторы товаров.
          type: array
          uniqueItems: true
          minItems: 1
          maxItems: 1000
          nullable: true
          items:
            $ref: '#/components/schemas/ShopSku'
    GenerateShelfsStatisticsRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - attributionType
      - dateFrom
      - dateTo
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          description: Начало периода, включительно.
          format: date
          type: string
        dateTo:
          description: Конец периода, включительно.
          format: date
          type: string
        attributionType:
          $ref: '#/components/schemas/StatisticsAttributionType'
          description: Тип атрибуции.
    GenerateShipmentListDocumentReportRequest:
      description: 'Данные, необходимые для генерации документа.

        '
      type: object
      required:
      - campaignId
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        shipmentId:
          description: 'Идентификатор отгрузки.


            В запросе обязательно передавайте `shipmentId` или `orderIds`.

            '
          format: int64
          type: integer
        orderIds:
          description: 'Фильтр по идентификаторам заказа в отгрузке.


            В запросе обязательно передавайте `shipmentId` или `orderIds`.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            description: Идентификатор заказа.
            format: int64
            type: integer
    GenerateShowsBoostRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - dateFrom
      - dateTo
      - attributionType
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
        attributionType:
          $ref: '#/components/schemas/StatisticsAttributionType'
          description: Тип атрибуции.
    GenerateShowsSalesReportRequest:
      description: 'Данные, необходимые для генерации отчета.


        В запросе обязательно должен быть либо `businessId`, либо `campaignId`, но не оба сразу.

        '
      type: object
      required:
      - dateFrom
      - dateTo
      - grouping
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
        grouping:
          $ref: '#/components/schemas/ShowsSalesGroupingType'
          description: Группировка данных отчета.
    GenerateStocksOnWarehousesReportRequest:
      description: 'Данные, необходимые для генерации отчета. Передавайте либо `businessId`, либо `campaignId`/`campaignIds`,
        но не все сразу.

        '
      type: object
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
          description: '{% note warning "Для моделей DBS, FBS и Экспресс параметр скоро станет недоступен" %}


            Для получения информации об остатках на складе магазина передайте `businessId` и идентификатор нужного магазина
            в `campaignIds`.


            {% endnote %}

            '
        businessId:
          $ref: '#/components/schemas/BusinessId'
          description: '**Только для моделей DBS, FBS и Экспресс**


            Идентификатор кабинета, по магазинам которого нужно сформировать отчет (кроме моделей FBY и LaaS).

            '
        warehouseIds:
          description: Фильтр по идентификаторам складов (только модели FBY и LaaS). Чтобы узнать идентификатор, воспользуйтесь
            запросом [GET v2/warehouses](../../reference/warehouses/getFulfillmentWarehouses.md).
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: integer
            format: int64
        reportDate:
          description: 'Фильтр по дате (для моделей FBY и LaaS). В отчет попадут данные за **предшествующий** дате день.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        categoryIds:
          description: Фильтр по категориям на Маркете (кроме моделей FBY и LaaS).
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: integer
            format: int32
            minimum: 0
            exclusiveMinimum: true
        hasStocks:
          description: Фильтр по наличию остатков (кроме моделей FBY и LaaS).
          type: boolean
        campaignIds:
          description: 'Фильтр по магазинам для отчета по кабинету (кроме моделей FBY и LaaS).


            Передавайте вместе с `businessId`.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
    GenerateUnitedMarketplaceServicesReportRequest:
      description: 'Данные, необходимые для генерации отчета: идентификатор кампании, период, за который нужен отчет, а также
        фильтры.

        '
      type: object
      required:
      - businessId
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateTimeFrom:
          description: Начало периода, включительно.
          format: date-time
          type: string
          deprecated: true
        dateTimeTo:
          description: Конец периода, включительно. Максимальный период — 3 месяца.
          format: date-time
          type: string
          deprecated: true
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          description: 'Конец периода, включительно. Максимальный период — 3 месяца.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          format: date
          type: string
        yearFrom:
          $ref: '#/components/schemas/Year'
          description: Начальный год формирования акта.
        monthFrom:
          $ref: '#/components/schemas/Month'
          description: Начальный номер месяца формирования акта.
        yearTo:
          $ref: '#/components/schemas/Year'
          description: Конечный год формирования акта.
        monthTo:
          $ref: '#/components/schemas/Month'
          description: Конечный номер месяца формирования акта.
        placementPrograms:
          description: 'Список моделей, которые нужны в отчете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/PlacementType'
        inns:
          description: Список ИНН, которые нужны в отчете.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        campaignIds:
          description: 'Список идентификаторов кампании тех магазинов, которые нужны в отчете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
    GenerateUnitedNettingReportRequest:
      description: 'Данные, необходимые для генерации отчета: идентификатор кампании, период, за который нужен отчет, а также
        фильтры.

        '
      type: object
      required:
      - businessId
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateTimeFrom:
          description: Начало периода, включительно.
          format: date-time
          type: string
          deprecated: true
        dateTimeTo:
          description: Конец периода, включительно. Максимальный период — 3 месяца.
          format: date-time
          type: string
          deprecated: true
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          description: 'Конец периода, включительно. Максимальный период — 3 месяца.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          format: date
          type: string
        bankOrderId:
          description: Номер платежного поручения.
          format: int64
          type: integer
        bankOrderDateTime:
          description: Дата платежного поручения.
          format: date-time
          type: string
        monthOfYear:
          $ref: '#/components/schemas/MonthOfYearDTO'
        placementPrograms:
          description: 'Список моделей, которые нужны в отчете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/PlacementType'
        inns:
          description: Список ИНН, которые нужны в отчете.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        campaignIds:
          description: 'Список идентификаторов кампании тех магазинов, которые нужны в отчете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
    GenerateUnitedOrdersRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - dateFrom
      - dateTo
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          description: 'Конец периода, включительно. Максимальный период — 1 год.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          format: date
          type: string
        campaignIds:
          description: 'Список идентификаторов кампании тех магазинов, которые нужны в отчете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
        promoId:
          description: Идентификатор акции, товары из которой нужны в отчете.
          type: string
    GenerateUnitedReturnsRequest:
      description: 'Данные, необходимые для генерации отчета.

        '
      type: object
      required:
      - businessId
      - dateFrom
      - dateTo
      properties:
        businessId:
          $ref: '#/components/schemas/BusinessId'
        dateFrom:
          $ref: '#/components/schemas/PeriodDateFrom'
        dateTo:
          $ref: '#/components/schemas/PeriodDateTo'
        campaignIds:
          description: 'Список идентификаторов кампании тех магазинов, которые нужны в отчете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
        returnType:
          $ref: '#/components/schemas/ReturnType'
          nullable: true
        returnStatusTypes:
          description: 'Статусы передачи возвратов, которые нужны в отчете.


            Если их не указать, вернется информация по всем возвратам.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ReturnShipmentStatusType'
    GetAnswersRequest:
      description: 'Фильтр запроса ответов на вопрос.

        '
      type: object
      properties:
        questionId:
          $ref: '#/components/schemas/QuestionId'
        answerIds:
          description: 'Идентификаторы ответов.

            '
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/AnswerId'
    GetAnswersResponse:
      type: object
      description: 'Ответ на запрос получения ответов на вопрос.

        '
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/AnswerListDTO'
    GetBidsInfoRequest:
      description: description.
      type: object
      properties:
        skus:
          description: 'Список товаров, для которых нужно получить значения ставок.


            Если список не задан, постранично возвращаются все товары со ставками.


            Если список задан, результаты возвращаются одной страницей, а параметры `pageToken` и `limit` игнорируются.

            '
          type: array
          minItems: 1
          maxItems: 500
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    GetBidsInfoResponse:
      description: description.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetBidsInfoResponseDTO'
    GetBidsInfoResponseDTO:
      description: Список товаров с указанными ставками.
      type: object
      required:
      - bids
      properties:
        bids:
          description: Страница списка товаров.
          type: array
          items:
            $ref: '#/components/schemas/SkuBidItemDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GetBidsRecommendationsRequest:
      description: description.
      type: object
      required:
      - skus
      properties:
        skus:
          description: 'Список товаров, для которых нужно получить рекомендации по ставкам.

            '
          type: array
          minItems: 1
          maxItems: 1500
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    GetBidsRecommendationsResponse:
      description: description.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetBidsRecommendationsResponseDTO'
    GetBidsRecommendationsResponseDTO:
      description: Список товаров с рекомендованными ставками.
      type: object
      required:
      - recommendations
      properties:
        recommendations:
          description: Список товаров с рекомендованными ставками.
          type: array
          items:
            $ref: '#/components/schemas/SkuBidRecommendationItemDTO'
    GetBusinessBuyerInfoResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderBusinessBuyerDTO'
    GetBusinessDocumentsInfoResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderBusinessDocumentsDTO'
    GetBusinessOrdersRequest:
      type: object
      description: Запрос на получение информации о заказах бизнеса.
      properties:
        orderIds:
          description: Идентификаторы заказов.
          nullable: true
          uniqueItems: true
          type: array
          items:
            type: integer
            format: int64
          minItems: 1
          maxItems: 50
        externalOrderIds:
          description: Внешние идентификаторы заказов.
          nullable: true
          uniqueItems: true
          type: array
          items:
            $ref: '#/components/schemas/ExternalOrderId'
          minItems: 1
          maxItems: 50
        programTypes:
          description: Модели работы магазина на Маркете.
          nullable: true
          uniqueItems: true
          type: array
          items:
            $ref: '#/components/schemas/SellingProgramType'
          minItems: 1
        campaignIds:
          description: Идентификаторы кампаний магазинов.
          nullable: true
          uniqueItems: true
          type: array
          items:
            $ref: '#/components/schemas/CampaignId'
          minItems: 1
          maxItems: 50
        statuses:
          description: Статусы заказов.
          nullable: true
          uniqueItems: true
          type: array
          items:
            $ref: '#/components/schemas/OrderStatusType'
          minItems: 1
        substatuses:
          description: Этапы обработки или причины отмены заказов.
          nullable: true
          uniqueItems: true
          type: array
          items:
            $ref: '#/components/schemas/OrderSubstatusType'
          minItems: 1
        dates:
          $ref: '#/components/schemas/OrderDatesFilterDTO'
          description: Даты заказов.
        fake:
          description: 'Тип заказа:


            * `false` — настоящий заказ покупателя.


            * `true` — [тестовый заказ](../../concepts/sandbox.md) Маркета.

            '
          type: boolean
        waitingForCancellationApprove:
          description: '**Только для модели DBS**


            Фильтр для получения заказов, по которым есть запросы на отмену.


            При значении `true` возвращаются только те заказы, которые находятся в статусе `DELIVERY` или `PICKUP`, и пользователи
            решили их отменить.

            '
          type: boolean
        sourcePlatforms:
          description: Площадки-источники заказов.
          nullable: true
          uniqueItems: true
          type: array
          items:
            $ref: '#/components/schemas/OrderSourcePlatformType'
          minItems: 1
    GetBusinessOrdersResponse:
      type: object
      description: Ответ со списком заказов бизнеса.
      required:
      - orders
      properties:
        orders:
          description: Список заказов в кабинете.
          type: array
          maxItems: 50
          items:
            $ref: '#/components/schemas/BusinessOrderDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах результатов.
    GetBusinessSettingsInfoDTO:
      type: object
      description: Информация о кабинете и его настройках.
      properties:
        info:
          $ref: '#/components/schemas/BusinessDTO'
          description: Базовая информация о кабинете.
        settings:
          $ref: '#/components/schemas/BusinessSettingsDTO'
          description: Настройки на уровне кабинета.
        subscriptionLevel:
          $ref: '#/components/schemas/BusinessSubscriptionLevelType'
          description: Уровень подписки кабинета. Подробнее о подписке для продавцов читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/marketing/subscription).
        traits:
          description: Свойства кабинета.
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          items:
            $ref: '#/components/schemas/BusinessTraitType'
    GetBusinessSettingsResponse:
      type: object
      description: Ответ на запрос настроек кабинета.
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetBusinessSettingsInfoDTO'
    GetCampaignOfferDTO:
      description: Параметры размещения товара в магазине.
      type: object
      allOf:
      - $ref: '#/components/schemas/BaseCampaignOfferDTO'
      - properties:
          basicPrice:
            $ref: '#/components/schemas/GetPriceWithDiscountDTO'
            description: Цена товара для всех магазинов.
          campaignPrice:
            $ref: '#/components/schemas/GetPriceWithVatDTO'
            description: Цена, установленная в отдельном магазине.
          status:
            $ref: '#/components/schemas/OfferCampaignStatusType'
            description: 'Статус товара.

              '
          errors:
            description: 'Ошибки, препятствующие размещению товара на витрине.

              '
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/OfferErrorDTO'
          warnings:
            description: 'Предупреждения, не препятствующие размещению товара на витрине.

              '
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/OfferErrorDTO'
    GetCampaignOffersRequest:
      description: 'Фильтрации товаров


        В запросе можно указать либо фильтр offerIds, либо любые другие фильтры товаров. Совместное использование фильтра
        `offerIds` с другими фильтрациями приведет к ошибке.

        '
      type: object
      properties:
        offerIds:
          description: 'Идентификаторы товаров, информация о которых нужна.


            {% note warning "Такой список возвращается только целиком" %}


            Не используйте это поле одновременно с фильтрами по статусам карточек, категориям, брендам или тегам. Если вы
            хотите воспользоваться фильтрами, оставьте поле пустым.


            Если вы запрашиваете информацию по конкретным SKU, не заполняйте:


            * `pageToken`

            * `limit`


            {% endnote %}


             

            '
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/ShopSku'
          minItems: 1
          maxItems: 200
          uniqueItems: true
        statuses:
          description: 'Фильтр по статусам товаров.

            '
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/OfferCampaignStatusType'
          minItems: 1
          uniqueItems: true
        categoryIds:
          description: Фильтр по категориям на Маркете.
          type: array
          nullable: true
          items:
            type: integer
            format: int32
            minimum: 0
            exclusiveMinimum: true
          minItems: 1
          uniqueItems: true
        vendorNames:
          description: Фильтр по брендам.
          type: array
          nullable: true
          items:
            type: string
          minItems: 1
          uniqueItems: true
        tags:
          description: Фильтр по тегам.
          type: array
          nullable: true
          items:
            type: string
          minItems: 1
          uniqueItems: true
    GetCampaignOffersResponse:
      description: Ответ на запрос списка товаров в магазине.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetCampaignOffersResultDTO'
    GetCampaignOffersResultDTO:
      description: Список товаров в заданном магазине.
      type: object
      required:
      - offers
      properties:
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        offers:
          description: Страница списка товаров.
          type: array
          items:
            $ref: '#/components/schemas/GetCampaignOfferDTO'
    GetCampaignResponse:
      description: Информация о магазине к данным идентификатора кампании.
      type: object
      properties:
        campaign:
          $ref: '#/components/schemas/CampaignDTO'
    GetCampaignSettingsResponse:
      description: Ответ на запрос настроек магазина.
      type: object
      properties:
        settings:
          $ref: '#/components/schemas/CampaignSettingsDTO'
    GetCampaignsResponse:
      description: Результаты поиска магазинов.
      type: object
      required:
      - campaigns
      properties:
        campaigns:
          description: Список с информацией по каждому магазину.
          type: array
          items:
            $ref: '#/components/schemas/CampaignDTO'
        pager:
          $ref: '#/components/schemas/FlippingPagerDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GetCategoriesMaxSaleQuantumDTO:
      description: Категории и лимит на установку кванта и минимального количества товаров.
      type: object
      required:
      - results
      properties:
        results:
          description: Категории и лимит на установку кванта и минимального количества товаров.
          type: array
          items:
            $ref: '#/components/schemas/MaxSaleQuantumDTO'
        errors:
          description: Ошибки, которые появились из-за переданных категорий.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/CategoryErrorDTO'
    GetCategoriesMaxSaleQuantumRequest:
      description: 'Список категорий, для которых нужно вернуть лимит на установку кванта и минимального количества товаров.

        '
      type: object
      required:
      - marketCategoryIds
      properties:
        marketCategoryIds:
          description: Идентификаторы [листовых категорий](*list-categories) на Маркете.
          type: array
          uniqueItems: true
          minItems: 1
          maxItems: 1500
          items:
            type: integer
            format: int64
    GetCategoriesMaxSaleQuantumResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - $ref: '#/components/schemas/GetCategoriesMaxSaleQuantumDTO'
    GetCategoriesRequest:
      description: 'Параметры запроса категорий.

        '
      type: object
      properties:
        language:
          $ref: '#/components/schemas/LanguageType'
          description: Язык категорий.
    GetCategoriesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CategoryDTO'
    GetCategoryContentParametersResponse:
      description: Ответ со списком характеристик для категории и их допустимыми значениями.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CategoryContentParametersDTO'
    GetChatHistoryRequest:
      description: 'Историю какого чата нужно получить — и начиная с какого сообщения.

        '
      type: object
      properties:
        messageIdFrom:
          description: Идентификатор сообщения, начиная с которого нужно получить все последующие сообщения.
          type: integer
          format: int64
          minimum: 1
    GetChatHistoryResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ChatMessagesResultDTO'
    GetChatInfoDTO:
      description: Информация о чате.
      type: object
      required:
      - chatId
      - context
      - type
      - status
      - createdAt
      - updatedAt
      properties:
        chatId:
          description: Идентификатор чата.
          type: integer
          format: int64
          minimum: 1
        orderId:
          description: Идентификатор заказа.
          type: integer
          format: int64
          minimum: 1
          deprecated: true
        context:
          $ref: '#/components/schemas/ChatFullContextDTO'
        type:
          $ref: '#/components/schemas/ChatType'
        status:
          $ref: '#/components/schemas/ChatStatusType'
        createdAt:
          description: 'Дата и время создания чата.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2017-11-21T00:00:00+03:00'
        updatedAt:
          description: 'Дата и время последнего сообщения в чате.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2017-11-21T00:00:00+03:00'
    GetChatMessageResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ChatMessageDTO'
    GetChatResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetChatInfoDTO'
    GetChatsInfoDTO:
      description: Список чатов.
      type: object
      required:
      - chats
      properties:
        chats:
          description: Информация о чатах.
          type: array
          items:
            $ref: '#/components/schemas/GetChatInfoDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах с результатами.
    GetChatsRequest:
      description: 'Фильтры по чатам, которые нужно вернуть.

        '
      type: object
      properties:
        orderIds:
          description: Фильтр по идентификаторам заказов на Маркете.
          type: array
          nullable: true
          uniqueItems: true
          items:
            type: integer
            format: int64
          minItems: 1
          deprecated: true
        contexts:
          description: Фильтр по контексту чата.
          type: array
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ChatContextDTO'
          minItems: 1
        contextTypes:
          description: Фильтр по типу контекста чата.
          type: array
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ChatContextType'
          minItems: 1
        types:
          description: Фильтр по типам чатов.
          type: array
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ChatType'
          minItems: 1
        statuses:
          description: Фильтр по статусам чатов.
          type: array
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ChatStatusType'
          minItems: 1
    GetChatsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetChatsInfoDTO'
    GetDefaultPricesRequest:
      description: Запрос списка цен.
      type: object
      properties:
        offerIds:
          description: 'Идентификаторы товаров, информация о которых нужна.

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 200
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
        archived:
          description: Находится ли товар в архиве.
          type: boolean
    GetDefaultPricesResponse:
      description: Ответ на запрос списка цен.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OfferDefaultPriceListResponseDTO'
    GetDeliveryOptionsDTO:
      type: object
      description: Список доступных вариантов для разных складов.
      required:
      - warehousesDeliveryOptions
      properties:
        warehousesDeliveryOptions:
          type: array
          description: Варианты доставки для разных складов.
          minItems: 1
          items:
            $ref: '#/components/schemas/WarehousesDeliveryOptionsDTO'
    GetDeliveryOptionsItemDTO:
      description: Товар, для которого нужно вернуть варианты доставки.
      type: object
      allOf:
      - $ref: '#/components/schemas/BasicOrderItemDTO'
      - properties:
          warehouseId:
            description: 'Идентификатор фулфилмент-склада Маркета.


              Передайте этот параметр, чтобы вернулись варианты доставки для указанного склада. Иначе Маркет сам выберет склад.


              [Как узнать остатки товаров на складах](../../reference/stocks/getStocks.md)

              '
            type: integer
            format: int64
            minimum: 1
    GetDeliveryOptionsRequest:
      type: object
      description: 'Запрос для получения вариантов доставки.


        Используйте `pickupDelivery` для получения вариантов доставки самовывозом и `courierDelivery` для получения вариантов
        доставки курьером.

        '
      required:
      - items
      properties:
        items:
          type: array
          description: 'Товары на складах, для которых нужно вернуть варианты доставки.


            В рамках одного запроса все значения `offerId` должны быть уникальными. Не допускается передача двух объектов
            с одинаковым `offerId`.

            '
          minItems: 1
          maxItems: 1000
          items:
            $ref: '#/components/schemas/GetDeliveryOptionsItemDTO'
        pickupDelivery:
          $ref: '#/components/schemas/PickupDeliveryParametersDTO'
        courierDelivery:
          $ref: '#/components/schemas/CourierDeliveryParametersDTO'
    GetDeliveryOptionsResponse:
      description: Ответ на запрос получения вариантов доставки.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetDeliveryOptionsDTO'
    GetDeliveryServicesResponse:
      description: Ответ на запрос списка служб доставки.
      type: object
      properties:
        result:
          $ref: '#/components/schemas/DeliveryServicesDTO'
    GetFulfillmentWarehousesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/FulfillmentWarehousesDTO'
    GetGoodsFeedbackCommentsRequest:
      description: 'Фильтр запроса комментариев отзыва.

        '
      type: object
      properties:
        feedbackId:
          $ref: '#/components/schemas/GoodsFeedbackId'
        commentIds:
          description: 'Идентификаторы комментариев.


            ⚠️ Не используйте это поле одновременно с другими фильтрами. Если вы хотите воспользоваться ими, оставьте поле
            пустым.

            '
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/GoodsFeedbackCommentId'
    GetGoodsFeedbackCommentsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GoodsFeedbackCommentListDTO'
    GetGoodsFeedbackRequest:
      description: 'Фильтр запроса отзывов в кабинете.

        '
      type: object
      properties:
        feedbackIds:
          description: 'Идентификаторы отзывов.


            ⚠️ Не используйте это поле одновременно с другими фильтрами. Если вы хотите воспользоваться ими, оставьте поле
            пустым.

            '
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/GoodsFeedbackId'
        dateTimeFrom:
          description: 'Начало периода. Не включительно.


            Если параметр не указан, возвращается информация за 6 месяцев до указанной в `dateTimeTo` даты.


            Максимальный интервал 6 месяцев.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        dateTimeTo:
          description: 'Конец периода. Не включительно.


            Если параметр не указан, используется текущая дата.


            Максимальный интервал 6 месяцев.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        reactionStatus:
          $ref: '#/components/schemas/FeedbackReactionStatusType'
          description: 'Нужно ли вернуть только непрочитанные отзывы. Для этого передайте значение `NEED_REACTION`.


            По умолчанию возвращаются все отзывы.

            '
        ratingValues:
          description: Оценка товара.
          type: array
          uniqueItems: true
          maxItems: 5
          nullable: true
          minItems: 1
          items:
            type: integer
            format: int32
        offerIds:
          description: 'Фильтр по идентификатору товара.

            '
          type: array
          uniqueItems: true
          maxItems: 20
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ShopSku'
        paid:
          description: Фильтр отзывов за баллы Плюса.
          type: boolean
    GetGoodsFeedbackResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GoodsFeedbackListDTO'
    GetGoodsFeedbackUrbanadsRequest:
      description: 'Фильтр запроса отзывов в кабинете.

        '
      type: object
      properties:
        feedbackIds:
          description: 'Идентификаторы отзывов.


            ⚠️ Не используйте это поле одновременно с другими фильтрами. Если вы хотите воспользоваться ими, оставьте поле
            пустым.

            '
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/GoodsFeedbackId'
        dateTimeFrom:
          description: 'Начало периода. Не включительно.


            Если параметр не указан, возвращается информация за 6 месяцев до указанной в `dateTimeTo` даты.


            Максимальный интервал 6 месяцев.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        dateTimeTo:
          description: 'Конец периода. Не включительно.


            Если параметр не указан, используется текущая дата.


            Максимальный интервал 6 месяцев.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        reactionStatus:
          $ref: '#/components/schemas/FeedbackReactionStatusType'
          description: 'Нужно ли вернуть только непрочитанные отзывы. Для этого передайте значение `NEED_REACTION`.


            По умолчанию возвращаются все отзывы.

            '
        ratingValues:
          description: Оценка товара.
          type: array
          uniqueItems: true
          maxItems: 5
          nullable: true
          minItems: 1
          items:
            type: integer
            format: int32
        paid:
          description: Фильтр отзывов за баллы Плюса.
          type: boolean
    GetGoodsFeedbackUrbanadsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GoodsFeedbackUrbanadsListDTO'
    GetGoodsStatsRequest:
      description: Запрос отчета по товарам.
      type: object
      required:
      - shopSkus
      properties:
        shopSkus:
          description: 'Список ваших идентификаторов SKU.

            '
          type: array
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
          minItems: 1
          maxItems: 500
    GetGoodsStatsResponse:
      description: Ответ на запрос отчета по товарам.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GoodsStatsDTO'
    GetHiddenOffersResponse:
      description: Ответ на запрос списка скрытий.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - type: object
        properties:
          result:
            $ref: '#/components/schemas/GetHiddenOffersResultDTO'
    GetHiddenOffersResultDTO:
      description: 'Список скрытых вами товаров.

        '
      type: object
      required:
      - hiddenOffers
      properties:
        paging:
          $ref: '#/components/schemas/PackagingScrollingPagerDTO'
        hiddenOffers:
          description: Список скрытых товаров.
          type: array
          items:
            $ref: '#/components/schemas/HiddenOfferDTO'
    GetLogisticPointsResponse:
      type: object
      description: Информация о пунктах выдачи.
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetLogisticsPointsDTO'
    GetLogisticsPointsDTO:
      type: object
      description: Информация о пунктах выдачи заказов.
      required:
      - logisticPoints
      properties:
        logisticPoints:
          type: array
          description: Пункты выдачи заказов.
          items:
            $ref: '#/components/schemas/LogisticPointDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Идентификатор следующей страницы.
    GetMappingDTO:
      description: 'Информация о товарах в каталоге.

        '
      type: object
      allOf:
      - $ref: '#/components/schemas/UpdateMappingDTO'
        description: 'Идентификатор карточки на Маркете. Показывает текущую привязку товара к карточке.


          Может отсутствовать в ответе, если товар еще не привязан к карточке. Проверьте статус карточки или исправьте ошибки.

          '
      - properties:
          marketSkuName:
            description: 'Название карточки товара.


              Может отсутствовать в ответе, если товар еще не привязан к карточке.

              '
            type: string
          marketModelName:
            description: 'Название модели на Маркете.


              Может отсутствовать в ответе, если товар еще не привязан к карточке.

              '
            type: string
            deprecated: true
          marketCategoryId:
            description: 'Идентификатор категории на Маркете, в которую попал товар.


              Может отсутствовать в ответе, если Маркет еще не определил категорию товара.

              '
            type: integer
            format: int64
          marketCategoryName:
            description: 'Название категории карточки на Маркете.


              Может отсутствовать в ответе, если Маркет еще не определил категорию товара.

              '
            type: string
    GetOfferCardsContentStatusRequest:
      type: object
      properties:
        offerIds:
          description: 'Идентификаторы товаров, информация о которых нужна.

            <br><br>

            ⚠️ Не используйте это поле одновременно с фильтрами по статусам карточек, категориям, брендам или тегам. Если
            вы хотите воспользоваться фильтрами, оставьте поле пустым.

            '
          type: array
          maxItems: 200
          uniqueItems: true
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ShopSku'
        cardStatuses:
          description: 'Фильтр по статусам карточек.


            [Что такое карточка товара](https://yandex.ru/support/marketplace/assortment/content/index.html)

            '
          type: array
          uniqueItems: true
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferCardStatusType'
        categoryIds:
          description: Фильтр по категориям на Маркете.
          type: array
          maxItems: 200
          uniqueItems: true
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/CategoryId'
        withRecommendations:
          description: 'Возвращать ли список рекомендаций к заполнению карточки и средний рейтинг карточки у товаров той категории,
            которая указана в `marketCategoryId`.


            Значение по умолчанию: `false`. Если информация нужна, передайте значение `true`.

            '
          type: boolean
          default: false
    GetOfferCardsContentStatusResponse:
      description: Ответ со списком состояний товаров и пагинацией.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OfferCardsContentStatusDTO'
    GetOfferDTO:
      description: Параметры товара.
      type: object
      allOf:
      - $ref: '#/components/schemas/BaseOfferResponseDTO'
      - properties:
          basicPrice:
            $ref: '#/components/schemas/GetPriceWithDiscountDTO'
            description: 'Цена.

              '
          purchasePrice:
            $ref: '#/components/schemas/GetPriceDTO'
            description: 'Себестоимость — затраты на самостоятельное производство товара или закупку у производителя или поставщиков.

              '
          additionalExpenses:
            $ref: '#/components/schemas/GetPriceDTO'
            description: 'Дополнительные расходы на товар. Например, на доставку или упаковку.

              '
          cardStatus:
            $ref: '#/components/schemas/OfferCardStatusType'
            description: 'Статус карточки товара.

              '
          campaigns:
            description: 'Список магазинов, в которых размещен товар.

              '
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/OfferCampaignStatusDTO'
          sellingPrograms:
            description: 'Информация о том, какие для товара доступны модели размещения.

              '
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/OfferSellingProgramDTO'
          mediaFiles:
            $ref: '#/components/schemas/OfferMediaFilesDTO'
          archived:
            description: 'Товар помещен в архив.

              '
            type: boolean
          groupId:
            type: string
            description: 'Идентификатор группы товаров.


              У товаров, которые объединены в одну группу, будет одинаковый идентификатор.


              [Как объединить товары на карточке](../../step-by-step/assortment-add-goods.md#combine-variants)

              '
    GetOfferMappingDTO:
      description: Информация о товаре.
      type: object
      properties:
        offer:
          $ref: '#/components/schemas/GetOfferDTO'
          description: Основные параметры товара.
        mapping:
          $ref: '#/components/schemas/GetMappingDTO'
          description: Информация о карточке товара на Маркете.
        showcaseUrls:
          description: Ссылки на один и тот же товар на разных витринах Маркета.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ShowcaseUrlDTO'
    GetOfferMappingsRequest:
      type: object
      properties:
        offerIds:
          description: 'Идентификаторы товаров, информация о которых нужна.


            {% note warning "Такой список возвращается только целиком" %}


            Если вы запрашиваете информацию по конкретным SKU, не заполняйте:

            * `pageToken`;

            * `limit`;

            * `cardStatuses`;

            * `categoryIds`;

            * `vendorNames`;

            * `tags`;

            * `archived`.


            {% endnote %}


             

            '
          type: array
          nullable: true
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
        cardStatuses:
          description: 'Фильтр по статусам карточек.


            [Что такое карточка товара](https://yandex.ru/support/marketplace/assortment/content/index.html)

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OfferCardStatusType'
        categoryIds:
          description: Фильтр по категориям на Маркете.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: integer
            format: int32
            minimum: 0
            exclusiveMinimum: true
        vendorNames:
          description: Фильтр по брендам.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        tags:
          description: Фильтр по тегам.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        archived:
          description: 'Фильтр по нахождению в архиве.


            Передайте `true`, чтобы получить товары, находящиеся в архиве. Если фильтр не заполнен или передано `false`, в
            ответе возвращаются товары, не находящиеся в архиве.

            '
          type: boolean
    GetOfferMappingsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetOfferMappingsResultDTO'
    GetOfferMappingsResultDTO:
      description: Информация о товарах.
      type: object
      required:
      - offerMappings
      properties:
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Идентификатор следующей страницы.
        offerMappings:
          description: Информация о товарах.
          type: array
          items:
            $ref: '#/components/schemas/GetOfferMappingDTO'
    GetOfferRecommendationsRequest:
      type: object
      properties:
        offerIds:
          description: Идентификаторы товаров, информация о которых нужна. ⚠️ Не используйте это поле одновременно с остальными
            фильтрами. Если вы хотите воспользоваться фильтрами, оставьте поле пустым.
          type: array
          nullable: true
          minItems: 1
          maxItems: 200
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
        competitivenessFilter:
          $ref: '#/components/schemas/PriceCompetitivenessType'
          description: Фильтр, выводящий товары, с привлекательными, умеренными и непривлекательными ценами.
    GetOfferRecommendationsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OfferRecommendationsResultDTO'
    GetOperationsRequest:
      type: object
      description: Запрос на получение статусов операций.
      required:
      - operationType
      - operationIds
      properties:
        operationType:
          $ref: '#/components/schemas/OperationType'
        operationIds:
          description: 'Список идентификаторов операций.

            '
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OperationId'
    GetOperationsResponse:
      type: object
      description: Ответ с статусами операций.
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetOperationsResultDTO'
    GetOperationsResultDTO:
      type: object
      description: Информация об операциях.
      required:
      - operations
      properties:
        operations:
          description: Список операций.
          type: array
          items:
            $ref: '#/components/schemas/OperationResultDTO'
    GetOrderBuyerInfoResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderBuyerInfoDTO'
    GetOrderIdentifiersStatusDTO:
      description: Информация по проверке кодов маркировки.
      type: object
      required:
      - items
      properties:
        items:
          description: Список идентификаторов товаров и информация по проверке кодов.
          type: array
          items:
            $ref: '#/components/schemas/OrderItemValidationStatusDTO'
    GetOrderIdentifiersStatusResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetOrderIdentifiersStatusDTO'
    GetOrderLabelsDataResponse:
      description: Ответ с информацией для печати ярлыков.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderLabelDTO'
    GetOrderResponse:
      type: object
      properties:
        order:
          $ref: '#/components/schemas/OrderDTO'
    GetOrderUpdateOptionsRequest:
      type: object
      description: Запрос вариантов изменения интервала курьерской доставки.
      required:
      - id
      properties:
        id:
          description: Идентификатор заказа.
          type: integer
          format: int64
    GetOrderUpdateOptionsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderUpdateOptionsDTO'
    GetOrdersResponse:
      type: object
      description: Модель для ответа API списка информации по заказам.
      required:
      - orders
      properties:
        pager:
          $ref: '#/components/schemas/FlippingPagerDTO'
        orders:
          description: 'Модель заказа.

            '
          type: array
          maxItems: 50
          items:
            $ref: '#/components/schemas/OrderDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах с результатами.
    GetOrdersStatsRequest:
      description: Запрос информации по заказам.
      type: object
      properties:
        dateFrom:
          description: 'Начальная дата, когда заказ был сформирован.


            Формат даты: `ГГГГ‑ММ‑ДД`.


            Нельзя использовать вместе с параметрами `updateFrom` и `updateTo`.

            '
          type: string
          format: date
        dateTo:
          description: 'Конечная дата, когда заказ был сформирован.


            Формат даты: `ГГГГ‑ММ‑ДД`.


            Нельзя использовать вместе с параметрами `updateFrom` и `updateTo`.

            '
          type: string
          format: date
        updateFrom:
          description: 'Начальная дата периода, за который были изменения в заказе (например, статуса или информации о платежах).


            Формат даты: `ГГГГ‑ММ‑ДД`.


            Нельзя использовать вместе с параметрами `dateFrom` и `dateTo`.

            '
          type: string
          format: date
        updateTo:
          description: 'Конечная дата периода, за который были изменения в заказе (например, статуса или информации о платежах).


            Формат даты: `ГГГГ‑ММ‑ДД`.


            Нельзя использовать вместе с параметрами `dateFrom` и `dateTo`.

            '
          type: string
          format: date
        orders:
          description: Список идентификаторов заказов.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            description: 'Список товаров в заказе после возможных изменений.


              В ходе обработки заказа Маркет может удалить из него единицы товаров — при проблемах на складе или по инициативе
              пользователя.


              * Если из заказа удалены все единицы товара, его не будет в списке `items` — только в списке `initialItems`.


              * Если в заказе осталась хотя бы одна единица товара, он будет и в списке `items` (с уменьшенным количеством
              единиц `count`), и в списке `initialItems` (с первоначальным количеством единиц `initialCount`).

              '
            type: integer
            format: int64
        statuses:
          description: Список статусов заказов.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OrderStatsStatusType'
        hasCis:
          description: 'Фильтр для получения заказов, в которых есть хотя бы один товар с кодом идентификации в системе [«Честный
            ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов :no-translate[Market
            Yandex Go]):


            * `true` — да.

            * `false` — нет.

            Такие коды присваиваются товарам, которые подлежат маркировке и относятся к определенным категориям.

            '
          type: boolean
    GetOrdersStatsResponse:
      description: Ответ на запрос информации по заказам.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrdersStatsDTO'
    GetOutletLicensesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OutletLicensesResponseDTO'
    GetOutletResponse:
      description: Ответ на запрос информации о точке продаж.
      type: object
      properties:
        outlet:
          $ref: '#/components/schemas/FullOutletDTO'
    GetOutletsResponse:
      description: Ответ на запрос информации о точках продаж.
      type: object
      required:
      - outlets
      properties:
        outlets:
          description: Информация о точках продаж.
          type: array
          items:
            $ref: '#/components/schemas/FullOutletDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GetPagedWarehousesRequest:
      description: Запрос на получение складов кабинета.
      type: object
      properties:
        components:
          description: 'Свойства складов, которые необходимо вернуть. Если какое-то значение параметра не задано, этой информации
            в ответе не будет.


            Передавайте параметр, только если нужна информация, которую он возвращает.


            Можно передать сразу несколько значений.

            '
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          items:
            $ref: '#/components/schemas/WarehouseComponentType'
        campaignIds:
          description: "Список идентификаторов кампании тех магазинов, склады которых необходимо вернуть.\n\nИх можно узнать\
            \ с помощью запроса [GET v2/campaigns](../../reference/campaigns/getCampaigns.md) или найти в кабинете продавца\
            \ на Маркете — нажмите на иконку вашего аккаунта → **Настройки** и в меню слева выберите **API и модули**:\n\n\
            \  * блок **Идентификатор кампании**;\n  * вкладка **Лог запросов** → выпадающий список в блоке **Показывать логи**.\n\
            \n⚠️ Не используйте вместо них идентификаторы магазинов, которые указаны в кабинете продавца на Маркете рядом\
            \ с названием магазина и в некоторых отчетах.\n"
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          maxItems: 25
          items:
            $ref: '#/components/schemas/CampaignId'
    GetPagedWarehousesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/PagedWarehousesDTO'
    GetPriceDTO:
      description: Цена с указанием времени последнего обновления.
      type: object
      allOf:
      - $ref: '#/components/schemas/BasePriceDTO'
      - $ref: '#/components/schemas/UpdateTimeDTO'
    GetPriceWithDiscountDTO:
      description: Цена с указанием валюты, скидки и времени последнего обновления.
      type: object
      allOf:
      - $ref: '#/components/schemas/PriceWithDiscountDTO'
      - $ref: '#/components/schemas/UpdateTimeDTO'
    GetPriceWithVatDTO:
      description: Цена с указанием НДС и времени последнего обновления.
      type: object
      allOf:
      - $ref: '#/components/schemas/PriceDTO'
      - $ref: '#/components/schemas/UpdateTimeDTO'
    GetPricesByOfferIdsRequest:
      description: Запрос списка цен.
      type: object
      properties:
        offerIds:
          description: 'Список SKU.


            {% note warning "Такой список возвращается только целиком" %}


            Если вы запрашиваете информацию по конкретным SKU, не заполняйте:


            * `pageToken`

            * `limit`


            {% endnote %}


             

            '
          type: array
          minItems: 1
          maxItems: 500
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ShopSku'
    GetPricesByOfferIdsResponse:
      description: Ответ на запрос списка цен.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OfferPriceByOfferIdsListResponseDTO'
    GetPricesResponse:
      description: Ответ на запрос списка цен.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OfferPriceListResponseDTO'
    GetPromoAssortmentInfoDTO:
      description: Информация о товарах в акции.
      type: object
      required:
      - activeOffers
      properties:
        activeOffers:
          description: 'Количество товаров, которые участвуют или участвовали в акции.


            Не учитываются товары, которые были добавлены автоматически.


            Об автоматическом и ручном добавлении товаров в акцию читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/index).

            '
          type: integer
          format: int32
        potentialOffers:
          description: 'Количество доступных товаров в акции.


            Параметр возвращается только для текущих и будущих акций.

            '
          type: integer
          format: int32
        processing:
          description: 'Есть ли изменения в ассортименте, которые еще не применились. Сохранение изменений занимает некоторое
            время.


            Параметр возвращается только для текущих и будущих акций.

            '
          type: boolean
    GetPromoBestsellerInfoDTO:
      description: Информация об акции «Бестселлеры Маркета».
      type: object
      required:
      - bestseller
      properties:
        bestseller:
          description: Является ли акция «Бестселлером Маркета». Подробнее об этой акции читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/bestsellers).
          type: boolean
        entryDeadline:
          description: 'До какой даты можно добавить товар в акцию «Бестселлеры Маркета».


            Параметр возвращается только для текущих и будущих акций «Бестселлеры Маркета».

            '
          type: string
          format: date-time
        renewalEnabled:
          description: 'Включен ли автоматический перенос ассортимента между акциями «Бестселлеры Маркета». О том, как это
            работает, читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/marketing/promos/market/bestsellers#next).


            Параметр возвращается только для текущих и будущих акций «Бестселлеры Маркета».

            '
          type: boolean
    GetPromoConstraintsDTO:
      description: Ограничения в акции.
      type: object
      properties:
        warehouseIds:
          description: 'Идентификаторы складов, для которых действует акция. Товары, которые лежат на других складах, не будут
            продаваться по акции.


            Параметр возвращается, только если в условиях акции есть ограничение по складу.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: integer
            format: int64
    GetPromoDTO:
      description: Информация об акции.
      type: object
      required:
      - id
      - name
      - period
      - participating
      - assortmentInfo
      - mechanicsInfo
      - bestsellerInfo
      properties:
        id:
          description: Идентификатор акции.
          type: string
        name:
          description: Название акции.
          type: string
        period:
          $ref: '#/components/schemas/PromoPeriodDTO'
        participating:
          description: 'Участвует или участвовал ли продавец в этой акции.


            Для текущих и будущих акций возвращается со значением `true`, если в акции есть товары, которые были добавлены
            вручную. Если товары не участвуют в акции или добавлены в нее автоматически, параметр возвращается со значением
            `false`.


            Для прошедших акций всегда возвращается со значением `true`.


            Об автоматическом и ручном добавлении товаров в акцию читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/index).

            '
          type: boolean
        assortmentInfo:
          $ref: '#/components/schemas/GetPromoAssortmentInfoDTO'
        mechanicsInfo:
          $ref: '#/components/schemas/GetPromoMechanicsInfoDTO'
        bestsellerInfo:
          $ref: '#/components/schemas/GetPromoBestsellerInfoDTO'
        channels:
          description: Список каналов продвижения товаров.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/ChannelType'
        constraints:
          $ref: '#/components/schemas/GetPromoConstraintsDTO'
    GetPromoMechanicsInfoDTO:
      description: Информация о типе акции.
      type: object
      required:
      - type
      properties:
        type:
          $ref: '#/components/schemas/MechanicsType'
        promocodeInfo:
          $ref: '#/components/schemas/GetPromoPromocodeInfoDTO'
    GetPromoOfferDTO:
      description: Товар, который участвует или может участвовать в акции.
      type: object
      required:
      - offerId
      - status
      - params
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        status:
          $ref: '#/components/schemas/PromoOfferParticipationStatusType'
        params:
          $ref: '#/components/schemas/PromoOfferParamsDTO'
        autoParticipatingDetails:
          $ref: '#/components/schemas/PromoOfferAutoParticipatingDetailsDTO'
    GetPromoOffersRequest:
      description: Получение списка товаров, которые участвуют или могут участвовать в акции.
      type: object
      required:
      - promoId
      properties:
        promoId:
          description: Идентификатор акции.
          type: string
        statusType:
          $ref: '#/components/schemas/PromoOfferParticipationStatusFilterType'
          description: '{% note warning "Вместо него используйте `statuses`." %}


             


            {% endnote %}


            Фильтр для товаров, которые добавлены в акцию вручную.


            Если не передать параметр `statusType`, вернутся все товары.

            '
          deprecated: true
        statuses:
          description: Фильтр для товаров, которые могут участвовать в акции. Можно задать несколько значений.
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          items:
            $ref: '#/components/schemas/PromoOfferParticipationStatusMultiFilterType'
    GetPromoOffersResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetPromoOffersResultDTO'
    GetPromoOffersResultDTO:
      description: Список товаров, которые участвуют или могут участвовать в акции.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Товары, которые участвуют или могут участвовать в акции.
          type: array
          items:
            $ref: '#/components/schemas/GetPromoOfferDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GetPromoPromocodeInfoDTO:
      description: 'Информация для типа `MARKET_PROMOCODE`.


        Параметр заполняется только для этого типа акции.

        '
      type: object
      required:
      - promocode
      - discount
      properties:
        promocode:
          description: Промокод.
          type: string
        discount:
          description: Процент скидки по промокоду.
          type: integer
          format: int32
    GetPromosRequest:
      description: Фильтры для получения списка акций.
      type: object
      properties:
        participation:
          $ref: '#/components/schemas/PromoParticipationType'
        mechanics:
          $ref: '#/components/schemas/MechanicsType'
          description: 'Фильтр по типу акции.


            По умолчанию возвращаются все типы акций.

            '
    GetPromosResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetPromosResultDTO'
    GetPromosResultDTO:
      description: Информация об акциях Маркета.
      type: object
      required:
      - promos
      properties:
        promos:
          description: Акции Маркета.
          type: array
          items:
            $ref: '#/components/schemas/GetPromoDTO'
    GetQualityRatingDetailsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/QualityRatingDetailsDTO'
    GetQualityRatingRequest:
      description: Запрос информации по индексу качества.
      type: object
      required:
      - campaignIds
      properties:
        dateFrom:
          description: 'Начало периода.


            Формат даты: `ГГГГ‑ММ‑ДД`.


            Не может быть раньше 30 дней от текущей даты.

            '
          type: string
          format: date
        dateTo:
          description: 'Конец периода.


            Формат даты: `ГГГГ‑ММ‑ДД`.


            Не может быть позже текущей даты.

            '
          type: string
          format: date
        campaignIds:
          description: 'Список идентификаторов кампаний магазинов.

            '
          type: array
          minItems: 1
          maxItems: 50
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
    GetQualityRatingResponse:
      description: Информация об индексе качества магазинов.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/CampaignsQualityRatingDTO'
    GetQuarantineOffersRequest:
      description: 'Фильтрации товаров


        В запросе можно указать либо фильтр `offerIds, либо любые другие фильтры товаров. Совместное использование фильтра
        offerIds с другими фильтрациями приведет к ошибке.

        '
      type: object
      properties:
        offerIds:
          description: 'Идентификаторы товаров, информация о которых нужна.

            <br><br>

            ⚠️ Не используйте это поле одновременно с фильтрами по статусам карточек, категориям, брендам или тегам. Если
            вы хотите воспользоваться фильтрами, оставьте поле пустым.

            '
          type: array
          minItems: 1
          maxItems: 200
          uniqueItems: true
          nullable: true
          items:
            $ref: '#/components/schemas/ShopSku'
        cardStatuses:
          description: 'Фильтр по статусам карточек.


            [Что такое карточка товара](https://yandex.ru/support/marketplace/assortment/content/index.html)

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OfferCardStatusType'
        categoryIds:
          description: Фильтр по категориям на Маркете.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: integer
            format: int32
            minimum: 0
            exclusiveMinimum: true
        vendorNames:
          description: Фильтр по брендам.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        tags:
          description: Фильтр по тегам.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
    GetQuarantineOffersResponse:
      description: Ответ на запрос списка товаров в карантине.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetQuarantineOffersResultDTO'
    GetQuarantineOffersResultDTO:
      description: Список товаров в карантине.
      type: object
      required:
      - offers
      properties:
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        offers:
          description: Страница списка товаров в карантине.
          type: array
          items:
            $ref: '#/components/schemas/QuarantineOfferDTO'
    GetQuestionsRequest:
      type: object
      description: Запрос на получение вопросов о товарах продавца.
      properties:
        categoryIds:
          type: array
          description: Идентификаторы категорий товаров.
          nullable: true
          uniqueItems: true
          minItems: 1
          items:
            type: integer
            format: int64
            minimum: 0
        questionIds:
          type: array
          description: Идентификаторы вопросов.
          nullable: true
          uniqueItems: true
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/QuestionId'
        dateFrom:
          description: 'Дата начала периода создания вопроса.


            Если параметр не указан, возвращается информация за 1 месяц до указанной в `dateTo` даты.


            Максимальный интервал 1 месяц.

            '
          type: string
          format: date
          example: '2020-02-02'
        dateTo:
          description: 'Дата окончания периода создания вопроса.


            Если параметр не указан, используется текущая дата.


            Максимальный интервал 1 месяц.

            '
          type: string
          format: date
          example: '2020-02-02'
        needAnswer:
          description: 'Нужен ли ответ на вопрос.


            * `true` — только вопросы, которые ждут ответа.

            * `false` — все вопросы.

            '
          type: boolean
          default: false
        sort:
          $ref: '#/components/schemas/QuestionSortOrderType'
    GetQuestionsResponse:
      type: object
      description: Ответ на запрос вопросов.
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/QuestionListDTO'
    GetRegionByIdResponse:
      type: object
      required:
      - region
      properties:
        regions:
          type: array
          deprecated: true
          nullable: true
          minItems: 1
          maxItems: 1
          description: 'Регион доставки.


            {% note warning %}


            В массиве всегда возвращается один регион, используйте поле `region` вместо него.


            {% endnote %}

            '
          items:
            $ref: '#/components/schemas/RegionDTO'
        region:
          $ref: '#/components/schemas/RegionDTO'
          description: Регион доставки.
    GetRegionWithChildrenResponse:
      type: object
      properties:
        pager:
          $ref: '#/components/schemas/FlippingPagerDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        regions:
          $ref: '#/components/schemas/RegionWithChildrenDTO'
    GetRegionsCodesResponse:
      type: object
      required:
      - countries
      properties:
        countries:
          type: array
          description: Список стран с их кодами в формате :no-translate[ISO 3166-1 alpha-2].
          items:
            $ref: '#/components/schemas/CountryDTO'
    GetRegionsResponse:
      type: object
      required:
      - regions
      properties:
        regions:
          type: array
          description: Регион доставки.
          items:
            $ref: '#/components/schemas/RegionDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Идентификатор следующей страницы.
    GetReportInfoResponse:
      description: Ответ на запрос информации об отчете или документе.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ReportInfoDTO'
    GetReturnAvailableDecisionsRequest:
      description: Запрос на получение возможных решений по возврату.
      type: object
      required:
      - campaignId
      - returnId
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        returnId:
          $ref: '#/components/schemas/ReturnId'
    GetReturnAvailableDecisionsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ReturnAvailableDecisionsResponse'
    GetReturnDeliveryOptionsDTO:
      type: object
      description: Информация о пунктах выдачи, которые могут принять возврат указанных товаров.
      required:
      - pickupDelivery
      properties:
        pickupDelivery:
          $ref: '#/components/schemas/PickupReturnDeliveryOptionsDTO'
    GetReturnDeliveryOptionsRequest:
      type: object
      description: Запрос для получения вариантов доставки возвратов.
      required:
      - items
      - pickupDelivery
      properties:
        items:
          type: array
          description: Товары в возврате.
          minItems: 1
          maxItems: 1000
          items:
            $ref: '#/components/schemas/BasicOrderItemDTO'
        pickupDelivery:
          $ref: '#/components/schemas/PickupDeliveryParametersDTO'
    GetReturnDeliveryOptionsResponse:
      description: Информация о доступных пунктах выдачи.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetReturnDeliveryOptionsDTO'
    GetReturnResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ReturnDTO'
    GetReturnsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/PagedReturnsDTO'
    GetShipmentOrdersInfoResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrdersShipmentInfoDTO'
    GetShipmentResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ShipmentDTO'
    GetSupplyRequestDocumentsDTO:
      type: object
      description: Информация о документах по заявке.
      required:
      - documents
      properties:
        documents:
          description: Список документов.
          type: array
          nullable: false
          minItems: 0
          items:
            $ref: '#/components/schemas/SupplyRequestDocumentDTO'
    GetSupplyRequestDocumentsRequest:
      type: object
      required:
      - requestId
      properties:
        requestId:
          $ref: '#/components/schemas/SupplyRequestId'
    GetSupplyRequestDocumentsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetSupplyRequestDocumentsDTO'
    GetSupplyRequestItemsDTO:
      type: object
      description: Информация о товарах в заявке.
      required:
      - items
      properties:
        items:
          description: Список товаров.
          type: array
          nullable: false
          minItems: 0
          maxItems: 100
          items:
            $ref: '#/components/schemas/SupplyRequestItemDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах с результатами.
    GetSupplyRequestItemsRequest:
      type: object
      required:
      - requestId
      properties:
        requestId:
          $ref: '#/components/schemas/SupplyRequestId'
    GetSupplyRequestItemsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetSupplyRequestItemsDTO'
    GetSupplyRequestsDTO:
      type: object
      description: Список заявок и информация по ним.
      required:
      - requests
      properties:
        requests:
          type: array
          description: Список заявок.
          nullable: false
          minItems: 0
          maxItems: 100
          items:
            $ref: '#/components/schemas/SupplyRequestDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах с результатами.
    GetSupplyRequestsRequest:
      type: object
      description: 'Модель для фильтрации и сортировки заявок на поставку.

        Фильтры по `requestDateFrom` и `requestDateTo` отбирают заявки по targetLocation->requestedDate и transitLocation->requestedDate.

        '
      properties:
        requestIds:
          type: array
          description: Идентификаторы заявок.
          minItems: 1
          maxItems: 100
          uniqueItems: true
          nullable: true
          items:
            $ref: '#/components/schemas/SupplyRequestId'
        requestDateFrom:
          type: string
          format: date-time
          nullable: true
          description: Дата начала периода для фильтрации заявок.
        requestDateTo:
          type: string
          format: date-time
          nullable: true
          description: Дата окончания периода для фильтрации заявок.
        requestTypes:
          type: array
          description: Типы заявок для фильтрации.
          minItems: 1
          uniqueItems: true
          nullable: true
          items:
            $ref: '#/components/schemas/SupplyRequestType'
        requestSubtypes:
          type: array
          description: Подтипы заявок для фильтрации.
          minItems: 1
          uniqueItems: true
          nullable: true
          items:
            $ref: '#/components/schemas/SupplyRequestSubType'
        requestStatuses:
          type: array
          description: Статусы заявок для фильтрации.
          minItems: 1
          uniqueItems: true
          nullable: true
          items:
            $ref: '#/components/schemas/SupplyRequestStatusType'
        sorting:
          $ref: '#/components/schemas/SupplyRequestSortingDTO'
    GetSupplyRequestsResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetSupplyRequestsDTO'
    GetTokenInfoResponse:
      description: Ответ на запрос получения информации о токене авторизации.
      type: object
      required:
      - result
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/TokenDTO'
    GetWarehouseStocksDTO:
      description: Список складов с информацией об остатках на каждом из них.
      type: object
      required:
      - warehouses
      properties:
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        warehouses:
          description: 'Страница списка складов.


            **Для моделей FBY и LaaS:** может содержать несколько складов Маркета.


            **Для модели FBS:** может содержать как партнерский склад, так и склад возвратов Маркета.

            '
          type: array
          items:
            $ref: '#/components/schemas/WarehouseOffersDTO'
    GetWarehouseStocksRequest:
      description: 'Фильтры для запроса остатков.

        '
      type: object
      properties:
        stocksWarehouseId:
          description: 'Идентификатор склада.


            Если параметр указан, возвращаются только товары на переданном складе.


            **Для моделей FBY и LaaS:** получить список складов Маркета можно с помощью метода [GET v2/warehouses](../../reference/warehouses/getFulfillmentWarehouses.md).

            '
          type: integer
          format: int64
          minimum: 1
        hasStocks:
          description: '**Только для моделей FBY и LaaS**


            Фильтр по наличию товаров. Используйте только вместе со `stocksWarehouseId`.


            Передайте `false`, чтобы получить информацию о товарах, которых нет в наличие. При значении `true` возвращаются
            данные о товарах, которые есть на указанном складе.

            '
          type: boolean
        withTurnover:
          description: '**Только для моделей FBY и LaaS**


            Возвращать ли информацию по оборачиваемости.


            Значение по умолчанию: `false`. Если информация нужна, передайте значение `true`.

            '
          type: boolean
          default: false
        archived:
          description: 'Фильтр по нахождению в архиве.


            Передайте `true`, чтобы получить информацию об остатках товаров, которые находятся в архиве. Если фильтр не заполнен
            или передано `false`, в ответе возвращается информация о товарах, которые не находятся в архиве.

            '
          type: boolean
        offerIds:
          description: 'Фильтр по вашим SKU товаров.


            Возвращается информация об остатках всех переданных SKU, включая товары в архиве.


            {% note warning "Такой список возвращается только целиком" %}


            Если вы запрашиваете информацию по конкретным SKU, не заполняйте:


            * `pageToken`

            * `limit`

            * `archived`

            * `stocksOnWarehouse`


            {% endnote %}


             

            '
          type: array
          uniqueItems: true
          minItems: 1
          maxItems: 500
          nullable: true
          items:
            $ref: '#/components/schemas/ShopSku'
    GetWarehouseStocksResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GetWarehouseStocksDTO'
    GetWarehousesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/WarehousesDTO'
    GoodsFeedbackCommentAuthorDTO:
      description: Информация об авторе комментария.
      type: object
      properties:
        type:
          $ref: '#/components/schemas/GoodsFeedbackCommentAuthorType'
        name:
          description: Имя автора или название кабинета.
          type: string
    GoodsFeedbackCommentAuthorType:
      description: 'Тип автора:


        * `USER` — пользователь.

        * `BUSINESS` — кабинет.

        * `BRAND` — бренд.

        '
      type: string
      enum:
      - USER
      - BUSINESS
      - BRAND
    GoodsFeedbackCommentDTO:
      description: Комментарий к отзыву.
      type: object
      required:
      - id
      - text
      - status
      - feedbackId
      properties:
        id:
          $ref: '#/components/schemas/GoodsFeedbackCommentId'
        text:
          $ref: '#/components/schemas/GoodsFeedbackCommentText'
        canModify:
          description: Может ли продавец изменять комментарий или удалять его.
          type: boolean
        parentId:
          description: Идентификатор родительского комментария.
          type: integer
          format: int64
        author:
          $ref: '#/components/schemas/GoodsFeedbackCommentAuthorDTO'
        status:
          $ref: '#/components/schemas/GoodsFeedbackCommentStatusType'
        feedbackId:
          $ref: '#/components/schemas/GoodsFeedbackId'
    GoodsFeedbackCommentId:
      description: 'Идентификатор комментария к отзыву.

        '
      type: integer
      format: int64
    GoodsFeedbackCommentListDTO:
      description: Комментарии к отзыву.
      type: object
      required:
      - comments
      properties:
        comments:
          description: Список комментариев.
          type: array
          items:
            $ref: '#/components/schemas/GoodsFeedbackCommentDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GoodsFeedbackCommentStatusType:
      description: 'Статус комментария:


        * `PUBLISHED` — опубликован.

        * `UNMODERATED` — не проверен.

        * `BANNED` — заблокирован.

        * `DELETED` — удален.

        '
      type: string
      enum:
      - PUBLISHED
      - UNMODERATED
      - BANNED
      - DELETED
    GoodsFeedbackCommentText:
      description: 'Текст комментария.


        Не должен содержать контакты магазина и ссылки на сайты, кроме Маркета.

        '
      type: string
      minLength: 1
      maxLength: 4096
    GoodsFeedbackContextUrbanadsDTO:
      description: Информация о товаре, бизнесе и бренде, которые связаны с отзывом.
      type: object
      properties:
        offerName:
          description: Название товара, под которым оставлен отзыв.
          type: string
        pictureUrl:
          $ref: '#/components/schemas/Url'
          description: Ссылка на фотографию товара.
        businessId:
          $ref: '#/components/schemas/BusinessId'
          description: Идентификатор бизнеса, под товаром которого оставлен отзыв.
        businessName:
          description: Название бизнеса, под товаром которого оставлен отзыв.
          type: string
        brandId:
          description: Идентификатор бренда товара.
          type: string
        brandName:
          description: Название бренда товара.
          type: string
    GoodsFeedbackDTO:
      description: Отзыв о товаре.
      type: object
      required:
      - identifiers
      - statistics
      - feedbackId
      - needReaction
      - createdAt
      properties:
        feedbackId:
          $ref: '#/components/schemas/GoodsFeedbackId'
        createdAt:
          description: Дата и время создания отзыва.
          type: string
          format: date-time
        needReaction:
          description: 'Прочитан ли отзыв.


            Принимает значение `false`, если магазин:


            * Прочитал отзыв в кабинете продавца на Маркете.

            * Отметил отзыв прочитанным — метод [POST v2/businesses/{businessId}/goods-feedback/skip-reaction](../../reference/goods-feedback/skipGoodsFeedbacksReaction.md).

            * Оставил комментарий к отзыву — метод [POST v2/businesses/{businessId}/goods-feedback/comments/update](../../reference/goods-feedback/updateGoodsFeedbackComment.md).

            '
          type: boolean
        identifiers:
          $ref: '#/components/schemas/GoodsFeedbackIdentifiersDTO'
        author:
          description: Имя автора отзыва.
          type: string
        description:
          $ref: '#/components/schemas/GoodsFeedbackDescriptionDTO'
        media:
          $ref: '#/components/schemas/GoodsFeedbackMediaDTO'
        statistics:
          $ref: '#/components/schemas/GoodsFeedbackStatisticsDTO'
    GoodsFeedbackDescriptionDTO:
      description: Текстовая часть отзыва.
      type: object
      properties:
        advantages:
          description: Описание плюсов товара в отзыве.
          type: string
        disadvantages:
          description: Описание минусов товара в отзыве.
          type: string
        comment:
          description: Комментарий в отзыве.
          type: string
    GoodsFeedbackId:
      description: 'Идентификатор отзыва.

        '
      type: integer
      format: int64
    GoodsFeedbackIdentifiersDTO:
      description: Идентификаторы, которые связаны с отзывом.
      type: object
      properties:
        orderId:
          description: Идентификатор заказа на Маркете.
          type: integer
          format: int64
        offerId:
          $ref: '#/components/schemas/ShopSku'
          description: Идентификатор товара.
    GoodsFeedbackListDTO:
      description: 'Список отзывов о товарах.

        '
      type: object
      required:
      - feedbacks
      properties:
        feedbacks:
          description: Список отзывов.
          type: array
          items:
            $ref: '#/components/schemas/GoodsFeedbackDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GoodsFeedbackMediaDTO:
      description: Фотографии и видео.
      type: object
      properties:
        photos:
          description: Ссылки на фотографии.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        videos:
          description: Ссылки на видео.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
    GoodsFeedbackStatisticsDTO:
      description: Статистическая информация по отзыву.
      type: object
      required:
      - rating
      - commentsCount
      properties:
        rating:
          description: Оценка товара.
          type: integer
          format: int32
          minimum: 1
          maximum: 5
        commentsCount:
          description: 'Количество комментариев к отзыву.


            Учитываются только ответы на отзывы, а не дочерние комментарии.

            '
          type: integer
          format: int64
        recommended:
          description: Рекомендуют ли этот товар.
          type: boolean
        paidAmount:
          description: Количество баллов Плюса, которое автор получил за отзыв.
          type: integer
          format: int64
    GoodsFeedbackUrbanadsDTO:
      description: Отзыв о товаре.
      type: object
      required:
      - context
      - statistics
      - feedbackId
      - needReaction
      - createdAt
      properties:
        feedbackId:
          $ref: '#/components/schemas/GoodsFeedbackId'
        createdAt:
          description: Дата и время создания отзыва.
          type: string
          format: date-time
        needReaction:
          description: 'Прочитан ли отзыв.


            Принимает значение `false`, если рекламодатель:


            * Прочитал отзыв в кабинете UrbanAds.

            * Пропустил реакцию на отзыв — метод [POST v2/businesses/{businessId}/goods-feedback/skip-reaction](../../reference/goods-feedback/skipGoodsFeedbacksReaction.md).

            * Оставил комментарий к отзыву — метод [POST v2/businesses/{businessId}/goods-feedback/comments/update](../../reference/goods-feedback/updateGoodsFeedbackComment.md).

            '
          type: boolean
        context:
          $ref: '#/components/schemas/GoodsFeedbackContextUrbanadsDTO'
        author:
          description: Имя автора отзыва.
          type: string
        description:
          $ref: '#/components/schemas/GoodsFeedbackDescriptionDTO'
        media:
          $ref: '#/components/schemas/GoodsFeedbackMediaDTO'
        statistics:
          $ref: '#/components/schemas/GoodsFeedbackStatisticsDTO'
    GoodsFeedbackUrbanadsListDTO:
      description: 'Список отзывов о товарах.

        '
      type: object
      required:
      - feedbacks
      properties:
        feedbacks:
          description: Список отзывов.
          type: array
          items:
            $ref: '#/components/schemas/GoodsFeedbackUrbanadsDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    GoodsStatsDTO:
      description: Отчет по товарам.
      type: object
      required:
      - shopSkus
      properties:
        shopSkus:
          description: Список товаров.
          type: array
          items:
            $ref: '#/components/schemas/GoodsStatsGoodsDTO'
    GoodsStatsGoodsDTO:
      description: Информация о товаре.
      type: object
      properties:
        shopSku:
          $ref: '#/components/schemas/ShopSku'
        marketSku:
          $ref: '#/components/schemas/MarketSku'
          description: SKU на Маркете — идентификатор карточки товара на Маркете.
        name:
          description: Название товара.
          type: string
        price:
          description: Цена товара в валюте, которая установлена [в кабинете продавца на Маркете](https://partner.market.yandex.ru/).
          type: number
        categoryId:
          description: Идентификатор категории товара на Маркете.
          type: integer
          format: int64
        categoryName:
          description: Название категории товара на Маркете.
          type: string
        weightDimensions:
          $ref: '#/components/schemas/GoodsStatsWeightDimensionsDTO'
          description: 'Информация о весе и габаритах товара.

            Если товар уже привязан к карточке (`marketSku`), в ответе вернутся габариты из карточки Маркета, а не размеры,
            которые вы передаете.

            '
        warehouses:
          description: 'Информация о складах, на которых хранится товар.


            Параметр не приходит, если товара нет ни на одном складе.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/GoodsStatsWarehouseDTO'
        tariffs:
          description: 'Информация о тарифах, по которым нужно заплатить за услуги Маркета.


            По некоторым услугам могут возвращаться несколько разных стоимостей. Например, в модели FBS стоимость услуги `SORTING`
            (обработка заказа) зависит от способа отгрузки

            и количества заказов в отгрузке. Подробнее о тарифах на услуги читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/introduction/rates/models/).

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/TariffDTO'
        pictures:
          description: Ссылки (URL) изображений товара в хорошем качестве.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/Url'
    GoodsStatsWarehouseDTO:
      description: Информация о складе.
      type: object
      required:
      - stocks
      properties:
        id:
          description: Идентификатор склада.
          type: integer
          format: int64
        name:
          description: Название склада.
          type: string
        stocks:
          description: Информация об остатках товаров на складе.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseStockDTO'
    GoodsStatsWeightDimensionsDTO:
      description: 'Информация о весе и габаритах товара.


        Если товар уже привязан к карточке (`marketSku`), в ответе вернутся габариты из карточки Маркета, а не размеры, которые
        вы передаете.

        '
      type: object
      properties:
        length:
          description: Длина товара в сантиметрах.
          type: number
        width:
          description: Ширина товара в сантиметрах.
          type: number
        height:
          description: Высота товара в сантиметрах.
          type: number
        weight:
          description: Вес товара в килограммах.
          type: number
    GpsDTO:
      description: 'GPS-координаты широты и долготы.

        '
      type: object
      required:
      - latitude
      - longitude
      properties:
        latitude:
          type: number
          description: Широта.
        longitude:
          type: number
          description: Долгота.
    HiddenOfferDTO:
      description: Информация о скрытии.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
          description: 'Идентификатор предложения из прайс-листа.

            '
    ItemPriceDTO:
      description: 'Информация о выплатах и вознаграждениях.


        {% note info "Как рассчитывается стоимость всех единиц товара и вознаграждение продавцу" %}


        * Стоимость всех единиц товара складывается из параметров `payment` и `cashback`.

        * Общая сумма вознаграждений продавцу возвращается в параметре `subsidy`.

        * Значение `payment`, `cashback` и `subsidy` может отличаться от стоимости в каталоге, потому что информация об акциях
        магазина не возвращается.

        * Если в заказе несколько единиц товаров, в параметрах возвращается суммарное значение.

        * Все суммы указаны в валюте магазина.


        {% endnote %}

        '
      type: object
      properties:
        payment:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: 'Общая стоимость всех единиц товара.

            '
        subsidy:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: 'Общая сумма вознаграждений продавцу.

            '
        cashback:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: 'Сумма, которая оплачена баллами Плюса.

            '
        vat:
          $ref: '#/components/schemas/OrderVatType'
          description: НДС на товар.
    KeyIndicatorsReportDetalizationLevelType:
      description: 'За какой период нужна детализация:


        * `WEEK` — по неделям.


        * `MONTH` — по месяцам.

        '
      type: string
      enum:
      - WEEK
      - MONTH
    LabelsSortingType:
      description: 'Тип сортировки ярлыков:


        * `SORT_BY_GIVEN_ORDER` — ярлыки заказов будут расположены в том же порядке, в каком были переданы идентификаторы
        заказов в запросе.

        * `SORT_BY_ORDER_CREATED_AT` — ярлыки будут расположены в соответствии с датой создания заказа с группировкой по магазинам.


        Если параметр не указан, ярлыки сортируются по дате создания.

        '
      type: string
      enum:
      - SORT_BY_GIVEN_ORDER
      - SORT_BY_ORDER_CREATED_AT
    LanguageType:
      description: 'Язык:


        * `RU` — русский.


        * `EN` — английский.

        '
      type: string
      enum:
      - RU
      - EN
    LicenseCheckStatusType:
      description: 'Статус проверки лицензии:


        * `NEW` — лицензия проверяется.

        * `SUCCESS` — лицензия прошла проверку.

        * `FAIL` — лицензия не прошла проверку.

        * `REVOKE` — лицензия отозвана службой качества.

        * `DONT_WANT` — не проверяется.

        * `FAIL_MANUAL` — лицензия не прошла проверку службы качества.

        '
      type: string
      enum:
      - NEW
      - SUCCESS
      - FAIL
      - REVOKE
      - DONT_WANT
      - FAIL_MANUAL
    LicenseType:
      description: 'Тип лицензии:


        * `ALCOHOL` — лицензия на розничную продажу алкогольной продукции.

        * `UNKNOWN` — неизвестный тип лицензии.

        '
      type: string
      enum:
      - ALCOHOL
      - UNKNOWN
    LogisticPickupPointDTO:
      type: object
      description: Описание пункта вывоза для возврата.
      properties:
        id:
          type: integer
          format: int64
          description: Идентификатор пункта вывоза.
        name:
          type: string
          description: Название пункта вывоза.
        address:
          $ref: '#/components/schemas/PickupAddressDTO'
          description: Адрес пункта вывоза.
        instruction:
          description: Дополнительные инструкции к вывозу.
          type: string
        type:
          $ref: '#/components/schemas/LogisticPointType'
          description: Тип логистической точки.
        logisticPartnerId:
          type: integer
          format: int64
          description: Идентификатор логистического партнера, к которому относится логистическая точка.
    LogisticPointAddressDTO:
      type: object
      description: Адрес пункта выдачи.
      required:
      - fullAddress
      - gps
      - regionId
      properties:
        fullAddress:
          type: string
          description: Полный адрес.
          minLength: 1
          maxLength: 512
        gps:
          $ref: '#/components/schemas/GpsDTO'
        regionId:
          type: integer
          format: int64
          description: 'Идентификатор региона.


            Информацию о регионе можно получить c помощью метода [GET v2/regions](../../reference/regions/searchRegionsById.md).

            '
        city:
          type: string
          description: Город.
          minLength: 1
          maxLength: 128
        street:
          type: string
          description: Улица.
          minLength: 1
          maxLength: 128
        house:
          type: string
          description: Номер дома.
          minLength: 1
          maxLength: 16
        building:
          type: string
          description: Номер строения.
          minLength: 1
          maxLength: 16
        block:
          type: string
          description: Номер корпуса.
          minLength: 1
          maxLength: 16
        km:
          description: 'Порядковый номер километра, на котором располагается пункт выдачи.


            Указывается, если в адресе нет улицы.

            '
          type: integer
          format: int32
          minimum: 0
        additional:
          type: string
          description: Дополнительная информация.
          minLength: 1
          maxLength: 1024
    LogisticPointBrandType:
      description: 'Тип пункта выдачи:


        * `MARKET` — пункт выдачи Маркета.

        '
      type: string
      enum:
      - MARKET
    LogisticPointDTO:
      type: object
      description: Информация о пункте выдачи.
      required:
      - logisticPointId
      - brand
      - address
      - workingSchedule
      - deliveryRestrictions
      - paymentTypes
      - storagePeriod
      properties:
        logisticPointId:
          $ref: '#/components/schemas/LogisticPointId'
        brand:
          $ref: '#/components/schemas/LogisticPointBrandType'
        address:
          $ref: '#/components/schemas/LogisticPointAddressDTO'
        workingSchedule:
          $ref: '#/components/schemas/LogisticPointScheduleDTO'
        deliveryRestrictions:
          $ref: '#/components/schemas/LogisticPointDeliveryRestrictionDTO'
        features:
          type: array
          description: Свойства пункта выдачи.
          minItems: 1
          nullable: true
          uniqueItems: true
          items:
            $ref: '#/components/schemas/LogisticPointFeatureType'
        storagePeriod:
          type: integer
          format: int64
          description: 'Срок хранения заказа в пункте выдачи.


            Указывается в днях.

            '
    LogisticPointDeliveryRestrictionDTO:
      type: object
      description: Ограничения на доставку в пункт выдачи.
      required:
      - dimensionsRestrictions
      properties:
        dimensionsRestrictions:
          $ref: '#/components/schemas/LogisticPointDimensionRestrictionsDTO'
    LogisticPointDimensionRestrictionsDTO:
      type: object
      description: Ограничения по размеру одного товара.
      required:
      - weight
      - height
      - width
      - length
      - dimensionsSum
      properties:
        weight:
          type: integer
          format: int32
          description: Максимальный вес в граммах.
          minimum: 1
        height:
          type: integer
          format: int32
          description: Максимальная высота в сантиметрах.
          minimum: 1
        width:
          type: integer
          format: int32
          description: Максимальная ширина в сантиметрах.
          minimum: 1
        length:
          type: integer
          format: int32
          description: Максимальная длина в сантиметрах.
          minimum: 1
        dimensionsSum:
          type: integer
          format: int32
          description: Максимальная сумма измерений в сантиметрах.
          minimum: 1
    LogisticPointFeatureType:
      description: 'Свойство пункта выдачи:


        * `RETURN_ALLOWED` — пункт выдачи принимает возвраты.


        {% note warning "Проверка ограничений" %}


        Признак не учитывает ограничения ПВЗ на прием различных товаров в возврате.

        Для проверки по конкретному набору товаров используйте метод [POST v1/campaigns/{campaignId}/return-delivery-options](../../reference/delivery-options/getReturnDeliveryOptions.md).


        {% endnote %}

        '
      type: string
      enum:
      - RETURN_ALLOWED
    LogisticPointId:
      type: integer
      description: 'Идентификатор пункта выдачи.


        Его можно узнать с помощью метода [POST v1/businesses/{businessId}/logistics-points](../../reference/logistic-points/getLogisticPoints.md).

        '
      format: int64
      minimum: 1
    LogisticPointScheduleDTO:
      type: object
      description: Расписание работы пункта выдачи.
      required:
      - schedule
      properties:
        schedule:
          description: График работы.
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/ScheduleDayDTO'
        holidays:
          description: Расписание праздничных дней.
          type: array
          minItems: 1
          uniqueItems: true
          nullable: true
          items:
            type: string
            format: date
            description: 'Праздничный день.


              Формат даты: `ГГГГ-ММ-ДД`.

              '
    LogisticPointType:
      description: "Тип логистической точки:\n\n  * `WAREHOUSE` — склад.\n  * `PICKUP_POINT` — обычная точка выдачи заказов\
        \ (ПВЗ).\n  * `PICKUP_TERMINAL` — постамат.\n  * `PICKUP_POST_OFFICE` — отделение почтовой связи (ОПС).\n  * `PICKUP_MIXED`\
        \ — торговый зал и пункт выдачи заказов.\n  * `PICKUP_RETAIL` — торговый зал.\n"
      type: string
      enum:
      - WAREHOUSE
      - PICKUP_POINT
      - PICKUP_TERMINAL
      - PICKUP_POST_OFFICE
      - PICKUP_MIXED
      - PICKUP_RETAIL
    MarketSku:
      description: Идентификатор карточки товара на Маркете.
      type: integer
      format: int64
      minimum: 1
    MaxSaleQuantumDTO:
      description: 'Лимит на установку кванта и минимального количества товаров по категориям.

        '
      type: object
      required:
      - id
      properties:
        id:
          description: Идентификатор категории.
          type: integer
          format: int64
        name:
          description: Название категории.
          type: string
        maxSaleQuantum:
          description: Лимит на установку кванта и минимального количества товаров.
          type: integer
    MechanicsType:
      description: 'Тип акции:


        * `DIRECT_DISCOUNT` — прямая скидка.


        * `BLUE_FLASH` — флеш-акция.


        * `MARKET_PROMOCODE` — скидка по промокоду.

        '
      type: string
      enum:
      - DIRECT_DISCOUNT
      - BLUE_FLASH
      - MARKET_PROMOCODE
    MediaFileUploadStateType:
      description: 'Состояние загрузки медиафайла:


        * `UPLOADING` — загружается.

        * `UPLOADED` — успешно загружен.

        * `FAILED` — при загрузке произошла ошибка. Повторите попытку позже.

        '
      type: string
      enum:
      - UPLOADING
      - UPLOADED
      - FAILED
    Month:
      description: Номер месяца.
      type: integer
      format: int32
      minimum: 1
      maximum: 12
      example: 12
    MonthOfYearDTO:
      description: Месяц, за который нужен отчет о баллах Маркета.
      type: object
      required:
      - year
      - month
      properties:
        year:
          $ref: '#/components/schemas/Year'
        month:
          $ref: '#/components/schemas/Month'
    OfferBarcodes:
      description: 'Штрихкод.


        Указывайте в виде последовательности цифр. Подойдут коды :no-translate[EAN-13, EAN-8, UPC-A, UPC-E] или :no-translate[Code
        128]. Для книг — :no-translate[ISBN].


        Для товаров [определенных категорий и торговых марок](https://yastatic.net/s3/doc-binary/src/support/market/ru/yandex-market-list-for-gtin.xlsx)
        штрихкод должен быть действительным кодом :no-translate[GTIN]. Обратите внимание: внутренние штрихкоды, начинающиеся
        на 2 или 02, и коды формата :no-translate[Code 128] не являются :no-translate[GTIN].


        [Что такое :no-translate[GTIN]](*gtin)

        '
      type: array
      nullable: true
      minItems: 1
      uniqueItems: true
      items:
        type: string
        example: '46012300000000'
    OfferCampaignStatusDTO:
      description: Статус товара в магазине.
      type: object
      required:
      - campaignId
      - status
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        status:
          $ref: '#/components/schemas/OfferCampaignStatusType'
          description: 'Статус товара.

            '
    OfferCampaignStatusType:
      description: 'Статус товара:


        * `PUBLISHED` — Готов к продаже.

        * `CHECKING` — На проверке.

        * `DISABLED_BY_PARTNER` — Скрыт вами.

        * `REJECTED_BY_MARKET` — Отклонен.

        * `DISABLED_AUTOMATICALLY` — Исправьте ошибки.

        * `CREATING_CARD` — Создается карточка.

        * `NO_CARD` — Нужна карточка.

        * `NO_STOCKS` — Нет на складе.

        * `ARCHIVED` — В архиве.

        * `READY_FOR_PUBLICATION` — Магазин в процессе подключения.


        [Что обозначает каждый из статусов](https://yandex.ru/support/marketplace/assortment/add/statuses.html)

        '
      type: string
      enum:
      - PUBLISHED
      - CHECKING
      - DISABLED_BY_PARTNER
      - DISABLED_AUTOMATICALLY
      - REJECTED_BY_MARKET
      - CREATING_CARD
      - NO_CARD
      - NO_STOCKS
      - ARCHIVED
      - READY_FOR_PUBLICATION
    OfferCardContentStatusType:
      description: 'Статус вычисления рейтинга карточки товара и рекомендаций:


        * `UPDATING` — рейтинг обновляется.

        * `ACTUAL` — рейтинг актуальный.

        '
      type: string
      enum:
      - UPDATING
      - ACTUAL
    OfferCardDTO:
      description: 'Информация о состоянии карточки товара.


        Если поле `mapping` отсутствует в ответе, Маркет еще не успел обработать информацию о товаре. Чтобы определить категорию
        такого товара, повторите запрос через несколько минут.

        '
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        mapping:
          $ref: '#/components/schemas/GetMappingDTO'
          description: Основная информация о карточке товара.
        parameterValues:
          description: 'Список характеристик с их значениями.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ParameterValueDTO'
        cardStatus:
          $ref: '#/components/schemas/OfferCardStatusType'
          description: Статус карточки.
        contentRating:
          description: Рейтинг карточки.
          type: integer
          format: int32
        averageContentRating:
          description: 'Средний рейтинг карточки у товаров той категории, которая указана в `marketCategoryId`.


            Возвращается, только если параметр `withRecommendations` имеет значение `true`.

            '
          type: integer
          format: int32
        contentRatingStatus:
          $ref: '#/components/schemas/OfferCardContentStatusType'
          description: Статус вычисления рейтинга карточки и рекомендаций.
        recommendations:
          description: 'Список рекомендаций к заполнению карточки.


            Возвращается, только если параметр `withRecommendations` имеет значение `true`.


            Рекомендации Маркета помогают заполнять карточку так, чтобы покупателям было проще найти ваш товар и решиться
            на покупку.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferCardRecommendationDTO'
        groupId:
          type: string
          description: 'Идентификатор группы товаров.


            У товаров, которые объединены в одну группу, будет одинаковый идентификатор.


            [Как объединить товары на карточке](../../step-by-step/assortment-add-goods.md#combine-variants)

            '
        errors:
          description: Ошибки в контенте, препятствующие размещению товара на витрине.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferErrorDTO'
        warnings:
          description: Связанные с контентом предупреждения, не препятствующие размещению товара на витрине.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferErrorDTO'
    OfferCardRecommendationDTO:
      description: Рекомендация по заполнению карточки товара.
      type: object
      required:
      - type
      properties:
        type:
          $ref: '#/components/schemas/OfferCardRecommendationType'
          description: Рекомендация.
        percent:
          description: 'Процент выполнения рекомендации.


            Указывается для рекомендаций некоторых типов:


            * `PICTURE_COUNT`.

            * `VIDEO_COUNT`.

            * `MAIN`.

            * `ADDITIONAL`.

            * `DISTINCTIVE`.

            '
          type: integer
          format: int32
          minimum: 0
          maximum: 100
          exclusiveMaximum: true
        remainingRatingPoints:
          description: 'Максимальное количество баллов рейтинга карточки, которые можно получить за выполнение рекомендаций.

            '
          type: integer
          format: int32
          minimum: 1
          maximum: 100
    OfferCardRecommendationType:
      description: "Рекомендация по дополнению или замене контента. Не возвращается для карточек, которые заполнены Маркетом\
        \ или содержат бывшие в употреблении товары.\n\nЧасть рекомендаций относятся к **основным параметрам**, которые есть\
        \ у товаров любых категорий. Другие — к тем **характеристикам**, которые есть у товара потому, что он относится к\
        \ определенной категории.\n\n**1. Рекомендации, относящиеся к основным параметрам**\n\nКаждая такая рекомендация относится\
        \ к **единственному параметру**. Чтобы заполнить этот параметр, пользуйтесь запросом [POST v2/businesses/{businessId}/offer-mappings/update](../../reference/business-offer-mappings/updateOfferMappings.md).\n\
        \nРекомендации по заполнению параметров в `updateOfferMappings`:\n\n* `RECOGNIZED_VENDOR` — напишите название производителя\
        \ так, как его пишет сам производитель (параметр `vendor`).\n* `PICTURE_COUNT` — добавьте изображения (параметр `pictures`).\
        \ [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/images)\n\n  Для рекомендации приходит\
        \ процент ее выполнения.\n* `FIRST_PICTURE_SIZE`— замените первое изображение более крупным (параметр `pictures`).\
        \ [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/images)\n* `TITLE_LENGTH` — измените название\
        \ (параметр `name`). Составьте название по схеме: тип + бренд или производитель + модель + особенности, если есть\
        \ (размер, вес, цвет). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/title)\n* `DESCRIPTION_LENGTH`\
        \ — добавьте описание рекомендуемого размера (параметр `description`). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/description)\n\
        * `AVERAGE_PICTURE_SIZE` — замените все изображения на изображения высокого качества (параметр `pictures`). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/images)\n\
        * `FIRST_VIDEO_LENGTH` — добавьте первое видео рекомендуемой длины (параметр `videos`). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/video)\n\
        * `FIRST_VIDEO_SIZE` — замените первое видео на видео высокого качества (параметр `videos`). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/video)\n\
        * `AVERAGE_VIDEO_SIZE` — замените все видео на видео высокого качества (параметр `videos`). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/video)\n\
        * `VIDEO_COUNT` — добавьте хотя бы одно видео (параметр `videos`). [Требования](https://yandex.ru/support2/marketplace/ru/assortment/fields/video)\n\
        \n  Для рекомендации приходит процент ее выполнения.\n\n**2. Рекомендации, относящиеся к характеристикам по категориям**\n\
        \nКаждая такая рекомендация предполагает заполнение **одной или нескольких характеристик**. Чтобы узнать, какие именно\
        \ характеристики нужно заполнить, воспользуйтесь запросом [POST v2/category/{categoryId}/parameters](../../reference/content/getCategoryContentParameters.md).\
        \ Например, если вы получили рекомендацию `MAIN`, нужно заполнить характеристики, имеющие `MAIN` в массиве `recommendationTypes`.\n\
        \nРекомендации:\n\n* `MAIN` — заполните ключевые характеристики товара, которые используются в поиске и фильтрах.\n\
        \n  Для рекомендации приходит процент ее выполнения.\n* `ADDITIONAL` — заполните дополнительные характеристики товара.\n\
        \n  Для рекомендации приходит процент ее выполнения.\n* `DISTINCTIVE` — заполните характеристики, которыми отличаются\
        \ друг от друга варианты товара.\n\n  Для рекомендации приходит процент ее выполнения.\n\n**3. Устаревшие рекомендации**\n\
        \n* `HAS_VIDEO`.\n* `FILTERABLE`.\n* `HAS_DESCRIPTION`.\n* `HAS_BARCODE`.\n"
      type: string
      enum:
      - HAS_VIDEO
      - RECOGNIZED_VENDOR
      - MAIN
      - ADDITIONAL
      - DISTINCTIVE
      - FILTERABLE
      - PICTURE_COUNT
      - HAS_DESCRIPTION
      - HAS_BARCODE
      - FIRST_PICTURE_SIZE
      - TITLE_LENGTH
      - DESCRIPTION_LENGTH
      - AVERAGE_PICTURE_SIZE
      - FIRST_VIDEO_SIZE
      - FIRST_VIDEO_LENGTH
      - AVERAGE_VIDEO_SIZE
      - VIDEO_COUNT
    OfferCardStatusType:
      description: 'Статус карточки товара:


        * `HAS_CARD_CAN_NOT_UPDATE` — Карточка Маркета.

        * `HAS_CARD_CAN_UPDATE` — Можно дополнить.

        * `HAS_CARD_CAN_UPDATE_ERRORS` — Изменения не приняты.

        * `HAS_CARD_CAN_UPDATE_PROCESSING` — Изменения на проверке.

        * `NO_CARD_NEED_CONTENT` — Создайте карточку.

        * `NO_CARD_MARKET_WILL_CREATE` — Создаст Маркет.

        * `NO_CARD_ERRORS` — Не создана из-за ошибки.

        * `NO_CARD_PROCESSING` — Проверяем данные.

        * `NO_CARD_ADD_TO_CAMPAIGN` — Разместите товар в магазине.

        '
      type: string
      enum:
      - HAS_CARD_CAN_NOT_UPDATE
      - HAS_CARD_CAN_UPDATE
      - HAS_CARD_CAN_UPDATE_ERRORS
      - HAS_CARD_CAN_UPDATE_PROCESSING
      - NO_CARD_NEED_CONTENT
      - NO_CARD_MARKET_WILL_CREATE
      - NO_CARD_ERRORS
      - NO_CARD_PROCESSING
      - NO_CARD_ADD_TO_CAMPAIGN
    OfferCardsContentStatusDTO:
      description: Список товаров с информацией о состоянии карточек.
      type: object
      required:
      - offerCards
      properties:
        offerCards:
          description: Страница списка товаров с информацией о состоянии карточек.
          type: array
          items:
            $ref: '#/components/schemas/OfferCardDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Идентификатор следующей страницы.
    OfferCategory:
      description: '{% note warning "Вместо него используйте `marketCategoryId`." %}


         


        {% endnote %}


        Категория товара в вашем магазине.

        '
      type: string
      deprecated: true
    OfferConditionDTO:
      description: 'Состояние уцененного товара.

        '
      type: object
      properties:
        type:
          $ref: '#/components/schemas/OfferConditionType'
          description: 'Тип уценки.

            '
        quality:
          $ref: '#/components/schemas/OfferConditionQualityType'
          description: 'Внешний вид товара.

            '
        reason:
          description: 'Описание товара. Подробно опишите дефекты, насколько они заметны и где их искать.

            '
          type: string
    OfferConditionQualityType:
      description: 'Внешний вид товара:


        * `PERFECT` — идеальный.

        * `EXCELLENT` — отличный.

        * `GOOD` — хороший.

        * `NOT_SPECIFIED` — не выбран.

        '
      type: string
      enum:
      - PERFECT
      - EXCELLENT
      - GOOD
      - NOT_SPECIFIED
    OfferConditionType:
      description: 'Тип уценки:


        * `PREOWNED` —  бывший в употреблении товар, раньше принадлежал другому человеку.

        * `SHOWCASESAMPLE` — витринный образец.

        * `REFURBISHED` — повторная продажа товара.

        * `REDUCTION` — товар с дефектами.

        * `RENOVATED` — восстановленный товар.

        * `NOT_SPECIFIED` — не выбран.


        `REFURBISHED` — специальное значение для одежды, обуви и аксессуаров. Используется только для уцененных товаров из
        этой категории. Другие значения для одежды, обуви и аксессуаров не используются.

        '
      type: string
      enum:
      - PREOWNED
      - SHOWCASESAMPLE
      - REFURBISHED
      - REDUCTION
      - RENOVATED
      - NOT_SPECIFIED
    OfferContentDTO:
      description: Товар с указанными характеристиками.
      type: object
      required:
      - offerId
      - categoryId
      - parameterValues
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        categoryId:
          $ref: '#/components/schemas/CategoryId'
        parameterValues:
          description: 'Список характеристик с их значениями.


            При **изменении** характеристик передавайте только те, значение которых нужно обновить. Если в `categoryId` вы
            меняете категорию, значения общих характеристик для старой и новой категории сохранятся, передавать их не нужно.


            Подробнее читайте в [«Передача значений характеристики»](../../step-by-step/parameter-values.md).

            '
          type: array
          maxItems: 300
          minItems: 1
          items:
            $ref: '#/components/schemas/ParameterValueDTO'
    OfferContentErrorDTO:
      description: Текст ошибки или предупреждения.
      type: object
      properties:
        type:
          $ref: '#/components/schemas/OfferContentErrorType'
        parameterId:
          description: Идентификатор характеристики, с которой связана ошибка или предупреждение.
          type: integer
          format: int64
        message:
          description: Текст ошибки или предупреждения.
          type: string
      required:
      - type
      - message
    OfferContentErrorType:
      description: 'Типы ошибок и предупреждений:


        * `OFFER_NOT_FOUND` — такого товара нет в каталоге.

        * `UNKNOWN_CATEGORY` — указана неизвестная категория.

        * `INVALID_CATEGORY` — указана нелистовая категория. Укажите ту, которая не имеет дочерних категорий.

        * `UNKNOWN_PARAMETER` — передана характеристика, которой нет среди характеристик категории.

        * `UNEXPECTED_BOOLEAN_VALUE` — вместо boolean-значения передано что-то другое.

        * `NUMBER_FORMAT` — передана строка, не обозначающая число, вместо числа.

        * `INVALID_UNIT_ID` — передана единица измерения, недопустимая для характеристики.

        * `INVALID_GROUP_ID_LENGTH` — в названии превышено допустимое значение символов — 255.

        * `INVALID_GROUP_ID_CHARACTERS` — переданы [недопустимые символы](*ascii-code).


        Проверить, какие категорийные характеристики доступны для заданной категории, и получить их настройки можно с помощью
        запроса [POST v2/category/{categoryId}/parameters](../../reference/content/getCategoryContentParameters).

        '
      type: string
      enum:
      - OFFER_NOT_FOUND
      - UNKNOWN_CATEGORY
      - INVALID_CATEGORY
      - UNKNOWN_PARAMETER
      - UNEXPECTED_BOOLEAN_VALUE
      - NUMBER_FORMAT
      - INVALID_UNIT_ID
      - INVALID_GROUP_ID_LENGTH
      - INVALID_GROUP_ID_CHARACTERS
    OfferDefaultPriceDTO:
      description: Цена с указанием валюты, скидки и времени последнего обновления, а также минимальная цена для попадания
        в акцию «Бестселлеры Маркета».
      type: object
      properties:
        minimumForBestseller:
          description: 'Минимальная цена товара для попадания в акцию «Бестселлеры Маркета». Подробнее об этом способе участия
            читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/marketing/promos/market/bestsellers#minimum).


            Передается в методе [POST v2/businesses/{businessId}/offer-prices/updates](../../reference/prices/updateBusinessPrices.md).

            '
          type: number
          minimum: 0
          maximum: 100000000
          exclusiveMinimum: true
        excludedFromBestsellers:
          description: 'Признак того, что товар не попадает в акцию «Бестселлеры Маркета». Подробнее об акции читайте [в Справке
            Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/bestsellers).


            Если значение `true`, в методе [POST v2/businesses/{businessId}/offer-prices/updates](../../reference/prices/updateBusinessPrices.md)
            параметр `minimumForBestseller` игнорируется.

            '
          type: boolean
        value:
          description: Цена товара.
          type: number
          minimum: 0
          exclusiveMinimum: true
        currencyId:
          $ref: '#/components/schemas/CurrencyType'
          description: Валюта.
        discountBase:
          $ref: '#/components/schemas/DiscountBase'
        updatedAt:
          description: Время последнего обновления.
          type: string
          format: date-time
    OfferDefaultPriceListResponseDTO:
      description: Список цен на товары.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Список товаров.
          type: array
          items:
            $ref: '#/components/schemas/OfferDefaultPriceResponseDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    OfferDefaultPriceResponseDTO:
      description: Информация об установленной цене на товар.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        price:
          $ref: '#/components/schemas/OfferDefaultPriceDTO'
    OfferDescription:
      description: 'Подробное описание товара: например, его преимущества и особенности.


        Не давайте в описании инструкций по установке и сборке. Не используйте слова «скидка», «распродажа», «дешевый», «подарок»
        (кроме подарочных категорий), «бесплатно», «акция», «специальная цена», «новинка», «new», «аналог», «заказ», «хит».
        Не указывайте никакой контактной информации и не давайте ссылок.


        Для форматирования текста можно использовать теги HTML:


        * \<h>, \<h1>, \<h2> и так далее — для заголовков;

        * \<br> и \<p> — для переноса строки;

        * \<ol> — для нумерованного списка;

        * \<ul> — для маркированного списка;

        * \<li> — для создания элементов списка (должен находиться внутри \<ol> или \<ul>);

        * \<div> — поддерживается, но не влияет на отображение текста.


        Оптимальная длина — 400–600 символов.


        [Рекомендации и правила](https://yandex.ru/support/marketplace/assortment/fields/description.html)

        '
      type: string
      maxLength: 6000
    OfferErrorDTO:
      description: Сообщение об ошибке, связанной с размещением товара.
      type: object
      properties:
        message:
          description: Тип ошибки.
          type: string
        comment:
          description: Пояснение.
          type: string
    OfferForRecommendationDTO:
      description: Информация о состоянии цены на товар.
      type: object
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        price:
          $ref: '#/components/schemas/BasePriceDTO'
          description: Цена товара в каталоге.
        competitiveness:
          $ref: '#/components/schemas/PriceCompetitivenessType'
          description: Привлекательность цены на товар.
        shows:
          description: Количество показов карточки товара за последние 7 дней.
          type: integer
          format: int64
    OfferManualDTO:
      type: object
      required:
      - url
      description: 'Инструкция по использованию товара.

        '
      properties:
        url:
          $ref: '#/components/schemas/Url'
          description: Ссылка на инструкцию.
        title:
          description: 'Название инструкции, которое будет отображаться на карточке товара.

            '
          type: string
          maxLength: 500
    OfferMappingErrorDTO:
      description: Текст ошибки или предупреждения.
      type: object
      properties:
        type:
          $ref: '#/components/schemas/OfferMappingErrorType'
        parameterId:
          description: Идентификатор характеристики, с которой связана ошибка или предупреждение.
          type: integer
          format: int64
        message:
          description: Текст ошибки или предупреждения.
          type: string
      required:
      - type
      - message
    OfferMappingErrorType:
      description: 'Типы ошибок и предупреждений:


        * `UNKNOWN_CATEGORY` — указана неизвестная категория.

        * `INVALID_CATEGORY` — указана нелистовая категория. Укажите ту, которая не имеет дочерних категорий.

        * `EMPTY_MARKET_CATEGORY` — не указана категория Маркета при передаче характеристик категории.

        * `UNKNOWN_PARAMETER` — передана характеристика, которой нет среди характеристик категории.

        * `UNEXPECTED_BOOLEAN_VALUE` — вместо boolean-значения передано что-то другое.

        * `NUMBER_FORMAT` — передана строка, не обозначающая число, вместо числа.

        * `INVALID_UNIT_ID` — передана единица измерения, недопустимая для характеристики.

        * `INVALID_GROUP_ID_LENGTH` — в названии превышено допустимое значение символов — 255.

        * `INVALID_GROUP_ID_CHARACTERS` — переданы [недопустимые символы](*ascii-code).

        * `INVALID_PICKER_URL` — передана ссылка на изображение для миниатюры, которой нет в переданных ссылках на изображение
        товара.

        * `LOCKED_DIMENSIONS` — переданы габариты упаковки, которые нельзя изменить.

        * `INVALID_COMMODITY_CODE` — передан некорректный товарный код.


        Проверить, какие категорийные характеристики доступны для заданной категории, и получить их настройки можно с помощью
        запроса [POST v2/category/{categoryId}/parameters](../../reference/content/getCategoryContentParameters).

        '
      type: string
      enum:
      - UNKNOWN_CATEGORY
      - INVALID_CATEGORY
      - EMPTY_MARKET_CATEGORY
      - UNKNOWN_PARAMETER
      - UNEXPECTED_BOOLEAN_VALUE
      - NUMBER_FORMAT
      - INVALID_UNIT_ID
      - INVALID_GROUP_ID_LENGTH
      - INVALID_GROUP_ID_CHARACTERS
      - INVALID_PICKER_URL
      - LOCKED_DIMENSIONS
      - INVALID_COMMODITY_CODE
    OfferMediaFileDTO:
      type: object
      description: 'Информация о медиафайле товара.

        '
      properties:
        url:
          $ref: '#/components/schemas/Url'
          description: 'Ссылка на медиафайл.

            '
        title:
          description: 'Название медиафайла.

            '
          type: string
        uploadState:
          $ref: '#/components/schemas/MediaFileUploadStateType'
          description: 'Состояние загрузки медиафайла.

            '
    OfferMediaFilesDTO:
      description: 'Информация о медиафайлах товара.

        '
      type: object
      properties:
        firstVideoAsCover:
          deprecated: true
          description: 'Использовать первое видео в карточке как видеообложку.


            Передайте `true`, чтобы первое видео использовалось как видеообложка, или `false`, чтобы видеообложка не отображалась
            в карточке товара.

            '
          type: boolean
        videos:
          description: 'Видеофайлы товара.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferMediaFileDTO'
        pictures:
          description: 'Изображения товара.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferMediaFileDTO'
        manuals:
          description: 'Руководства по использованию товара.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferMediaFileDTO'
    OfferName:
      description: 'Составляйте название по схеме: тип + бренд или производитель + модель + особенности, если есть (например,
        цвет, размер или вес) и количество в упаковке.


        Не включайте в название условия продажи (например, «скидка», «бесплатная доставка» и т. д.), эмоциональные характеристики
        («хит», «супер» и т. д.). Не пишите слова большими буквами — кроме устоявшихся названий брендов и моделей.


        Оптимальная длина — 50–60 символов.


        [Рекомендации и правила](https://yandex.ru/support/marketplace/assortment/fields/title.html)

        '
      example: Ударная дрель Makita HP1630, 710 Вт
      type: string
      maxLength: 256
    OfferParamDTO:
      description: "Параметры товара.\n\nЕсли у товара несколько значений одного параметра, передайте их с одним и тем же\
        \ `name`, но разными `value`.\n\n{% cut \"Пример\" %}\n\n```json translate=no\n\"params\": [\n  {\n    \"name\": \"\
        Цвет для фильтра\",\n    \"value\": \"Зеленый\"\n  },\n  {\n    \"name\": \"Цвет для фильтра\",\n    \"value\": \"\
        Желтый\"\n  }\n]\n```\n\n{% endcut %}\n"
      type: object
      required:
      - name
      - value
      properties:
        name:
          description: 'Название характеристики.


            Должно совпадать с названием характеристики на Маркете. Узнать его можно из Excel-шаблона категории или через
            запрос [POST v2/category/{categoryId}/parameters](../../reference/content/getCategoryContentParameters.md).

            '
          example: Wi-Fi
          type: string
          maxLength: 200
        value:
          description: 'Значение.

            '
          example: есть
          type: string
    OfferPriceByOfferIdsListResponseDTO:
      description: Список цен.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Страница списка цен.
          type: array
          items:
            $ref: '#/components/schemas/OfferPriceByOfferIdsResponseDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    OfferPriceByOfferIdsResponseDTO:
      description: Информация об установленной цене.
      type: object
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        price:
          $ref: '#/components/schemas/PriceDTO'
        updatedAt:
          description: Дата и время последнего обновления цены.
          type: string
          format: date-time
    OfferPriceDTO:
      description: Товар с информацией о новой цене на него.
      type: object
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        price:
          $ref: '#/components/schemas/PriceDTO'
    OfferPriceListResponseDTO:
      description: Список цен на товары.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Страница списка.
          type: array
          items:
            $ref: '#/components/schemas/OfferPriceResponseDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        total:
          description: Количество всех цен магазина, измененных через API.
          type: integer
          format: int32
    OfferPriceResponseDTO:
      description: Информация об установленной цене на товар.
      type: object
      properties:
        id:
          description: Идентификатор предложения из прайс-листа.
          type: string
        price:
          $ref: '#/components/schemas/PriceDTO'
        marketSku:
          $ref: '#/components/schemas/MarketSku'
        updatedAt:
          description: Дата и время последнего обновления цены на товар.
          type: string
          format: date-time
    OfferRecommendationDTO:
      description: 'Информация о состоянии цен и рекомендации.

        '
      type: object
      properties:
        offer:
          $ref: '#/components/schemas/OfferForRecommendationDTO'
          description: Информация о состоянии цен.
        recommendation:
          $ref: '#/components/schemas/OfferRecommendationInfoDTO'
          description: Рекомендации.
    OfferRecommendationInfoDTO:
      description: Рекомендации, касающиеся цены на товар.
      type: object
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        competitivenessThresholds:
          $ref: '#/components/schemas/PriceCompetitivenessThresholdsDTO'
          description: Максимальные значения цены, при которых она является привлекательной или умеренной.
    OfferRecommendationsResultDTO:
      description: Список товаров с рекомендациями.
      type: object
      required:
      - offerRecommendations
      properties:
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        offerRecommendations:
          description: Страница списка товаров.
          type: array
          items:
            $ref: '#/components/schemas/OfferRecommendationDTO'
    OfferSellingProgramDTO:
      description: Информация о том, по каким моделям можно продавать товар, а по каким нельзя.
      type: object
      required:
      - sellingProgram
      - status
      properties:
        sellingProgram:
          $ref: '#/components/schemas/SellingProgramType'
          description: 'Модель работы.

            '
        status:
          $ref: '#/components/schemas/OfferSellingProgramStatusType'
          description: 'Информация о том, можно ли по этой модели продавать товар.

            '
    OfferSellingProgramStatusType:
      description: 'Информация о доступности или недоступности.


        * `FINE` — доступно.

        * `REJECT` — недоступно.

        '
      type: string
      enum:
      - FINE
      - REJECT
    OfferType:
      description: 'Особый тип товара:


        * `DEFAULT` — товары, для которых вы передавали особый тип ранее и хотите убрать его.

        * `MEDICINE` — лекарства.

        * `BOOK` — бумажные и электронные книги.

        * `AUDIOBOOK` — аудиокниги.

        * `ARTIST_TITLE` — музыкальная и видеопродукция.

        * `ON_DEMAND` — товары на заказ.

        * `ALCOHOL` — алкоголь.


        {% note info "Если ваш товар — книга" %}


        Укажите год издания в характеристиках товара. [Подробнее о параметре](../../reference/business-offer-mappings/updateOfferMappings.md#offerparamdto)


        {% endnote %}

        '
      type: string
      enum:
      - DEFAULT
      - MEDICINE
      - BOOK
      - AUDIOBOOK
      - ARTIST_TITLE
      - ON_DEMAND
      - ALCOHOL
    OfferVendor:
      description: Название бренда или производителя. Должно быть записано так, как его пишет сам бренд.
      example: LEVENHUK
      type: string
    OfferVendorCode:
      description: Артикул товара от производителя.
      example: VNDR-0005A
      type: string
    OfferWeightDimensionsDTO:
      description: 'Габариты упаковки и вес товара.


        Если товар занимает несколько коробок, перед измерением размеров сложите их компактно.


        ![Схема измерения многоместных грузов](../../_images/reference/boxes-measure.png)

        '
      type: object
      required:
      - length
      - width
      - height
      - weight
      properties:
        length:
          description: 'Длина упаковки в см.

            '
          example: 65.55
          type: number
          minimum: 0
        width:
          description: 'Ширина упаковки в см.

            '
          example: 50.7
          type: number
          minimum: 0
        height:
          description: 'Высота упаковки в см.

            '
          example: 20
          type: number
          minimum: 0
        weight:
          description: 'Вес товара в кг с учетом упаковки (брутто).

            '
          example: 1.001
          type: number
          minimum: 0
    OperationDTO:
      type: object
      description: Информация об операции.
      required:
      - id
      - type
      properties:
        id:
          $ref: '#/components/schemas/OperationId'
        type:
          $ref: '#/components/schemas/OperationType'
    OperationId:
      description: Идентификатор операции.
      type: string
      minLength: 1
      maxLength: 1000
    OperationResultDTO:
      type: object
      description: Результат выполнения.
      required:
      - id
      - type
      - status
      properties:
        id:
          $ref: '#/components/schemas/OperationId'
        type:
          $ref: '#/components/schemas/OperationType'
        status:
          $ref: '#/components/schemas/OperationStatusType'
    OperationStatusType:
      description: 'Статус выполнения операции:


        * `IN_PROGRESS` — выполняется.


        * `DONE` — успешно завершена.


        * `FAILED` — завершена с ошибкой.

        '
      type: string
      enum:
      - IN_PROGRESS
      - DONE
      - FAILED
    OperationType:
      description: 'Тип операции:


        * `ORDER_RECIPIENT_UPDATE` — изменение данных получателя.


        * `ORDER_DELIVERY_INTERVAL_UPDATE` — изменение интервала дат доставки.


        * `ORDER_STATUS_UPDATE` — обновление статуса заказа для его отмены.


        * `RETURN_CANCELLATION` — отмена возврата.

        '
      type: string
      enum:
      - ORDER_RECIPIENT_UPDATE
      - ORDER_DELIVERY_INTERVAL_UPDATE
      - ORDER_STATUS_UPDATE
      - RETURN_CANCELLATION
    OptionValuesLimitedDTO:
      description: Значение ограничивающей характеристики и список допустимых значений ограничиваемой характеристики.
      type: object
      required:
      - limitingOptionValueId
      - optionValueIds
      properties:
        limitingOptionValueId:
          description: Идентификатор значения ограничивающей характеристики.
          type: integer
          format: int64
        optionValueIds:
          description: 'Идентификаторы допустимых значений ограничиваемой характеристики.

            '
          type: array
          uniqueItems: true
          items:
            type: integer
            format: int64
            minimum: 1
    OrderBoxLayoutDTO:
      description: Информация о коробке.
      type: object
      required:
      - items
      properties:
        items:
          description: 'Список товаров в коробке.


            Если в коробке едет часть большого товара, в списке может быть только один пункт.

            '
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderBoxLayoutItemDTO'
    OrderBoxLayoutItemDTO:
      description: Информация о товаре в коробке.
      type: object
      required:
      - id
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Он приходит в ответе метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md)
            — параметр `id` в `items`.

            '
          type: integer
          format: int64
        fullCount:
          description: 'Количество единиц товара в коробке.


            Используйте это поле, если в коробке поедут целые товары, не разделенные на части. Не используйте это поле одновременно
            с `partialCount`.

            '
          type: integer
          format: int32
          minimum: 1
        partialCount:
          $ref: '#/components/schemas/OrderBoxLayoutPartialCountDTO'
          description: 'Информация о части товара в коробке.


            Используйте это поле, если в коробке поедет часть большого товара. Не используйте это поле одновременно с `fullCount`.

            '
        instances:
          description: Переданные коды маркировки.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/BriefOrderItemInstanceDTO'
    OrderBoxLayoutPartialCountDTO:
      description: Информация о части товара в коробке.
      type: object
      required:
      - current
      - total
      properties:
        current:
          description: Номер части, начиная с 1.
          type: integer
          format: int32
          minimum: 1
        total:
          description: На сколько всего частей разделен товар.
          type: integer
          format: int32
          minimum: 2
    OrderBoxesLayoutDTO:
      description: Распределение товаров по коробкам.
      type: object
      required:
      - boxes
      properties:
        boxes:
          description: Список коробок.
          type: array
          items:
            $ref: '#/components/schemas/EnrichedOrderBoxLayoutDTO'
    OrderBusinessBuyerDTO:
      description: 'Информация о покупателе.

        '
      type: object
      properties:
        inn:
          description: ИНН.
          type: string
        kpp:
          description: КПП.
          type: string
        organizationName:
          description: Наименование юридического лица.
          type: string
        organizationJurAddress:
          description: Юридический адрес.
          type: string
    OrderBusinessDocumentsDTO:
      description: 'Информация о документах.

        '
      type: object
      properties:
        upd:
          $ref: '#/components/schemas/DocumentDTO'
          description: Информация об УПД-1 или УПД-2.
        ukd:
          $ref: '#/components/schemas/DocumentDTO'
          description: Информация об УКД-1 или УКД-2.
        torgTwelve:
          $ref: '#/components/schemas/DocumentDTO'
          description: Информация о товарной накладной.
        sf:
          $ref: '#/components/schemas/DocumentDTO'
          description: Информация о счете-фактуре.
        ksf:
          $ref: '#/components/schemas/DocumentDTO'
          description: Информация о корректировочном счете-фактуре.
    OrderBuyerBasicInfoDTO:
      description: Информация о покупателе с базовыми полями.
      type: object
      required:
      - type
      properties:
        id:
          description: Идентификатор покупателя.
          type: string
        lastName:
          description: Фамилия.
          type: string
        firstName:
          description: Имя.
          type: string
        middleName:
          description: Отчество.
          type: string
        type:
          $ref: '#/components/schemas/OrderBuyerType'
          description: 'Тип покупателя: физическое лицо или организация.


            Только для FBS- и FBY-магазинов, которые размещают товары на витрине [business.market.yandex.ru](https://business.market.yandex.ru).

            '
    OrderBuyerDTO:
      description: 'Информация о покупателе.


        Параметры `id`, `lastName`, `firstName` и `middleName` возвращаются, только если вы работаете по модели DBS.

        '
      type: object
      allOf:
      - $ref: '#/components/schemas/OrderBuyerBasicInfoDTO'
      - {}
    OrderBuyerInfoDTO:
      description: Информация о покупателе и его номере телефона.
      type: object
      allOf:
      - $ref: '#/components/schemas/OrderBuyerBasicInfoDTO'
      - properties:
          phone:
            description: 'Подменный номер телефона покупателя. Подробнее о таких номерах читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/orders/dbs/call#fake-number).


              Формат номера: `+<код_страны><код_региона><номер_телефона>`.

              '
            type: string
          trusted:
            description: 'Проверенный покупатель.


              Если параметр `trusted` вернулся со значением `true`, Маркет уже проверил покупателя — не звоните ему. Обработайте
              заказ как обычно и передайте его курьеру или отвезите в ПВЗ.


              При необходимости свяжитесь с покупателем в чате. [Как это сделать](../../step-by-step/chats.md)


              Подробнее о звонках покупателю читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/orders/dbs/call).

              '
            type: boolean
    OrderBuyerType:
      description: 'Тип покупателя:


        * `PERSON` — физическое лицо.


        * `BUSINESS` — организация.

        '
      type: string
      enum:
      - PERSON
      - BUSINESS
    OrderCancellationReasonType:
      description: 'Варианты причин, по которым заказ не может быть отменен.


        * `ORDER_DELIVERED` — заказ уже доставлен.

        * `ORDER_IN_DELIVERY` — заказ уже у курьера.

        '
      type: string
      enum:
      - ORDER_DELIVERED
      - ORDER_IN_DELIVERY
    OrderCourierDTO:
      description: Информация о курьере.
      type: object
      properties:
        fullName:
          description: Полное имя.
          type: string
        phone:
          description: Номер телефона.
          type: string
        phoneExtension:
          description: Добавочный номер телефона.
          type: string
        vehicleNumber:
          description: Номер транспортного средства.
          type: string
        vehicleDescription:
          description: Описание транспортного средства. Например, модель и цвет.
          type: string
    OrderCourierDeliveryDTO:
      type: object
      description: 'Информация о курьерской доставке.


        Не передавайте вместе с `pickupDelivery`.

        '
      required:
      - address
      properties:
        address:
          $ref: '#/components/schemas/CourierDeliveryAddressDTO'
        notes:
          type: string
          description: Комментарий к заказу.
          minLength: 1
          maxLength: 3000
    OrderDTO:
      description: Заказ.
      type: object
      required:
      - id
      - status
      - substatus
      - creationDate
      - currency
      - itemsTotal
      - deliveryTotal
      - buyerItemsTotalBeforeDiscount
      - paymentType
      - paymentMethod
      - fake
      - items
      - delivery
      - buyer
      - taxSystem
      properties:
        id:
          description: Идентификатор заказа.
          type: integer
          format: int64
        externalOrderId:
          $ref: '#/components/schemas/ExternalOrderId'
        status:
          $ref: '#/components/schemas/OrderStatusType'
        substatus:
          $ref: '#/components/schemas/OrderSubstatusType'
        creationDate:
          $ref: '#/components/schemas/DateDdMmYyyyHhMmSs'
          description: 'Дата и время оформления заказа.


            Формат даты и времени: `ДД-ММ-ГГГГ ЧЧ:ММ:СС`. Часовой пояс — UTC+03:00 (Москва).

            '
        updatedAt:
          $ref: '#/components/schemas/DateDdMmYyyyHhMmSs'
          description: 'Дата и время последнего обновления заказа.


            Формат даты и времени: `ДД-ММ-ГГГГ ЧЧ:ММ:СС`. Часовой пояс — UTC+03:00 (Москва).

            '
        currency:
          $ref: '#/components/schemas/CurrencyType'
          description: 'Валюта, в которой указаны цены на товары в заказе.

            '
        itemsTotal:
          description: 'Платеж покупателя.

            '
          type: number
        deliveryTotal:
          description: 'Стоимость доставки.

            '
          type: number
        buyerItemsTotal:
          deprecated: true
          description: Стоимость всех товаров в заказе в валюте покупателя после применения скидок и без учета стоимости доставки.
          type: number
        buyerTotal:
          deprecated: true
          description: Стоимость всех товаров в заказе в валюте покупателя после применения скидок и с учетом стоимости доставки.
          type: number
        buyerItemsTotalBeforeDiscount:
          description: 'Стоимость всех товаров в заказе в валюте покупателя без учета стоимости доставки и до применения скидок
            по:


            * акциям;

            * купонам;

            * промокодам.

            '
          type: number
        buyerTotalBeforeDiscount:
          deprecated: true
          description: Стоимость всех товаров в заказе в валюте покупателя до применения скидок и с учетом стоимости доставки
            (`buyerItemsTotalBeforeDiscount` + стоимость доставки).
          type: number
        paymentType:
          $ref: '#/components/schemas/OrderPaymentType'
        paymentMethod:
          $ref: '#/components/schemas/OrderPaymentMethodType'
        fake:
          description: 'Тип заказа:


            * `false` — настоящий заказ покупателя.


            * `true` — [тестовый заказ](../../concepts/sandbox.md) Маркета.

            '
          type: boolean
        items:
          description: Список товаров в заказе.
          type: array
          items:
            $ref: '#/components/schemas/OrderItemDTO'
        subsidies:
          description: Список субсидий по типам.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderSubsidyDTO'
        delivery:
          $ref: '#/components/schemas/OrderDeliveryDTO'
        buyer:
          $ref: '#/components/schemas/OrderBuyerDTO'
        notes:
          description: Комментарий к заказу.
          type: string
        taxSystem:
          $ref: '#/components/schemas/OrderTaxSystemType'
        cancelRequested:
          description: '**Только для модели DBS**


            Запрошена ли отмена.

            '
          type: boolean
        expiryDate:
          $ref: '#/components/schemas/DateDdMmYyyyHhMmSs'
          description: 'Дата, после которой заказ будет отменен, если не сменит статус.


            Формат даты: `ДД-ММ-ГГГГ`.

            '
    OrderDatesFilterDTO:
      type: object
      description: Фильтр по датам заказов.
      properties:
        creationDateFrom:
          description: 'Начальная дата для фильтрации заказов по дате оформления.


            Формат даты: `ГГГГ-ММ-ДД`.


            Между начальной и конечной датой (параметр `creationDateTo`) должно быть не больше 30 дней.


            Значение по умолчанию: 30 дней назад от текущей даты.


            Начальная дата включается в интервал для фильтрации.

            '
          type: string
          format: date
        creationDateTo:
          description: 'Конечная дата для фильтрации заказов по дате оформления.


            Формат даты: `ГГГГ-ММ-ДД`.


            Между начальной (параметр `creationDateFrom`) и конечной датой должно быть не больше 30 дней.


            Значение по умолчанию: текущая дата.


            Если промежуток времени между `creationDateTo` и `creationDateFrom` меньше суток, то `creationDateTo` равен `creationDateFrom`
            + сутки.


            Конечная дата не включается в интервал для фильтрации.

            '
          type: string
          format: date
        shipmentDateFrom:
          description: 'Начальная дата для фильтрации заказов по дате отгрузки в службу доставки (параметр `shipmentDate`).


            Формат даты: `ГГГГ-ММ-ДД`.


            Между начальной и конечной датой (параметр `shipmentDateTo`) должно быть не больше 30 дней.


            Начальная дата включается в интервал для фильтрации.

            '
          type: string
          format: date
        shipmentDateTo:
          description: 'Конечная дата для фильтрации заказов по дате отгрузки в службу доставки (параметр `shipmentDate`).


            Формат даты: `ГГГГ-ММ-ДД`.


            Между начальной (параметр `shipmentDateFrom`) и конечной датой должно быть не больше 30 дней.


            Если промежуток времени между `shipmentDateTo` и `shipmentDateFrom` меньше суток, то `shipmentDateTo` равен `shipmentDateFrom`
            + сутки.


            Конечная дата не включается в интервал для фильтрации.

            '
          type: string
          format: date
        updateDateFrom:
          description: Начальная дата обновления заказа (ISO 8601).
          type: string
          format: date-time
        updateDateTo:
          description: Конечная дата обновления заказа (ISO 8601).
          type: string
          format: date-time
    OrderDeliveryAddressDTO:
      description: 'Адрес доставки.


        Указывается, если параметр `type` принимает значение `DELIVERY`, `POST` или `PICKUP` (только для модели DBS). Если
        `type=PICKUP`, возвращается адрес пункта выдачи.

        '
      type: object
      properties:
        country:
          description: 'Страна.

            '
          type: string
        postcode:
          description: 'Почтовый индекс.


            Указывается, если выбрана доставка почтой (`delivery type=POST`).

            '
          type: string
        city:
          description: 'Город или населенный пункт.

            '
          type: string
        district:
          description: Район.
          type: string
        subway:
          description: Станция метро.
          type: string
        street:
          description: 'Улица.

            '
          type: string
        house:
          description: 'Номер дома.

            '
          type: string
        estate:
          description: 'Номер владения.

            '
          type: string
        block:
          description: Корпус.
          type: string
        building:
          description: Строение.
          type: string
        entrance:
          description: Номер подъезда.
          type: string
        entryphone:
          description: Код домофона.
          type: string
        floor:
          description: Этаж.
          type: string
        apartment:
          description: Номер квартиры или офиса.
          type: string
        phone:
          description: 'Телефон получателя заказа.

            '
          type: string
        recipient:
          description: 'Фамилия, имя и отчество получателя заказа.

            '
          type: string
        gps:
          $ref: '#/components/schemas/GpsDTO'
          description: GPS-координаты.
    OrderDeliveryDTO:
      description: Информация о доставке.
      type: object
      required:
      - type
      - serviceName
      - deliveryPartnerType
      - dates
      - deliveryServiceId
      properties:
        id:
          description: 'Идентификатор доставки, присвоенный магазином.


            Указывается, только если магазин передал данный идентификатор в ответе на запрос методом `POST cart`.

            '
          deprecated: true
          type: string
        type:
          $ref: '#/components/schemas/OrderDeliveryType'
          description: 'Способ доставки заказа.

            '
        serviceName:
          description: Название службы доставки.
          type: string
        price:
          deprecated: true
          description: '{% note warning "Стоимость доставки смотрите в параметре `deliveryTotal`." %}


             


            {% endnote %}


            Стоимость доставки в валюте заказа.

            '
          type: number
        deliveryPartnerType:
          $ref: '#/components/schemas/OrderDeliveryPartnerType'
          description: Тип сотрудничества со службой доставки в рамках указанного заказа.
        courier:
          $ref: '#/components/schemas/OrderCourierDTO'
        dates:
          $ref: '#/components/schemas/OrderDeliveryDatesDTO'
        region:
          $ref: '#/components/schemas/RegionDTO'
        address:
          $ref: '#/components/schemas/OrderDeliveryAddressDTO'
        vat:
          $ref: '#/components/schemas/OrderVatType'
        deliveryServiceId:
          description: Идентификатор службы доставки.
          type: integer
          format: int64
        liftType:
          $ref: '#/components/schemas/OrderLiftType'
        liftPrice:
          description: Стоимость подъема на этаж.
          type: number
        outletCode:
          description: Идентификатор пункта выдачи, присвоенный магазином.
          type: string
        outletStorageLimitDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: 'Дата, до которой заказ будет храниться в пункте выдачи. Возвращается, когда заказ переходит в статус
            `PICKUP`.


            Один раз дату можно поменять с помощью метода [PUT v2/campaigns/{campaignId}/orders/{orderId}/delivery/storage-limit](../../reference/order-delivery/updateOrderStorageLimit.md).

            '
        dispatchType:
          $ref: '#/components/schemas/OrderDeliveryDispatchType'
        tracks:
          description: Информация для отслеживания посылки.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderTrackDTO'
        shipments:
          description: Информация о посылках.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderShipmentDTO'
        estimated:
          description: Приблизительная ли дата доставки.
          type: boolean
        eacType:
          $ref: '#/components/schemas/OrderDeliveryEacType'
          description: Тип кода подтверждения ЭАПП.
        eacCode:
          description: 'Код подтверждения ЭАПП (для типа `MERCHANT_TO_COURIER`).

            '
          type: string
        receiveCode:
          description: '**Только для модели LaaS**


            Код получения заказа на ПВЗ.

            '
          type: string
    OrderDeliveryDateDTO:
      description: Информация о новой дате доставки заказа.
      type: object
      required:
      - toDate
      properties:
        toDate:
          description: 'Новая дата доставки заказа.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    OrderDeliveryDateReasonType:
      description: "Причина переноса доставки заказа. Возможные причины изменения даты:\n  - ```USER_MOVED_DELIVERY_DATES```\
        \ — покупатель попросил изменить дату или вы договорились привезти ему заказ раньше изначальной даты. Кроме этого\
        \ указывается для подтверждения даты доставки товаров на заказ с долгой (31-60 дней) доставкой.\n  - ```PARTNER_MOVED_DELIVERY_DATES```\
        \ — магазин не может доставить заказ в срок.\n"
      type: string
      enum:
      - USER_MOVED_DELIVERY_DATES
      - PARTNER_MOVED_DELIVERY_DATES
    OrderDeliveryDatesDTO:
      description: Диапазон дат доставки.
      type: object
      required:
      - fromDate
      properties:
        fromDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: 'Ближайшая дата доставки.


            Формат даты: `ДД-ММ-ГГГГ`.

            '
        toDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: 'Самая поздняя дата доставки.


            Если `toDate` не указан, считается дата в параметре `fromDate`.


            Формат даты: `ДД-ММ-ГГГГ`.

            '
        fromTime:
          description: 'Начало интервала времени доставки.


            Передается только вместе с параметром `type=DELIVERY`.


            Формат времени: 24-часовой, `ЧЧ:ММ`. Вместо `ММ` всегда указывайте `00` (исключение — `23:59`).


            Минимальное значение: `00:00`.

            '
          type: string
          format: time
        toTime:
          description: "Конец интервала времени доставки.\n\nПередается только вместе с параметром `type=DELIVERY`.\n\n Формат\
            \ времени: 24-часовой, `ЧЧ:ММ`. Вместо `ММ` всегда указывайте `00` (исключение — `23:59`).\n\nМаксимальное значение:\
            \ `23:59`.\n"
          type: string
          format: time
        realDeliveryDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: Дата, когда товар доставлен до пункта выдачи (в случае самовывоза) или до покупателя (если заказ доставляет
            курьер).
    OrderDeliveryDispatchType:
      description: 'Способ доставки:


        * `BUYER` — курьерская доставка покупателю.


        * `MARKET_BRANDED_OUTLET` — доставка в пункт выдачи заказов Маркета.


        * `SHOP_OUTLET` — доставка в пункт выдачи заказов магазина.


        * `UNKNOWN` — неизвестный тип.

        '
      type: string
      enum:
      - UNKNOWN
      - BUYER
      - MARKET_BRANDED_OUTLET
      - SHOP_OUTLET
    OrderDeliveryEacType:
      description: 'Тип кода подтверждения ЭАПП:


        * `MERCHANT_TO_COURIER` (временно не возвращается) — продавец передает код курьеру для получения невыкупа.


        * `COURIER_TO_MERCHANT` — курьер передает код продавцу для получения заказа.


        * `CHECKING_BY_MERCHANT` — продавец проверяет код на своей стороне.

        '
      type: string
      enum:
      - MERCHANT_TO_COURIER
      - COURIER_TO_MERCHANT
      - CHECKING_BY_MERCHANT
    OrderDeliveryPartnerType:
      description: 'Тип сотрудничества со службой доставки в рамках конкретного заказа:


        * `SHOP` — магазин работает со службой доставки напрямую или доставляет заказы самостоятельно.


        * `YANDEX_MARKET` — магазин работает со службой доставки через Маркет.


        * `UNKNOWN` — неизвестный тип.

        '
      type: string
      enum:
      - SHOP
      - YANDEX_MARKET
      - UNKNOWN
    OrderDeliveryType:
      description: 'Способ доставки заказа:


        * `DELIVERY` — курьерская доставка.


        * `PICKUP` — самовывоз.


        * `POST` — почта.


        * `DIGITAL` — для цифровых товаров.


        * `UNKNOWN` — неизвестный тип.

        '
      type: string
      enum:
      - DELIVERY
      - PICKUP
      - POST
      - DIGITAL
      - UNKNOWN
    OrderDigitalItemDTO:
      description: Цифровой товар.
      type: object
      required:
      - id
      - slip
      - activate_till
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Он приходит в ответе метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md)
            — параметр `id` в `items`.

            '
          type: integer
          format: int64
        codes:
          description: 'Ключи, относящиеся к товару.


            Поле обязательно для заполнения.

            '
          type: array
          items:
            type: string
            maxLength: 256
          minItems: 1
          maxItems: 5000
          uniqueItems: true
          nullable: true
        slip:
          description: 'Инструкция по активации.


            Для форматирования текста можно использовать теги HTML:


            * \<h>, \<h1>, \<h2> и так далее — для заголовков;

            * \<br> и \<p> — для переноса строки;

            * \<ol> — для нумерованного списка;

            * \<ul> — для маркированного списка;

            * \<li> — для создания элементов списка (должен находиться внутри \<ol> или \<ul>);

            * \<div> — поддерживается, но не влияет на отображение текста.

            '
          type: string
          maxLength: 10000
        activate_till:
          description: 'Дата, до которой нужно активировать ключи. Если ключи действуют бессрочно, укажите любую дату в отдаленном
            будущем.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    OrderDocumentStatusType:
      description: 'Статус документа:


        * `READY` — готов.

        * `NOT_READY` — не готов.

        '
      type: string
      enum:
      - READY
      - NOT_READY
    OrderItemDTO:
      description: Список товаров в заказе.
      type: object
      required:
      - id
      - offerId
      - offerName
      - price
      - buyerPrice
      - buyerPriceBeforeDiscount
      - count
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Позволяет идентифицировать товар в рамках заказа.

            '
          type: integer
          format: int64
        offerId:
          $ref: '#/components/schemas/ShopSku'
          description: Идентификатор вашего товарного предложения для определенного товара. [Описание поля в Справке Маркета
            для продавцов](https://yandex.ru/support/marketplace/assortment/fields/index.html#sku)
        offerName:
          description: Название товара.
          type: string
        price:
          description: 'Цена товара в валюте заказа без учета вознаграждения продавцу за скидки по промокодам, купонам и акциям
            (параметр `subsidies`).


            Включает НДС.

            '
          type: number
        buyerPrice:
          description: 'Цена товара в валюте покупателя. В цене уже учтены скидки по:


            * акциям;

            * купонам;

            * промокодам.

            '
          type: number
        buyerPriceBeforeDiscount:
          description: 'Стоимость товара в валюте покупателя до применения скидок по:


            * акциям;

            * купонам;

            * промокодам.


            Это зачеркнутая цена, которая отображается покупателю на карточке товара до применения скидок.

            '
          type: number
        priceBeforeDiscount:
          deprecated: true
          description: Стоимость товара в валюте магазина до применения скидок.
          type: number
        count:
          description: Количество единиц товара.
          type: integer
        vat:
          $ref: '#/components/schemas/OrderVatType'
          description: НДС на товар.
        shopSku:
          $ref: '#/components/schemas/ShopSku'
          deprecated: true
          description: '{% note warning "Вместо него используйте `offerId`." %}


             


            {% endnote %}


            Ваш SKU — идентификатор товара в вашей системе.

            '
        subsidy:
          deprecated: true
          description: '{% note warning "Вместо него используйте `subsidies`." %}


             


            {% endnote %}


            Общее вознаграждение продавцу за DBS-доставку и все скидки на товар:


            * по промокодам;

            * по купонам;

            * по баллам Плюса;

            * по акциям.

            '
          type: number
        partnerWarehouseId:
          description: '**Только для моделей FBY и LaaS**


            Идентификатор склада, на который сформирован заказ.

            '
          type: string
        promos:
          description: Информация о вознаграждении продавцу за скидки на товар по промокодам, купонам и акциям.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemPromoDTO'
        instances:
          description: 'Информация о маркировке единиц товара.


            Возвращаются данные для маркировки, переданные в запросе:


            * Для DBS — [PUT v2/campaigns/{campaignId}/orders/{orderId}/identifiers](../../reference/orders/provideOrderItemIdentifiers.md)
            или [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md).

            * Для FBS и EXPRESS — [PUT v2/campaigns/{campaignId}/orders/{orderId}/boxes](../../reference/orders/setOrderBoxLayout.md).


            Для FBY возвращаются коды маркировки, переданные во время поставки.


            Если магазин еще не передавал коды для этого заказа, `instances` отсутствует.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemInstanceDTO'
        details:
          deprecated: true
          description: '{% note warning "Для получения информации о невыкупах и возвратах используйте [GET v2/campaigns/{campaignId}/returns](../../reference/returns/getReturns.md)."
            %}


             


            {% endnote %}


            Информация о невыкупленных или возвращенных товарах в заказе.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemDetailDTO'
        subsidies:
          description: Список субсидий по типам.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemSubsidyDTO'
        requiredInstanceTypes:
          description: Список необходимых маркировок товара.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OrderItemInstanceType'
        tags:
          description: Признаки товара.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/OrderItemTagType'
    OrderItemDetailDTO:
      description: Детали по товару в заказе.
      type: object
      required:
      - itemCount
      - itemStatus
      - updateDate
      properties:
        itemCount:
          description: Количество единиц товара.
          type: integer
          format: int64
        itemStatus:
          $ref: '#/components/schemas/OrderItemStatusType'
        updateDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
    OrderItemInstanceDTO:
      description: 'Переданные для данной позиции коды маркировки или УИНы. Коды «Честного знака» возвращаются в двух вариантах
        — с криптохвостом и без.

        '
      type: object
      properties:
        cis:
          description: Код идентификации единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/) без криптохвоста
            или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов Market Yandex Go).
          type: string
        cisFull:
          description: Код идентификации единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/) с криптохвостом.
          type: string
        uin:
          description: 'УИН ювелирного изделия (16-значный код)

            Производитель получает УИН, когда регистрирует изделие в системе контроля за оборотом драгоценных металлов и камней
            — ГИИС ДМДК.

            '
          type: string
        rnpt:
          description: 'Регистрационный номер партии товара.


            Представляет собой строку из четырех чисел, разделенных косой чертой: ХХХХХХХХ/ХХХХХХ/ХХХХХХХ/ХХХ.


            Первая часть — код таможни, которая зарегистрировала декларацию на партию товара. Далее — дата, номер декларации
            и номер маркированного товара в декларации.

            '
          type: string
        gtd:
          description: 'Грузовая таможенная декларация.


            Представляет собой строку из трех чисел, разделенных косой чертой: ХХХХХХХХ/ХХХХХХ/ХХХХХХХ.


            Первая часть — код таможни, которая зарегистрировала декларацию на ввезенные товары. Далее — дата и номер декларации.

            '
          type: string
        countryCode:
          $ref: '#/components/schemas/CountryCode'
    OrderItemInstanceModificationDTO:
      description: Позиция в корзине, требующая маркировки.
      type: object
      required:
      - id
      - instances
      properties:
        id:
          description: 'Идентификатор товара в заказе.


            Он приходит в ответе метода [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md)
            — параметр `id` в `items`.

            '
          type: integer
          format: int64
        instances:
          description: 'Список кодов маркировки единиц товара.

            '
          type: array
          items:
            $ref: '#/components/schemas/BriefOrderItemInstanceDTO'
    OrderItemInstanceType:
      description: 'Вид маркировки товара:


        * `CIS` — КИЗ, идентификатор единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz)
        (для продавцов Market Yandex Go). Обязателен для заполнения.


        * `CIS_OPTIONAL` — КИЗ, идентификатор единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/). Необязателен
        для заполнения, но в ближайшее время потребуется его передача.


        * `UIN` — УИН, уникальный идентификационный номер.


        * `RNPT` — РНПТ, регистрационный номер партии товара.


        * `GTD` — номер ГТД, грузовой таможенной декларации.

        '
      type: string
      enum:
      - CIS
      - CIS_OPTIONAL
      - UIN
      - RNPT
      - GTD
    OrderItemModificationDTO:
      description: 'Список товаров в заказе.


        Если магазин не передал информацию о товаре во входных данных, он будет удален из заказа.


        Обязательный параметр.

        '
      type: object
      properties:
        id:
          description: 'Идентификатор товара в рамках заказа.


            Получить идентификатор можно с помощью метода:


            * [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md).


            Обязательный параметр.

            '
          type: integer
          format: int64
        count:
          description: Новое количество товара.
          type: integer
          format: int32
          minimum: 0
        instances:
          description: 'Информация о маркировке единиц товара.


            Передавайте в запросе все единицы товара, который подлежит маркировке.


            Обязательный параметр, если в заказе от бизнеса есть товары, подлежащие маркировке в системе [«Честный ЗНАК»](https://честныйзнак.рф/)
            или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов :no-translate[Market Yandex Go]).

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/BriefOrderItemInstanceDTO'
      required:
      - id
      - count
    OrderItemPromoDTO:
      description: Информация о вознаграждении продавцу за скидки на товар по промокодам, купонам и акциям.
      type: object
      required:
      - type
      - subsidy
      properties:
        type:
          $ref: '#/components/schemas/OrderPromoType'
          description: 'Тип скидки.

            '
        discount:
          description: 'Размер пользовательской скидки в валюте покупателя.

            '
          type: number
        subsidy:
          description: 'Вознаграждение продавцу от Маркета за товар, проданный в рамках акции.

            '
          type: number
        shopPromoId:
          description: 'Идентификатор акции поставщика.

            '
          type: string
        marketPromoId:
          description: 'Идентификатор акции в рамках соглашения на оказание услуг по продвижению сервиса между Маркетом и
            продавцом.

            '
          type: string
    OrderItemStatusType:
      description: 'Невыкупленный или возвращенный товар:


        * `REJECTED` — невыкупленный.


        * `RETURNED` — возвращенный.

        '
      type: string
      enum:
      - REJECTED
      - RETURNED
    OrderItemSubsidyDTO:
      description: 'Общее вознаграждение продавцу за все скидки на товар:


        * по промокодам, купонам и акциям;

        * по баллам Плюса.


        Включает НДС.

        '
      type: object
      required:
      - type
      - amount
      properties:
        type:
          $ref: '#/components/schemas/OrderItemSubsidyType'
        amount:
          description: Сумма субсидии.
          type: number
    OrderItemSubsidyType:
      description: 'Тип субсидии:


        * `YANDEX_CASHBACK` — скидка по подписке Яндекс Плюс.


        * `SUBSIDY` — скидка Маркета (по акциям, промокодам, купонам и т. д.).

        '
      type: string
      enum:
      - YANDEX_CASHBACK
      - SUBSIDY
    OrderItemTagType:
      description: 'Признак товара:


        * `ULTIMA` — премиум-товар.

        * `SAFE_TAG` — товар с [защитной меткой](*safe-tag).

        '
      type: string
      enum:
      - ULTIMA
      - SAFE_TAG
    OrderItemUnitStatusDTO:
      description: Количество единиц товара с определенным статусом.
      type: object
      required:
      - status
      - count
      properties:
        status:
          $ref: '#/components/schemas/OrderItemUnitStatusType'
        count:
          description: Количество единиц товара.
          type: integer
          format: int64
    OrderItemUnitStatusType:
      description: 'Статус единицы товара в заказе.


        * `CREATED` — создана.


        * `SHIPPED` — передана в доставку.


        * `CANCELLED` — отменена или удалена из заказа.


        * `DELIVERED_TO_BUYER` — передана покупателю.


        * `LOST` — утеряна.


        * `REJECTED` — невыкупленная.


        * `RETURNED` — возвращенная.

        '
      type: string
      enum:
      - CREATED
      - SHIPPED
      - CANCELLED
      - DELIVERED_TO_BUYER
      - LOST
      - REJECTED
      - RETURNED
    OrderItemValidationStatusDTO:
      description: Идентификаторы товаров и информация по проверке их кодов.
      type: object
      required:
      - id
      properties:
        id:
          description: Идентификатор товара в заказе.
          type: integer
          format: int64
        uin:
          description: Информация по проверке :no-translate[УИНов].
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/UinDTO'
        cis:
          description: Информация по проверке кодов маркировки в системе :no-translate[«Честный ЗНАК»].
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/CisDTO'
    OrderItemsModificationRequestReasonType:
      description: 'Причина, почему обновился состав заказа:


        * `PARTNER_REQUESTED_REMOVE` — магазин удалил товар.

        * `USER_REQUESTED_REMOVE` — покупатель попросил удалить товар.

        '
      type: string
      enum:
      - PARTNER_REQUESTED_REMOVE
      - USER_REQUESTED_REMOVE
    OrderItemsModificationResultDTO:
      description: 'Краткие сведения о промаркированных товарах. Параметр возвращается, если ответ `OK`.

        '
      type: object
      required:
      - items
      properties:
        items:
          description: Список позиций в заказе, подлежащих маркировке.
          type: array
          items:
            $ref: '#/components/schemas/BriefOrderItemDTO'
    OrderLabelDTO:
      description: Данные для печати ярлыка.
      type: object
      required:
      - orderId
      - placesNumber
      - url
      - parcelBoxLabels
      properties:
        orderId:
          description: Идентификатор заказа.
          type: integer
          format: int64
        placesNumber:
          description: Количество коробок в заказе.
          type: integer
          format: int32
        url:
          $ref: '#/components/schemas/Url'
          description: 'URL файла с ярлыками‑наклейками на все коробки в заказе.


            Соответствует URL, по которому выполняется запрос [GET v2/campaigns/{campaignId}/orders/{orderId}/delivery/labels](../../reference/order-labels/generateOrderLabels.md).

            '
          deprecated: true
        parcelBoxLabels:
          description: Информация на ярлыке.
          type: array
          items:
            $ref: '#/components/schemas/ParcelBoxLabelDTO'
    OrderLiftType:
      description: 'Тип подъема заказа на этаж:


        * `NOT_NEEDED` — не требуется.


        * `MANUAL` — ручной.


        * `ELEVATOR` — лифт.


        * `CARGO_ELEVATOR` — грузовой лифт.


        * `FREE` — любой из перечисленных выше, если включена опция бесплатного подъема.


        * `UNKNOWN` — неизвестный тип.

        '
      type: string
      enum:
      - NOT_NEEDED
      - MANUAL
      - ELEVATOR
      - CARGO_ELEVATOR
      - FREE
      - UNKNOWN
    OrderParcelBoxDTO:
      description: Информация о грузоместе.
      type: object
      required:
      - id
      - fulfilmentId
      properties:
        id:
          description: Идентификатор грузового места.
          type: integer
          format: int64
        fulfilmentId:
          description: Идентификатор грузового места в системе магазина.
          type: string
    OrderPaymentMethodType:
      description: "Способ оплаты заказа:\n\n* Значения, если выбрана оплата при оформлении заказа (`\"paymentType\": \"PREPAID\"\
        `):\n\n  * `YANDEX` — банковской картой.\n\n  * `APPLE_PAY` — Apple Pay (не используется).\n\n  * `GOOGLE_PAY` — Google\
        \ Pay (не используется).\n\n  * `CREDIT` — в кредит.\n\n  * `TINKOFF_CREDIT` — в кредит в Тинькофф Банке.\n\n  * `TINKOFF_INSTALLMENTS`\
        \ — рассрочка в Тинькофф Банке.\n\n  * `EXTERNAL_CERTIFICATE` — подарочным сертификатом (например, из приложения «Сбербанк\
        \ Онлайн»).\n\n  * `SBP` — через систему быстрых платежей.\n\n  * `B2B_ACCOUNT_PREPAYMENT` — заказ оплачивает организация.\n\
        \n  * `MICROCREDIT` - Сплит на основе МКК (Микрокредитной компании).\n\n\n* Значения, если выбрана оплата при получении\
        \ заказа (`\"paymentType\": \"POSTPAID\"`):\n\n  * `CARD_ON_DELIVERY` — банковской картой.\n\n  * `BOUND_CARD_ON_DELIVERY`\
        \ — привязанной картой при получении.\n\n  * `BNPL_BANK_ON_DELIVERY` — супер Сплитом.\n\n  * `BNPL_ON_DELIVERY` —\
        \ Сплитом.\n\n  * `BNPL_TBYB` - Оплата после доставки на основе Сплита.\n\n  * `CASH_ON_DELIVERY` — наличными.\n\n\
        \  * `B2B_ACCOUNT_POSTPAYMENT` — заказ оплачивает организация после доставки.\n\n* `UNKNOWN` — неизвестный тип.\n\n\
        Значение по умолчанию: `CASH_ON_DELIVERY`.\n"
      type: string
      enum:
      - CASH_ON_DELIVERY
      - CARD_ON_DELIVERY
      - BOUND_CARD_ON_DELIVERY
      - BNPL_BANK_ON_DELIVERY
      - BNPL_ON_DELIVERY
      - YANDEX
      - APPLE_PAY
      - EXTERNAL_CERTIFICATE
      - CREDIT
      - GOOGLE_PAY
      - TINKOFF_CREDIT
      - SBP
      - TINKOFF_INSTALLMENTS
      - B2B_ACCOUNT_PREPAYMENT
      - B2B_ACCOUNT_POSTPAYMENT
      - MICROCREDIT
      - BNPL_TBYB
      - UNKNOWN
    OrderPaymentType:
      description: 'Тип оплаты заказа:


        * `PREPAID` — оплата при оформлении заказа.


        * `POSTPAID` — оплата при получении заказа.


        * `UNKNOWN` — неизвестный тип.


        Если параметр отсутствует, заказ будет оплачен при получении.

        '
      type: string
      enum:
      - PREPAID
      - POSTPAID
      - UNKNOWN
    OrderPickupDeliveryDTO:
      type: object
      description: 'Информация о доставке в пункт выдачи.


        Не передавайте вместе с `courierDelivery`.

        '
      required:
      - logisticPointId
      properties:
        logisticPointId:
          $ref: '#/components/schemas/LogisticPointId'
    OrderPickupReturnDTO:
      type: object
      description: 'Информация о пункте выдачи, в который нужно вернуть товары.


        [Как получить список подходящих пунктов выдачи](../../reference/delivery-options/getReturnDeliveryOptions.md)

        '
      required:
      - logisticPointId
      properties:
        logisticPointId:
          $ref: '#/components/schemas/LogisticPointId'
    OrderPriceDTO:
      description: 'Информация о стоимости заказа.


        {% note info "Как рассчитывается стоимость заказа, доставки и вознаграждение продавцу" %}


        * Стоимость товаров в заказе складывается из параметров `payment` и `cashback`.

        * Общая сумма вознаграждений продавцу возвращается в параметре `subsidy`.

        * Значение `payment`, `cashback` и `subsidy` может отличаться от стоимости в каталоге, потому что информация об акциях
        магазина не возвращается.

        * Стоимость доставки складывается из параметров `payment` и `subsidy` в `delivery`.

        * Все суммы указаны в валюте магазина.


        {% endnote %}

        '
      type: object
      properties:
        payment:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Сумма платежа покупателя.
        subsidy:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: 'Общая сумма вознаграждений продавцу.

            '
        cashback:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: 'Сумма, которая оплачена баллами Плюса.


            Возвращается баллами.

            '
        delivery:
          $ref: '#/components/schemas/DeliveryPriceDTO'
          description: 'Информация о стоимости доставки, включая подъем на этаж.

            '
    OrderPromoType:
      description: 'Тип скидки:


        * `DIRECT_DISCOUNT` — прямая скидка, которую устанавливает продавец или Маркет.


        * `BLUE_SET` — комплекты.


        * `BLUE_FLASH` — флеш-акция.


        * `MARKET_COUPON` — скидка по промокоду Маркета.


        * `MARKET_PROMOCODE` — скидка по промокоду магазина.


        * `MARKET_BLUE` — скидка на Маркете.


        * `CHEAPEST_AS_GIFT` — самый дешевый товар в подарок.


        * `CASHBACK` — кешбэк.


        * `SPREAD_DISCOUNT_COUNT` — скидка за количество одинаковых товаров.


        * `SPREAD_DISCOUNT_RECEIPT` — скидка от суммы чека.


        * `DISCOUNT_BY_PAYMENT_TYPE` — прямая скидка при оплате картой Плюса.


        * `PERCENT_DISCOUNT` — прямая скидка в процентах.


        * `DCO_EXTRA_DISCOUNT` — дополнительная скидка, необходимая для расчета субсидии от Маркета.


        * `UNKNOWN` — неизвестный тип.


        Устаревшие типы:


        * `GENERIC_BUNDLE`.


        * `MARKET_COIN`.


        * `PRICE_DROP_AS_YOU_SHOP`.


        * `SECRET_SALE`.

        '
      type: string
      enum:
      - DIRECT_DISCOUNT
      - BLUE_SET
      - BLUE_FLASH
      - GENERIC_BUNDLE
      - MARKET_COUPON
      - MARKET_PROMOCODE
      - MARKET_BLUE
      - MARKET_COIN
      - PRICE_DROP_AS_YOU_SHOP
      - SECRET_SALE
      - CHEAPEST_AS_GIFT
      - CASHBACK
      - SPREAD_DISCOUNT_COUNT
      - SPREAD_DISCOUNT_RECEIPT
      - DISCOUNT_BY_PAYMENT_TYPE
      - PERCENT_DISCOUNT
      - DCO_EXTRA_DISCOUNT
      - UNKNOWN
    OrderShipmentDTO:
      description: 'Список посылок.


        В параметре может указываться несколько посылок.

        '
      type: object
      properties:
        id:
          deprecated: true
          description: Идентификатор посылки, присвоенный Маркетом.
          type: integer
          format: int64
        shipmentDate:
          $ref: '#/components/schemas/DateDdMmYyyy'
          description: 'День, в который нужно отгрузить заказ службе доставки.


            Формат даты: `ДД-ММ-ГГГГ`.


            Если заказ сделан организацией, параметр не возвращается до согласования даты доставки.


            {% cut "Иногда Маркет может перенести дату отгрузки" %}


            У таких заказов обновится параметр `updatedAt`. Чтобы найти их, в запросе [POST v1/businesses/{businessId}/orders](../../reference/orders/getBusinessOrders.md)
            укажите параметры `updateDateFrom` и `updateDateTo`.


            {% endcut %}


             

            '
        shipmentTime:
          description: '**Только для модели Экспресс**


            Время, к которому магазин должен упаковать заказ и перевести его в статус `READY_TO_SHIP`. После смены статуса
            за заказом приедет курьер.


            Поле может появиться не сразу. Запрашивайте информацию о заказе в течении 5–10 минут, пока оно не вернется.


            Формат времени: 24-часовой, `ЧЧ:ММ`.


            Если заказ сделан организацией, параметр не возвращается до согласования даты доставки.

            '
          type: string
        tracks:
          description: '**Только для модели DBS**


            Информация для отслеживания посылки.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderTrackDTO'
        boxes:
          description: Список грузовых мест.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderParcelBoxDTO'
    OrderSourcePlatformType:
      description: 'Площадка-источник заказа:


        * `MARKET` — заказ, оформленный на Маркете.


        * `OTHER` — LaaS-заказ, созданный продавцом.

        '
      type: string
      enum:
      - MARKET
      - OZON
      - WILDBERRIES
      - OTHER
    OrderStateDTO:
      description: Информация по заказу.
      type: object
      required:
      - id
      - status
      properties:
        id:
          description: Идентификатор заказа.
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/OrderStatusType'
        substatus:
          $ref: '#/components/schemas/OrderSubstatusType'
    OrderStatsStatusType:
      description: "Текущий статус заказа:\n\n* `CANCELLED_BEFORE_PROCESSING` — заказ отменен до начала его обработки.\n\n\
        * `CANCELLED_IN_DELIVERY` — заказ отменен во время его доставки.\n\n* `CANCELLED_IN_PROCESSING` — заказ отменен во\
        \ время его обработки.\n\n* `DELIVERY` — заказ передан службе доставки.\n\n* `DELIVERED` — заказ доставлен.\n\n* `PARTIALLY_DELIVERED`\
        \ — заказ частично доставлен.\n\n    {% note warning \"Статус заказа может перейти в `PARTIALLY_DELIVERED` не сразу\"\
        \ %}\n\n    Если в доставленном заказе был невыкуп, статус изменится только после получения заказа на складе Маркета.\n\
        \n    {% endnote %}\n\n* `PARTIALLY_RETURNED` — заказ частично возвращен покупателем.\n\n* `PENDING` — заказ ожидает\
        \ подтверждения.\n\n* `PICKUP` — заказ доставлен в пункт выдачи.\n\n* `PROCESSING` — заказ в обработке.\n\n* `RESERVED`\
        \ — товар зарезервирован на складе.\n\n* `RETURNED` — заказ полностью возвращен покупателем.\n\n* `UNKNOWN` — неизвестный\
        \ статус заказа.\n\n* `UNPAID` — заказ от юридического лица ожидает оплаты.\n\n* `LOST` — заказ утерян.\n"
      type: string
      enum:
      - CANCELLED_BEFORE_PROCESSING
      - CANCELLED_IN_DELIVERY
      - CANCELLED_IN_PROCESSING
      - DELIVERY
      - DELIVERED
      - PARTIALLY_DELIVERED
      - PARTIALLY_RETURNED
      - PENDING
      - PICKUP
      - PROCESSING
      - RESERVED
      - RETURNED
      - UNKNOWN
      - UNPAID
      - LOST
    OrderStatusChangeDTO:
      description: Заказ.
      type: object
      required:
      - status
      properties:
        status:
          $ref: '#/components/schemas/OrderStatusType'
        substatus:
          $ref: '#/components/schemas/OrderSubstatusType'
        delivery:
          $ref: '#/components/schemas/OrderStatusChangeDeliveryDTO'
    OrderStatusChangeDeliveryDTO:
      description: Информация о доставке.
      type: object
      properties:
        dates:
          $ref: '#/components/schemas/OrderStatusChangeDeliveryDatesDTO'
    OrderStatusChangeDeliveryDatesDTO:
      description: Диапазон дат доставки.
      type: object
      properties:
        realDeliveryDate:
          description: "**Только для модели DBS**\n\nФактическая дата доставки.\n<br><br>\nКогда передавать параметр `realDeliveryDate`:\n\
            \n* Не передавайте параметр, если:\n  * переводите заказ в любой статус, кроме `PICKUP` или `DELIVERED`;\n  *\
            \ меняете статус заказа на `PICKUP` или `DELIVERED` в день доставки — будет указана дата выполнения запроса.\n\
            * Передавайте дату доставки, если переводите заказ в статус `PICKUP` или `DELIVERED` не в день доставки. Нельзя\
            \ указывать дату доставки в будущем.\n\n  {% note warning \"Передача статуса после установленного срока снижает\
            \ индекс качества\" %}\n\n  О сроках читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/quality/tech#dbs).\n\
            \n  {% endnote %}\n\n   \nФормат даты: `ГГГГ-ММ-ДД`.\n"
          type: string
          format: date
    OrderStatusType:
      description: 'Статус заказа:


        * `PLACING` — оформляется, подготовка к резервированию.


        * `RESERVED` — зарезервирован, но недооформлен (только для LaaS).


        * `UNPAID` — оформлен, но еще не оплачен (если выбрана оплата при оформлении).


        * `PROCESSING` — находится в обработке.


        * `DELIVERY` — передан в службу доставки.


        * `PICKUP` — доставлен в пункт выдачи.


        * `DELIVERED` — получен покупателем.


        * `CANCELLED` — отменен.


        * `PENDING` — ожидает обработки со стороны продавца.


        * `PARTIALLY_RETURNED` — возвращен частично.


        * `RETURNED` — возвращен полностью.


        * `UNKNOWN` — неизвестный статус.


        Также могут возвращаться другие значения. Обрабатывать их не нужно.

        '
      type: string
      enum:
      - PLACING
      - RESERVED
      - UNPAID
      - PROCESSING
      - DELIVERY
      - PICKUP
      - DELIVERED
      - CANCELLED
      - PENDING
      - PARTIALLY_RETURNED
      - RETURNED
      - UNKNOWN
    OrderSubsidyDTO:
      description: 'Общее вознаграждение продавцу за DBS-доставку и все скидки на товар:


        * по промокодам, купонам и акциям;

        * по баллам Плюса;

        * по доставке (DBS).


        Включает НДС.

        '
      type: object
      required:
      - type
      - amount
      properties:
        type:
          $ref: '#/components/schemas/OrderSubsidyType'
        amount:
          description: Сумма субсидии.
          type: number
    OrderSubsidyType:
      description: 'Тип субсидии:


        * `YANDEX_CASHBACK` — скидка по подписке Яндекс Плюс.


        * `SUBSIDY` — скидка Маркета (по акциям, промокодам, купонам и т. д.)


        * `DELIVERY` — скидка за доставку (DBS).

        '
      type: string
      enum:
      - YANDEX_CASHBACK
      - SUBSIDY
      - DELIVERY
    OrderSubstatusType:
      description: "Этап обработки заказа (статус `PROCESSING`) или причина отмены заказа (статус `CANCELLED`).\n\n* Значения\
        \ для заказа в статусе `PROCESSING`:\n\n    * `STARTED` — заказ подтвержден, его можно начать обрабатывать.\n\n  \
        \  * `READY_TO_SHIP` — заказ собран и готов к отправке.\n\n* Значения для заказа в статусе `CANCELLED`:\n\n    * `RESERVATION_EXPIRED`\
        \ — покупатель не завершил оформление зарезервированного заказа в течение 10 минут.\n\n    * `USER_NOT_PAID` — покупатель\
        \ не оплатил заказ (для типа оплаты `PREPAID`) в течение 30 минут.\n\n    * `USER_UNREACHABLE` — не удалось связаться\
        \ с покупателем. Для отмены с этой причиной необходимо выполнить условия:\n\n      * не менее 3 звонков с 8 до 21\
        \ в часовом поясе покупателя;\n      * перерыв между первым и третьим звонком не менее 90 минут;\n      * соединение\
        \ не короче 5 секунд.\n\n      Если хотя бы одно из этих условий не выполнено (кроме случая, когда номер недоступен),\
        \ отменить заказ не получится. Вернется ответ с кодом ошибки 400.\n\n    * `USER_CHANGED_MIND` — покупатель отменил\
        \ заказ по личным причинам.\n\n    * `USER_REFUSED_DELIVERY` — покупателя не устроили условия доставки.\n\n    * `USER_REFUSED_PRODUCT`\
        \ — покупателю не подошел товар.\n\n    * `SHOP_FAILED` — магазин не может выполнить заказ.\n\n    * `USER_REFUSED_QUALITY`\
        \ — покупателя не устроило качество товара.\n\n    * `USER_IDENTIFICATION_MISMATCH` — идентификационный документ покупателя\
        \ не совпадает с данными в заказе.\n\n    * `PURCHASE_GROUP_THRESHOLD_NOT_REACHED_CANCELLED` — заказ участвовал в\
        \ групповой покупке и был отменен, потому что не было достигнуто нужное количество покупок.\n\n    * `REPLACING_ORDER`\
        \ — покупатель решил заменить товар другим по собственной инициативе.\n\n    * `PROCESSING_EXPIRED` — значение более\
        \ не используется.\n\n    * `PICKUP_EXPIRED` — закончился срок хранения заказа в пункт выдачи.\n\n    * `TOO_MANY_DELIVERY_DATE_CHANGES`\
        \ — заказ переносили слишком много раз.\n\n    * `TOO_LONG_DELIVERY` — заказ доставляется слишком долго.\n\n    *\
        \ `INCORRECT_PERSONAL_DATA` — для заказа из-за рубежа указаны неправильные данные получателя, заказ не пройдет проверку\
        \ на таможне.\n\n* `TECHNICAL_ERROR` — техническая ошибка на стороне Маркета. Обратитесь в поддержку.\n\nТакже могут\
        \ возвращаться другие значения. Обрабатывать их не нужно.\n"
      type: string
      enum:
      - RESERVATION_EXPIRED
      - USER_NOT_PAID
      - USER_UNREACHABLE
      - USER_CHANGED_MIND
      - USER_REFUSED_DELIVERY
      - USER_REFUSED_PRODUCT
      - SHOP_FAILED
      - USER_REFUSED_QUALITY
      - REPLACING_ORDER
      - PROCESSING_EXPIRED
      - PENDING_EXPIRED
      - SHOP_PENDING_CANCELLED
      - PENDING_CANCELLED
      - USER_FRAUD
      - RESERVATION_FAILED
      - USER_PLACED_OTHER_ORDER
      - USER_BOUGHT_CHEAPER
      - MISSING_ITEM
      - BROKEN_ITEM
      - WRONG_ITEM
      - PICKUP_EXPIRED
      - DELIVERY_PROBLEMS
      - LATE_CONTACT
      - CUSTOM
      - DELIVERY_SERVICE_FAILED
      - WAREHOUSE_FAILED_TO_SHIP
      - DELIVERY_SERVICE_UNDELIVERED
      - PREORDER
      - AWAIT_CONFIRMATION
      - STARTED
      - PACKAGING
      - READY_TO_SHIP
      - SHIPPED
      - ASYNC_PROCESSING
      - WAITING_USER_INPUT
      - WAITING_BANK_DECISION
      - BANK_REJECT_CREDIT_OFFER
      - CUSTOMER_REJECT_CREDIT_OFFER
      - CREDIT_OFFER_FAILED
      - AWAIT_DELIVERY_DATES_CONFIRMATION
      - SERVICE_FAULT
      - DELIVERY_SERVICE_RECEIVED
      - USER_RECEIVED
      - WAITING_FOR_STOCKS
      - AS_PART_OF_MULTI_ORDER
      - READY_FOR_LAST_MILE
      - LAST_MILE_STARTED
      - ANTIFRAUD
      - DELIVERY_USER_NOT_RECEIVED
      - DELIVERY_SERVICE_DELIVERED
      - DELIVERED_USER_NOT_RECEIVED
      - USER_WANTED_ANOTHER_PAYMENT_METHOD
      - USER_RECEIVED_TECHNICAL_ERROR
      - USER_FORGOT_TO_USE_BONUS
      - DELIVERY_SERVICE_NOT_RECEIVED
      - DELIVERY_SERVICE_LOST
      - SHIPPED_TO_WRONG_DELIVERY_SERVICE
      - DELIVERED_USER_RECEIVED
      - WAITING_TINKOFF_DECISION
      - COURIER_SEARCH
      - COURIER_FOUND
      - COURIER_IN_TRANSIT_TO_SENDER
      - COURIER_ARRIVED_TO_SENDER
      - COURIER_RECEIVED
      - COURIER_NOT_FOUND
      - COURIER_NOT_DELIVER_ORDER
      - COURIER_RETURNS_ORDER
      - COURIER_RETURNED_ORDER
      - WAITING_USER_DELIVERY_INPUT
      - PICKUP_SERVICE_RECEIVED
      - PICKUP_USER_RECEIVED
      - CANCELLED_COURIER_NOT_FOUND
      - COURIER_NOT_COME_FOR_ORDER
      - DELIVERY_NOT_MANAGED_REGION
      - INCOMPLETE_CONTACT_INFORMATION
      - INCOMPLETE_MULTI_ORDER
      - INAPPROPRIATE_WEIGHT_SIZE
      - TECHNICAL_ERROR
      - SORTING_CENTER_LOST
      - COURIER_SEARCH_NOT_STARTED
      - LOST
      - AWAIT_PAYMENT
      - AWAIT_LAVKA_RESERVATION
      - USER_WANTS_TO_CHANGE_ADDRESS
      - FULL_NOT_RANSOM
      - PRESCRIPTION_MISMATCH
      - DROPOFF_LOST
      - DROPOFF_CLOSED
      - DELIVERY_TO_STORE_STARTED
      - USER_WANTS_TO_CHANGE_DELIVERY_DATE
      - WRONG_ITEM_DELIVERED
      - DAMAGED_BOX
      - AWAIT_DELIVERY_DATES
      - LAST_MILE_COURIER_SEARCH
      - PICKUP_POINT_CLOSED
      - LEGAL_INFO_CHANGED
      - USER_HAS_NO_TIME_TO_PICKUP_ORDER
      - DELIVERY_CUSTOMS_ARRIVED
      - DELIVERY_CUSTOMS_CLEARED
      - FIRST_MILE_DELIVERY_SERVICE_RECEIVED
      - AWAIT_AUTO_DELIVERY_DATES
      - AWAIT_USER_PERSONAL_DATA
      - NO_PERSONAL_DATA_EXPIRED
      - CUSTOMS_PROBLEMS
      - AWAIT_CASHIER
      - WAITING_POSTPAID_BUDGET_RESERVATION
      - AWAIT_SERVICEABLE_CONFIRMATION
      - POSTPAID_BUDGET_RESERVATION_FAILED
      - AWAIT_CUSTOM_PRICE_CONFIRMATION
      - READY_FOR_PICKUP
      - TOO_MANY_DELIVERY_DATE_CHANGES
      - TOO_LONG_DELIVERY
      - DEFERRED_PAYMENT
      - POSTPAID_FAILED
      - INCORRECT_PERSONAL_DATA
      - CUSTOMS_FAILED_MARKET
      - CUSTOMS_FAILED_USER_COMMERCIAL_ITEMS
      - CUSTOMS_FAILED_USER_DUTY_NOT_PAID
      - CUSTOMS_FAILED_USER_INVALID_PERSONAL_DATA
      - CUSTOMS_FAILED_USER_ADDITIONAL_DATA_NOT_PROVIDED
      - AWAIT_PAYMENT_AFTER_DELIVERY
      - AWAIT_USER_STEAM_FAST_URL
      - USER_IDENTIFICATION_MISMATCH
      - PURCHASE_GROUP_THRESHOLD_NOT_REACHED_CANCELLED
      - UNKNOWN
    OrderTaxSystemType:
      description: 'Система налогообложения (СНО) магазина на момент оформления заказа:


        * `ECHN` — единый сельскохозяйственный налог (ЕСХН).


        * `ENVD` — единый налог на вмененный доход (ЕНВД).


        * `OSN` — общая система налогообложения (ОСН).


        * `PSN` — патентная система налогообложения (ПСН).


        * `USN` — упрощенная система налогообложения (УСН).


        * `USN_MINUS_COST` — упрощенная система налогообложения, доходы, уменьшенные на величину расходов (УСН «Доходы минус
        расходы»).


        * `NPD` — налог на профессиональный доход (НПД).


        * `AUSN` — автоматизированная упрощенная система налогообложения (АУСН).


        * `AUSN_MINUS_COST` — автоматизированная упрощенная система налогообложения, доходы, уменьшенные на величину расходов
        (АУСН «Доходы минус расходы»).


        * `UNKNOWN_VALUE` — неизвестное значение.

        '
      type: string
      enum:
      - OSN
      - USN
      - USN_MINUS_COST
      - ENVD
      - ECHN
      - PSN
      - NPD
      - AUSN
      - AUSN_MINUS_COST
      - UNKNOWN_VALUE
    OrderTrackDTO:
      description: Информация о трек-номере посылки (DBS).
      type: object
      required:
      - deliveryServiceId
      properties:
        trackCode:
          description: Трек‑номер посылки.
          type: string
        deliveryServiceId:
          description: Идентификатор службы доставки. Информацию о службе доставки можно получить с помощью запроса [GET delivery/services](../../reference/delivery-services/getDeliveryServices.md).
          type: integer
          format: int64
    OrderUpdateOptionsDTO:
      type: object
      description: Список доступных интервалов.
      required:
      - deliveryIntervals
      properties:
        deliveryIntervals:
          $ref: '#/components/schemas/DeliveryIntervalsUpdateOptionsDTO'
    OrderUpdateStatusType:
      description: 'Изменился ли статус заказа:


        * `OK` — статус изменен.


        * `ERROR` — статус не изменен. В этом случае появится сообщение об ошибке в параметре `errorDetails`.

        '
      type: string
      enum:
      - OK
      - ERROR
    OrderVatType:
      description: 'НДС на товар или доставку:


        * `NO_VAT` — НДС не облагается, используется только для отдельных видов услуг.


        * `VAT_0` — НДС 0%. Например, используется при продаже товаров, вывезенных в таможенной процедуре экспорта, или при
        оказании услуг по международной перевозке товаров.


        * `VAT_10` — НДС 10%. Например, используется при реализации отдельных продовольственных и медицинских товаров.


        * `VAT_10_110` — НДС 10/110. НДС 10%, применяется только при предоплате.


        * `VAT_20` — НДС 20%. Основной НДС с 2019 года до 1 января 2026 года.


        * `VAT_20_120` — НДС 20/120. НДС 20%, применяется только при предоплате.


        * `VAT_18` — НДС 18%. Основной НДС до 2019 года.


        * `VAT_18_118` — НДС 18/118. НДС использовался до 1 января 2019 года при предоплате.


        * `VAT_12` — НДС 12%. Используется только в Узбекистане.


        * `VAT_05` — НДС 5%. НДС для упрощенной системы налогообложения (УСН).


        * `VAT_07` — НДС 7%. НДС для упрощенной системы налогообложения (УСН).


        * `VAT_22` — НДС 22%. Основной НДС с 1 января 2026 года.


        * `UNKNOWN_VALUE` — неизвестный тип.

        '
      type: string
      enum:
      - NO_VAT
      - VAT_0
      - VAT_10
      - VAT_10_110
      - VAT_20
      - VAT_20_120
      - VAT_18
      - VAT_18_118
      - VAT_12
      - VAT_05
      - VAT_07
      - VAT_22
      - UNKNOWN_VALUE
    OrdersShipmentInfoDTO:
      description: Годные/негодные ярлыки по заказам в отгрузке.
      type: object
      required:
      - orderIdsWithLabels
      - orderIdsWithoutLabels
      properties:
        orderIdsWithLabels:
          description: Идентификаторы заказов в отгрузке, для которых можно распечатать ярлыки.
          type: array
          uniqueItems: true
          items:
            type: integer
            format: int64
        orderIdsWithoutLabels:
          description: Идентификаторы заказов в отгрузке, для которых нельзя распечатать ярлыки.
          type: array
          uniqueItems: true
          items:
            type: integer
            format: int64
    OrdersStatsCommissionDTO:
      description: Информация о стоимости услуг.
      type: object
      properties:
        type:
          $ref: '#/components/schemas/OrdersStatsCommissionType'
          description: Услуга.
        actual:
          description: 'Сумма, которая была выставлена в момент создания заказа и которую нужно оплатить.

            Точность — два знака после запятой.

            '
          type: number
    OrdersStatsCommissionType:
      description: 'Услуга:


        * `FEE` — размещение товара на Маркете.

        * `FULFILLMENT` — складская обработка. Не возвращается с 1 января 2024 года.

        * `LOYALTY_PARTICIPATION_FEE` — участие в программе лояльности и отзывы за баллы.

        * `AUCTION_PROMOTION` — буст продаж с оплатой за продажи.

        * `INSTALLMENT` — рассрочка. Не возвращается с 24 февраля 2022 года.

        * `DELIVERY_TO_CUSTOMER` — доставка покупателю (FBY, FBS). Для DBS и Экспресс — если заказ возвращается через логистику
        Маркета.

        * `EXPRESS_DELIVERY_TO_CUSTOMER` — экспресс-доставка покупателю (Экспресс).

        * `AGENCY` — прием платежа покупателя.

        * `PAYMENT_TRANSFER` — перевод платежа покупателя.

        * `RETURNED_ORDERS_STORAGE` — хранение невыкупов и возвратов (FBS). Для DBS и Экспресс — если заказ возвращается через
        логистику Маркета.

        * `SORTING` — обработка заказа (FBS).

        * `INTAKE_SORTING` — организация забора заказов со склада продавца (FBS).

        * `RETURN_PROCESSING` — обработка заказов на складе (FBS). Для DBS и Экспресс — если заказ возвращается через логистику
        Маркета.

        * `ILLIQUID_GOODS_SALE` — вознаграждение за продажу невывезенных товаров.

        * `CROSSREGIONAL_DELIVERY` - доставка средней мили.

        * `FULFILLMENT_WITHDRAW` - вывоз со склада.

        * `ITEM_BOOKING` - бронирование товара (только для продавцов Market Yandex Go).

        * `SHOP_SERVICE_PLACEMENT` - размещение услуг (DBS).

        '
      type: string
      enum:
      - FEE
      - FULFILLMENT
      - LOYALTY_PARTICIPATION_FEE
      - AUCTION_PROMOTION
      - INSTALLMENT
      - DELIVERY_TO_CUSTOMER
      - EXPRESS_DELIVERY_TO_CUSTOMER
      - AGENCY
      - PAYMENT_TRANSFER
      - RETURNED_ORDERS_STORAGE
      - SORTING
      - INTAKE_SORTING
      - RETURN_PROCESSING
      - ILLIQUID_GOODS_SALE
      - CROSSREGIONAL_DELIVERY
      - FULFILLMENT_WITHDRAW
      - ITEM_BOOKING
      - SHOP_SERVICE_PLACEMENT
    OrdersStatsDTO:
      description: Информация по заказам.
      type: object
      required:
      - orders
      properties:
        orders:
          description: Список заказов.
          type: array
          items:
            $ref: '#/components/schemas/OrdersStatsOrderDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    OrdersStatsDeliveryRegionDTO:
      description: Информация о регионе доставки.
      type: object
      properties:
        id:
          description: Идентификатор региона доставки.
          type: integer
          format: int64
        name:
          description: Название региона доставки.
          type: string
    OrdersStatsDetailsDTO:
      description: Информация об удалении товара из заказа.
      type: object
      properties:
        itemStatus:
          $ref: '#/components/schemas/OrdersStatsItemStatusType'
          description: Статус товара.
        itemCount:
          description: Количество товара со статусом, указанном в параметре `itemStatus`.
          type: integer
          format: int64
        updateDate:
          description: '**Только для модели FBY**


            Дата, когда возврат был обработан на складе Маркета.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        stockType:
          $ref: '#/components/schemas/OrdersStatsStockType'
          description: '**Только для модели FBY**


            Тип товара.


            Возвращается только после обработки возврата на складе Маркета.

            '
    OrdersStatsItemDTO:
      description: 'Список товаров в заказе после возможных изменений.


        В ходе обработки заказа Маркет может удалить из него единицы товаров — при проблемах на складе или по инициативе пользователя.


        * Если из заказа удалены все единицы товара, его не будет в списке `items` — только в списке `initialItems`.


        * Если в заказе осталась хотя бы одна единица товара, он будет и в списке `items` (с уменьшенным количеством единиц
        `count`), и в списке `initialItems` (с первоначальным количеством единиц `initialCount`).

        '
      type: object
      properties:
        offerName:
          description: Название товара.
          type: string
        marketSku:
          $ref: '#/components/schemas/MarketSku'
        shopSku:
          $ref: '#/components/schemas/ShopSku'
        count:
          description: 'Количество единиц товара с учетом удаленных единиц.


            Если из заказа удалены все единицы товара, он попадет только в список `initialItems`.

            '
          type: integer
          format: int32
        prices:
          description: Цена или скидки на товар.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrdersStatsPriceDTO'
        warehouse:
          $ref: '#/components/schemas/OrdersStatsWarehouseDTO'
          description: Информация о складе, на котором хранится товар.
        details:
          description: Информация об удалении товара из заказа.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrdersStatsDetailsDTO'
        cisList:
          description: Список кодов идентификации товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz)
            (для продавцов :no-translate[Market Yandex Go]).
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
        initialCount:
          description: Первоначальное количество единиц товара.
          type: integer
          format: int32
        bidFee:
          description: 'Списанная ставка ближайшего конкурента.


            Указывается в процентах от стоимости товара и умножается на 100. Например, ставка 5% обозначается как 500.

            '
          type: integer
          format: int32
          minimum: 0
          maximum: 10000
          example: 570
        cofinanceThreshold:
          description: 'Порог для скидок с Маркетом на момент оформления заказа. [Что это такое?](https://yandex.ru/support/marketplace/marketing/smart-pricing.html#sponsored-discounts)


            Точность — два знака после запятой.

            '
          type: number
        cofinanceValue:
          description: 'Скидка с Маркетом. [Что это такое?](https://yandex.ru/support/marketplace/marketing/smart-pricing.html#sponsored-discounts)


            Точность — два знака после запятой.

            '
          type: number
    OrdersStatsItemStatusType:
      description: 'Статус товара:


        * `REJECTED` — товар был добавлен в созданный заказ, но не был оплачен.

        * `RETURNED` — товар вернули.

        '
      type: string
      enum:
      - REJECTED
      - RETURNED
    OrdersStatsOrderDTO:
      description: Информация о заказе.
      type: object
      required:
      - items
      - payments
      - commissions
      - currency
      properties:
        id:
          description: Идентификатор заказа.
          type: integer
          format: int64
        creationDate:
          description: 'Дата создания заказа.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        statusUpdateDate:
          description: 'Дата и время, когда статус заказа был изменен в последний раз.


            Формат даты и времени: ISO 8601. Например, `2017-11-21T00:00:00`. Часовой пояс — UTC+03:00 (Москва).

            '
          type: string
          format: date-time
        status:
          $ref: '#/components/schemas/OrderStatsStatusType'
        partnerOrderId:
          description: Идентификатор заказа в информационной системе магазина.
          type: string
        paymentType:
          $ref: '#/components/schemas/OrdersStatsOrderPaymentType'
          description: Тип оплаты заказа.
        fake:
          description: 'Тип заказа:


            * `false` — настоящий заказ покупателя.


            * `true` — [тестовый заказ](../../concepts/sandbox.md) Маркета.

            '
          type: boolean
        deliveryRegion:
          $ref: '#/components/schemas/OrdersStatsDeliveryRegionDTO'
        items:
          description: 'Список товаров в заказе после возможных изменений.


            Информация о доставке заказа добавляется отдельным элементом в массиве `items`— параметр `offerName` со значением
            `Доставка`.

            '
          type: array
          items:
            $ref: '#/components/schemas/OrdersStatsItemDTO'
        initialItems:
          description: 'Список товаров в заказе.


            Возвращается, только если было изменение количества товаров.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrdersStatsItemDTO'
        payments:
          description: "Информация о расчетах по заказу.\n\nВозвращается пустым, если заказ:\n  * только начали обрабатывать\
            \ (даже если он оплачен);\n  * отменили до момента передачи в доставку.\n\nОкончательная информация о расчетах\
            \ по заказу вернется после его финальной обработки (например, после перехода в статус `DELIVERED`).\n"
          type: array
          items:
            $ref: '#/components/schemas/OrdersStatsPaymentDTO'
        commissions:
          description: Информация о стоимости услуг.
          type: array
          items:
            $ref: '#/components/schemas/OrdersStatsCommissionDTO'
        subsidies:
          description: Начисление баллов, которые используются для уменьшения стоимости размещения, и их списание в случае
            невыкупа или возврата.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OrdersStatsSubsidyDTO'
        currency:
          $ref: '#/components/schemas/CurrencyType'
          description: 'Валюта, в которой указаны цены в заказе.

            '
    OrdersStatsOrderPaymentType:
      description: 'Тип оплаты заказа:

        - `POSTPAID` — заказ оплачен после того, как был получен.

        - `PREPAID` — заказ оплачен до того, как был получен.

        - `UNKNOWN` — неизвестный тип оплаты. Скорее всего покупатель отменил или вернул заказ или не было его оплаты.

        '
      type: string
      enum:
      - POSTPAID
      - PREPAID
      - UNKNOWN
    OrdersStatsPaymentDTO:
      description: Информация о денежных переводах по заказу.
      type: object
      properties:
        id:
          description: Идентификатор денежного перевода.
          type: string
        date:
          description: 'Дата денежного перевода.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
        type:
          $ref: '#/components/schemas/OrdersStatsPaymentType'
          description: Тип денежного перевода.
        source:
          $ref: '#/components/schemas/OrdersStatsPaymentSourceType'
          description: Способ денежного перевода.
        total:
          description: 'Сумма денежного перевода.


            Точность — два знака после запятой.

            '
          type: number
        paymentOrder:
          $ref: '#/components/schemas/OrdersStatsPaymentOrderDTO'
          description: Информация о платежном поручении.
    OrdersStatsPaymentOrderDTO:
      description: Информация о платежном поручении.
      type: object
      properties:
        id:
          description: Номер платежного поручения.
          type: string
        date:
          description: 'Дата платежного поручения.


            Формат даты: `ГГГГ‑ММ‑ДД`.

            '
          type: string
          format: date
    OrdersStatsPaymentSourceType:
      description: 'Способ денежного перевода:

        - `BUYER` — оплата или возврат деньгами.

        - `MARKET_CESSION` — уступка задолженности покупателя.


        Устаревшие способы:

        - `CASHBACK`.

        - `MARKETPLACE`.

        - `SPLIT`.

        '
      type: string
      enum:
      - BUYER
      - CASHBACK
      - MARKETPLACE
      - MARKET_CESSION
      - SPLIT
    OrdersStatsPaymentType:
      description: 'Тип денежного перевода:

        - `PAYMENT` — оплата.

        - `REFUND` — возврат.

        '
      type: string
      enum:
      - PAYMENT
      - REFUND
    OrdersStatsPriceDTO:
      description: Цена или скидки на товар.
      type: object
      properties:
        type:
          $ref: '#/components/schemas/OrdersStatsPriceType'
          description: Тип скидки или цена товара.
        costPerItem:
          description: 'Цена или скидка на единицу товара в заказе.


            Точность — два знака после запятой.


            Включает НДС.

            '
          type: number
        total:
          description: 'Суммарная цена или скидка на все единицы товара в заказе.


            Точность — два знака после запятой.


            Включает НДС.

            '
          type: number
    OrdersStatsPriceType:
      description: 'Тип скидки или цена товара:

        - `BUYER` — цена товара с учетом скидок, в том числе купонов.

        - `CASHBACK` — баллы Плюса.

        - `MARKETPLACE` — купоны.

        '
      type: string
      enum:
      - BUYER
      - CASHBACK
      - MARKETPLACE
    OrdersStatsStockType:
      description: 'Тип товара:


        * `FIT` — товар надлежащего качества.


        * `DEFECT` — товар бракованный.


        * `EXPIRED` — товар с истекшим сроком годности.

        '
      type: string
      enum:
      - FIT
      - DEFECT
      - EXPIRED
    OrdersStatsSubsidyDTO:
      description: Информация о начислении баллов, которые используются для уменьшения стоимости размещения, и их списании
        в случае невыкупа или возврата.
      type: object
      required:
      - operationType
      - type
      - amount
      properties:
        operationType:
          $ref: '#/components/schemas/OrdersStatsSubsidyOperationType'
          description: Тип операции c баллами, которые используются для уменьшения стоимости размещения.
        type:
          $ref: '#/components/schemas/OrdersStatsSubsidyType'
          description: Источник баллов, которые используются для уменьшения стоимости размещения.
        amount:
          description: 'Количество баллов, которые используются для уменьшения стоимости размещения, с точностью до двух знаков
            после запятой.

            '
          type: number
    OrdersStatsSubsidyOperationType:
      description: 'Тип операции с баллами, которые используются для уменьшения стоимости размещения:


        * `ACCRUAL` — начисление баллов.

        * `DEDUCTION` — списание баллов.

        '
      type: string
      enum:
      - ACCRUAL
      - DEDUCTION
    OrdersStatsSubsidyType:
      description: 'Источник баллов, которые используются для уменьшения стоимости размещения:


        * `YANDEX_CASHBACK` — скидка по подписке Яндекс Плюс.


        * `SUBSIDY` — скидка Маркета (по акциям, промокодам, купонам и т. д.)


        * `DELIVERY` — скидка за доставку (DBS).

        '
      type: string
      enum:
      - YANDEX_CASHBACK
      - SUBSIDY
      - DELIVERY
    OrdersStatsWarehouseDTO:
      description: Информация о складе, на котором хранится товар.
      type: object
      properties:
        id:
          description: Идентификатор склада.
          type: integer
          format: int64
        name:
          description: Название склада.
          type: string
    OutletAddressDTO:
      description: 'Адрес точки продаж.

        '
      type: object
      required:
      - regionId
      properties:
        regionId:
          description: 'Идентификатор региона.


            Идентификатор можно получить c помощью запроса [GET v2/regions](../../reference/regions/searchRegionsByName.md).


            {% note alert "Типы регионов при создании и редактировании точек продаж" %}


            Указывайте только регионы типов `TOWN` (город), `CITY` (крупный город) и `REPUBLIC_AREA` (район субъекта федерации).
            Тип региона указан в выходных параметрах `type` запросов [GET v2/regions](../../reference/regions/searchRegionsByName.md)
            и [GET v2/regions/{regionId}](../../reference/regions/searchRegionsById.md).


            {% endnote %}

            '
          type: integer
          format: int64
        street:
          description: Улица.
          type: string
          maxLength: 512
        number:
          description: Номер дома.
          type: string
          maxLength: 256
        building:
          description: Номер строения.
          type: string
          maxLength: 16
        estate:
          description: Номер владения.
          type: string
          maxLength: 16
        block:
          description: Номер корпуса.
          type: string
          maxLength: 16
        additional:
          description: Дополнительная информация.
          type: string
        km:
          description: Порядковый номер километра дороги, на котором располагается точка продаж, если отсутствует улица.
          type: integer
          format: int32
        city:
          description: '{% note warning "В ответах города и населенные пункты возвращаются в параметре `regionId`." %}


             


            {% endnote %}

            '
          maxLength: 200
          type: string
          deprecated: true
    OutletDTO:
      description: Информация о точке продаж.
      type: object
      required:
      - name
      - type
      - address
      - phones
      - workingSchedule
      properties:
        name:
          description: 'Название точки продаж.

            '
          type: string
        type:
          $ref: '#/components/schemas/OutletType'
        coords:
          description: 'Координаты точки продаж.


            Формат: долгота, широта. Разделители: запятая и / или пробел. Например, `20.4522144, 54.7104264`.


            Если параметр не передан, координаты будут определены по значениям параметров, вложенных в `address`.

            '
          type: string
        isMain:
          description: 'Признак основной точки продаж.


            Возможные значения:


            * `false` — неосновная точка продаж.

            * `true` — основная точка продаж.

            '
          type: boolean
        shopOutletCode:
          description: Идентификатор точки продаж, присвоенный магазином.
          type: string
        visibility:
          $ref: '#/components/schemas/OutletVisibilityType'
        address:
          $ref: '#/components/schemas/OutletAddressDTO'
        phones:
          description: 'Номера телефонов точки продаж.

            Передавайте номер в формате: `+<код страны>(<код города>)<номер>[#<добавочный>]`.


            Примеры:

            - `+7 (999) 999-99-99`

            - `+7 (999) 999-99-99#1234`

            '
          type: array
          minItems: 1
          uniqueItems: true
          items:
            type: string
            minLength: 1
        workingSchedule:
          $ref: '#/components/schemas/OutletWorkingScheduleDTO'
        deliveryRules:
          description: 'Информация об условиях доставки для данной точки продаж.


            Обязательный параметр, если параметр `type=DEPOT` или `type=MIXED`.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OutletDeliveryRuleDTO'
        storagePeriod:
          description: Срок хранения заказа в собственном пункте выдачи заказов. Считается в днях.
          type: integer
          format: int64
    OutletDeliveryRuleDTO:
      description: Информация об условиях доставки для данной точки продаж.
      type: object
      properties:
        minDeliveryDays:
          description: 'Минимальный срок доставки товаров в точку продаж. Указан в рабочих днях.


            Минимальное значение: `0` — доставка в день заказа.


            Максимальное значение: `60`.


            Допустимые сроки доставки (разница между `minDeliveryDays` и `maxDeliveryDays`) зависят от региона.


            Для доставки по своему региону разница не должна превышать двух дней. Например, если `minDeliveryDays` равно 1,
            то для `maxDeliveryDays` допускаются значения от 1 до 3.


            Для доставки в другие регионы:


            * Если `minDeliveryDays` до 18 дней, разница не должна превышать четырех дней. Например, если `minDeliveryDays`
            равно 10, то для `maxDeliveryDays` допускаются значения от 10 до 14.

            * Если `minDeliveryDays` больше 18 дней, разница должна быть не больше чем в два раза. Например, если `minDeliveryDays`
            равно 21, то для `maxDeliveryDays` допускаются значения от 21 до 42.


            Обязательный параметр, если `type="DEPOT"` или `type="MIXED"`.


            Взаимоисключающий с параметром `unspecifiedDeliveryInterval`.

            '
          type: integer
          format: int32
          minimum: 0
          maximum: 60
        maxDeliveryDays:
          description: 'Максимальный срок доставки товаров в точку продаж. Указан в рабочих днях.


            Минимальное значение: `0` — доставка в день заказа.


            Максимальное значение: `60`.


            Допустимые сроки доставки (разница между `minDeliveryDays` и `maxDeliveryDays`) зависят от региона.


            Для доставки по своему региону разница не должна превышать двух дней. Например, если `minDeliveryDays` равно 1,
            то для `maxDeliveryDays` допускаются значения от 1 до 3.


            Для доставки в другие регионы:


            * Если `minDeliveryDays` до 18 дней, разница не должна превышать четырех дней. Например, если `minDeliveryDays`
            равно 10, то для `maxDeliveryDays` допускаются значения от 10 до 14.

            * Если `minDeliveryDays` больше 18 дней, разница должна быть не больше чем в два раза. Например, если `minDeliveryDays`
            равно 21, то для `maxDeliveryDays` допускаются значения от 21 до 42.


            Обязательный параметр, если `type="DEPOT"` или `type="MIXED"`.


            Взаимоисключающий с параметром `unspecifiedDeliveryInterval`.

            '
          type: integer
          format: int32
          minimum: 0
          maximum: 60
        deliveryServiceId:
          description: 'Идентификатор службы доставки товаров в точку продаж.


            Информацию о службе доставки можно получить с помощью запроса [GET delivery/services](../../reference/delivery-services/getDeliveryServices.md).

            '
          type: integer
          format: int64
          minimum: 1
        orderBefore:
          description: 'Час, до которого покупателю нужно сделать заказ, чтобы он был доставлен в точку продаж в сроки от
            `minDeliveryDays` до `maxDeliveryDays`.


            Если покупатель оформит заказ после указанного часа, он будет доставлен в сроки от `minDeliveryDays` + 1 рабочий
            день до `maxDeliveryDays` + 1 рабочий день.


            Значение по умолчанию: `24`.

            '
          type: integer
          format: int32
          minimum: 0
          maximum: 24
        priceFreePickup:
          description: Цена товара, начиная с которой действует бесплатный самовывоз товара из точки продаж.
          type: number
        unspecifiedDeliveryInterval:
          description: 'Признак доставки товаров в точку продаж на заказ.


            Признак выставлен, если:


            * точный срок доставки в точку продаж заранее неизвестен (например, если магазин собирает несколько заказов для
            отправки в точку или населенный пункт);

            * все товары изготавливаются или поставляются на заказ.


            Возможные значения:

            * `true` — товары доставляются в точку продаж на заказ.


            Параметр указывается только со значением `true`.


            Взаимоисключающий с параметрами `minDeliveryDays` и `maxDeliveryDays`.

            '
          type: boolean
    OutletLicenseDTO:
      description: Информация о лицензии.
      type: object
      required:
      - outletId
      - licenseType
      - number
      - dateOfIssue
      - dateOfExpiry
      properties:
        id:
          description: 'Идентификатор лицензии.


            Параметр указывается, только если нужно изменить информацию о существующей лицензии. Ее идентификатор можно узнать
            с помощью запроса [GET v2/campaigns/{campaignId}/outlets/licenses](../../reference/outlet-licenses/getOutletLicenses.md).
            При передаче информации о новой лицензии указывать идентификатор не нужно.


            Идентификатор лицензии присваивается Маркетом. Не путайте его с номером, указанным на лицензии: он передается
            в параметре `number`.

            '
          type: integer
          format: int64
        outletId:
          description: Идентификатор точки продаж, для которой действительна лицензия.
          type: integer
          format: int64
          minimum: 1
        licenseType:
          $ref: '#/components/schemas/LicenseType'
        number:
          description: Номер лицензии.
          type: string
        dateOfIssue:
          description: 'Дата выдачи лицензии.


            Формат даты: ISO 8601 со смещением относительно UTC. Нужно передать дату, указанную на лицензии, время `00:00:00`
            и часовой пояс, соответствующий региону точки продаж. Например, если лицензия для точки продаж в Москве выдана
            13 ноября 2017 года, то параметр должен иметь значение `2017-11-13T00:00:00+03:00`.


            Не может быть позже даты окончания срока действия, указанной в параметре `dateOfExpiry`.

            '
          type: string
          format: date-time
          example: '2017-11-13T00:00:00+03:00'
        dateOfExpiry:
          description: 'Дата окончания действия лицензии.


            Формат даты: ISO 8601 со смещением относительно UTC. Нужно передать дату, указанную на лицензии, время `00:00:00`
            и часовой пояс, соответствующий региону точки продаж. Например, если действие лицензии для точки продаж в Москве
            заканчивается 20 ноября 2022 года, то параметр должен иметь значение `2022-11-20T00:00:00+03:00`.


            Не может быть раньше даты выдачи, указанной в параметре `dateOfIssue`.

            '
          type: string
          format: date-time
          example: '2022-11-20T00:00:00+03:00'
    OutletLicensesResponseDTO:
      description: Ответ на запрос информации о лицензиях для точек продаж.
      type: object
      required:
      - licenses
      properties:
        licenses:
          description: Список лицензий.
          type: array
          items:
            $ref: '#/components/schemas/FullOutletLicenseDTO'
    OutletResponseDTO:
      description: 'Результат выполнения запроса.

        Выводится, если `status="OK"`.

        '
      type: object
      properties:
        id:
          description: Идентификатор точки продаж, присвоенный Маркетом.
          type: integer
          format: int64
    OutletStatusType:
      description: 'Статус точки продаж.


        Возможные значения:


        * `AT_MODERATION` — проверяется.

        * `FAILED` — не прошла проверку и отклонена модератором.

        * `MODERATED` — проверена и одобрена.

        * `NONMODERATED` — новая точка, нуждается в проверке.

        * `UNKNOWN` — статус не указан. При определении статуса произошла ошибка.

        '
      type: string
      enum:
      - AT_MODERATION
      - FAILED
      - MODERATED
      - NONMODERATED
      - UNKNOWN
    OutletType:
      description: 'Тип точки продаж.


        Возможные значения:


        * `DEPOT` — пункт выдачи заказов.

        * `MIXED` — смешанный тип точки продаж (торговый зал и пункт выдачи заказов).

        * `RETAIL` — розничная точка продаж (торговый зал).

        * `NOT_DEFINED` — неизвестный тип точки продажи. При определении типа произошла ошибка.

        '
      type: string
      enum:
      - DEPOT
      - MIXED
      - RETAIL
      - NOT_DEFINED
    OutletVisibilityType:
      description: 'Состояние точки продаж.


        Возможные значения:


        * `HIDDEN` — точка продаж выключена.

        * `VISIBLE` — точка продаж включена.

        * `UNKNOWN` — неизвестное состояние точки продажи. При определении состояния произошла ошибка.

        '
      type: string
      enum:
      - HIDDEN
      - VISIBLE
      - UNKNOWN
    OutletWorkingScheduleDTO:
      description: 'Список режимов работы точки продаж.

        '
      type: object
      required:
      - scheduleItems
      properties:
        workInHoliday:
          description: 'Признак, работает ли точка продаж в дни государственных праздников.


            Возможные значения:


            * `false` — точка продаж не работает в дни государственных праздников.

            * `true` — точка продаж работает в дни государственных праздников.

            '
          type: boolean
        scheduleItems:
          description: 'Список расписаний работы точки продаж.

            '
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OutletWorkingScheduleItemDTO'
    OutletWorkingScheduleItemDTO:
      description: Расписание работы точки продаж.
      type: object
      required:
      - startDay
      - endDay
      - startTime
      - endTime
      properties:
        startDay:
          $ref: '#/components/schemas/DayOfWeekType'
          description: 'Точка продаж работает с указанного дня недели.


            Возможные значения:


            * `MONDAY` — понедельник.

            * `TUESDAY` — вторник.

            * `WEDNESDAY` — среда.

            * `THURSDAY` — четверг.

            * `FRIDAY` — пятница.

            * `SATURDAY` — суббота.

            * `SUNDAY` — воскресенье.

            '
        endDay:
          $ref: '#/components/schemas/DayOfWeekType'
          description: 'Точка продаж работает до указанного дня недели.


            Возможные значения:


            * `MONDAY` — понедельник.

            * `TUESDAY` — вторник.

            * `WEDNESDAY` — среда.

            * `THURSDAY` — четверг.

            * `FRIDAY` — пятница.

            * `SATURDAY` — суббота.

            * `SUNDAY` — воскресенье.

            '
        startTime:
          description: 'Точка продаж работает c указанного часа.


            Формат: `ЧЧ:ММ`.

            '
          type: string
          pattern: ^([0-1][0-9]|2[0-3]):[0-5][0-9]$
          example: 09:59
        endTime:
          description: 'Точка продаж работает до указанного часа.


            Формат: `ЧЧ:ММ`.

            '
          type: string
          pattern: ^([0-1][0-9]|2[0-3]):[0-5][0-9]$
          example: '23:59'
    PackagingForwardScrollingPagerDTO:
      description: 'Идентификатор следующей страницы.

        '
      type: object
      properties:
        nextPageToken:
          description: Идентификатор следующей страницы результатов.
          type: string
          x-serialize-transform: token
    PackagingScrollingPagerDTO:
      description: Информация о страницах результатов.
      type: object
      properties:
        nextPageToken:
          description: Идентификатор следующей страницы результатов.
          type: string
          x-serialize-transform: token
        prevPageToken:
          description: Идентификатор предыдущей страницы результатов.
          type: string
          deprecated: true
          x-serialize-transform: token
    PageFormatType:
      description: "Размещение ярлыков на странице PDF-файла:\n\n* `A9_HORIZONTALLY` — ярлык размером 58 × 40 мм без полей,\
        \ близок к формату :no-translate[A9].\n\n  {% cut \"Пример ярлыка для продавцов Маркета\" %}\n\n  ![Изображение горизонтального\
        \ ярлыка формата :no-translate[A9] для продавцов Маркета](../../_images/labels/label-A9-horizontally.png)\n\n  {%\
        \ endcut %}\n\n  {% cut \"Пример ярлыка для продавцов Market Yandex Go\" %}\n\n  ![Изображение горизонтального ярлыка\
        \ формата A9 для продавцов Market Yandex Go](../../_images/labels/label-A9-horizontally-uz.png)\n\n  {% endcut %}\n\
        \n* `A9` — ярлык размером 40 × 58 мм без полей, близок к формату A9.\n\n  {% cut \"Пример ярлыка для продавцов Маркета\"\
        \ %}\n\n  ![Изображение вертикального ярлыка формата :no-translate[A9] для продавцов Маркета](../../_images/labels/label-A9.png)\n\
        \n  {% endcut %}\n\n  {% cut \"Пример ярлыка для продавцов Market Yandex Go\" %}\n\n  ![Изображение вертикального\
        \ ярлыка формата :no-translate[A9] для продавцов Market Yandex Go](../../_images/labels/label-A9-uz.png)\n\n  {% endcut\
        \ %}\n\n* `A7` — ярлык размером 75 × 120 мм (80,4 × 125,6 мм с учетом полей), близок к формату :no-translate[A7].\n\
        \n  {% cut \"Пример ярлыка для продавцов Маркета\" %}\n\n  ![Изображение ярлыка формата A7 для продавцов Маркета](../../_images/labels/label-A7.jpg)\n\
        \n  {% endcut %}\n\n  {% cut \"Пример ярлыка для продавцов Market Yandex Go\" %}\n\n  ![Изображение ярлыка формата\
        \ :no-translate[A7] для продавцов Market Yandex Go](../../_images/labels/label-A7-uz.png)\n\n  {% endcut %}\n\n* `A4`\
        \ — на листе A4 располагается ярлык того формата, который выбран в кабинете продавца на Маркете — перейдите на страницу\
        \ **Заказы** → **Заказы и отгрузки** → на вкладке нужной модели работы нажмите кнопку **Формат ярлыков**.\n"
      type: string
      enum:
      - A9_HORIZONTALLY
      - A9
      - A7
      - A4
    PagedReturnsDTO:
      description: Невыкупы или возвраты.
      type: object
      required:
      - returns
      properties:
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Информация о страницах с результатами.
        returns:
          description: Список невыкупов или возвратов.
          type: array
          items:
            $ref: '#/components/schemas/ReturnDTO'
    PagedWarehousesDTO:
      description: Информация о складах в кабинете.
      type: object
      required:
      - warehouses
      properties:
        warehouses:
          description: Список складов.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseDetailsDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
    PalletsCountDTO:
      type: object
      description: Количество палет в отгрузке.
      properties:
        planned:
          description: Количество палет, которое заявил продавец.
          type: integer
          format: int32
          minimum: 0
        fact:
          description: Количество палет, которое приняли в сортировочном центре.
          type: integer
          format: int32
          minimum: 0
    ParameterType:
      description: 'Тип данных:


        * `TEXT` — текст.

        * `ENUM` — список возможных значений.

        * `BOOLEAN` — `true` или `false`.

        * `NUMERIC` — число.

        '
      type: string
      enum:
      - TEXT
      - ENUM
      - BOOLEAN
      - NUMERIC
    ParameterValueConstraintsDTO:
      description: Ограничения на значения характеристик.
      type: object
      properties:
        minValue:
          description: Минимальное число.
          type: number
          format: double
        maxValue:
          description: Максимальное число.
          type: number
          format: double
        maxLength:
          description: Максимальная длина текста.
          type: integer
          format: int32
    ParameterValueDTO:
      description: 'Значение характеристики.

        '
      type: object
      required:
      - parameterId
      properties:
        parameterId:
          description: Идентификатор характеристики.
          type: integer
          format: int64
          minimum: 1
        unitId:
          description: Идентификатор единицы измерения. Если вы не передали параметр `unitId`, используется единица измерения
            по умолчанию.
          type: integer
          format: int64
        valueId:
          description: 'Идентификатор значения.


            - Обязательно указывайте идентификатор, если передаете значение из перечня допустимых значений, полученного от
            Маркета.

            - Не указывайте для собственных значений.

            - Только для характеристик типа `ENUM`.

            '
          type: integer
          format: int64
        value:
          description: 'Значение.


            Для характеристик типа `ENUM` передавайте:

            - вместе с `valueId`, если значение берете из справочника;

            - без `valueId`, если значение собственное.

            '
          type: string
    ParameterValueOptionDTO:
      description: Значение характеристики.
      type: object
      required:
      - id
      - value
      properties:
        id:
          description: Идентификатор значения.
          type: integer
          format: int64
        value:
          description: Значение.
          type: string
        description:
          description: Описание значения.
          type: string
    ParcelBoxDTO:
      description: Параметр отображает одно грузовое место.
      type: object
      properties:
        id:
          description: Идентификатор коробки в составе заказа.
          type: integer
          format: int64
        fulfilmentId:
          description: '{% note warning "Не используйте этот параметр." %}


             


            {% endnote %}

            '
          type: string
          pattern: ^[[a-zA-Z0-9]- ]*$
          deprecated: true
    ParcelBoxLabelDTO:
      description: Информация о ярлыке для коробки.
      type: object
      required:
      - url
      - supplierName
      - deliveryServiceName
      - orderId
      - orderNum
      - recipientName
      - boxId
      - fulfilmentId
      - place
      - weight
      - deliveryServiceId
      properties:
        url:
          $ref: '#/components/schemas/Url'
          description: 'Соответствует URL, по которому выполняется запрос [GET v2/campaigns/{campaignId}/orders/{orderId}/delivery/shipments/{shipmentId}/boxes/{boxId}/label](../../reference/order-labels/generateOrderLabel.md).

            '
        supplierName:
          description: Юридическое название магазина.
          type: string
        deliveryServiceName:
          description: Юридическое название службы доставки.
          type: string
        orderId:
          description: Идентификатор заказа в системе Маркета.
          type: integer
          format: int64
        orderNum:
          description: 'Идентификатор заказа в информационной системе магазина.


            Совпадает с `orderId`, если Маркету неизвестен номер заказа в системе магазина.

            '
          type: string
        recipientName:
          description: Фамилия и инициалы получателя заказа.
          type: string
        boxId:
          description: Идентификатор коробки.
          type: integer
          format: int64
        fulfilmentId:
          description: 'Идентификатор коробки в информационной системе магазина.


            Возвращается в формате: `номер заказа на Маркете-номер коробки`. Например, `7206821‑1`, `7206821‑2` и т. д.

            '
          type: string
        place:
          description: 'Номер коробки в заказе. Возвращается в формате: `номер места/общее количество мест`.

            '
          type: string
        weight:
          description: 'Общая масса всех товаров в заказе.


            Возвращается в формате `weight кг`.

            '
          type: string
          deprecated: true
        deliveryServiceId:
          description: Идентификатор службы доставки. Информацию о службе доставки можно получить с помощью запроса [GET delivery/services](../../reference/delivery-services/getDeliveryServices.md).
          type: string
        deliveryAddress:
          description: Адрес получателя.
          type: string
        shipmentDate:
          description: Дата отгрузки в формате `dd.MM.yyyy`.
          type: string
    ParcelBoxRequestDTO:
      description: Параметр отображает одно грузовое место. Вложенные поля больше не используются, передавайте параметр пустым.
      type: object
      properties:
        fulfilmentId:
          description: '{% note warning "Не используйте этот параметр." %}


             


            {% endnote %}

            '
          type: string
          pattern: ^[[a-zA-Z0-9]- ]*$
          deprecated: true
    ParcelRequestDTO:
      description: Информация о грузовых местах в заказе.
      type: object
      required:
      - boxes
      properties:
        boxes:
          description: Список грузовых мест. По его длине Маркет определяет количество мест.
          type: array
          items:
            $ref: '#/components/schemas/ParcelBoxRequestDTO'
          minItems: 1
    PartialCompensationBoundsDTO:
      description: 'Пределы суммы частичной компенсации. Используется только в элементе с `decisionType` = `PARTIAL_MONEY_REFUND`.

        '
      type: object
      required:
      - minAmount
      - maxAmount
      - maxPercent
      properties:
        minAmount:
          $ref: '#/components/schemas/BasePriceDTO'
        maxAmount:
          $ref: '#/components/schemas/BasePriceDTO'
        maxPercent:
          description: Верхний предел доли суммы позиции, которую можно компенсировать (в процентах).
          type: integer
          format: int64
    PartnerMarketCategoryId:
      description: 'Идентификатор категории на Маркете, к которой вы относите свой товар.


        {% note warning "Всегда указывайте, когда передаете `parameterValues`" %}


        Если при изменении характеристик передать `parameterValues` и не указать `marketCategoryId`, характеристики обновятся,
        но в ответе придет предупреждение (параметр `warnings`).


        Если не передать их оба, будет использована информация из устаревших параметров `params` и `category`, а `marketCategoryId`
        будет определен автоматически.


        {% endnote %}


        При изменении категории убедитесь, что характеристики товара и их значения в параметре `parameterValues` вы передаете
        для новой категории.


        Список категорий Маркета можно получить с помощью запроса  [POST v2/categories/tree](../../reference/categories/getCategoriesTree.md).

        '
      type: integer
      format: int64
      minimum: 0
      exclusiveMinimum: true
    PartnerShipmentWarehouseDTO:
      description: Данные о складе отправления.
      type: object
      required:
      - id
      properties:
        id:
          description: Идентификатор склада отправления.
          format: int64
          type: integer
          minimum: 1
        name:
          description: Наименование склада отправления.
          type: string
        address:
          description: Адрес склада отправления.
          type: string
    PaymentFrequencyType:
      description: 'Частота выплат:


        * `DAILY` — ежедневно.

        * `WEEKLY` — раз в неделю.

        * `BIWEEKLY` — раз в две недели.

        * `MONTHLY` — раз в месяц.


        Подробнее о графике выплат читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/introduction/rates/acquiring.html).

        '
      type: string
      enum:
      - DAILY
      - WEEKLY
      - BIWEEKLY
      - MONTHLY
    PeriodDateFrom:
      type: string
      format: date
      description: 'Начало периода, включительно.


        Формат даты: `ГГГГ-ММ-ДД`.

        '
      example: '2025-08-22'
    PeriodDateTo:
      type: string
      format: date
      description: 'Конец периода, включительно.


        Формат даты: `ГГГГ-ММ-ДД`.

        '
      example: '2025-09-22'
    PickupAddressDTO:
      type: object
      description: Адрес доставки.
      properties:
        country:
          type: string
          description: Страна.
          example: Россия
        city:
          type: string
          description: Город.
          example: Москва
        street:
          type: string
          description: Улица.
          example: Стрелецкая улица
        house:
          type: string
          description: Номер дома.
          example: 9к2
        postcode:
          type: string
          description: Почтовый индекс.
          example: '123518'
    PickupDeliveryOptionsDTO:
      type: object
      description: Информация о доставке в пункт выдачи.
      required:
      - pickupOptions
      properties:
        pickupOptions:
          type: array
          description: Информация о доставке в пункт выдачи.
          items:
            $ref: '#/components/schemas/PickupOptionsDTO'
    PickupDeliveryParametersDTO:
      type: object
      description: 'Информация о доставке в пункт выдачи.


        Не передавайте вместе с `courierDelivery`.

        '
      required:
      - logisticPointsIds
      properties:
        logisticPointsIds:
          description: Идентификаторы пунктов выдачи.
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items:
            $ref: '#/components/schemas/LogisticPointId'
    PickupOptionDTO:
      type: object
      description: Временной интервал и стоимость доставки.
      required:
      - deliveryDateInterval
      - price
      properties:
        deliveryDateInterval:
          $ref: '#/components/schemas/DeliveryDateIntervalDTO'
        price:
          $ref: '#/components/schemas/DeliveryOptionPriceDTO'
    PickupOptionsDTO:
      type: object
      description: Временной интервал, идентификатор пункта выдачи и способ оплаты.
      required:
      - logisticPointId
      - options
      - paymentMethods
      properties:
        logisticPointId:
          $ref: '#/components/schemas/LogisticPointId'
        options:
          type: array
          description: Варианты доставки в ПВЗ.
          minItems: 1
          items:
            $ref: '#/components/schemas/PickupOptionDTO'
    PickupReturnDeliveryOptionsDTO:
      type: object
      description: Информация о пунктах выдачи.
      required:
      - logisticPointIds
      properties:
        logisticPointIds:
          type: array
          description: Идентификаторы пунктов выдачи.
          uniqueItems: true
          items:
            $ref: '#/components/schemas/LogisticPointId'
    PlacementType:
      description: 'Модель, по которой работает магазин:


        * `FBS` — FBS или Экспресс.

        * `FBY` — FBY.

        * `DBS` — DBS.

        * `LAAS` — LaaS.

        '
      type: string
      enum:
      - FBS
      - FBY
      - DBS
      - LAAS
    PriceCompetitivenessThresholdsDTO:
      description: Максимальные значения цены, при которых она является привлекательной или умеренной.
      type: object
      properties:
        optimalPrice:
          $ref: '#/components/schemas/BasePriceDTO'
          description: Максимальная привлекательная цена.
        averagePrice:
          $ref: '#/components/schemas/BasePriceDTO'
          description: Максимальная умеренная цена.
    PriceCompetitivenessType:
      description: 'Привлекательность цены:


        * `OPTIMAL` — привлекательная.

        * `AVERAGE` — умеренная.

        * `LOW` — непривлекательная.

        '
      type: string
      enum:
      - OPTIMAL
      - AVERAGE
      - LOW
    PriceDTO:
      description: Цена с указанием скидки, валюты и времени последнего обновления.
      type: object
      properties:
        value:
          description: Цена товара.
          type: number
          minimum: 0
          exclusiveMinimum: true
        discountBase:
          $ref: '#/components/schemas/DiscountBase'
        currencyId:
          $ref: '#/components/schemas/CurrencyType'
          description: 'Валюта, в которой указана цена товара.

            '
        vat:
          $ref: '#/components/schemas/VatType'
    PriceQuarantineVerdictDTO:
      description: Причина попадания товара в карантин.
      type: object
      required:
      - params
      properties:
        type:
          $ref: '#/components/schemas/PriceQuarantineVerdictType'
          description: Тип карантина.
        params:
          description: Цена, из-за которой товар попал в карантин, и значения для сравнения. Конкретный набор параметров зависит
            от типа карантина.
          type: array
          items:
            $ref: '#/components/schemas/PriceQuarantineVerdictParameterDTO'
    PriceQuarantineVerdictParamNameType:
      description: 'Имя параметра причины скрытия товара по цене.


        * `CURRENT_PRICE` — цена, из-за которой товар попал в карантин.

        * `LAST_VALID_PRICE` — последняя цена до попадания в карантин (только для карантина типа `PRICE_CHANGE`).

        * `MIN_PRICE` — порог попадания в карантин (только для карантина типов `LOW_PRICE` и `LOW_PRICE_PROMO`).

        * `CURRENCY` — валюта.

        '
      type: string
      enum:
      - CURRENT_PRICE
      - LAST_VALID_PRICE
      - MIN_PRICE
      - CURRENCY
    PriceQuarantineVerdictParameterDTO:
      description: Параметр карантина.
      type: object
      properties:
        name:
          $ref: '#/components/schemas/PriceQuarantineVerdictParamNameType'
          description: Название параметра.
        value:
          description: Значение параметра.
          type: string
      required:
      - name
      - value
    PriceQuarantineVerdictType:
      description: 'Типы карантина:


        * `PRICE_CHANGE` — новая цена слишком сильно отличается от прежней. В поле `params` будет  новая цена `CURRENT_PRICE`
        и последняя цена до попадания в карантин `LAST_VALID_PRICE`.

        * `LOW_PRICE` — установленная цена слишком сильно отличается от рыночной. В поле `params` будет установленная вами
        цена `CURRENT_PRICE` и порог попадания в карантин `MIN_PRICE`.

        * `LOW_PRICE_PROMO` — цена после применения акций слишком сильно отличается от рыночной. В поле `params` будет цена
        после применения акций `CURRENT_PRICE` и порог попадания в карантин `MIN_PRICE`.

        '
      type: string
      enum:
      - PRICE_CHANGE
      - LOW_PRICE
      - LOW_PRICE_PROMO
    PriceRecommendationItemDTO:
      description: Рекомендованная цена.
      type: object
      required:
      - campaignId
      - price
      properties:
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        price:
          description: 'Рекомендованная цена товара. Чтобы продвижение хорошо работало, цена товара должна быть не выше этого
            значения. [Подробно о рекомендованных ценах](https://yandex.ru/support/marketplace/marketing/campaigns.html#prices)

            '
          type: number
          minimum: 1
    PriceWithDiscountDTO:
      description: Цена с указанием скидки.
      type: object
      allOf:
      - $ref: '#/components/schemas/BasePriceDTO'
      - properties:
          discountBase:
            $ref: '#/components/schemas/DiscountBase'
    PromoOfferAutoParticipatingDetailsDTO:
      description: 'Информация об автоматическом добавлении товара в акцию.


        Причины, по которым товар не был добавлен автоматически в других магазинах, можно узнать в кабинете продавца на Маркете
        на странице акции.


        Об автоматическом и ручном добавлении товаров в акцию читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/index).

        '
      type: object
      properties:
        campaignIds:
          description: 'Идентификаторы кампаний тех магазинов, в которых товар добавлен в акцию автоматически.


            Возвращается, если статус товара в акции — `PARTIALLY_AUTO`.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
    PromoOfferDiscountParamsDTO:
      description: Параметры товара в акции с типом `DIRECT_DISCOUNT` или `BLUE_FLASH`.
      type: object
      properties:
        price:
          description: 'Зачеркнутая цена — та, по которой товар продавался до акции.


            Указывается в рублях.


            Возвращается, только если товар участвует в акции.

            '
          type: integer
          format: int64
        promoPrice:
          description: 'Цена по акции — та, по которой вы хотите продавать товар.


            Указывается в рублях.


            Возвращается, только если товар участвует в акции.

            '
          type: integer
          format: int64
        maxPromoPrice:
          description: 'Максимально возможная цена для участия в акции.

            Если значение не заполнено, ограничение отсутствует.


            Указывается в рублях.

            '
          type: integer
          format: int64
    PromoOfferParamsDTO:
      description: 'Параметры товара в акции.


        Возвращается параметр, который соответствует типу акции.

        '
      type: object
      properties:
        discountParams:
          $ref: '#/components/schemas/PromoOfferDiscountParamsDTO'
    PromoOfferParticipationStatusFilterType:
      description: 'Фильтр для товаров, которые добавлены в акцию вручную:


        * `MANUALLY_ADDED` — товары, которые добавлены вручную.


        * `NOT_MANUALLY_ADDED`— товары, которые не участвуют в акции и те, которые добавлены автоматически.


        Об автоматическом и ручном добавлении товаров в акцию читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/index).

        '
      type: string
      enum:
      - MANUALLY_ADDED
      - NOT_MANUALLY_ADDED
    PromoOfferParticipationStatusMultiFilterType:
      description: 'Фильтр для товаров, которые могут участвовать в акции:


        * `MANUALLY_ADDED` — товары, которые добавлены вручную.


        * `RENEWED` — товары, которые добавлены автоматически из предыдущей акции «Бестселлеры Маркета». Только для акций
        «Бестселлеры Маркета».


        * `RENEW_FAILED` — товары, которые не получилось перенести из предыдущей акции «Бестселлеры Маркета». Только для акций
        «Бестселлеры Маркета».


        * `NOT_MANUALLY_ADDED` — товары, которые не участвуют в акции и те, которые добавлены автоматически.


        * `MINIMUM_FOR_PROMOS` — товары с [установленным минимумом по цене для акций](*minimumForBestseller), который соответствует
        порогу `maxPromoPrice`. Такие товары участвуют в акции с ценой `maxPromoPrice`. Только для акций «Бестселлеры Маркета».


        Если не передать параметр `statuses`, вернутся все товары.


        Об автоматическом и ручном добавлении товаров в акцию читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/index).

        '
      type: string
      enum:
      - MANUALLY_ADDED
      - RENEWED
      - RENEW_FAILED
      - NOT_MANUALLY_ADDED
      - MINIMUM_FOR_PROMOS
    PromoOfferParticipationStatusType:
      description: 'Статус товара в акции:


        * `AUTO` — добавлен автоматически во всех магазинах кабинета, в которых товар доступен для покупки.


        * `PARTIALLY_AUTO` — добавлен автоматически у части магазинов.


        * `MANUAL` — добавлен вручную.


        * `NOT_PARTICIPATING` — не участвует в акции.


        * `RENEWED` — успешно перенесен из предыдущей акции «Бестселлеры Маркета». Только для акций «Бестселлеры Маркета».


        * `RENEW_FAILED` — не получилось перенести из предыдущей акции «Бестселлеры Маркета». Только для акций «Бестселлеры
        Маркета».


        * `MINIMUM_FOR_PROMOS` — участвует в акции с ценой `maxPromoPrice` ([установлен минимум по цене для акций](*minimumForBestseller),
        который соответствует порогу `maxPromoPrice`). Только для акций «Бестселлеры Маркета».


        Об автоматическом и ручном добавлении товаров в акцию читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/promos/market/index).

        '
      type: string
      enum:
      - AUTO
      - PARTIALLY_AUTO
      - MANUAL
      - NOT_PARTICIPATING
      - RENEWED
      - RENEW_FAILED
      - MINIMUM_FOR_PROMOS
    PromoOfferUpdateWarningCodeType:
      description: 'Предупреждение, которое появилось при добавлении товара:


        * `DEEP_DISCOUNT_OFFER` — большая разница с ценой в каталоге. Проверьте, нет ли ошибки.


        * `CATALOG_PRICE_IS_LOWER_THAN_PROMO` — цена, которая действует во всех магазинах, ниже цены по акции. У товара не
        будет отображаться цена по акции.


        * `SHOP_PRICES_ARE_LOWER_THAN_PROMO` — цена в отдельном магазине ниже цены по акции. У товара в акции будет отображаться
        цена в магазине. Для остальных магазинов будет действовать цена по акции.


        * `SHOP_OFFER_NOT_ELIGIBLE_FOR_PROMO` — товар в отдельном магазине не подходит под условия акции.

        '
      type: string
      enum:
      - DEEP_DISCOUNT_OFFER
      - CATALOG_PRICE_IS_LOWER_THAN_PROMO
      - SHOP_PRICES_ARE_LOWER_THAN_PROMO
      - SHOP_OFFER_NOT_ELIGIBLE_FOR_PROMO
    PromoOfferUpdateWarningDTO:
      description: Предупреждение, которое появилось при добавлении товара в акцию или изменении его цен.
      type: object
      required:
      - code
      properties:
        code:
          $ref: '#/components/schemas/PromoOfferUpdateWarningCodeType'
        campaignIds:
          description: 'Идентификаторы кампаний тех магазинов, для которых получены предупреждения.


            Не возвращается, если предупреждения действуют для всех магазинов в кабинете.

            '
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            $ref: '#/components/schemas/CampaignId'
    PromoParticipationType:
      description: 'Без указания фильтра возвращаются акции, в которых продавец участвует или может принять участие.


        Какие акции вернутся при указании фильтра:


        * `PARTICIPATING_NOW` — текущие акции, в которых участвует продавец.


        * `PARTICIPATED` — завершенные акции, в которых продавец участвовал за последний год. Если за год их было меньше 15,
        в ответе придут 15 последних акций за все время.

        '
      type: string
      enum:
      - PARTICIPATING_NOW
      - PARTICIPATED
    PromoPeriodDTO:
      description: Время проведения акции.
      type: object
      required:
      - dateTimeFrom
      - dateTimeTo
      properties:
        dateTimeFrom:
          description: Дата и время начала акции.
          type: string
          format: date-time
        dateTimeTo:
          description: Дата и время окончания акции.
          type: string
          format: date-time
    ProvideOrderDigitalCodesRequest:
      description: Запрос на передачу ключей цифровых товаров.
      type: object
      required:
      - items
      properties:
        items:
          description: 'Список проданных товаров.


            Для товара с одинаковым `id` передавайте один элемент и массив `codes` по количеству ключей.

            '
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/OrderDigitalItemDTO'
    ProvideOrderItemIdentifiersRequest:
      type: object
      required:
      - items
      properties:
        items:
          description: 'Список позиций, требующих маркировки.

            '
          type: array
          items:
            $ref: '#/components/schemas/OrderItemInstanceModificationDTO'
    ProvideOrderItemIdentifiersResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderItemsModificationResultDTO'
    PutSkuBidsRequest:
      description: description.
      type: object
      required:
      - bids
      properties:
        bids:
          description: Список товаров и ставки для продвижения, которые на них нужно установить.
          type: array
          minItems: 1
          maxItems: 1500
          items:
            $ref: '#/components/schemas/SkuBidItemDTO'
    QualityRatingAffectedOrderDTO:
      type: object
      description: Информация о заказе, который повлиял на индекс качества.
      required:
      - orderId
      - description
      - componentType
      properties:
        orderId:
          description: Идентификатор заказа.
          type: integer
          format: int64
          minimum: 0
        description:
          description: Описание проблемы.
          type: string
        componentType:
          $ref: '#/components/schemas/AffectedOrderQualityRatingComponentType'
          description: Тип составляющей индекса качества.
    QualityRatingComponentDTO:
      description: Составляющая индекса качества.
      type: object
      required:
      - value
      - componentType
      properties:
        value:
          description: Значение составляющей в процентах.
          minimum: 0
          maximum: 100
          type: number
          format: double
        componentType:
          $ref: '#/components/schemas/QualityRatingComponentType'
          description: Тип составляющей.
    QualityRatingComponentType:
      description: 'Составляющие индекса качества.


        **Для модели DBS:**

        * `DBS_CANCELLATION_RATE` — доля отмененных товаров.

        * `DBS_LATE_DELIVERY_RATE` — доля заказов, доставленных после плановой даты.


        **Для моделей FBS и Экспресс:**

        * `FBS_CANCELLATION_RATE` — доля отмененных товаров.

        * `FBS_LATE_SHIP_RATE` — доля не вовремя отгруженных заказов.


        **Для модели FBY:**

        * `FBY_LATE_DELIVERY_RATE` — доля товаров, которые приехали на склад с опозданием.

        * `FBY_CANCELLATION_RATE` — доля отмененных или недоставленных товаров.

        * `FBY_DELIVERY_DIFF_RATE` — доля товаров, которые не прибыли вместе с поставкой или которые не приняли.

        * `FBY_LATE_EDITING_RATE` — доля товаров, которые поздно убрали из заявки.

        '
      type: string
      enum:
      - DBS_CANCELLATION_RATE
      - DBS_LATE_DELIVERY_RATE
      - FBS_CANCELLATION_RATE
      - FBS_LATE_SHIP_RATE
      - FBY_LATE_DELIVERY_RATE
      - FBY_CANCELLATION_RATE
      - FBY_DELIVERY_DIFF_RATE
      - FBY_LATE_EDITING_RATE
    QualityRatingDTO:
      description: Информация об индексе качества.
      type: object
      required:
      - rating
      - calculationDate
      - components
      properties:
        rating:
          description: Значение индекса качества.
          minimum: 0
          maximum: 100
          type: integer
          format: int64
        calculationDate:
          description: 'Дата вычисления.


            Формат даты: `ГГГГ‑ММ‑ДД`.

            '
          type: string
          format: date
        components:
          description: Составляющие индекса качества.
          type: array
          items:
            $ref: '#/components/schemas/QualityRatingComponentDTO'
    QualityRatingDetailsDTO:
      type: object
      description: Информация о заказах, которые повлияли на индекс качества.
      required:
      - affectedOrders
      properties:
        affectedOrders:
          description: Список заказов, которые повлияли на индекс качества.
          type: array
          items:
            $ref: '#/components/schemas/QualityRatingAffectedOrderDTO'
    QuarantineOfferDTO:
      description: Товар в карантине.
      type: object
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        currentPrice:
          $ref: '#/components/schemas/BasePriceDTO'
          description: '{% note warning "Вместо него используйте значение из `verdicts` → `params`." %}


             


            {% endnote %}


            Новая цена.

            '
          deprecated: true
        lastValidPrice:
          $ref: '#/components/schemas/BasePriceDTO'
          description: '{% note warning "Вместо него используйте значение из `verdicts` → `params`." %}


             


            {% endnote %}


            Последняя цена до попадания в карантин.

            '
          deprecated: true
        verdicts:
          description: Причины попадания товара в карантин.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/PriceQuarantineVerdictDTO'
    QuestionDTO:
      type: object
      description: Вопрос.
      required:
      - questionIdentifiers
      - businessId
      - text
      - createdAt
      - votes
      - author
      properties:
        questionIdentifiers:
          $ref: '#/components/schemas/QuestionIdentifiersDTO'
        businessId:
          $ref: '#/components/schemas/BusinessId'
        text:
          $ref: '#/components/schemas/QuestionsTextContent'
        createdAt:
          description: Дата и время создания вопроса.
          type: string
          format: date-time
        votes:
          $ref: '#/components/schemas/VotesDTO'
        author:
          $ref: '#/components/schemas/QuestionsTextContentAuthorDTO'
    QuestionId:
      description: 'Идентификатор вопроса.

        '
      type: integer
      format: int64
      minimum: 1
    QuestionIdentifiersDTO:
      type: object
      description: Идентификаторы вопроса.
      required:
      - offerId
      - id
      properties:
        id:
          $ref: '#/components/schemas/QuestionId'
        categoryId:
          description: Идентификатор категории.
          type: integer
          format: int64
          minimum: 0
        offerId:
          $ref: '#/components/schemas/ShopSku'
    QuestionListDTO:
      type: object
      description: Список вопросов.
      required:
      - questions
      - totalCount
      properties:
        questions:
          type: array
          description: Список вопросов.
          items:
            $ref: '#/components/schemas/QuestionDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
        totalCount:
          description: 'Общее количество вопросов, которые попадают под фильтр.

            '
          type: integer
          format: int64
    QuestionSortOrderType:
      type: string
      description: 'Порядок сортировки вопросов.

        * `CREATED_AT_DESC` — по дате создания вопроса по убыванию;

        * `CREATED_AT_ASC` — по дате создания вопроса по возрастанию.

        '
      enum:
      - CREATED_AT_DESC
      - CREATED_AT_ASC
    QuestionsTextContent:
      description: 'Текстовое содержимое.

        '
      type: string
      minLength: 1
      maxLength: 5000
    QuestionsTextContentAuthorDTO:
      description: Информация об авторе комментария.
      type: object
      properties:
        type:
          $ref: '#/components/schemas/QuestionsTextContentAuthorType'
        name:
          description: Имя автора или название кабинета.
          type: string
    QuestionsTextContentAuthorType:
      description: 'Тип автора:

        * `USER` — пользователь.

        * `BUSINESS` — кабинет.

        * `VENDOR` — производитель.

        * `BRAND` — бренд.

        '
      type: string
      enum:
      - USER
      - BUSINESS
      - VENDOR
      - BRAND
    QuestionsTextContentModerationStatusType:
      description: 'Статус модерации ответа или комментария:

        * `PUBLISHED` — опубликован.

        * `UNMODERATED` — не проверен.

        * `BANNED` — заблокирован.

        * `DELETED` — удален.

        '
      type: string
      enum:
      - PUBLISHED
      - UNMODERATED
      - BANNED
      - DELETED
    QuestionsTextEntityOperationType:
      description: 'Операция над ответом или комментарием.

        * `UPDATE` — обновление.

        * `CREATE` — создание.

        * `DELETE` — удаление.

        '
      type: string
      enum:
      - UPDATE
      - CREATE
      - DELETE
    QuestionsTextEntityType:
      description: 'Тип сущности:


        * `QUESTION` — вопрос о товаре.

        * `ANSWER` — ответ на вопрос.

        * `COMMENT` — комментарий к ответу.

        '
      type: string
      enum:
      - QUESTION
      - ANSWER
      - COMMENT
    RecipientType:
      description: 'Способ возврата товара покупателем:


        * `SHOP` — в точку возврата магазина.


        * `DELIVERY_SERVICE` — отправить курьером.


        * `POST` — почта.

        '
      type: string
      enum:
      - SHOP
      - DELIVERY_SERVICE
      - POST
    RefundStatusType:
      description: 'Статус возврата денег:


        * `STARTED_BY_USER` — создан покупателем из личного кабинета.


        * `REFUND_IN_PROGRESS` — ждет решение о возврате денег (на рассмотрении).


        * `REFUNDED` — деньги возвращены.


        * `FAILED` — невозможно провести возврат покупателю.


        * `WAITING_FOR_DECISION` — ожидает решения (DBS).


        * `DECISION_MADE` — по возврату принято решение (DBS).


        * `REFUNDED_WITH_BONUSES` — возврат осуществлен баллами Плюса или промокодом.


        * `REFUNDED_BY_SHOP` — магазин сделал самостоятельно возврат денег.


        * `COMPLETE_WITHOUT_REFUND` — возврат денег не требуется.


        * `CANCELLED` — возврат отменен.


        * `REJECTED` — возврат отклонен модерацией или в ПВЗ.


        * `PREMODERATION_DISPUTE` — по возврату открыт спор (FBY, FBS и Экспресс).


        * `PREMODERATION_DECISION_WAITING` — ожидает решения (FBY, FBS и Экспресс).


        * `PREMODERATION_DECISION_MADE` — по возврату принято решение (FBY, FBS и Экспресс).


        * `PREMODERATION_SELECT_DELIVERY` — пользователь выбирает способ доставки (FBY, FBS и Экспресс).


        * `UNKNOWN` — неизвестный статус, обратитесь в поддержку.

        '
      type: string
      enum:
      - STARTED_BY_USER
      - REFUND_IN_PROGRESS
      - REFUNDED
      - FAILED
      - WAITING_FOR_DECISION
      - DECISION_MADE
      - REFUNDED_WITH_BONUSES
      - REFUNDED_BY_SHOP
      - CANCELLED
      - REJECTED
      - COMPLETE_WITHOUT_REFUND
      - PREMODERATION_DISPUTE
      - PREMODERATION_DECISION_WAITING
      - PREMODERATION_DECISION_MADE
      - PREMODERATION_SELECT_DELIVERY
      - UNKNOWN
    RegionDTO:
      description: Регион доставки.
      type: object
      required:
      - id
      - name
      - type
      properties:
        id:
          description: Идентификатор региона.
          type: integer
          format: int64
        name:
          description: Название региона.
          type: string
        type:
          $ref: '#/components/schemas/RegionType'
          description: Тип региона.
        parent:
          $ref: '#/components/schemas/RegionDTO'
          description: 'Информация о родительском регионе.


            Указываются родительские регионы до уровня страны.

            '
    RegionType:
      description: 'Тип региона.


        Возможные значения:


        * `CITY_DISTRICT` — район города.


        * `CITY` — крупный город.


        * `CONTINENT` — континент.


        * `COUNTRY_DISTRICT` — область.


        * `COUNTRY` — страна.


        * `REGION` — регион.


        * `REPUBLIC_AREA` — район субъекта федерации.


        * `REPUBLIC` — субъект федерации.


        * `SUBWAY_STATION` — станция метро.


        * `VILLAGE` — город.


        * `OTHER` — неизвестный регион.

        '
      type: string
      enum:
      - OTHER
      - CONTINENT
      - REGION
      - COUNTRY
      - COUNTRY_DISTRICT
      - REPUBLIC
      - CITY
      - VILLAGE
      - CITY_DISTRICT
      - SUBWAY_STATION
      - REPUBLIC_AREA
    RegionWithChildrenDTO:
      description: Информация о родительском и дочерних регионах.
      type: object
      allOf:
      - $ref: '#/components/schemas/RegionDTO'
      - properties:
          children:
            description: Дочерние регионы.
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/RegionDTO'
    RejectedPromoOfferDeleteDTO:
      description: Информация о товаре и ошибки, которые появились при его удалении.
      type: object
      required:
      - offerId
      - reason
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        reason:
          $ref: '#/components/schemas/RejectedPromoOfferDeleteReasonType'
    RejectedPromoOfferDeleteReasonType:
      description: 'Описание ошибки:


        * `OFFER_DOES_NOT_EXIST` — в кабинете нет товара с таким SKU.

        '
      type: string
      enum:
      - OFFER_DOES_NOT_EXIST
    RejectedPromoOfferUpdateDTO:
      description: Описание отклоненного изменения.
      type: object
      required:
      - offerId
      - reason
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        reason:
          $ref: '#/components/schemas/RejectedPromoOfferUpdateReasonType'
    RejectedPromoOfferUpdateReasonType:
      description: 'Причина отклонения изменения:


        * `OFFER_DOES_NOT_EXIST` — в кабинете нет товара с таким SKU.


        * `OFFER_DUPLICATION` — один и тот же товар передан несколько раз.


        * `OFFER_NOT_ELIGIBLE_FOR_PROMO` — товар не подходит под условия акции.


        * `OFFER_PROMOS_MAX_BYTE_SIZE_EXCEEDED` — товар не добавлен в акцию по техническим причинам.


        * `DEADLINE_FOR_FOCUS_PROMOS_EXCEEDED` — истек срок добавления товаров в акцию.


        * `EMPTY_OLD_PRICE` — не указана зачеркнутая цена.


        * `EMPTY_PROMO_PRICE` — не указана цена по акции.


        * `MAX_PROMO_PRICE_EXCEEDED` — цена по акции превышает максимально возможную цену для участия в акции.


        * `PROMO_PRICE_BIGGER_THAN_MAX` — цена по акции больше 95% от зачеркнутой цены.


        * `PROMO_PRICE_SMALLER_THAN_MIN` — цена по акции меньше 1% от зачеркнутой цены.


        * `PRICE_TOO_BIG` — слишком большая цена по акции.


        * `OLD_PRICE_TOO_BIG` — слишком большая зачеркнутая цена.

        '
      type: string
      enum:
      - OFFER_DOES_NOT_EXIST
      - OFFER_DUPLICATION
      - OFFER_NOT_ELIGIBLE_FOR_PROMO
      - OFFER_PROMOS_MAX_BYTE_SIZE_EXCEEDED
      - DEADLINE_FOR_FOCUS_PROMOS_EXCEEDED
      - EMPTY_OLD_PRICE
      - EMPTY_PROMO_PRICE
      - MAX_PROMO_PRICE_EXCEEDED
      - PROMO_PRICE_BIGGER_THAN_MAX
      - PROMO_PRICE_SMALLER_THAN_MIN
      - PRICE_TOO_BIG
      - OLD_PRICE_TOO_BIG
    ReportFormatType:
      description: 'Формат отчета:


        * `FILE` — файл с электронной таблицей (:no-translate[XLSX]).

        * `CSV` — ZIP-архив с CSV-файлами на каждый лист отчета.

        * `JSON` — ZIP-архив с JSON-файлами на каждый лист отчета.

        '
      type: string
      enum:
      - FILE
      - CSV
      - JSON
      default: FILE
    ReportInfoDTO:
      description: Статус генерации и ссылка на готовый отчет или документ.
      type: object
      required:
      - status
      - generationRequestedAt
      properties:
        status:
          $ref: '#/components/schemas/ReportStatusType'
        subStatus:
          $ref: '#/components/schemas/ReportSubStatusType'
        generationRequestedAt:
          description: Дата и время запроса на генерацию.
          type: string
          format: date-time
        generationFinishedAt:
          description: Дата и время завершения генерации.
          type: string
          format: date-time
        file:
          description: 'Ссылка на готовый отчет или документ.


            {% note warning "Срок действия ссылки" %}


            Ссылка актуальна **60 минут** с момента получения ответа. При каждом запросе `GET /v2/reports/info/{reportId}`
            генерируется новая ссылка, срок действия которой ограничен.


            **Рекомендация для интеграций:** сразу после получения ссылки скачайте отчет и сохраните его у себя. Не сохраняйте
            ссылку для последующего использования — она станет недействительной через после истечения срока действия.


            {% endnote %}

            '
          type: string
        estimatedGenerationTime:
          description: Ожидаемая продолжительность генерации в миллисекундах.
          type: integer
          format: int64
    ReportLanguageType:
      description: 'Язык отчета:


        * `RU` — русский язык.

        * `EN` — английский язык.

        '
      type: string
      enum:
      - RU
      - EN
    ReportStatusType:
      description: 'Статус генерации:


        * `PENDING` — ожидает генерации.

        * `PROCESSING` — генерируется.

        * `FAILED` — во время генерации произошла ошибка.

        * `DONE` — отчет или документ готов.

        '
      type: string
      enum:
      - PENDING
      - PROCESSING
      - FAILED
      - DONE
    ReportSubStatusType:
      description: 'Подстатус генерации:

        * `NO_DATA` — для такого отчета или документа нет данных.

        * `TOO_LARGE` — отчет или документ превысил допустимый размер — укажите меньший период времени или уточните условия
        запроса.

        * `RESOURCE_NOT_FOUND` — для такого отчета или документа не удалось найти часть сущностей.

        '
      type: string
      enum:
      - NO_DATA
      - TOO_LARGE
      - RESOURCE_NOT_FOUND
    ReturnAvailableDecisionDTO:
      description: Доступное решение по возврату.
      type: object
      required:
      - decisionType
      properties:
        decisionType:
          $ref: '#/components/schemas/ReturnDecisionType'
        decisionReasonTypes:
          description: Возможные причины отказа (только для решения DECLINE_REFUND).
          type: array
          nullable: true
          uniqueItems: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ReturnRequestDecisionReasonType'
        partialCompensationBounds:
          $ref: '#/components/schemas/PartialCompensationBoundsDTO'
          description: 'Пределы суммы частичной компенсации. Заполнено только при `decisionType` = `PARTIAL_MONEY_REFUND`;

            для остальных типов решений поле отсутствует.

            '
    ReturnAvailableDecisionsResponse:
      description: Возможные решения по возврату.
      type: object
      required:
      - availableDecisions
      properties:
        availableDecisions:
          description: Список доступных решений.
          type: array
          items:
            $ref: '#/components/schemas/ReturnAvailableDecisionDTO'
    ReturnDTO:
      description: "Невыкуп или возврат в заказе.\n\nПараметров `logisticPickupPoint`, `shipmentRecipientType` и `shipmentStatus`\
        \ может не быть в случае возврата:\n  * С опцией **Быстрый возврат денег за дешевый брак**, когда товар остается у\
        \ покупателя (`fastReturn=true`).\n  * По заказу от бизнеса, если:\n    * статус возврата `STARTED_BY_USER` или `WAITING_FOR_DECISION`;\n\
        \    * возврат отменен до передачи товара.\n\nСтатус возврата денег `refundStatus` актуален только для `returnType=RETURN`.\n"
      type: object
      required:
      - id
      - orderId
      - items
      - returnType
      properties:
        id:
          description: Идентификатор невыкупа или возврата.
          format: int64
          type: integer
        orderId:
          description: Номер заказа.
          format: int64
          type: integer
        creationDate:
          description: 'Дата создания невыкупа или возврата.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        updateDate:
          description: 'Дата обновления невыкупа или возврата.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        refundStatus:
          $ref: '#/components/schemas/RefundStatusType'
        logisticPickupPoint:
          $ref: '#/components/schemas/LogisticPickupPointDTO'
          description: Пункт вывоза.
        pickupTillDate:
          description: 'Дата, до которой можно забрать товар.


            Только для невыкупов и возвратов в логистическом статусе `READY_FOR_PICKUP`.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          type: string
          format: date-time
          example: '2020-02-02T14:30:30+03:00'
        shipmentRecipientType:
          $ref: '#/components/schemas/RecipientType'
          description: Способ возврата товара покупателем.
        shipmentStatus:
          $ref: '#/components/schemas/ReturnShipmentStatusType'
          description: Статус передачи возврата.
        refundAmount:
          description: '{% note warning "Вместо него используйте `amount`." %}


             


            {% endnote %}


            Сумма возврата в копейках.

            '
          type: integer
          format: int64
          deprecated: true
        amount:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Сумма возврата.
        items:
          description: Список товаров в невыкупе или возврате.
          type: array
          items:
            $ref: '#/components/schemas/ReturnItemDTO'
        returnType:
          $ref: '#/components/schemas/ReturnType'
          description: Тип возврата.
        fastReturn:
          description: 'Используется ли опция **Быстрый возврат денег за дешевый брак**.


            Актуально только для `returnType=RETURN`.

            '
          type: boolean
    ReturnDecisionDTO:
      description: Решения по возвратам.
      type: object
      properties:
        returnItemId:
          description: Идентификатор товара в возврате.
          type: integer
          format: int64
        count:
          description: Количество единиц товара.
          type: integer
          format: int32
        comment:
          description: Комментарий.
          type: string
        reasonType:
          $ref: '#/components/schemas/ReturnDecisionReasonType'
        subreasonType:
          $ref: '#/components/schemas/ReturnDecisionSubreasonType'
        decisionType:
          $ref: '#/components/schemas/ReturnDecisionType'
        refundAmount:
          description: '{% note warning "Вместо него используйте `amount`." %}


             


            {% endnote %}


            Сумма возврата в копейках.

            '
          type: integer
          format: int64
          deprecated: true
        amount:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Сумма возврата.
        partnerCompensation:
          description: '{% note warning "Вместо него используйте `partnerCompensationAmount`." %}


             


            {% endnote %}


            Компенсация за обратную доставку в копейках.

            '
          type: integer
          format: int64
          deprecated: true
        partnerCompensationAmount:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Компенсация за обратную доставку.
        images:
          description: Список хеш-кодов фотографий товара от покупателя.
          type: array
          nullable: true
          minItems: 1
          uniqueItems: true
          items:
            type: string
    ReturnDecisionReasonType:
      description: 'Причины возврата:


        * `BAD_QUALITY` — бракованный товар (есть недостатки).


        * `DOES_NOT_FIT` — товар не подошел.


        * `WRONG_ITEM` — привезли не тот товар.


        * `DAMAGE_DELIVERY` — товар поврежден при доставке.


        * `LOYALTY_FAIL` — невозможно установить виновного в браке/пересорте.


        * `CONTENT_FAIL` — ошибочное описание товара по вине Маркета.


        * `DELIVERY_FAIL` — товар не привезли.


        * `UNKNOWN` — причина не известна.

        '
      type: string
      enum:
      - BAD_QUALITY
      - DOES_NOT_FIT
      - WRONG_ITEM
      - DAMAGE_DELIVERY
      - LOYALTY_FAIL
      - CONTENT_FAIL
      - DELIVERY_FAIL
      - UNKNOWN
    ReturnDecisionSubreasonType:
      description: "Детали причин возврата:\n  * `DOES_NOT_FIT`:\n    * `USER_DID_NOT_LIKE` — товар не понравился.\n    *\
        \ `USER_CHANGED_MIND` — передумал покупать.\n    * `DELIVERED_TOO_LONG` — передумал покупать из-за длительного срока\
        \ доставки.\n\n  * `BAD_QUALITY`:\n    * `BAD_PACKAGE` — заводская упаковка повреждена.\n    * `DAMAGED` — царапины,\
        \ сколы.\n    * `NOT_WORKING` — не включается, не работает.\n    * `INCOMPLETENESS` — некомплект (не хватает детали\
        \ в наборе, к товару).\n    * `WRAPPING_DAMAGED` — транспортная упаковка повреждена.\n    * `ITEM_WAS_USED` — следы\
        \ использования на товаре.\n    * `BROKEN` — товар разбит.\n    * `BAD_FLOWERS` — некачественные цветы.\n\n  * `WRONG_ITEM`:\n\
        \    * `WRONG_ITEM` — не тот товар.\n    * `WRONG_COLOR` — цвет не соответствует заявленному.\n    * `DID_NOT_MATCH_DESCRIPTION`\
        \ — описание или характеристики не соответствуют заявленным.\n    * `WRONG_ORDER` — доставили чужой заказ.\n    *\
        \ `WRONG_AMOUNT_DELIVERED` — неверное количество товара.\n    * `PARCEL_MISSING` — часть заказа отсутствует.\n   \
        \ * `INCOMPLETE` — заказ не привезли полностью.\n\n  * `UNKNOWN` — детали причины не указаны.\n"
      type: string
      enum:
      - USER_DID_NOT_LIKE
      - USER_CHANGED_MIND
      - DELIVERED_TOO_LONG
      - BAD_PACKAGE
      - DAMAGED
      - NOT_WORKING
      - INCOMPLETENESS
      - WRONG_ITEM
      - WRONG_COLOR
      - DID_NOT_MATCH_DESCRIPTION
      - WRONG_ORDER
      - WRONG_AMOUNT_DELIVERED
      - WRAPPING_DAMAGED
      - ITEM_WAS_USED
      - BROKEN
      - BAD_FLOWERS
      - PARCEL_MISSING
      - INCOMPLETE
      - UNKNOWN
    ReturnDecisionType:
      description: 'Решение по возврату:


        * `FAST_REFUND_MONEY` — вернуть покупателю деньги без возврата товара.


        * `REFUND_MONEY` — вернуть покупателю деньги за товар.


        * `REFUND_MONEY_INCLUDING_SHIPMENT` — вернуть покупателю деньги за товар и обратную пересылку.


        * `REPAIR` — отремонтировать товар.


        * `REPLACE` — заменить товар.


        * `SEND_TO_EXAMINATION` — взять товар на экспертизу.


        * `DECLINE_REFUND` — отказать в возврате.


        * `PARTIAL_MONEY_REFUND` — частичный возврат денег.


        * `OTHER_DECISION` — другое решение.


        * `UNKNOWN` — не указано.

        '
      type: string
      enum:
      - FAST_REFUND_MONEY
      - REFUND_MONEY
      - REFUND_MONEY_INCLUDING_SHIPMENT
      - REPAIR
      - REPLACE
      - SEND_TO_EXAMINATION
      - DECLINE_REFUND
      - PARTIAL_MONEY_REFUND
      - OTHER_DECISION
      - UNKNOWN
    ReturnId:
      description: Идентификатор невыкупа или возврата.
      type: integer
      format: int64
      minimum: 1
    ReturnInstanceDTO:
      description: Логистическая информация по возврату.
      type: object
      properties:
        stockType:
          $ref: '#/components/schemas/ReturnInstanceStockType'
          description: Тип остатка на складе.
        status:
          $ref: '#/components/schemas/ReturnInstanceStatusType'
          description: Логистический статус конкретного товара.
        cis:
          description: Код идентификации единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz)
            (для продавцов :no-translate[Market Yandex Go]).
          type: string
        imei:
          description: Международный идентификатор мобильного оборудования.
          type: string
    ReturnInstanceStatusType:
      description: 'Логистический статус конкретного товара:


        * `CREATED` — возврат создан.


        * `RECEIVED` — возврат принят у отправителя.


        * `IN_TRANSIT` — возврат в пути.


        * `READY_FOR_PICKUP` — возврат готов к выдаче магазину.


        * `PICKED` — возврат выдан магазину.


        * `RECEIVED_ON_FULFILLMENT` — возврат принят на складе Маркета.


        * `CANCELLED` — возврат отменен.


        * `LOST` — возврат утерян.


        * `UTILIZED` — возврат утилизирован.


        * `PREPARED_FOR_UTILIZATION` — возврат готов к утилизации.


        * `EXPROPRIATED` — товары в возврате направлены на перепродажу.


        * `NOT_IN_DEMAND` — возврат не забрали с почты.

        '
      type: string
      enum:
      - CREATED
      - RECEIVED
      - IN_TRANSIT
      - READY_FOR_PICKUP
      - PICKED
      - RECEIVED_ON_FULFILLMENT
      - CANCELLED
      - LOST
      - UTILIZED
      - PREPARED_FOR_UTILIZATION
      - EXPROPRIATED
      - NOT_IN_DEMAND
    ReturnInstanceStockType:
      description: 'Тип остатка на складе:


        * `FIT` — годный.


        * `DEFECT` — бракованный.


        * `ANOMALY` — аномалия.


        * `SURPLUS` — лишний.


        * `EXPIRED` — просроченный.


        * `MISGRADING` — пересортица.


        * `UNDEFINED` — с неизвестным статусом.


        * `INCORRECT_IMEI` — товар с некорректным [IMEI](https://ru.wikipedia.org/wiki/IMEI).


        * `INCORRECT_SERIAL_NUMBER` — товар с некорректным серийным номером.


        * `INCORRECT_CIS` — товар с некорректным кодом идентификации единицы товара в системе [«Честный ЗНАК»](https://честныйзнак.рф/)
        или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов :no-translate[Market Yandex Go]).


        * `PART_MISSING` — недостача.


        * `NON_COMPLIENT` — товар с множественными несоответствиями.


        * `NOT_ACCEPTABLE` — товар, который Маркет не принимает.


        * `SERVICE` — сервисный сток.


        * `MARKDOWN` — уценка.


        * `DEMO` — демо.


        * `REPAIR` — ремонт.


        * `FIRMWARE` — прошивка.


        * `UNKNOWN` — неизвестный тип товара.

        '
      type: string
      enum:
      - FIT
      - DEFECT
      - ANOMALY
      - SURPLUS
      - EXPIRED
      - MISGRADING
      - UNDEFINED
      - INCORRECT_IMEI
      - INCORRECT_SERIAL_NUMBER
      - INCORRECT_CIS
      - PART_MISSING
      - NON_COMPLIENT
      - NOT_ACCEPTABLE
      - SERVICE
      - MARKDOWN
      - DEMO
      - REPAIR
      - FIRMWARE
      - UNKNOWN
    ReturnItemDTO:
      description: Список товаров в невыкупе или возврате.
      type: object
      required:
      - shopSku
      - count
      properties:
        marketSku:
          $ref: '#/components/schemas/MarketSku'
          description: Идентификатор карточки товара на Маркете.
        shopSku:
          $ref: '#/components/schemas/ShopSku'
        count:
          description: Количество единиц товара.
          type: integer
          format: int64
        decisions:
          description: Список решений по возврату.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ReturnDecisionDTO'
        instances:
          description: Список логистических позиций возврата.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ReturnInstanceDTO'
        tracks:
          description: Список трек-кодов для почтовых отправлений.
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/TrackDTO'
    ReturnItemDecisionComment:
      description: 'Комментарий к решению. Укажите:


        * для `REFUND_MONEY_INCLUDING_SHIPMENT`— стоимость обратной пересылки.


        * для `REPAIR` — когда вы устраните недостатки товара.


        * для `DECLINE_REFUND` — причину отказа.


        * для `OTHER_DECISION` — какое решение вы предлагаете.

        '
      type: string
    ReturnItemDecisionDTO:
      description: Решения по товару в возврате.
      type: object
      required:
      - returnItemId
      - decisionType
      properties:
        returnItemId:
          $ref: '#/components/schemas/ReturnItemId'
        decisionType:
          $ref: '#/components/schemas/ReturnRequestDecisionType'
          description: Решение по товару в возврате.
        decisionReasonType:
          $ref: '#/components/schemas/ReturnRequestDecisionReasonType'
          description: Причина отказа.
        comment:
          $ref: '#/components/schemas/ReturnItemDecisionComment'
        compensation:
          $ref: '#/components/schemas/BasePriceDTO'
          description: 'Сумма добровольной компенсации по позиции возврата.

            Указывайте только при `decisionType` = `PARTIAL_MONEY_REFUND`.

            '
    ReturnItemId:
      description: Идентификатор товара в возврате.
      type: integer
      format: int64
    ReturnRequestDecisionReasonType:
      description: 'Причина отказа:


        * `ISSUE_WITH_THE_PRODUCT_WAS_NOT_CONFIRMED` — проблема с товаром не подтвердилась.

        * `MECHANICAL_DAMAGE` — есть механические повреждения товара.

        * `WARRANTY_PERIOD_HAS_EXPIRED` — истек гарантийный срок.

        * `CONFIGURATION_OR_PACKAGING_COMPROMISED` — нарушена комплектация или упаковка.

        * `PRODUCT_APPEARANCE_COMPROMISED` — нарушен товарный вид.

        * `WARRANTY_TERMS_VIOLATED` — нарушены условия гарантии.

        * `DEVICE_ACTIVATED` — устройство активировано.

        '
      type: string
      enum:
      - ISSUE_WITH_THE_PRODUCT_WAS_NOT_CONFIRMED
      - MECHANICAL_DAMAGE
      - WARRANTY_PERIOD_HAS_EXPIRED
      - CONFIGURATION_OR_PACKAGING_COMPROMISED
      - PRODUCT_APPEARANCE_COMPROMISED
      - WARRANTY_TERMS_VIOLATED
      - DEVICE_ACTIVATED
    ReturnRequestDecisionType:
      description: 'Решение по возврату:


        * `FAST_REFUND_MONEY` — вернуть покупателю деньги без возврата товара.


        * `REFUND_MONEY` — вернуть покупателю деньги за товар.


        * `REFUND_MONEY_INCLUDING_SHIPMENT` — вернуть покупателю деньги за товар и обратную пересылку.


        * `REPAIR` — отремонтировать товар.


        * `REPLACE` — заменить товар.


        * `SEND_TO_EXAMINATION` — взять товар на экспертизу.


        * `DECLINE_REFUND` — отказать в возврате.


        * `PARTIAL_MONEY_REFUND` — частичный возврат денег.


        * `OTHER_DECISION` — другое решение.

        '
      type: string
      enum:
      - FAST_REFUND_MONEY
      - REFUND_MONEY
      - REFUND_MONEY_INCLUDING_SHIPMENT
      - REPAIR
      - REPLACE
      - SEND_TO_EXAMINATION
      - DECLINE_REFUND
      - PARTIAL_MONEY_REFUND
      - OTHER_DECISION
    ReturnShipmentStatusType:
      description: 'Статус передачи возврата или невыкупа:


        * `CREATED` — возврат или невыкуп создан покупателем (оформлен).


        * `RECEIVED` — возврат подготовлен к отправке (принят у покупателя).


        * `IN_TRANSIT` — возврат или невыкуп в пути (отправлен).


        * `READY_FOR_PICKUP` — возврат или невыкуп готов к выдаче магазину.


        * `PICKED` — возврат или невыкуп выдан магазину.


        * `LOST` — возврат или невыкуп утерян (при транспортировке).


        * `EXPIRED` — покупатель не принес товар на возврат вовремя (возврат отменен).


        * `CANCELLED` — возврат или невыкуп отменен.


        * `FULFILMENT_RECEIVED` — возврат или невыкуп принят на складе Маркета.


        * `PREPARED_FOR_UTILIZATION` — возврат или невыкуп передан в очередь на утилизацию.


        * `NOT_IN_DEMAND` — возврат или невыкуп не забрали с почты.


        * `UTILIZED` — возврат или невыкуп утилизирован.


        * `READY_FOR_EXPROPRIATION` — товары в возврате или невыкупе направлены на перепродажу (проверка перед реализацией).


        * `RECEIVED_FOR_EXPROPRIATION` — товары в возврате или невыкупе приняты для перепродажи (реализация).


        * `UNKNOWN` — неизвестный статус, обратитесь в поддержку.

        '
      type: string
      enum:
      - CREATED
      - RECEIVED
      - IN_TRANSIT
      - READY_FOR_PICKUP
      - PICKED
      - LOST
      - EXPIRED
      - CANCELLED
      - FULFILMENT_RECEIVED
      - PREPARED_FOR_UTILIZATION
      - NOT_IN_DEMAND
      - UTILIZED
      - READY_FOR_EXPROPRIATION
      - RECEIVED_FOR_EXPROPRIATION
      - UNKNOWN
    ReturnType:
      description: 'Тип фильтрации:


        * `UNREDEEMED` — невыкупы.


        * `RETURN` — возвраты.


        Если не указывать, в ответе будут и невыкупы, и возвраты.

        '
      type: string
      enum:
      - UNREDEEMED
      - RETURN
    ScheduleDayDTO:
      type: object
      description: День и время работы.
      required:
      - day
      - startTime
      - endTime
      properties:
        day:
          $ref: '#/components/schemas/DayOfWeekType'
          description: День недели.
        startTime:
          description: 'Время начала рабочего дня.


            Формат: `ЧЧ:ММ`.

            '
          type: string
          pattern: ^([0-1][0-9]|2[0-3]):[0-5][0-9]$
        endTime:
          description: 'Время конца рабочего дня.


            Формат: `ЧЧ:ММ`.

            '
          type: string
          pattern: ^([0-1][0-9]|2[0-3]):[0-5][0-9]$
    SearchShipmentsRequest:
      required:
      - dateFrom
      - dateTo
      description: Запрос информации об отгрузках.
      type: object
      properties:
        dateFrom:
          description: 'Начальная дата для фильтрации по дате отгрузки (включительно).


            Формат даты: `ДД-ММ-ГГГГ`.

            '
          type: string
          format: date
        dateTo:
          description: 'Конечная дата для фильтрации по дате отгрузки (включительно).


            Формат даты: `ДД-ММ-ГГГГ`.

            '
          type: string
          format: date
        statuses:
          description: Список статусов отгрузок.
          type: array
          uniqueItems: true
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/ShipmentStatusType'
            description: Статус отгрузки
        orderIds:
          description: Список идентификаторов заказов из отгрузок.
          type: array
          uniqueItems: true
          nullable: true
          minItems: 1
          items:
            description: Идентификатор заказа.
            type: integer
            format: int64
        cancelledOrders:
          description: 'Возвращать ли отмененные заказы.


            Значение по умолчанию: `true`. Если возвращать отмененные заказы не нужно, передайте значение `false`.

            '
          type: boolean
          default: true
    SearchShipmentsResponse:
      description: Ответ на запрос информации об отгрузках.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/SearchShipmentsResponseDTO'
    SearchShipmentsResponseDTO:
      description: Информация об отгрузках.
      type: object
      required:
      - shipments
      properties:
        shipments:
          description: Список с информацией об отгрузках.
          type: array
          items:
            $ref: '#/components/schemas/ShipmentInfoDTO'
        paging:
          $ref: '#/components/schemas/PackagingForwardScrollingPagerDTO'
          description: Страницы с результатами поиска.
    SellingProgramType:
      description: 'Модель работы:


        * `FBY` — FBY.

        * `FBS` — FBS.

        * `DBS` — DBS.

        * `EXPRESS` — Экспресс.

        * `LAAS` — LaaS.

        '
      type: string
      enum:
      - FBY
      - FBS
      - DBS
      - EXPRESS
      - LAAS
    SendFileToChatRequest:
      description: Информация о чате и отправляемом файле.
      type: object
      required:
      - file
      properties:
        file:
          description: Содержимое файла. Максимальный размер файла — 5 Мбайт.
          type: string
          format: binary
    SendMessageToChatRequest:
      description: В какой чат нужно отправить сообщение и текст сообщения.
      type: object
      required:
      - message
      properties:
        message:
          description: Текст сообщения.
          type: string
          minLength: 1
          maxLength: 4096
    SetOrderBoxLayoutRequest:
      type: object
      required:
      - boxes
      properties:
        boxes:
          description: Список коробок.
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderBoxLayoutDTO'
        allowRemove:
          description: Передайте `true`, если вы собираетесь удалить часть товаров из заказа.
          type: boolean
          default: false
    SetOrderBoxLayoutResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/OrderBoxesLayoutDTO'
    SetOrderDeliveryDateRequest:
      type: object
      required:
      - dates
      - reason
      properties:
        dates:
          $ref: '#/components/schemas/OrderDeliveryDateDTO'
          description: Информация о новой дате доставки заказа.
        reason:
          $ref: '#/components/schemas/OrderDeliveryDateReasonType'
          description: Причина переноса доставки заказа.
    SetOrderDeliveryTrackCodeRequest:
      type: object
      required:
      - trackCode
      - deliveryServiceId
      properties:
        trackCode:
          description: Трек‑номер посылки.
          type: string
        deliveryServiceId:
          description: Идентификатор службы доставки. Информацию о службе доставки можно получить с помощью запроса [GET delivery/services](../../reference/delivery-services/getDeliveryServices.md).
          type: integer
          format: int64
    SetOrderShipmentBoxesRequest:
      type: object
      allOf:
      - $ref: '#/components/schemas/ParcelRequestDTO'
    SetOrderShipmentBoxesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/ShipmentBoxesDTO'
    SetReturnDecisionRequest:
      description: Решения по товару в возврате.
      type: object
      required:
      - returnItemId
      - decisionType
      properties:
        returnItemId:
          $ref: '#/components/schemas/ReturnItemId'
        decisionType:
          $ref: '#/components/schemas/ReturnRequestDecisionType'
          description: Решение по товару в возврате.
        comment:
          $ref: '#/components/schemas/ReturnItemDecisionComment'
        compensation:
          $ref: '#/components/schemas/BasePriceDTO'
          description: 'Сумма добровольной компенсации по позиции возврата.

            Указывайте только при `decisionType` = `PARTIAL_MONEY_REFUND`.

            '
      example:
        returnItemId: 1
        decisionType: REFUND_MONEY_INCLUDING_SHIPMENT
        comment: Вернуть 149 рублей за пересылку.
    SetShipmentPalletsCountRequest:
      type: object
      description: Запрос на передачу количества упаковок в отгрузке.
      properties:
        placesCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество упаковок в отгрузке.
      required:
      - placesCount
    ShipmentActionType:
      description: 'Действия с отгрузкой:


        * `CONFIRM` — подтвердить отгрузку.

        * `DOWNLOAD_ACT` — скачать акт приема-передачи отгрузки.

        * `DOWNLOAD_INBOUND_ACT` — скачать список принятых заказов.

        * `DOWNLOAD_DISCREPANCY_ACT` — скачать акт расхождений.

        * `DOWNLOAD_TRANSPORTATION_WAYBILL` — скачать транспортную накладную.

        * `CHANGE_PALLETS_COUNT` — указать количество палет.

        '
      type: string
      enum:
      - CONFIRM
      - DOWNLOAD_ACT
      - DOWNLOAD_INBOUND_ACT
      - DOWNLOAD_DISCREPANCY_ACT
      - DOWNLOAD_TRANSPORTATION_WAYBILL
      - CHANGE_PALLETS_COUNT
    ShipmentBoxesDTO:
      description: 'В ответе Маркет возвращает переданный вами список грузовых мест. Не обращайте на это поле внимания.

        '
      type: object
      required:
      - boxes
      properties:
        boxes:
          description: 'Список грузовых мест. По его длине Маркет определил количество мест.

            '
          type: array
          items:
            $ref: '#/components/schemas/ParcelBoxDTO'
    ShipmentDTO:
      description: Информация об отгрузке.
      type: object
      allOf:
      - $ref: '#/components/schemas/BaseShipmentDTO'
      - $ref: '#/components/schemas/ExtensionShipmentDTO'
    ShipmentInfoDTO:
      description: Список с информацией об отгрузках.
      type: object
      allOf:
      - $ref: '#/components/schemas/BaseShipmentDTO'
      - properties:
          status:
            $ref: '#/components/schemas/ShipmentStatusType'
            description: Статус отгрузки.
          statusDescription:
            description: Описание статуса отгрузки.
            type: string
          statusUpdateTime:
            description: 'Время последнего изменения статуса отгрузки


              Формат даты: ISO 8601 со смещением относительно UTC.

              '
            format: date-time
            type: string
            example: '2017-11-21T00:00:00+03:00'
    ShipmentPalletLabelPageFormatType:
      description: 'Формат страницы:

        * `A4` — формат страницы :no-translate[A4].

        * `A8` — формат страницы :no-translate[A8].

        '
      type: string
      default: A8
      enum:
      - A4
      - A8
    ShipmentStatusChangeDTO:
      description: Статус отгрузки.
      type: object
      properties:
        status:
          $ref: '#/components/schemas/ShipmentStatusType'
          description: Статус отгрузки.
        description:
          description: Описание статуса отгрузки.
          type: string
        updateTime:
          description: 'Время последнего изменения статуса отгрузки.


            Формат даты: ISO 8601 со смещением относительно UTC.

            '
          format: date-time
          type: string
          example: '2017-11-21T00:00:00+03:00'
    ShipmentStatusType:
      description: 'Статус отгрузки:


        * `OUTBOUND_CREATED` — формируется.

        * `OUTBOUND_READY_FOR_CONFIRMATION` — можно обрабатывать.

        * `OUTBOUND_CONFIRMED` — подтверждена и готова к отправке.

        * `OUTBOUND_SIGNED` — по ней подписан электронный акт приема-передачи.

        * `ACCEPTED` — принята в сортировочном центре или пункте приема.

        * `ACCEPTED_WITH_DISCREPANCIES` — принята с расхождениями.

        * `FINISHED` — завершена.

        * `ERROR` — отменена из-за ошибки.

        '
      type: string
      enum:
      - OUTBOUND_CREATED
      - OUTBOUND_READY_FOR_CONFIRMATION
      - OUTBOUND_CONFIRMED
      - OUTBOUND_SIGNED
      - FINISHED
      - ACCEPTED
      - ACCEPTED_WITH_DISCREPANCIES
      - ERROR
    ShipmentType:
      description: 'Способ отгрузки заказов:


        * `IMPORT` — вы самостоятельно привозите заказы в выбранный сортировочный центр или пункт приема заказов.

        * `WITHDRAW` — вы отгружаете заказы со своего склада курьерам Яндекс Маркета.

        '
      type: string
      enum:
      - IMPORT
      - WITHDRAW
    ShopSku:
      description: 'Ваш SKU — идентификатор товара в вашей системе.


        Правила использования SKU:


        * У каждого товара SKU должен быть свой.


        * Уже заданный SKU нельзя освободить и использовать заново для другого товара. Каждый товар должен получать новый
        идентификатор, до того никогда не использовавшийся в вашем каталоге.


        SKU товара можно изменить в кабинете продавца на Маркете. О том, как это сделать, читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/assortment/operations/edit-sku).


        {% note warning %}


        Пробельные символы в начале и конце значения автоматически удаляются. Например, `"  SKU123  "` и `"SKU123"` будут
        обработаны как одинаковые значения.


        {% endnote %}


        [Что такое SKU и как его назначать](https://yandex.ru/support/marketplace/assortment/add/index.html#fields)

        '
      type: string
      pattern: ^(?=.*\S.*)[^\x00-\x08\x0A-\x1f\x7f]{1,255}$
      x-transform: trim
      minLength: 1
      maxLength: 255
    ShowcaseType:
      description: 'Тип витрины:


        * `B2B` — [Яндекс Маркет для бизнеса](https://business.market.yandex.ru/) (товары для юридических лиц и ИП). Подробнее
        о сервисе читайте в [Справке](https://yandex.ru/support/market-for-enterprise/ru/).

        * `B2C` — [основная витрина Маркета](https://market.yandex.ru/) (товары для физических лиц).

        '
      type: string
      enum:
      - B2B
      - B2C
    ShowcaseUrlDTO:
      description: Ссылка на товар на витрине и ее тип.
      type: object
      required:
      - showcaseType
      - showcaseUrl
      properties:
        showcaseType:
          $ref: '#/components/schemas/ShowcaseType'
          description: Тип витрины.
        showcaseUrl:
          description: Ссылка на товар.
          type: string
    ShowsSalesGroupingType:
      type: string
      description: 'Группировка данных отчета.

        Возможные значения:

        * `CATEGORIES` — группировка по категориям.

        * `OFFERS` — группировка по товарам.

        '
      enum:
      - CATEGORIES
      - OFFERS
    SignatureDTO:
      type: object
      description: Информация о подписи акта приема-передачи.
      required:
      - signed
      properties:
        signed:
          description: Подписан ли акт приема-передачи.
          type: boolean
    SkipGoodsFeedbackReactionRequest:
      description: Идентификаторы отзывов.
      type: object
      required:
      - feedbackIds
      properties:
        feedbackIds:
          description: Список идентификаторов отзывов, на которые магазин не будет отвечать.
          type: array
          minItems: 1
          maxItems: 50
          uniqueItems: true
          items:
            $ref: '#/components/schemas/GoodsFeedbackId'
    SkuBidItemDTO:
      description: Список товаров и ставок на них.
      type: object
      required:
      - sku
      - bid
      properties:
        sku:
          $ref: '#/components/schemas/ShopSku'
          description: SKU товара, которому соответствует ставка из параметра `bid`.
        bid:
          $ref: '#/components/schemas/Bid'
          description: 'Значение ставки для товара из параметра `sku`, от 50 до 9999.


            Указывается в процентах от стоимости товара и умножается на 100. Например, ставка 5% обозначается как 500.

            '
    SkuBidRecommendationItemDTO:
      description: Список товаров с рекомендованными ставками.
      type: object
      required:
      - sku
      - bid
      properties:
        sku:
          $ref: '#/components/schemas/ShopSku'
          description: SKU товара, которому соответствует ставка из параметра `bid`.
        bid:
          $ref: '#/components/schemas/Bid'
          description: 'Значение рекомендованной ставки для товара из параметра `sku`, от 50 до 9999.


            Указывается в процентах от стоимости товара и умножается на 100. Например, ставка 5% обозначается как 500.


            {% note tip "В ответе присутствует не пустой `bidRecommendations`" %}


            Не обращайте внимания на это поле.


            {% endnote %}


             

            '
        bidRecommendations:
          description: 'Список рекомендованных ставок с соответствующими долями показов и доступными дополнительными инструментами
            продвижения.


            Чем больше ставка, тем большую долю показов она помогает получить и тем больше дополнительных инструментов продвижения
            доступно.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/BidRecommendationItemDTO'
        priceRecommendations:
          description: Рекомендованные цены.
          deprecated: true
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/PriceRecommendationItemDTO'
    SortOrderType:
      type: string
      description: 'Направление сортировки:


        - `ASC` — сортировка по возрастанию.

        - `DESC` — сортировка по убыванию.

        '
      enum:
      - ASC
      - DESC
    SourceType:
      description: 'Тип кабинета:


        * `SELLER` — продавец.

        * `ADVERTISER` — рекламодатель.

        '
      type: string
      enum:
      - SELLER
      - ADVERTISER
    StatisticsAttributionType:
      type: string
      description: "Тип атрибуции:\n  * `CLICKS` — по кликам.\n  * `SHOWS` — по показам.\n{% if audience == \"partner\" %}\n\
        <br><br>\n\nО том, какие данные в отчете зависят и не зависят от типа атрибуции, читайте [в Справке Маркета для продавцов](https://yandex.ru/support2/marketplace/ru/marketing/shelf#stats).\n\
        {% endif %}\n"
      enum:
      - CLICKS
      - SHOWS
    SubmitReturnDecisionRequest:
      description: Запрос на подтверждение решения по возврату.
      type: object
      required:
      - returnItemDecisions
      properties:
        returnItemDecisions:
          description: Решения по товарам в возврате.
          minItems: 1
          type: array
          items:
            $ref: '#/components/schemas/ReturnItemDecisionDTO'
    SupplyRequestCountersDTO:
      type: object
      description: Количество товаров, коробок и палет в заявке.
      properties:
        planCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество товаров в заявке на поставку.
        factCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество товаров, которые приняты на складе.
        undefinedCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество непринятых товаров.
        surplusCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество лишних товаров.
        shortageCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество товаров с недостатками.
        defectCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество товаров с браком.
        acceptableCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество товаров, которые можно привезти дополнительно.
        unacceptableCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество товаров, которые нельзя привезти дополнительно.
        actualPalletsCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество палет, которые приняты на складе.
        actualBoxCount:
          type: integer
          format: int32
          minimum: 0
          description: Количество коробок, которые приняты на складе.
    SupplyRequestDTO:
      type: object
      description: Информация о заявке на поставку, вывоз или утилизацию.
      required:
      - id
      - type
      - subtype
      - status
      - updatedAt
      - counters
      - targetLocation
      properties:
        id:
          $ref: '#/components/schemas/SupplyRequestIdDTO'
          description: Идентификатор и номера заявки.
        type:
          $ref: '#/components/schemas/SupplyRequestType'
          description: Тип заявки.
        subtype:
          $ref: '#/components/schemas/SupplyRequestSubType'
          description: Подтип заявки.
        status:
          $ref: '#/components/schemas/SupplyRequestStatusType'
          description: Статус заявки.
        updatedAt:
          type: string
          format: date-time
          description: Дата и время последнего обновления заявки.
        counters:
          $ref: '#/components/schemas/SupplyRequestCountersDTO'
          description: Количество товаров, коробок и палет в заявке.
        parentLink:
          $ref: '#/components/schemas/SupplyRequestReferenceDTO'
          description: Ссылка на родительскую заявку.
          nullable: true
        childrenLinks:
          type: array
          nullable: true
          minItems: 1
          description: Ссылки на дочерние заявки.
          items:
            $ref: '#/components/schemas/SupplyRequestReferenceDTO'
        targetLocation:
          $ref: '#/components/schemas/SupplyRequestLocationDTO'
          description: Информация о складе хранения или ПВЗ.
        transitLocation:
          $ref: '#/components/schemas/SupplyRequestLocationDTO'
          description: Информация о транзитном складе или ПВЗ.
    SupplyRequestDocumentDTO:
      type: object
      required:
      - url
      - type
      - createdAt
      description: Документ по заявке.
      properties:
        type:
          $ref: '#/components/schemas/SupplyRequestDocumentType'
        url:
          $ref: '#/components/schemas/Url'
          description: Ссылка на документ.
        createdAt:
          type: string
          format: date-time
          description: Дата и время создания документа.
    SupplyRequestDocumentType:
      type: string
      description: "Тип документа:\n\n* **Документы, которые загружает магазин**\n  * `SUPPLY` — список товаров.\n  * `ADDITIONAL_SUPPLY`\
        \ — список товаров в дополнительной поставке.\n  * `VIRTUAL_DISTRIBUTION_CENTER_SUPPLY` — список товаров в [мультипоставке](*multisupply).\n\
        \  * `TRANSFER` — список товаров для утилизации.\n  * `WITHDRAW` — список товаров для вывоза.\n\n* **Поставка товаров**\n\
        \  * `VALIDATION_ERRORS` — ошибки по товарам в поставке.\n  * `CARGO_UNITS` — ярлыки для грузомест.\n\n* **Дополнительная\
        \ поставка и непринятые товары**\n  * `ADDITIONAL_SUPPLY_ACCEPTABLE_GOODS` — товары, которые подходят для дополнительной\
        \ поставки.\n  * `ADDITIONAL_SUPPLY_UNACCEPTABLE_GOODS` — вывоз непринятых товаров.\n\n* **Маркировка товаров**\n\
        \  * `INBOUND_UTD` — входящий УПД.\n  * `OUTBOUND_UTD` — исходящий УПД.\n  * `IDENTIFIERS` — коды маркировки товаров.\n\
        \  * `CIS_FACT` — принятые товары с кодами маркировки.\n  * `ITEMS_WITH_CISES` — товары, для которых нужна маркировка.\n\
        \  * `REPORT_OF_WITHDRAW_WITH_CISES` — маркированные товары для вывоза со склада.\n  * `SECONDARY_ACCEPTANCE_CISES`\
        \ — маркированные товары, которые приняты после вторичной приемки.\n  * `RNPT_FACT` — принятые товары с регистрационным\
        \ номером партии товара (РНПТ).\n\n* **Акты**\n  * `ACT_OF_WITHDRAW` — акт возврата.\n  * `ANOMALY_CONTAINERS_WITHDRAW_ACT`\
        \ — акт изъятия непринятого товара.\n  * `ACT_OF_WITHDRAW_FROM_STORAGE` — акт списания с ответственного хранения.\n\
        \  * `ACT_OF_RECEPTION_TRANSFER` — акт приема-передачи.\n  * `ACT_OF_DISCREPANCY` — акт о расхождениях.\n  * `SECONDARY_RECEPTION_ACT`\
        \ — акт вторичной приемки.\n"
      enum:
      - SUPPLY
      - ADDITIONAL_SUPPLY
      - VIRTUAL_DISTRIBUTION_CENTER_SUPPLY
      - TRANSFER
      - INBOUND_UTD
      - OUTBOUND_UTD
      - ADDITIONAL_SUPPLY_ACCEPTABLE_GOODS
      - ADDITIONAL_SUPPLY_UNACCEPTABLE_GOODS
      - VALIDATION_ERRORS
      - WITHDRAW
      - ACT_OF_WITHDRAW
      - ANOMALY_CONTAINERS_WITHDRAW_ACT
      - ACT_OF_WITHDRAW_FROM_STORAGE
      - ACT_OF_RECEPTION_TRANSFER
      - ACT_OF_DISCREPANCY
      - SECONDARY_RECEPTION_ACT
      - CARGO_UNITS
      - IDENTIFIERS
      - CIS_FACT
      - ITEMS_WITH_CISES
      - REPORT_OF_WITHDRAW_WITH_CISES
      - SECONDARY_ACCEPTANCE_CISES
      - RNPT_FACT
    SupplyRequestId:
      type: integer
      format: int64
      minimum: 1
      description: 'Идентификатор заявки.


        {% note warning "Используется только в API" %}


        По нему не получится найти заявки в кабинете продавца на Маркете. Для этого используйте `marketplaceRequestId` или
        `warehouseRequestId`.


        {% endnote %}

        '
    SupplyRequestIdDTO:
      type: object
      description: Идентификатор и номера заявки.
      required:
      - id
      properties:
        id:
          $ref: '#/components/schemas/SupplyRequestId'
        marketplaceRequestId:
          type: string
          description: 'Номер заявки на маркетплейсе.


            Также указывается в кабинете продавца на Маркете.

            '
        warehouseRequestId:
          type: string
          description: 'Номер заявки на складе.


            Также указывается в кабинете продавца на Маркете.

            '
    SupplyRequestItemCountersDTO:
      type: object
      description: Количество товаров в заявке.
      properties:
        planCount:
          description: Количество товаров в заявке на поставку.
          type: integer
          format: int32
          minimum: 0
        factCount:
          description: Количество товаров, которые приняты на складе.
          type: integer
          format: int32
          minimum: 0
        surplusCount:
          description: Количество лишних товаров.
          type: integer
          format: int32
          minimum: 0
        shortageCount:
          description: Количество товаров с недостатками.
          type: integer
          format: int32
          minimum: 0
        defectCount:
          description: Количество товаров с браком.
          type: integer
          format: int32
          minimum: 0
    SupplyRequestItemDTO:
      type: object
      description: Информация о товаре в заявке.
      required:
      - offerId
      - name
      - counters
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        name:
          description: Название товара.
          type: string
        price:
          $ref: '#/components/schemas/CurrencyValueDTO'
          description: Цена за единицу товара.
        counters:
          $ref: '#/components/schemas/SupplyRequestItemCountersDTO'
          description: Количество товаров в заявке.
    SupplyRequestLocationAddressDTO:
      type: object
      description: Адрес склада или ПВЗ.
      required:
      - fullAddress
      - gps
      properties:
        fullAddress:
          type: string
          description: Полный адрес склада или ПВЗ.
        gps:
          $ref: '#/components/schemas/GpsDTO'
    SupplyRequestLocationDTO:
      type: object
      description: Информации о складе или ПВЗ в заявке.
      required:
      - serviceId
      - name
      - address
      - type
      properties:
        requestedDate:
          type: string
          format: date-time
          description: Дата и время поставки на склад или в ПВЗ.
        serviceId:
          type: integer
          format: int64
          description: Идентификатор склада или логистического партнера ПВЗ.
        name:
          type: string
          description: Название склада или ПВЗ.
        address:
          $ref: '#/components/schemas/SupplyRequestLocationAddressDTO'
        type:
          $ref: '#/components/schemas/SupplyRequestLocationType'
    SupplyRequestLocationType:
      type: string
      description: 'Тип склада или ПВЗ:


        * `FULFILLMENT` — склад хранения.

        * `XDOC` — транзитный склад.

        * `PICKUP_POINT` — ПВЗ.

        '
      enum:
      - FULFILLMENT
      - XDOC
      - PICKUP_POINT
    SupplyRequestReferenceDTO:
      type: object
      description: Информация о связанных заявках.
      required:
      - id
      - type
      properties:
        id:
          $ref: '#/components/schemas/SupplyRequestIdDTO'
          description: Идентификаторы связанной заявки.
        type:
          $ref: '#/components/schemas/SupplyRequestReferenceType'
          description: Тип связи.
    SupplyRequestReferenceType:
      type: string
      description: "Тип связи между двумя заявками:\n\n* `VIRTUAL_DISTRIBUTION` — [мультипоставка](*multisupply).\n\n* `WITHDRAW`\
        \ — вывоз непринятых товаров.\n\n    Подтипы заявки: `DEFAULT`, `XDOC`, `VIRTUAL_DISTRIBUTION_CENTER_CHILD` и `ANOMALY_WITHDRAW`.\n\
        \n* `UTILIZATION` — утилизация непринятых товаров.\n\n    Подтипы заявки: `DEFAULT`, `XDOC`, `VIRTUAL_DISTRIBUTION_CENTER_CHILD`\
        \ и `FORCE_PLAN_ANOMALY_PER_SUPPLY`.\n\n* `ADDITIONAL_SUPPLY` — дополнительная поставка.\n\n    Подтипы заявки: `DEFAULT`,\
        \ `XDOC`, `VIRTUAL_DISTRIBUTION_CENTER_CHILD` и `ADDITIONAL_SUPPLY`.\n"
      enum:
      - VIRTUAL_DISTRIBUTION
      - WITHDRAW
      - UTILIZATION
      - ADDITIONAL_SUPPLY
    SupplyRequestSortAttributeType:
      type: string
      description: "По какому параметру сортировать заявки:\n\n* `ID` — идентификатор заявки.\n* `REQUESTED_DATE` — дата поставки\
        \ на склад хранения.\n\n    Если товары проходили через транзитный склад, сортирует по датам поставки на оба склада.\n\
        * `UPDATED_AT` — время обновления заявки.\n* `STATUS` — статус заявки.\n"
      enum:
      - ID
      - REQUESTED_DATE
      - UPDATED_AT
      - STATUS
    SupplyRequestSortingDTO:
      type: object
      description: Параметры сортировки.
      required:
      - direction
      - attribute
      properties:
        direction:
          $ref: '#/components/schemas/SortOrderType'
        attribute:
          $ref: '#/components/schemas/SupplyRequestSortAttributeType'
    SupplyRequestStatusType:
      type: string
      description: "Статус заявки на поставку:\n\n* `CREATED` — заявка создана.\n* `FINISHED` — заявка завершена, товары:\n\
        \  * приняты на складе;\n  * переданы на другой склад при перемещении;\n  * переданы продавцу при вывозе;\n  * утилизированы.\n\
        * `CANCELLED` — заявка отменена.\n* `INVALID` — ошибка обработки.\n* `VALIDATED` — заявка в обработке.\n* `PUBLISHED`\
        \ — заявка отправлена на утверждение.\n* `ARRIVED_TO_SERVICE` — поставка отгружена.\n* `ARRIVED_TO_XDOC_SERVICE` —\
        \ поставка ждет отправки на конечный склад.\n* `SHIPPED_TO_SERVICE` — поставка отправлена с транзитного склада на\
        \ склад хранения.\n* `CANCELLATION_REQUESTED` — запрошена отмена заявки.\n* `CANCELLATION_REJECTED` — заявка не будет\
        \ отменена.\n* `REGISTERED_IN_ELECTRONIC_QUEUE` — поставка зарегистрирована в электронной очереди.\n* `READY_FOR_UTILIZATION`\
        \ — товары готовы к утилизации.\n* `TRANSIT_MOVING` — перемещение товаров на склад вывоза.\n* `WAREHOUSE_HANDLING`\
        \ — вторичная приемка товаров или их сборка для вывоза или утилизации (потоварная приемка).\n* `ACCEPTED_BY_WAREHOUSE_SYSTEM`\
        \ — заявка утверждена.\n* `READY_TO_WITHDRAW` — товары готовы к выдаче.\n* `NEED_PREPARATION` — ожидается информация\
        \ от продавца.\n* `WAREHOUSE_SIGNED_ACT` — ЭАПП подписан складом.\n"
      enum:
      - CREATED
      - FINISHED
      - CANCELLED
      - INVALID
      - VALIDATED
      - PUBLISHED
      - ARRIVED_TO_SERVICE
      - ARRIVED_TO_XDOC_SERVICE
      - SHIPPED_TO_SERVICE
      - CANCELLATION_REQUESTED
      - CANCELLATION_REJECTED
      - REGISTERED_IN_ELECTRONIC_QUEUE
      - READY_FOR_UTILIZATION
      - TRANSIT_MOVING
      - WAREHOUSE_HANDLING
      - ACCEPTED_BY_WAREHOUSE_SYSTEM
      - READY_TO_WITHDRAW
      - NEED_PREPARATION
      - WAREHOUSE_SIGNED_ACT
    SupplyRequestSubType:
      type: string
      description: "Подтип заявки:\n\n* `DEFAULT` — поставка товаров на склад хранения или вывоз с него.\n* `XDOC` — поставка\
        \ товаров через транзитный склад или вывоз с него.\n* `INVENTORYING_SUPPLY` — инвентаризация на складе по запросу\
        \ магазина.\n* `INVENTORYING_SUPPLY_WAREHOUSE_BASED_PER_SUPPLIER` — инвентаризация на складе по запросу склада.\n\
        * `MOVEMENT_SUPPLY` — входящее перемещение между складами.\n\n    При перемещении между складами создаются 2 заявки\
        \ — `MOVEMENT_SUPPLY` и `MOVEMENT_WITHDRAW`.\n* `ADDITIONAL_SUPPLY` — дополнительная поставка непринятых товаров.\n\
        * `VIRTUAL_DISTRIBUTION_CENTER` — родительская заявка при поставке товаров на склад хранения или [мультипоставке](*multisupply).\n\
        * `VIRTUAL_DISTRIBUTION_CENTER_CHILD` — дочерняя заявка при поставке товаров на склад хранения или [мультипоставке](*multisupply).\n\
        \n    Для нее не возвращается `transitLocation`.\n* `FORCE_PLAN` — автоматическая утилизация по запросу склада.\n\
        * `FORCE_PLAN_ANOMALY_PER_SUPPLY` — утилизация непринятых товаров.\n* `PLAN_BY_SUPPLIER` — утилизация по запросу магазина.\n\
        * `ANOMALY_WITHDRAW` — вывоз непринятых товаров.\n* `FIX_LOST_INVENTORYING` — товары, которые не нашли после второй\
        \ инвентаризации.\n* `OPER_LOST_INVENTORYING` — товары, которые не нашли после первой инвентаризации.\n* `MOVEMENT_WITHDRAW`\
        \ — исходящее перемещение между складами.\n\n    При перемещении между складами создаются 2 заявки — `MOVEMENT_SUPPLY`\
        \ и `MOVEMENT_WITHDRAW`.\n* `MISGRADING_SUPPLY` — пересортица в большую сторону.\n* `MISGRADING_WITHDRAW` — пересортица\
        \ в меньшую сторону.\n* `MAN_UTIL` — ручная утилизация по запросу склада.\n* `WITHDRAW_AUTO_UTILIZATION` — автоматическая\
        \ утилизация товаров в заявке на вывоз, когда истек срок их хранения.\n* `EXTERNAL_WITHDRAW_INT_OZON` — вывоз товаров\
        \ на маркетплейс Ozon. Заявка на поставку оформлена в Личном кабинете Ozon.\n* `EXTERNAL_WITHDRAW_INT_WB` — вывоз\
        \ товаров на маркетплейс Wildberries. Заявка на поставку оформлена в Личном кабинете Wildberries.\n"
      enum:
      - DEFAULT
      - XDOC
      - INVENTORYING_SUPPLY
      - INVENTORYING_SUPPLY_WAREHOUSE_BASED_PER_SUPPLIER
      - MOVEMENT_SUPPLY
      - ADDITIONAL_SUPPLY
      - VIRTUAL_DISTRIBUTION_CENTER
      - VIRTUAL_DISTRIBUTION_CENTER_CHILD
      - FORCE_PLAN
      - FORCE_PLAN_ANOMALY_PER_SUPPLY
      - PLAN_BY_SUPPLIER
      - ANOMALY_WITHDRAW
      - FIX_LOST_INVENTORYING
      - OPER_LOST_INVENTORYING
      - MOVEMENT_WITHDRAW
      - MISGRADING_SUPPLY
      - MISGRADING_WITHDRAW
      - MAN_UTIL
      - WITHDRAW_AUTO_UTILIZATION
      - EXTERNAL_WITHDRAW_INT_OZON
      - EXTERNAL_WITHDRAW_INT_WB
    SupplyRequestType:
      type: string
      description: 'Тип заявки:


        * `SUPPLY` — поставка товаров.

        * `WITHDRAW` — вывоз товаров.

        * `UTILIZATION` — утилизация товаров.

        '
      enum:
      - SUPPLY
      - WITHDRAW
      - UTILIZATION
    TariffDTO:
      description: Информация о тарифах, по которым нужно заплатить за услуги Маркета.
      type: object
      required:
      - type
      - amount
      - currency
      - parameters
      properties:
        type:
          $ref: '#/components/schemas/TariffType'
          description: Услуга Маркета, за которую начисляется тариф.
        percent:
          description: Значение тарифа в процентах.
          deprecated: true
          type: number
        amount:
          description: Значение тарифа.
          type: number
        currency:
          $ref: '#/components/schemas/CurrencyType'
          description: Валюта, в которой указано значение тарифа.
        parameters:
          description: Параметры расчета тарифа.
          type: array
          items:
            $ref: '#/components/schemas/TariffParameterDTO'
    TariffParameterDTO:
      description: Детали расчета конкретной услуги Маркета.
      type: object
      required:
      - name
      - value
      properties:
        name:
          description: "{% cut \"Название параметра.\" %}\n\nПараметр `name` может принимать следующие значения:\n\n**Основные\
            \ параметры:**\n  - `value` — значение тарифа.\n  - `billingUnit` — единицы измерения для товара.\n  - `valueType`\
            \ — тип значения тарифа: `absolute` (денежное значение) или `relative` (процент).\n  - `priceDependence` — зависимость\
            \ тарифа от цены товара.\n\n**Параметры диапазонов цен товара:**\n- `priceFrom` — минимальная цена товара, от\
            \ которой применяется тариф.\n- `priceTo` — максимальная цена товара, до которой применяется тариф.\n\n**Параметры\
            \ ограничений значения тарифа:**\n- `minValue` — минимальное значение тарифа.\n- `maxValue` — максимальное значение\
            \ тарифа.\n\n**Параметры диапазонов дней:**\n- `dayFrom` — день, с которого начинает действовать тариф (для хранения).\n\
            - `dayTo` — день, на который тариф прекращает действие (для хранения).\n\n**Параметры маршрута транзитной поставки:**\n\
            - `xdocFrom` — начальная точка маршрута.\n- `xdocTo` — конечная точка маршрута.\n\n**Параметры частоты:**\n- `frequency`\
            \ — частота выплат.\n- `paymentDelayWeeks` — отсрочка выплат при еженедельном графике — сколько недель назад были\
            \ доставлены заказы, за которые приходит выплата.\n\n**Параметры складов и логистики:**\n- `transitWarehouseType`\
            \ — тип места привоза товаров продавцом.\n- `orderCargoType` — тип груза заказа.\n\n**Параметры оборачиваемости:**\n\
            - `turnoverFrom` — нижняя граница диапазона оборачиваемости, начиная с которой применяется тариф.\n- `turnoverTo`\
            \ — верхняя граница диапазона оборачиваемости, исключая которую применяется тариф.\n\n**Параметры рейтинга:**\n\
            - `ratingLowerBound` — нижняя граница диапазона рейтинга, по которому применяется тариф.\n- `ratingUpperBound`\
            \ — верхняя граница диапазона рейтинга, по которому применяется тариф.\n\n**Параметры количества заказов:**\n\
            - `fromOrders` — количество заказов, начиная с которого применяется тариф (включительно).\n- `toOrders` — количество\
            \ заказов, до которого применяется тариф (не включительно).\n\n**Параметры доставки из-за рубежа:**\n- `shippingPrice`\
            \ — стоимость отправления при доставке из-за рубежа.\n- `totalSurchargeForWeight` — сумма наценки за вес отправления\
            \ при доставке из-за рубежа.\n\n{% note info \"Не все параметры возвращаются для каждого тарифа.\" %}\n\nНабор\
            \ параметров зависит от типа услуги и условий применения тарифа.\n\n{% endnote %}\n\n{% endcut %}\n"
          type: string
        value:
          description: Значение параметра.
          type: string
    TariffType:
      description: 'Услуга Маркета или дополнительный тариф к услуге размещения:


        * `AGENCY_COMMISSION` — прием платежа покупателя.


        * `PAYMENT_TRANSFER` — перевод платежа покупателя.


        * `STORAGE` — хранение товара на складе Маркета в течение суток.


        * `SURPLUS` — хранение излишков на складе Маркета.


        * `WITHDRAW` — вывоз товара со склада Маркета.


        * `FEE` — размещение товара на Маркете.


        * `DELIVERY_TO_CUSTOMER` — доставка покупателю.


        * `CROSSREGIONAL_DELIVERY` — доставка в федеральный округ, город или населенный пункт.


        * `CROSSREGIONAL_DELIVERY_RETURN` — доставка невыкупов и возвратов.


        * `DISPOSAL` — утилизация.


        * `SORTING_CENTER_STORAGE` — хранение невыкупов и возвратов.


        * `EXPRESS_DELIVERY` — экспресс-доставка покупателю.


        * `FF_XDOC_SUPPLY_BOX` — поставка товара через транзитный склад (за короб).


        * `FF_XDOC_SUPPLY_PALLET` — поставка товара через транзитный склад (за палету).


        * `SORTING` — обработка заказа.


        * `MIDDLE_MILE` — средняя миля.


        * `RETURN_PROCESSING` — обработка невыкупов и возвратов.


        * `EXPRESS_CANCELLED_BY_PARTNER` — отмена заказа с экспресс-доставкой.


        * `CROSSBORDER_DELIVERY` — доставка из-за рубежа.


        * `INTAKE_SORTING_BULKY_CARGO` — сортировка заказов с крупногабаритными товарами, которые Маркет забрал со склада
        продавца.


        * `INTAKE_SORTING_SMALL_GOODS` — сортировка заказов с малогабаритными товарами, которые Маркет забрал со склада продавца.


        * `INTAKE_SORTING_DAILY` — организация забора заказов со склада продавца.


        * `FF_STORAGE_BILLING` — хранение товаров на складе.


        * `CANCELLED_ORDER_FEE_QI` — отмена заказа по вине продавца.


        * `LATE_ORDER_EXECUTION_FEE_QI` — несвоевременная отгрузка или доставка.


        * `VOLUME_STORAGE` — стоимость хранения товара на складе — из расчёта за один кубический метр в сутки.


        * `GOODS_ACCEPTANCE` — окончательная приемка товара на складе.


        * `CARGO_ACCEPTANCE` — первичная приемка товара на складе.


        * `ORDER_PROCESSING` — обработка заказа.


        * `WITHDRAW_EXTERNAL` — отгрузка на внешний маркетплейс.


        * `ITEM_BOOKING` — бронирование товара.


        * `SHOP_SERVICE_PLACEMENT` — размещение услуг магазина.


        Подробнее об услугах Маркета читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/introduction/rates/index.html).

        '
      type: string
      enum:
      - AGENCY_COMMISSION
      - PAYMENT_TRANSFER
      - STORAGE
      - WITHDRAW
      - SURPLUS
      - FEE
      - DELIVERY_TO_CUSTOMER
      - CROSSREGIONAL_DELIVERY
      - CROSSREGIONAL_DELIVERY_RETURN
      - DISPOSAL
      - SORTING_CENTER_STORAGE
      - EXPRESS_DELIVERY
      - FF_XDOC_SUPPLY_BOX
      - FF_XDOC_SUPPLY_PALLET
      - SORTING
      - MIDDLE_MILE
      - RETURN_PROCESSING
      - EXPRESS_CANCELLED_BY_PARTNER
      - CROSSBORDER_DELIVERY
      - INTAKE_SORTING_BULKY_CARGO
      - INTAKE_SORTING_SMALL_GOODS
      - INTAKE_SORTING_DAILY
      - FF_STORAGE_BILLING
      - CANCELLED_ORDER_FEE_QI
      - LATE_ORDER_EXECUTION_FEE_QI
      - VOLUME_STORAGE
      - GOODS_ACCEPTANCE
      - CARGO_ACCEPTANCE
      - ORDER_PROCESSING
      - WITHDRAW_EXTERNAL
      - ITEM_BOOKING
      - SHOP_SERVICE_PLACEMENT
    TimeIntervalDTO:
      type: object
      description: Интервал времени доставки.
      required:
      - fromTime
      - toTime
      properties:
        fromTime:
          type: string
          description: 'Начало интервала.


            Формат: `ЧЧ:ММ`.

            '
          pattern: ^([0-1][0-9]|2[0-3]):[0-5][0-9]$
        toTime:
          type: string
          description: 'Конец интервала.


            Формат: `ЧЧ:ММ`.

            '
          pattern: ^([0-1][0-9]|2[0-3]):[0-5][0-9]$
    TimePeriodDTO:
      description: Временной отрезок с комментарием. Требования к содержанию комментария зависят от контекста использования
        параметра и указаны в описании поля, которое его содержит.
      type: object
      required:
      - timePeriod
      - timeUnit
      properties:
        timePeriod:
          description: Продолжительность в указанных единицах.
          type: integer
        timeUnit:
          $ref: '#/components/schemas/TimeUnitType'
          description: Единица измерения.
        comment:
          description: Комментарий.
          type: string
          maxLength: 500
    TimeUnitType:
      description: 'Единица измерения времени:


        * `HOUR` — час.

        * `DAY` — сутки.

        * `WEEK` — неделя.

        * `MONTH` — месяц.

        * `YEAR` — год.

        '
      type: string
      enum:
      - HOUR
      - DAY
      - WEEK
      - MONTH
      - YEAR
    TokenDTO:
      description: Информация о токене авторизации.
      type: object
      required:
      - apiKey
      properties:
        apiKey:
          $ref: '#/components/schemas/ApiKeyDTO'
    TrackDTO:
      description: Информация о трек-номерах.
      type: object
      properties:
        trackCode:
          description: Трек-код почтового отправления.
          type: string
    TransferOrdersFromShipmentRequest:
      description: Запрос переноса заказов из отгрузки.
      type: object
      required:
      - orderIds
      properties:
        orderIds:
          description: Список заказов, которые вы не успеваете подготовить.
          type: array
          minItems: 1
          maxItems: 500
          uniqueItems: true
          items:
            description: Идентификатор заказа.
            type: integer
            format: int64
    TurnoverDTO:
      description: 'Информация об оборачиваемости товара.


        Подробнее о хранении и оборачиваемости товаров читайте в [Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/storage/logistics#turnover).

        '
      type: object
      required:
      - turnover
      properties:
        turnover:
          $ref: '#/components/schemas/TurnoverType'
          description: Оценка оборачиваемости.
        turnoverDays:
          description: Значение в днях.
          type: number
          format: double
    TurnoverType:
      description: 'Оценка оборачиваемости.


        |enum|Диапазон оборачиваемости|Комментарий|

        |-|-|-|

        |`LOW`|`turnoverDays` ≥ 120||

        |`ALMOST_LOW`|100 ≤ `turnoverDays` < 120||

        |`HIGH`|45 ≤ `turnoverDays` < 100||

        |`VERY_HIGH`|0 ≤ `turnoverDays` < 45||

        |`NO_SALES`|—|Продаж нет.|

        |`FREE_STORE`|Любое значение.|Платить за хранение товаров этой категории не требуется.|

        '
      type: string
      enum:
      - LOW
      - ALMOST_LOW
      - HIGH
      - VERY_HIGH
      - NO_SALES
      - FREE_STORE
    TypedQuestionsTextEntityIdDTO:
      type: object
      description: 'Идентификатор вопроса, ответа или комментария.

        '
      required:
      - id
      - type
      properties:
        id:
          description: Идентификатор вопроса, ответа или комментария.
          type: integer
          format: int64
          minimum: 1
        type:
          $ref: '#/components/schemas/QuestionsTextEntityType'
          description: Тип сущности (вопрос, ответ или комментарий).
    UinDTO:
      description: Статус проверки и :no-translate[УИН].
      type: object
      required:
      - value
      - status
      properties:
        value:
          type: string
          description: УИН товара.
        status:
          $ref: '#/components/schemas/UinStatusType'
        substatus:
          $ref: '#/components/schemas/UinSubstatusType'
    UinStatusType:
      description: 'Статус проверки УИНа:


        * `FAILED` — не прошел проверку.


        * `IN_PROGRESS` — в процессе проверки.


        * `NOT_ON_VALIDATION` — УИН не отправлен на проверку или переданы не все УИНы в заказе.


        * `OK` — проверка успешно пройдена.

        '
      type: string
      enum:
      - OK
      - IN_PROGRESS
      - FAILED
      - NOT_ON_VALIDATION
    UinSubstatusType:
      description: 'Детализация ошибки при проверке :no-translate[УИНа].


        * `UIN_MERCHANT_MISMATCH` — :no-translate[УИН] не принадлежит вашему магазину.


        * `UIN_MERCHANT_UNREGISTERED` — магазин не подключен к системе :no-translate[ГИИС ДМДК].


        * `UIN_NO_DATA` — :no-translate[УИН] не найден или заблокирован.


        Возвращается только для статуса `FAILED`.

        '
      type: string
      enum:
      - UIN_MERCHANT_MISMATCH
      - UIN_MERCHANT_UNREGISTERED
      - UIN_NO_DATA
    UnitDTO:
      description: Единица измерения.
      type: object
      required:
      - id
      - name
      - fullName
      properties:
        id:
          description: Идентификатор единицы измерения.
          type: integer
          format: int64
        name:
          description: Сокращенное название единицы измерения.
          type: string
          example: кг
        fullName:
          description: Полное название единицы измерения.
          type: string
          example: килограмм
    UpdateBusinessOfferPriceDTO:
      description: Товар с новой ценой.
      type: object
      required:
      - offerId
      - price
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        price:
          $ref: '#/components/schemas/UpdateBusinessPricesDTO'
    UpdateBusinessPricesDTO:
      description: Цены.
      type: object
      allOf:
      - $ref: '#/components/schemas/PriceWithDiscountDTO'
      - properties:
          minimumForBestseller:
            description: "Минимальная цена товара для попадания в акцию «Бестселлеры Маркета». Подробнее об этом способе участия\
              \ читайте [в Справке Маркета для продавцов](https://yandex.ru/support/marketplace/ru/marketing/promos/market/bestsellers#minimum).\n\
              \nПри передаче цены ориентируйтесь на значение параметра `maxPromoPrice` (максимально возможная цена для участия\
              \ в акции) в методе [POST v2/businesses/{businessId}/promos/offers](../../reference/promos/getPromoOffers.md).\n\
              \nТовар не попадет в акцию с помощью этого способа, если:\n\n* Не передать этот параметр. Удалится значение,\
              \ которое вы указали ранее.\n* В методе [POST v2/businesses/{businessId}/offer-prices](../../reference/prices/getDefaultPrices.md)\
              \ для этого товара возвращается параметр `excludedFromBestsellers` со значением `true`.\n\n  Но товар по-прежнему\
              \ сможет попасть в акцию через [автоматическое участие](*auto) или [ручное добавление](*updatePromoOffers).\n"
            type: number
            minimum: 0
            maximum: 100000000
            exclusiveMinimum: true
    UpdateBusinessPricesRequest:
      description: Запрос на установку цен, которые действуют во всех магазинах.
      type: object
      required:
      - offers
      properties:
        offers:
          description: 'Список товаров с ценами.


            В рамках одного запроса все значения `offerId` должны быть уникальными. Не допускается передача двух объектов
            с одинаковым `offerId`.

            '
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/UpdateBusinessOfferPriceDTO'
    UpdateCampaignOfferDTO:
      description: Параметры размещения товара в магазине.
      type: object
      allOf:
      - $ref: '#/components/schemas/BaseCampaignOfferDTO'
      - properties:
          vat:
            $ref: '#/components/schemas/VatType'
    UpdateCampaignOffersRequest:
      description: Запрос на обновление предложений товаров магазина.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Параметры размещения товаров в заданном магазине.
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/UpdateCampaignOfferDTO'
    UpdateExternalOrderIdRequest:
      description: Список заказов.
      type: object
      required:
      - externalOrderId
      properties:
        externalOrderId:
          description: Внешний идентификатор заказа.
          type: string
          minLength: 1
    UpdateGoodsFeedbackCommentDTO:
      description: Комментарий к отзыву или другому комментарию.
      type: object
      required:
      - text
      properties:
        id:
          $ref: '#/components/schemas/GoodsFeedbackCommentId'
          description: 'Идентификатор комментария, который нужно изменить.


            Оставьте поле пустым, если хотите добавить новый комментарий.

            '
        parentId:
          description: Идентификатор родительского комментария, на который нужно ответить.
          type: integer
          format: int64
        text:
          $ref: '#/components/schemas/GoodsFeedbackCommentText'
    UpdateGoodsFeedbackCommentRequest:
      description: Комментарий к отзыву.
      type: object
      required:
      - feedbackId
      - comment
      properties:
        feedbackId:
          $ref: '#/components/schemas/GoodsFeedbackId'
        comment:
          $ref: '#/components/schemas/UpdateGoodsFeedbackCommentDTO'
          description: Параметры комментария.
    UpdateGoodsFeedbackCommentResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/GoodsFeedbackCommentDTO'
    UpdateGoodsQuestionTextEntityDTO:
      type: object
      description: 'Информация о созданном ответе или комментария.


        Возвращается только для запроса создания.

        '
      required:
      - entity
      properties:
        entity:
          $ref: '#/components/schemas/TypedQuestionsTextEntityIdDTO'
    UpdateGoodsQuestionTextEntityRequest:
      type: object
      required:
      - operationType
      description: "Запрос на создание, обновление или удаление вопроса, ответа или комментария.\n\n**Параметры для разных\
        \ операций:**\n\n* `CREATE` — создание ответа или комментария:\n  * Обязательно: `operationType`, `parentEntityId`,\
        \ `text`.\n  * `parentEntityId` — идентификатор родителя (для ответа — вопроса, для комментария — ответа).\n\n* `UPDATE`\
        \ — обновление ответа или комментария:\n  * Обязательно: `operationType`, `entityId`, `text`.\n  * `entityId` — идентификатор\
        \ ответа или комментария.\n\n* `DELETE` — удаление ответа или комментария:\n  * Обязательно: `operationType`, `entityId`.\n\
        \  * `text` не требуется.\n"
      properties:
        entityId:
          $ref: '#/components/schemas/TypedQuestionsTextEntityIdDTO'
          description: 'Идентификатор обновляемого или удаляемого ответа или комментария.


            Обязателен для операций `UPDATE` и `DELETE`.

            '
        parentEntityId:
          $ref: '#/components/schemas/TypedQuestionsTextEntityIdDTO'
          description: 'Идентификатор родительского вопроса или ответа.


            Обязателен для операции `CREATE`. Используется для создания ответа или комментария:

            * При создании ответа — указывайте идентификатор вопроса.

            * При создании комментария к ответу — указывайте идентификатор ответа.

            '
        text:
          $ref: '#/components/schemas/QuestionsTextContent'
          description: 'Текст ответа или комментария.


            Обязателен для операций `CREATE` и `UPDATE`. Не требуется для операции `DELETE`.

            '
        operationType:
          $ref: '#/components/schemas/QuestionsTextEntityOperationType'
    UpdateGoodsQuestionTextEntityResponse:
      type: object
      description: 'Ответ на запрос создания, обновления или удаления вопроса, ответа или комментария.

        '
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/UpdateGoodsQuestionTextEntityDTO'
    UpdateMappingDTO:
      description: 'Карточка на Маркете, которая, с вашей точки зрения, подходит товару. Чтобы определить идентификатор подходящей
        карточки, воспользуйтесь поиском в кабинете (**Товары** → **Каталог** → **Загрузить товары**).


        По результатам проверки Маркет может привязать товар к более подходящей карточке.

        '
      type: object
      properties:
        marketSku:
          $ref: '#/components/schemas/MarketSku'
          description: 'Идентификатор карточки на Маркете.

            '
    UpdateOfferContentRequest:
      description: Запрос на установку новых значений для параметров.
      type: object
      required:
      - offersContent
      properties:
        offersContent:
          description: Список товаров с указанными характеристиками.
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: '#/components/schemas/OfferContentDTO'
    UpdateOfferContentResponse:
      description: Описывает проблемы, которые появились при сохранении товара.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          results:
            description: 'Ошибки и предупреждения, которые появились при обработке переданных значений. Каждый элемент списка
              соответствует одному товару.


              Если ошибок и предупреждений нет, поле не передается.

              '
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/UpdateOfferContentResultDTO'
    UpdateOfferContentResultDTO:
      description: Ошибки и предупреждения, которые появились из-за переданных характеристик.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        errors:
          description: 'Ошибки.


            Если хотя бы по одному товару есть ошибка, информация в каталоге не обновится по всем переданным товарам.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferContentErrorDTO'
        warnings:
          description: 'Предупреждения.


            Информация в каталоге обновится.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferContentErrorDTO'
    UpdateOfferDTO:
      description: Параметры товара.
      type: object
      allOf:
      - $ref: '#/components/schemas/BaseOfferDTO'
      - properties:
          parameterValues:
            description: 'Список характеристик с их значениями.


              {% note warning "Всегда передавайте вместе с `marketCategoryId`" %}


              Если не передать `marketCategoryId` при изменении характеристик, они обновятся, но в ответе придет предупреждение
              (параметр `warnings`).


              Если не передать их оба, будет использована информация из устаревших параметров `params` и `category`, а `marketCategoryId`
              будет определен автоматически.


              {% endnote %}


              При **изменении** характеристик передавайте только те, значение которых нужно обновить. Если в `marketCategoryId`
              вы меняете категорию, значения общих характеристик для старой и новой категории сохранятся, передавать их не
              нужно.


              Подробнее читайте в [«Передача значений характеристики»](../../step-by-step/parameter-values.md).

              '
            type: array
            nullable: true
            minItems: 1
            maxItems: 300
            items:
              $ref: '#/components/schemas/ParameterValueDTO'
          basicPrice:
            $ref: '#/components/schemas/PriceWithDiscountDTO'
            description: 'Цена.

              '
          purchasePrice:
            $ref: '#/components/schemas/BasePriceDTO'
            description: Себестоимость — затраты на самостоятельное производство товара или закупку у производителя или поставщиков.
          additionalExpenses:
            $ref: '#/components/schemas/BasePriceDTO'
            description: Дополнительные расходы на товар. Например, на доставку или упаковку.
          firstVideoAsCover:
            deprecated: true
            description: 'Использовать первое видео в карточке как видеообложку.


              Передайте `true`, чтобы первое видео использовалось как видеообложка, или `false`, чтобы видеообложка не отображалась
              в карточке товара.

              '
            type: boolean
          deleteParameters:
            description: 'Параметры, которые вы ранее передали в `UpdateOfferDTO`, а теперь хотите удалить.


              Если передать `adult`, `downloadable` и `firstVideoAsCover`, они не удалятся — их значение изменится на `false`.


              Можно передать сразу несколько значений.


              Не используйте вместе с соответствующим параметром в `UpdateOfferDTO`. Это приведет к ошибке `400`.

              '
            type: array
            nullable: true
            minItems: 1
            uniqueItems: true
            items:
              $ref: '#/components/schemas/DeleteOfferParameterType'
    UpdateOfferMappingDTO:
      description: Информация о товаре.
      type: object
      required:
      - offer
      properties:
        offer:
          $ref: '#/components/schemas/UpdateOfferDTO'
          description: Параметры товара.
        mapping:
          $ref: '#/components/schemas/UpdateMappingDTO'
          description: Информация о карточке товара на Маркете.
    UpdateOfferMappingResultDTO:
      description: Ошибки и предупреждения, которые появились из-за переданных характеристик.
      type: object
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        errors:
          description: 'Ошибки.


            Если хотя бы по одному товару есть ошибка, информация в каталоге не обновится по всем переданным товарам.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferMappingErrorDTO'
        warnings:
          description: 'Предупреждения.


            Информация в каталоге обновится.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/OfferMappingErrorDTO'
      required:
      - offerId
    UpdateOfferMappingsRequest:
      type: object
      required:
      - offerMappings
      properties:
        offerMappings:
          description: 'Список товаров, которые нужно добавить или обновить.


            {% note warning "Скоро мы уменьшим максимальное количество товаров в запросе" %}


            Уже сейчас не передавайте больше 100.


            {% endnote %}


             

            '
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/UpdateOfferMappingDTO'
        onlyPartnerMediaContent:
          description: 'Будут ли использоваться только переданные вами данные о товарах.


            Значение по умолчанию: `false`. Чтобы удалить данные, которые добавил Маркет, передайте значение `true`.

            '
          type: boolean
    UpdateOfferMappingsResponse:
      description: Описывает проблемы, возникшие при сохранении товара.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          results:
            description: 'Ошибки и предупреждения, которые появились при обработке списка характеристик. Каждый элемент списка
              соответствует одному товару.


              Если ошибок и предупреждений нет, поле не передается.

              '
            type: array
            nullable: true
            minItems: 1
            items:
              $ref: '#/components/schemas/UpdateOfferMappingResultDTO'
    UpdateOrderDTO:
      type: object
      description: Информация, которую нужно изменить.
      required:
      - id
      properties:
        id:
          description: Идентификатор заказа, в котором нужны изменения.
          type: integer
          format: int64
        deliveryInterval:
          $ref: '#/components/schemas/DeliveryIntervalsUpdateOptionDTO'
          description: 'Интервалы дат и времени, на которые можно изменить.


            Только для курьерской доставки.

            '
        customer:
          $ref: '#/components/schemas/CustomerDTO'
          description: Данные получателя заказа.
    UpdateOrderItemRequest:
      description: Запрос на обновление состава заказа.
      type: object
      required:
      - items
      properties:
        items:
          description: 'Список товаров в заказе.


            Если магазин не передал информацию о товаре во входных данных, он будет удален из заказа.


            Обязательный параметр.

            '
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OrderItemModificationDTO'
        reason:
          $ref: '#/components/schemas/OrderItemsModificationRequestReasonType'
    UpdateOrderRequest:
      type: object
      required:
      - order
      properties:
        order:
          $ref: '#/components/schemas/UpdateOrderDTO'
    UpdateOrderResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/UpdateOrderResultDTO'
    UpdateOrderResultDTO:
      type: object
      description: Информация об операции по изменению заказа.
      required:
      - operations
      properties:
        operations:
          description: 'Информация о запущенных операциях по изменению заказа.

            '
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/OperationDTO'
    UpdateOrderStatusDTO:
      description: Список заказов.
      type: object
      properties:
        id:
          description: Идентификатор заказа.
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/OrderStatusType'
        substatus:
          $ref: '#/components/schemas/OrderSubstatusType'
        updateStatus:
          $ref: '#/components/schemas/OrderUpdateStatusType'
          description: Статус обновления.
        errorDetails:
          description: 'Ошибка при изменении статуса заказа. Содержит описание ошибки и идентификатор заказа.


            Возвращается, если параметр `updateStatus` принимает значение `ERROR`.

            '
          type: string
        operation:
          $ref: '#/components/schemas/OperationDTO'
          description: '**Только для модели LaaS**


            Информация о запущенной операции по обновлению статуса.

            '
    UpdateOrderStatusRequest:
      type: object
      required:
      - order
      properties:
        order:
          $ref: '#/components/schemas/OrderStatusChangeDTO'
    UpdateOrderStatusResponse:
      description: Информация об изменении статуса заказа.
      type: object
      properties:
        order:
          $ref: '#/components/schemas/OrderDTO'
        operation:
          $ref: '#/components/schemas/OperationDTO'
          description: '**Только для модели LaaS**


            Информация о запущенной операции по обновлению статуса.

            '
    UpdateOrderStatusesDTO:
      description: Список заказов, статус которых обновился.
      type: object
      required:
      - orders
      properties:
        orders:
          description: Список с обновленными заказами.
          type: array
          items:
            $ref: '#/components/schemas/UpdateOrderStatusDTO'
    UpdateOrderStatusesRequest:
      description: Список заказов.
      type: object
      required:
      - orders
      properties:
        orders:
          description: Список заказов.
          type: array
          minItems: 1
          maxItems: 30
          items:
            $ref: '#/components/schemas/OrderStateDTO'
    UpdateOrderStatusesResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/UpdateOrderStatusesDTO'
    UpdateOrderStorageLimitRequest:
      description: Запрос на обновление срока хранения заказа в ПВЗ.
      type: object
      required:
      - newDate
      properties:
        newDate:
          description: 'Новая дата, до которой заказ будет храниться в пункте выдачи.


            Срок хранения можно увеличить не больше, чем на 30 дней.


            Формат даты: `ГГГГ-ММ-ДД`.

            '
          type: string
          format: date
    UpdateOutletLicenseRequest:
      description: Запрос на создание или изменение лицензий для точек продаж.
      type: object
      required:
      - licenses
      properties:
        licenses:
          description: 'Список лицензий.

            Обязательный параметр, должен содержать информацию хотя бы об одной лицензии.

            '
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/OutletLicenseDTO'
    UpdatePricesRequest:
      description: Запрос на установку цен на товары.
      type: object
      required:
      - offers
      properties:
        offers:
          description: Список товаров.
          type: array
          minItems: 1
          maxItems: 2000
          items:
            $ref: '#/components/schemas/OfferPriceDTO'
    UpdatePromoOfferDTO:
      description: Описание товаров, которые участвуют в акции.
      type: object
      required:
      - offerId
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        params:
          $ref: '#/components/schemas/UpdatePromoOfferParamsDTO'
    UpdatePromoOfferDiscountParamsDTO:
      description: 'Параметры товара в акции с типом `DIRECT_DISCOUNT` или `BLUE_FLASH`.


        Обязательный параметр для акций с этими типами.

        '
      type: object
      properties:
        price:
          description: 'Зачеркнутая цена — та, по которой товар продавался до акции.


            Указывается в рублях.


            Число должно быть целым.

            '
          type: integer
          format: int64
          minimum: 1
        promoPrice:
          description: 'Цена по акции — та, по которой вы хотите продавать товар.


            Указывается в рублях.


            Число должно быть целым.

            '
          type: integer
          format: int64
          minimum: 1
    UpdatePromoOfferParamsDTO:
      description: Параметры товара, который участвует в акции.
      type: object
      properties:
        discountParams:
          $ref: '#/components/schemas/UpdatePromoOfferDiscountParamsDTO'
    UpdatePromoOffersRequest:
      description: 'Добавление товаров в акцию или обновление их параметров.


        Чтобы добавить товары в акцию или обновить параметры каких-то товаров, передайте их в параметре `offers`.

        '
      type: object
      required:
      - promoId
      - offers
      properties:
        promoId:
          description: Идентификатор акции.
          type: string
        offers:
          description: Товары, которые необходимо добавить в акцию или цены которых нужно изменить.
          type: array
          minItems: 1
          maxItems: 500
          items:
            $ref: '#/components/schemas/UpdatePromoOfferDTO'
    UpdatePromoOffersResponse:
      description: Результат добавления товаров в акцию.
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/UpdatePromoOffersResultDTO'
    UpdatePromoOffersResultDTO:
      description: Ошибки и предупреждения, которые появились при добавлении товаров в акцию.
      type: object
      properties:
        rejectedOffers:
          description: 'Изменения, которые были отклонены.


            Возвращается, только если есть отклоненные изменения.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/RejectedPromoOfferUpdateDTO'
        warningOffers:
          description: 'Изменения, по которым есть предупреждения. Они информируют о возможных проблемах. Информация о товарах
            обновится.


            Возвращается, только если есть предупреждения.

            '
          type: array
          nullable: true
          minItems: 1
          items:
            $ref: '#/components/schemas/WarningPromoOfferUpdateDTO'
    UpdateStockDTO:
      description: Информация об остатках одного товара на одном из складов.
      type: object
      required:
      - sku
      - items
      properties:
        sku:
          $ref: '#/components/schemas/ShopSku'
        items:
          description: 'Информация об остатках товара.

            '
          type: array
          minItems: 1
          maxItems: 1
          items:
            $ref: '#/components/schemas/UpdateStockItemDTO'
    UpdateStockItemDTO:
      description: 'Информация об остатках товара.

        '
      type: object
      required:
      - count
      properties:
        count:
          description: 'Количество доступного товара.

            '
          format: int64
          type: integer
          minimum: 0
          maximum: 2000000000
        updatedAt:
          description: 'Дата и время последнего обновления информации об остатках.

            <br><br>

            Если вы не передали параметр `updatedAt`, используется текущее время.

            <br><br>

            Формат даты и времени: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:42:42+03:00`.

            '
          type: string
          format: date-time
    UpdateStocksRequest:
      description: Запрос на изменение информации по остаткам товаров.
      type: object
      required:
      - skus
      properties:
        skus:
          description: 'Данные об остатках товаров.


            В рамках одного запроса все значения `sku` должны быть уникальными. Не допускается передача двух объектов с одинаковым
            `sku`.

            '
          type: array
          minItems: 1
          maxItems: 2000
          items:
            $ref: '#/components/schemas/UpdateStockDTO'
    UpdateTimeDTO:
      description: Время последнего обновления.
      type: object
      required:
      - updatedAt
      properties:
        updatedAt:
          description: Время последнего обновления.
          type: string
          format: date-time
    UpdateWarehouseStatusRequest:
      description: 'Запрос на изменение статуса склада.


        Позволяет выключить склад или включить ранее отключенный вами склад.


        Если склад был отключен Маркетом, то включить его вручную с помощью этого метода не получится.

        '
      type: object
      required:
      - enabled
      properties:
        enabled:
          description: 'Статус склада:


            * `true` — включен.

            * `false` — отключен.

            '
          type: boolean
    UpdateWarehouseStatusResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/WarehouseStatusDTO'
    Url:
      type: string
      minLength: 1
      maxLength: 2000
    ValueRestrictionDTO:
      description: 'Ограничение на возможные значения, накладываемое другой характеристикой.


        Если ограничивающая характеристика принимает определенное значение, список возможных значений ограничиваемой характеристики
        сокращается.


        **Пример**


        Характеристика **размер** сама по себе может принимать девять разных значений: `S`, `M`, `L`, `44`, `46`, `48`, `42/164`,
        `46/176`, `44S`.


        Если ограничивающая характеристика **размерная сетка** принимает значение `RU`, список возможных значений размера
        сокращается до `44`, `46`, `48`.

        '
      type: object
      required:
      - limitingParameterId
      - limitedValues
      properties:
        limitingParameterId:
          description: Идентификатор ограничивающей характеристики.
          type: integer
          format: int64
          minimum: 1
        limitedValues:
          description: Значения ограничивающей характеристики и соответствующие допустимые значения текущей характеристики.
          type: array
          items:
            $ref: '#/components/schemas/OptionValuesLimitedDTO'
    VatType:
      description: 'Идентификатор НДС, применяемый для товара:


        * `2` — НДС 10%. Например, используется при реализации отдельных продовольственных и медицинских товаров.

        * `5` — НДС 0%. Например, используется при продаже товаров, вывезенных в таможенной процедуре экспорта, или при оказании
        услуг по международной перевозке товаров.

        * `6` — НДС не облагается, используется только для отдельных видов услуг.

        * `7` — НДС 20%. Основной НДС с 2019 года до 1 января 2026 года. При передаче автоматически заменяется на НДС 22%
        (14). С 1 июля 2026 года значение будет больше недоступно для передачи.

        * `10` — НДС 5%. НДС для упрощенной системы налогообложения (УСН).

        * `11` — НДС 7%. НДС для упрощенной системы налогообложения (УСН).

        * `14` — НДС 22%. Основной НДС с 1 января 2026 года.


        Если параметр не указан, используется НДС, установленный в кабинете.


        **Для продавцов :no-translate[Market Yandex Go]** недоступна передача и получение НДС.

        '
      type: integer
      format: int32
    VerifyOrderEacRequest:
      type: object
      required:
      - code
      properties:
        code:
          description: Код для подтверждения ЭАПП.
          type: string
    VerifyOrderEacResponse:
      type: object
      allOf:
      - $ref: '#/components/schemas/ApiResponse'
      - properties:
          result:
            $ref: '#/components/schemas/EacVerificationResultDTO'
    VotesDTO:
      type: object
      required:
      - likes
      - dislikes
      description: 'Количество лайков и дизлайков на вопросе, ответе или комментарии.

        '
      properties:
        likes:
          description: Количество лайков.
          type: integer
          format: int64
        dislikes:
          description: Количество дизлайков.
          type: integer
          format: int64
    WarehouseAddressDTO:
      description: Адрес склада.
      type: object
      required:
      - city
      - gps
      properties:
        city:
          description: Город.
          type: string
          maxLength: 200
        street:
          description: Улица.
          type: string
          maxLength: 512
        number:
          description: Номер дома.
          type: string
          maxLength: 256
        building:
          description: Номер строения.
          type: string
          maxLength: 16
        block:
          description: Номер корпуса.
          type: string
          maxLength: 16
        gps:
          $ref: '#/components/schemas/GpsDTO'
    WarehouseComponentType:
      description: 'Свойства складов, которые необходимо вернуть:


        * `ADDRESS` — адрес склада.

        * `STATUS` — статус склада.

        '
      type: string
      enum:
      - ADDRESS
      - STATUS
    WarehouseDTO:
      description: Информация о складе.
      type: object
      required:
      - id
      - name
      - campaignId
      - express
      properties:
        id:
          description: Идентификатор склада.
          type: integer
          format: int64
        name:
          description: Название склада.
          type: string
        campaignId:
          $ref: '#/components/schemas/CampaignId'
        express:
          description: Возможна ли доставка по модели Экспресс.
          type: boolean
        address:
          $ref: '#/components/schemas/WarehouseAddressDTO'
          description: Адрес склада.
    WarehouseDeliveryOptionsDTO:
      type: object
      description: Варианты доставки.
      properties:
        pickupDelivery:
          $ref: '#/components/schemas/PickupDeliveryOptionsDTO'
        courierDelivery:
          $ref: '#/components/schemas/CourierDeliveryOptionsDTO'
    WarehouseDetailsDTO:
      description: Информация о складе.
      type: object
      required:
      - id
      - name
      - campaignId
      - express
      properties:
        id:
          description: Идентификатор склада.
          type: integer
          format: int64
        name:
          description: Название склада.
          type: string
        campaignId:
          $ref: '#/components/schemas/CampaignId'
          description: 'Идентификатор кампании того магазина, который связан со складом.

            '
        express:
          description: Возможна ли доставка для модели Экспресс.
          type: boolean
        groupInfo:
          $ref: '#/components/schemas/WarehouseGroupInfoDTO'
        address:
          $ref: '#/components/schemas/WarehouseAddressDTO'
          description: 'Адрес склада.


            Возвращается, только если в запросе параметр `components` принимает значение `ADDRESS`.

            '
        status:
          $ref: '#/components/schemas/WarehouseStatusDTO'
          description: 'Статус склада.


            Возвращается, только если в запросе параметр `components` принимает значение `STATUS`.


            {% note info "Статус склада, полученный через API, может не совпадать со статусом в кабинете" %}


            Например, сначала Маркет отключил склад, а затем вы с помощью метода [POST v2/campaigns/{campaignId}/warehouse/status](../../reference/warehouses/updateWarehouseStatus.md).


            Статус в кабинете — **Отключен Маркетом**, а через API вернется **DISABLED_MANUALLY** (отключен вами).


            {% endnote %}

            '
    WarehouseGroupDTO:
      description: Информация о группе складов.
      type: object
      required:
      - name
      - mainWarehouse
      - warehouses
      properties:
        name:
          description: Название группы складов.
          type: string
        mainWarehouse:
          $ref: '#/components/schemas/WarehouseDTO'
          description: Склад, идентификатор которого нужно использовать для передачи остатков по всей группе.
        warehouses:
          description: Список складов, входящих в группу.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseDTO'
            description: Склад.
    WarehouseGroupInfoDTO:
      description: 'Информация о группе, к которой принадлежит склад.


        Возвращается только для складов в группах.


        [Что такое группы складов и зачем они нужны](https://yandex.ru/support/marketplace/assortment/operations/stocks.html#unified-stocks)

        '
      type: object
      required:
      - name
      - id
      properties:
        name:
          description: Название группы, к которой принадлежит склад.
          type: string
        id:
          description: Идентификатор группы складов.
          type: integer
          format: int64
    WarehouseId:
      description: Идентификатор фулфилмент-склада Маркета.
      type: integer
      format: int64
      minimum: 1
    WarehouseOfferDTO:
      description: Информация об остатках товара.
      type: object
      required:
      - offerId
      - stocks
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        turnoverSummary:
          $ref: '#/components/schemas/TurnoverDTO'
          description: Информация об оборачиваемости.
        stocks:
          description: Информация об остатках.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseStockDTO'
        updatedAt:
          description: 'Дата и время последнего обновления информации об остатках.


            Формат даты и времени: ISO 8601 со смещением относительно UTC. Например, `2023-11-21T00:42:42+03:00`.

            '
          type: string
          format: date-time
    WarehouseOffersDTO:
      description: Информация об остатках товаров на складе.
      type: object
      required:
      - warehouseId
      - offers
      properties:
        warehouseId:
          description: 'Идентификатор склада.


            **Для моделей FBY и LaaS:** возвращается идентификатор склада Маркета.


            **Для модели FBS:** может возвращаться идентификатор как партнерского склада, так и склада возвратов Маркета.

            '
          type: integer
          format: int64
        offers:
          description: Информация об остатках.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseOfferDTO'
    WarehouseStatusDTO:
      description: Информация о статусе склада.
      type: object
      required:
      - status
      properties:
        status:
          $ref: '#/components/schemas/WarehouseStatusType'
    WarehouseStatusType:
      description: 'Статус склада:


        * `DISABLED_MANUALLY` – отключен вами.

        * `OTHER` – другой статус. Например, склад включен или отключен Маркетом.

        '
      type: string
      enum:
      - DISABLED_MANUALLY
      - OTHER
    WarehouseStockDTO:
      description: Информация об остатках товара.
      type: object
      required:
      - type
      - count
      properties:
        type:
          $ref: '#/components/schemas/WarehouseStockType'
          description: Тип остатков.
        count:
          description: Значение остатков.
          type: integer
          format: int64
    WarehouseStockType:
      description: 'Тип остатков товаров на складе:


        * `AVAILABLE` (соответствует типу «Доступный к заказу» в отчете «Остатки на складе» в кабинете продавца на Маркете)
        — товар, доступный для продажи.


        * `DEFECT` (соответствует типу «Брак») — товар с браком.


        * `EXPIRED` (соответствует типу «Просрочен») — товар с истекшим сроком годности.


        * `FIT` (соответствует типу «Годный») — товар, который доступен для продажи или уже зарезервирован.


        * `FREEZE` — товар, который зарезервирован для заказов.


        * `QUARANTINE` (соответствует типу «Карантин») — товар, временно недоступный для продажи (например, товар перемещают
        из одного помещения склада в другое).


        * `UTILIZATION` — товар, который будет утилизирован.

        '
      type: string
      enum:
      - FIT
      - FREEZE
      - AVAILABLE
      - QUARANTINE
      - UTILIZATION
      - DEFECT
      - EXPIRED
    WarehousesDTO:
      description: Информация о складах и группах складов.
      type: object
      required:
      - warehouses
      - warehouseGroups
      properties:
        warehouses:
          description: Список складов, не входящих в группы.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseDTO'
        warehouseGroups:
          description: Список групп складов.
          type: array
          items:
            $ref: '#/components/schemas/WarehouseGroupDTO'
    WarehousesDeliveryOptionsDTO:
      type: object
      description: Варианты доставки со склада.
      required:
      - warehouseId
      - deliveryOptions
      - items
      properties:
        warehouseId:
          $ref: '#/components/schemas/WarehouseId'
        deliveryOptions:
          $ref: '#/components/schemas/WarehouseDeliveryOptionsDTO'
        items:
          type: array
          description: Товары в заказе.
          minItems: 1
          items:
            $ref: '#/components/schemas/BasicOrderItemDTO'
    WarningPromoOfferUpdateDTO:
      description: Описание предупреждения, которое появилось при добавлении товара.
      type: object
      required:
      - offerId
      - warnings
      properties:
        offerId:
          $ref: '#/components/schemas/ShopSku'
        warnings:
          description: Предупреждения, которые появились при добавлении товара в акцию или изменении его цен.
          type: array
          items:
            $ref: '#/components/schemas/PromoOfferUpdateWarningDTO'
    Year:
      description: Год.
      type: integer
      format: int32
      example: 2025
  responses:
    '200':
      description: Пустой ответ.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/EmptyApiResponse'
    '400':
      description: Запрос содержит неправильные данные. [Подробнее об ошибке](../../concepts/error-codes.md#400)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiClientDataErrorResponse'
    400Orders:
      description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с заказами](../../concepts/error-codes#orders)

        '
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiClientDataErrorResponse'
    400Stocks:
      description: 'Запрос содержит неправильные данные. [Подробнее об ошибках при работе с остатками](../../concepts/error-codes#stocks)

        '
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiClientDataErrorResponse'
    '401':
      description: В запросе не указаны данные для авторизации. [Подробнее об ошибке](../../concepts/error-codes.md#401)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiUnauthorizedErrorResponse'
    '403':
      description: Данные для авторизации неверны или доступ к ресурсу запрещен. [Подробнее об ошибке](../../concepts/error-codes.md#403)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiForbiddenErrorResponse'
    '404':
      description: Запрашиваемый ресурс не найден. [Подробнее об ошибке](../../concepts/error-codes.md#404)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiNotFoundErrorResponse'
    '420':
      description: Превышено ограничение на доступ к ресурсу. [Подробнее об ошибке](../../concepts/error-codes.md#420)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiLimitErrorResponse'
    '423':
      description: К ресурсу нельзя применить указанный метод. [Подробнее об ошибке](../../concepts/error-codes.md#423)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiLockedErrorResponse'
    '500':
      description: Внутренняя ошибка Маркета. [Подробнее об ошибке](../../concepts/error-codes.md#500)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiServerErrorResponse'
  parameters:
    AliasedPageToken:
      name: pageToken
      description: 'Идентификатор страницы c результатами.


        Если параметр не указан, возвращается первая страница.


        Передавайте значение выходного параметра `nextPageToken`, полученное при последнем запросе.

        '
      in: query
      required: false
      x-transform: token
      x-aliases:
      - pageToken
      - page_token
      schema:
        type: string
    BoxId:
      description: Идентификатор коробки.
      name: boxId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    BusinessId:
      description: 'Идентификатор кабинета.


        {% if audience == "partner" %}


        Чтобы его узнать, воспользуйтесь запросом [GET v2/campaigns](../../reference/campaigns/getCampaigns.md).


        ℹ️ [Что такое кабинет и магазин на Маркете](https://yandex.ru/support/marketplace/account/introduction.html)


        {% endif %}

        '
      name: businessId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    BusinessIdQuery:
      description: 'Идентификатор кабинета. Чтобы его узнать, воспользуйтесь запросом [GET v2/campaigns](../../reference/campaigns/getCampaigns.md).


        Передайте параметр, чтобы получить характеристики, которые являются особенностями варианта товара в данном кабинете.

        '
      name: businessId
      in: query
      required: false
      schema:
        type: integer
        format: int64
        minimum: 1
    CampaignId:
      description: 'Идентификатор кампании (магазина) — технический идентификатор, который представляет ваш магазин в системе
        Яндекс Маркета при работе через API. Он однозначно связывается с вашим магазином, но предназначен только для автоматизированного
        взаимодействия.


        Его можно узнать с помощью запроса [GET v2/campaigns](../../reference/campaigns/getCampaigns.md) или найти в кабинете
        продавца на Маркете. Нажмите на иконку вашего аккаунта → **Настройки** и в меню слева выберите **API и модули**:


        * блок **Идентификатор кампании**;

        * вкладка **Лог запросов** → выпадающий список в блоке **Показывать логи**.


        ⚠️ Не путайте его с:

        - идентификатором магазина, который отображается в личном кабинете продавца;

        - рекламными кампаниями.

        '
      name: campaignId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    CancelledOrders:
      description: 'Возвращать ли отмененные заказы.


        Значение по умолчанию: `true`. Если возвращать отмененные заказы не нужно, передайте значение `false`.

        '
      name: cancelledOrders
      in: query
      required: false
      schema:
        type: boolean
        default: true
    CatalogLanguage:
      description: 'Язык, на котором принимаются и возвращаются значения в параметрах `name` и `description`.


        Значение по умолчанию: `RU`.

        '
      name: language
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/CatalogLanguageType'
    ChatId:
      description: Идентификатор чата.
      name: chatId
      in: query
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    DeleteLicenseIds:
      description: Список идентификаторов лицензий для удаления.
      name: ids
      in: query
      required: true
      schema:
        type: array
        uniqueItems: true
        minItems: 1
        maxItems: 500
        items:
          type: integer
          format: int64
    GetFulfillmentWarehousesCampaignId:
      description: 'Идентификатор кампании магазина.


        Указывается, если нужно вернуть все склады Маркета, которые привязаны к определенной кампании магазина.

        '
      name: campaignId
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/CampaignId'
    GetOrdersBuyerType:
      description: 'Фильтрация заказов по типу покупателя.

        '
      name: buyerType
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/OrderBuyerType'
    GetOrdersDispatchType:
      description: Способ отгрузки
      name: dispatchType
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/OrderDeliveryDispatchType'
    GetOrdersFake:
      description: 'Фильтрация заказов по типам:


        * `false` — настоящий заказ покупателя.


        * `true` — [тестовый заказ](../../concepts/sandbox.md) Маркета.

        '
      name: fake
      in: query
      required: false
      schema:
        type: boolean
        default: false
    GetOrdersFromDate:
      description: 'Начальная дата для фильтрации заказов по дате оформления.


        Формат даты: `ДД-ММ-ГГГГ`.


        Между начальной и конечной датой (параметр `toDate`) должно быть не больше 30 дней.


        Значение по умолчанию: 30 дней назад от текущей даты.

        '
      name: fromDate
      in: query
      required: false
      schema:
        type: string
        format: date
    GetOrdersHasCis:
      description: 'Фильтр для получения заказов, в которых есть хотя бы один товар с кодом идентификации в системе [«Честный
        ЗНАК»](https://честныйзнак.рф/) или [«ASL BELGISI»](https://aslbelgisi.uz) (для продавцов :no-translate[Market Yandex
        Go]):


        * `true` — да.


        * `false` — нет.


        Такие коды присваиваются товарам, которые подлежат маркировке и относятся к определенным категориям.

        '
      name: hasCis
      in: query
      required: false
      schema:
        type: boolean
        default: false
    GetOrdersOnlyEstimatedDelivery:
      description: 'Фильтрация заказов с долгой доставкой (31-60 дней) по подтвержденной дате доставки:


        * `true` — возвращаются только заказы с неподтвержденной датой доставки.

        * `false` — фильтрация не применяется.

        '
      name: onlyEstimatedDelivery
      in: query
      required: false
      schema:
        type: boolean
        default: false
    GetOrdersOnlyWaitingForCancellationApprove:
      description: '**Только для модели DBS**


        Фильтр для получения заказов, по которым был запрос на отмену.


        При значении `true` возвращаются только заказы, которые находятся в статусе `DELIVERY` или `PICKUP` и которые пользователи
        решили отменить.


        Чтобы подтвердить или отклонить отмену, отправьте запрос [PUT v2/campaigns/{campaignId}/orders/{orderId}/cancellation/accept](../../reference/orders/acceptOrderCancellation).

        '
      name: onlyWaitingForCancellationApprove
      in: query
      required: false
      schema:
        type: boolean
        default: false
    GetOrdersOrderIds:
      description: 'Фильтрация заказов по идентификаторам.

        <br><br>

        ⚠️ Не используйте это поле одновременно с другими фильтрами. Если вы хотите воспользоваться ими, оставьте поле пустым.

        '
      name: orderIds
      in: query
      required: false
      schema:
        type: array
        items:
          type: integer
          format: int64
        minItems: 1
        maxItems: 50
    GetOrdersStatus:
      description: 'Статус заказа:


        * `CANCELLED` — заказ отменен.


        * `DELIVERED` — заказ получен покупателем.


        * `DELIVERY` — заказ передан в службу доставки.


        * `PICKUP` — заказ доставлен в пункт выдачи.


        * `PROCESSING` — заказ находится в обработке.


        * `UNPAID` — заказ оформлен, но еще не оплачен (если выбрана оплата при оформлении).


        * `RESERVED` — заказ оформлен, но ожидает подтвеждения от магазина (LaaS).

        '
      name: status
      in: query
      required: false
      schema:
        type: array
        uniqueItems: true
        items:
          $ref: '#/components/schemas/OrderStatusType'
    GetOrdersSubstatus:
      description: "Этап обработки заказа (статус `PROCESSING`) или причина отмены заказа (статус `CANCELLED`).\n\nВозможные\
        \ значения для заказа в статусе `PROCESSING`:\n\n* `STARTED` — заказ подтвержден, его можно начать обрабатывать.\n\
        * `READY_TO_SHIP` — заказ собран и готов к отправке.\n* `SHIPPED` — заказ передан службе доставки.\n\nВозможные значения\
        \ для заказа в статусе `CANCELLED`:\n\n* `RESERVATION_EXPIRED` — покупатель не завершил оформление зарезервированного\
        \ заказа в течение 10 минут.\n\n* `USER_NOT_PAID` — покупатель не оплатил заказ (для типа оплаты `PREPAID`) в течение\
        \ 30 минут.\n\n* `USER_UNREACHABLE` — не удалось связаться с покупателем. Для отмены с этой причиной необходимо выполнить\
        \ условия:\n\n  * не менее 3 звонков с 8 до 21 в часовом поясе покупателя;\n  * перерыв между первым и третьим звонком\
        \ не менее 90 минут;\n  * соединение не короче 5 секунд.\n\n  Если хотя бы одно из этих условий не выполнено (кроме\
        \ случая, когда номер недоступен), отменить заказ не получится. Вернется ответ с кодом ошибки 400\n\n* `USER_CHANGED_MIND`\
        \ — покупатель отменил заказ по личным причинам.\n\n* `USER_REFUSED_DELIVERY` — покупателя не устроили условия доставки.\n\
        \n* `USER_REFUSED_PRODUCT` — покупателю не подошел товар.\n\n* `SHOP_FAILED` — магазин не может выполнить заказ.\n\
        \n* `USER_REFUSED_QUALITY` — покупателя не устроило качество товара.\n\n* `REPLACING_ORDER` — покупатель решил заменить\
        \ товар другим по собственной инициативе.\n\n* `PROCESSING_EXPIRED` — значение более не используется.\n\n* `PICKUP_EXPIRED`\
        \ — закончился срок хранения заказа в ПВЗ.\n\n* `DELIVERY_SERVICE_UNDELIVERED` — служба доставки не смогла доставить\
        \ заказ.\n\n* `CANCELLED_COURIER_NOT_FOUND` — не удалось найти курьера.\n\n* `USER_WANTS_TO_CHANGE_DELIVERY_DATE`\
        \ — покупатель хочет получить заказ в другой день.\n\n* `RESERVATION_FAILED` — Маркет не может продолжить дальнейшую\
        \ обработку заказа.\n"
      name: substatus
      in: query
      required: false
      schema:
        type: array
        uniqueItems: true
        items:
          $ref: '#/components/schemas/OrderSubstatusType'
    GetOrdersSupplierShipmentDateFrom:
      description: 'Начальная дата для фильтрации заказов по дате отгрузки в службу доставки (параметр `shipmentDate`).


        Формат даты: `ДД-ММ-ГГГГ`.


        Между начальной и конечной датой (параметр `supplierShipmentDateTo`) должно быть не больше 30 дней.


        Начальная дата включается в интервал для фильтрации.

        '
      name: supplierShipmentDateFrom
      in: query
      required: false
      schema:
        type: string
        format: date
    GetOrdersSupplierShipmentDateTo:
      description: 'Конечная дата для фильтрации заказов по дате отгрузки в службу доставки (параметр `shipmentDate`).


        Формат даты: `ДД-ММ-ГГГГ`.


        Между начальной (параметр `supplierShipmentDateFrom`) и конечной датой должно быть не больше 30 дней.


        Конечная дата не включается в интервал для фильтрации.


        Если промежуток времени между `supplierShipmentDateTo` и `supplierShipmentDateFrom` меньше суток, то `supplierShipmentDateTo`
        равен `supplierShipmentDateFrom` + сутки.

        '
      name: supplierShipmentDateTo
      in: query
      required: false
      schema:
        type: string
        format: date
    GetOrdersToDate:
      description: 'Конечная дата для фильтрации заказов по дате оформления.


        Показываются заказы, созданные до 00:00 указанного дня.


        Формат даты: `ДД-ММ-ГГГГ`.


        Между начальной (параметр `fromDate`) и конечной датой должно быть не больше 30 дней.


        Значение по умолчанию: текущая дата.


        Если промежуток времени между `toDate` и `fromDate` меньше суток, то `toDate` равен `fromDate` + сутки.

        '
      name: toDate
      in: query
      required: false
      schema:
        type: string
        format: date
    GetOrdersUpdatedAtFrom:
      description: 'Начальная дата для фильтрации заказов по дате и времени обновления (параметр `updatedAt`).


        Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:42:42+03:00`.


        Между начальной и конечной датой (параметр `updatedAtTo`) должно быть не больше 30 дней.


        Начальная дата включается в интервал для фильтрации.

        '
      name: updatedAtFrom
      in: query
      required: false
      schema:
        type: string
        format: date-time
    GetOrdersUpdatedAtTo:
      description: 'Конечная дата для фильтрации заказов по дате и времени обновления (параметр `updatedAt`).


        Формат даты: ISO 8601 со смещением относительно UTC. Например, `2017-11-21T00:42:42+03:00`.


        Между начальной (параметр `updatedAtFrom`) и конечной датой должно быть не больше 30 дней.


        Конечная дата не включается в интервал для фильтрации.

        '
      name: updatedAtTo
      in: query
      required: false
      schema:
        type: string
        format: date-time
    ImageHash:
      description: Хеш ссылки изображения для загрузки.
      name: imageHash
      in: path
      required: true
      schema:
        type: string
    ItemId:
      description: Идентификатор товара в возврате.
      name: itemId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    LicenseIds:
      description: Список идентификаторов лицензий.
      name: ids
      in: query
      required: false
      schema:
        type: array
        uniqueItems: true
        maxItems: 500
        minItems: 1
        items:
          type: integer
          format: int64
          minimum: 1
    MarketCategoryId:
      in: path
      name: categoryId
      description: 'Идентификатор категории на Маркете.


        Чтобы узнать идентификатор категории, к которой относится интересующий вас товар, воспользуйтесь запросом [POST v2/categories/tree](../../reference/categories/getCategoriesTree.md).

        '
      required: true
      schema:
        type: integer
        format: int64
        minimum: 0
        exclusiveMinimum: true
    MassOrderLabelsGenerationPageFormat:
      description: Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата :no-translate[A7].
      name: format
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/PageFormatType'
    MessageId:
      description: Идентификатор сообщения.
      name: messageId
      in: query
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    OfferIds:
      description: 'Идентификатор скрытого предложения.

        '
      name: offer_id
      in: query
      x-transform: trim
      schema:
        type: array
        uniqueItems: true
        items:
          $ref: '#/components/schemas/ShopSku'
    OrderId:
      description: Идентификатор заказа.
      name: orderId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    OutletId:
      description: Идентификатор точки продаж.
      name: outletId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    OutletIds:
      description: 'Список идентификаторов точек продаж, для которых нужно получить информацию о лицензиях. Идентификаторы
        указываются через запятую.


        В запросе должен быть либо параметр `outletIds`, либо параметр `ids`. Запрос с обоими параметрами или без них приведет
        к ошибке.

        '
      name: outletIds
      in: query
      required: false
      schema:
        type: array
        uniqueItems: true
        maxItems: 500
        minItems: 1
        items:
          type: integer
          format: int64
          minimum: 1
    PageFormat:
      description: Настройка размещения ярлыков на странице. Если параметра нет, возвращается PDF с ярлыками формата :no-translate[A7].
      name: format
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/PageFormatType'
    PageLimit100:
      name: limit
      description: '{{ limit-param-description }}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 50
        maximum: 100
    PageLimit100Truncate:
      name: limit
      description: '{{ limit-truncate-param-description }}

        '
      in: query
      required: false
      x-transform: truncateLimit
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 50
        maximum: 100
    PageLimit100WithoutDefault:
      name: limit
      description: '{{ limit-param-description }}


        {% note warning %}


        У данного лимита нет значения по умолчанию.


        {% endnote %}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        maximum: 100
    PageLimit20:
      name: limit
      description: '{{ limit-param-description }}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 10
        maximum: 20
    PageLimit200:
      name: limit
      description: '{{ limit-param-description }}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 100
        maximum: 200
    PageLimit30:
      name: limit
      description: '{{ limit-param-description }}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 15
        maximum: 30
    PageLimit50:
      name: limit
      description: '{{ limit-param-description }}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 25
        maximum: 50
    PageLimit500:
      name: limit
      description: '{{ limit-param-description }}

        '
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 250
        maximum: 500
    PageLimit500Truncate:
      name: limit
      description: '{{ limit-truncate-param-description }}

        '
      in: query
      required: false
      x-transform: truncateLimit
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 250
        maximum: 500
    PageLimit50Truncate:
      name: limit
      description: '{{ limit-truncate-param-description }}

        '
      in: query
      required: false
      x-transform: truncateLimit
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 50
        maximum: 50
    PageLimit50TruncateWithoutDefault:
      name: limit
      description: '{{ limit-truncate-param-description }}


        {% note warning %}


        У данного лимита нет значения по умолчанию.


        {% endnote %}

        '
      in: query
      required: false
      x-transform: truncateLimit
      schema:
        type: integer
        format: int32
        minimum: 1
        maximum: 50
    PageNum:
      name: page
      description: '{% note warning "Устаревший параметр" %}


        Вместо `page` и `pageSize` используйте пагинацию по `pageToken` и `limit`.


        [Подробнее о типах пагинации и их использовании](../../concepts/pagination.md)


        {% endnote %}


        Номер страницы результатов.


        Используется вместе с параметром `pageSize`.

        '
      in: query
      required: false
      deprecated: true
      schema:
        type: integer
        format: int32
        default: 1
        maximum: 10000
    PageSize:
      name: pageSize
      description: '{% note warning "Устаревший параметр" %}


        Вместо `page` и `pageSize` используйте пагинацию по `pageToken` и `limit`.


        [Подробнее о типах пагинации и их использовании](../../concepts/pagination.md)


        {% endnote %}


        Размер страницы.


        Используется вместе с параметром `page`.

        '
      in: query
      required: false
      deprecated: true
      schema:
        type: integer
        format: int32
    RegionId:
      description: 'Идентификатор региона.


        Идентификатор региона можно получить c помощью запроса [GET v2/regions](../../reference/regions/searchRegionsByName.md).

        '
      name: regionId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    RegionName:
      description: 'Название региона.


        Важно учитывать регистр: первая буква должна быть заглавной, остальные — строчными. Например, `Москва`.

        '
      name: name
      in: query
      required: true
      schema:
        type: string
    ReportFormat:
      description: Формат отчета или документа.
      name: format
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/ReportFormatType'
    ReportId:
      description: 'Идентификатор отчета или документа, который вы получили после запуска генерации.

        '
      name: reportId
      in: path
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 255
    ReportLanguage:
      description: Язык отчета или документа.
      name: language
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/ReportLanguageType'
    ReturnId:
      description: Идентификатор невыкупа или возврата.
      name: returnId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    ShipmentId-2:
      deprecated: true
      description: '{ % note warning "Параметр больше не используется" % }


        Передайте любое число, чтобы получился корректный URL.


        { % endnote % }


        Идентификатор грузового места.

        '
      name: shipmentId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    ShipmentId-3:
      description: Идентификатор отгрузки.
      name: shipmentId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    ShipmentId:
      description: '{ % note warning "Параметр больше не используется" % }


        Передайте любое число, чтобы получился корректный URL.


        { % endnote % }


        Идентификатор грузового места.

        '
      name: shipmentId
      in: path
      required: true
      schema:
        type: integer
        format: int64
    ShipmentPalletLabelsPageFormat:
      name: format
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/ShipmentPalletLabelPageFormatType'
      description: 'Формат страниц PDF-файла с ярлыками:


        * `A4` — по 16 ярлыков на странице.

        * `A8` — по одному ярлыку на странице.

        '
    SourceType:
      name: sourceType
      in: query
      required: false
      description: 'Признак типа кабинета, от имени которого вызывается метод:

        {% if audience == "partner" %}


        - `SELLER` — продавец.


        {% endif %}


        - `ADVERTISER` — рекламодатель.


        {% if audience == "advertiser" %}


        {% note info "Обязательно указывайте sourceType=ADVERTISER в каждом запросе." %}


         


        {% endnote %}


        {% endif %}

        '
      schema:
        $ref: '#/components/schemas/SourceType'
        default: SELLER
